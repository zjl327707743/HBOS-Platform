# 安全策略

## 报告安全漏洞

**请勿在公开 Issue 中披露安全漏洞、Secret 或真实业务数据。**

如果你发现安全漏洞，请直接联系仓库维护者，不要在公开渠道讨论。

## 支持范围

本仓库是新乡海滨智能运营管理平台（HBOS）的开发和演示项目，不代表正式 GMP 生产系统。

## 敏感内容

以下内容**禁止**出现在本仓库中：

- 真实 API 密钥、Token、密码或 Secret
- 真实 `.env` 文件（仅允许 `.env.example` 占位符模板）
- 数据库备份、SQL dump 或真实数据库凭据
- 真实员工姓名、手机号、邮箱、工号
- 真实 Excel/CSV 附件
- 内部会议纪要、故障日志或审计记录
- 个人绝对路径或内部服务器地址
- 任何形式的内部认证凭据

## 安全最佳实践

- 所有 Secret 使用环境变量注入，不得硬编码
- 数据库密码使用 `docker compose` 的 `env_file` 或 `.env` 提供
- 提交前使用 `git diff --staged` 检查是否误提交敏感文件
- 如发现已泄露的 Secret，立即轮换并联系维护者