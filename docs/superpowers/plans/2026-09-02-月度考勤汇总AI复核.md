# 月度考勤汇总 AI 复核 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在「月度考勤汇总」报表加「AI复核」：enable_ai 临时列，把当月异常逐条交给真实大模型复核（OpenAI 兼容 + env），生成逐日「结论+理由」，随 CSV/Excel 导出，再次筛选 AI 列消失、每次点按钮重新复核。

**Architecture:** 新纯模块 `ai_review.py`（模块顶层不 import frappe，可离线测 build_prompt / parse_review / env 读取；call_llm 内联 import frappe+requests）。报表 `execute()` 读隐藏过滤 `enable_ai`，为有异常员工构造事实包→调 LLM→解析写入 `ai_review` 键；`_columns()` 追加「AI复核」列。JS onload 加按钮（先 preview 人次→确认→enable_ai=1→refresh），可见过滤 onChange 时把 enable_ai 复位 0。export_xlsx 透传 enable_ai。

**Tech Stack:** Python（requests、openpyxl 不需在此）+ Frappe query report + JavaScript。

## Global Constraints

- **测试运行方式**：`cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests`。测试环境无 frappe；`ai_review.py` 模块顶层禁止 `import frappe`（call_llm 内部 import）。
- **密钥走 env，不入 git/入库/聊天**：`HBOS_AI_BASE_URL`、`HBOS_AI_API_KEY`、`HBOS_AI_MODEL`、`HBOS_AI_TIMEOUT`（默认 60）。未配置 → `frappe.throw` 中文提示，不静默。
- **AI 不改写考勤结果**：只生成复核文本（临时列），不写库、不落盘、不触 regenerate_attendance、不改 DocType。
- **结论枚举**：`属实 / 存疑 / 非异常`；输出格式逐日一行 `日期|类型|结论|理由`。
- 只复核「当月有异常（迟到/早退/缺勤任一）的员工」，每人一次调用；剔豁免名单。
- Git commit 用中文；只 add 本任务文件（分支有大量未提交工作，禁 `git add -A`）。
- 每 Task 结束跑全量测试（基线 98）再 commit。

---

### Task 1: ai_review.py 纯函数模块

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/ai_review.py`
- Test: `apps/hb_attendance_app/tests/test_ai_review.py`（新建）

**Interfaces:**
- Produces（Task 2/3 消费）：
  - `env_config() -> dict`：`{"base_url","api_key","model","timeout"}`；缺失项以 `""` 返回（调用方 throw）。
  - `build_prompt(emp, anomaly_items, checkin_lines, rule_line) -> str`：纯函数构造发给 LLM 的完整 prompt。`emp={name,num,dept}`；`anomaly_items=[(date_str,"迟到"),...]`；`checkin_lines` 字符串；`rule_line` 字符串。
  - `parse_review(text, anomaly_keys) -> dict`：`{date_str: "结论：理由"}`；容错（跳过无 `|` 行、截断到异常数、非法结论行回落）。
  - `call_llm(cfg, prompt) -> str`：内联 `import frappe`，OpenAI 兼容 `POST {base_url}/chat/completions`，抛异常带中文原因。env 缺省 → `frappe.throw("未配置 AI（HBOS_AI_BASE_URL / HBOS_AI_API_KEY / HBOS_AI_MODEL）")`。

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_ai_review.py`：

```python
import unittest
from hb_attendance_app.hbos_attendance import ai_review


class AiBuildPromptTest(unittest.TestCase):
    def test_prompt_contains_employee_rules_anomalies_and_format(self):
        emp = {"name": "张三", "num": "11008018", "dept": "无菌车间"}
        items = [("2026-07-15", "迟到"), ("2026-07-16", "缺勤")]
        checkins = "7/15 08:41 上班机；7/15 20:44 下班机\n7/16 无打卡"
        rule = "行政班 08:30-17:30，08:31 起算迟到；豁免：否"
        p = ai_review.build_prompt(emp, items, checkins, rule)
        for s in ("张三", "11008018", "无菌车间", "行政班", "2026-07-15", "2026-07-16",
                  "属实", "存疑", "非异常", "日期|类型|结论|理由", "迟到", "缺勤"):
            self.assertIn(s, p)

    def test_env_config_returns_keys(self):
        cfg = ai_review.env_config()
        self.assertIn("base_url", cfg)
        self.assertIn("api_key", cfg)
        self.assertIn("model", cfg)
        self.assertIn("timeout", cfg)


class AiParseReviewTest(unittest.TestCase):
    def test_parse_normal_output(self):
        keys = ["2026-07-15", "2026-07-16"]
        text = "2026-07-15|迟到|属实|08:41 打卡超 08:31\n2026-07-16|缺勤|非异常|当天有排班休息记录"
        out = ai_review.parse_review(text, keys)
        self.assertEqual(out["2026-07-15"], "属实：08:41 打卡超 08:31")
        self.assertEqual(out["2026-07-16"], "非异常：当天有排班休息记录")

    def test_parse_skips_malformed_lines_and_truncates(self):
        keys = ["2026-07-15"]
        text = "随便一行没有分隔符\n2026-07-15|迟到|属实|理由\n2026-07-16|缺勤|存疑|多余行"
        out = ai_review.parse_review(text, keys)
        self.assertEqual(list(out.keys()), ["2026-07-15"])  # 只保留 keys 内、跳过多余

    def test_parse_unknown_verdict_falls_back(self):
        keys = ["2026-07-15"]
        text = "2026-07-15|迟到|不知道|理由"
        out = ai_review.parse_review(text, keys)
        self.assertIn("存疑", out["2026-07-15"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests -p "test_ai_review.py" -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'hb_attendance_app.hbos_attendance.ai_review'`。

- [ ] **Step 3: 实现 ai_review.py**

创建 `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/ai_review.py`：

```python
"""月度考勤汇总 AI 复核：事实包/prompt/返回解析（纯函数，无顶层 frappe 依赖可离线测试）。

LLM 走 OpenAI 兼容协议，配置走 env（HBOS_AI_BASE_URL / HBOS_AI_API_KEY /
HBOS_AI_MODEL / HBOS_AI_TIMEOUT），不入 git 不入库。
AI 只生成复核意见文本，不改写考勤结果、不落库。
结论枚举：属实 / 存疑 / 非异常。
"""
import os

CONCLUSIONS = ("属实", "存疑", "非异常")


def env_config():
    """读取 AI 配置。缺失项为空字符串，由调用方判断并 throw。"""
    try:
        timeout = int(os.environ.get("HBOS_AI_TIMEOUT", "60"))
    except ValueError:
        timeout = 60
    return {
        "base_url": (os.environ.get("HBOS_AI_BASE_URL") or "").rstrip("/"),
        "api_key": os.environ.get("HBOS_AI_API_KEY") or "",
        "model": os.environ.get("HBOS_AI_MODEL") or "",
        "timeout": timeout,
    }


def build_prompt(emp, anomaly_items, checkin_lines, rule_line):
    """构造发给 LLM 的复核 prompt。

    emp: {"name","num","dept"}
    anomaly_items: [(date_str, "迟到|早退|缺勤"), ...]
    checkin_lines / rule_line: 字符串（含换行的事实文本）
    """
    lines = [
        "你是海滨考勤审核助手。对每条被系统判为异常的考勤记录给复核结论。",
        "逐条输出，格式严格为：日期|类型|结论|理由",
        "结论取值仅限：属实 / 存疑 / 非异常（非异常=系统误判，须给出依据）。",
        "类型取值：迟到 / 早退 / 缺勤。",
        "",
        f"【员工】{emp.get('name','')} 工号{emp.get('num','')} 部门{emp.get('dept','')}",
        f"【班次规则】{rule_line}",
        f"【打卡流水】\n{checkin_lines}",
        "",
        "待复核异常（每行一条）：",
    ]
    for d, t in anomaly_items:
        lines.append(f"{d}|{t}")
    lines.append("输出：")
    return "\n".join(lines)


def parse_review(text, anomaly_keys):
    """解析 LLM 输出。返回 {date_str: "结论：理由"}。

    - 只保留 date 在 anomaly_keys 中的行
    - 跳过无 | 的行；结论非法时回落「存疑」
    - 输出行数截断到 anomaly_keys 数内
    """
    if not text:
        return {}
    out = {}
    for line in text.splitlines():
        line = line.strip()
        if "|" not in line:
            continue
        parts = [p.strip() for p in line.split("|")]
        if len(parts) < 4:
            continue
        date_str, typ, verdict, reason = parts[0], parts[1], parts[2], "".join(parts[3:])
        if date_str not in anomaly_keys:
            continue
        if verdict not in CONCLUSIONS:
            verdict = "存疑"
        out[date_str] = f"{verdict}：{reason}"
        if len(out) >= len(anomaly_keys):
            break
    return out


def call_llm(cfg, prompt):
    """调用 OpenAI 兼容接口，返回模型文本。失败抛中文异常。"""
    import frappe
    if not (cfg["base_url"] and cfg["api_key"] and cfg["model"]):
        frappe.throw("未配置 AI（HBOS_AI_BASE_URL / HBOS_AI_API_KEY / HBOS_AI_MODEL）")
    import requests
    url = f"{cfg['base_url']}/chat/completions"
    headers = {"Authorization": f"Bearer {cfg['api_key']}", "Content-Type": "application/json"}
    body = {
        "model": cfg["model"],
        "messages": [{"role": "system", "content": "你是海滨考勤审核助手，输出严格按用户要求格式。"},
                     {"role": "user", "content": prompt}],
        "temperature": 0,
        "max_tokens": 1200,
    }
    try:
        resp = requests.post(url, json=body, headers=headers, timeout=cfg["timeout"])
    except Exception as e:
        frappe.throw(f"AI 调用失败：{e}")
    if resp.status_code != 200:
        frappe.throw(f"AI 返回异常：HTTP {resp.status_code} {resp.text[:200]}")
    try:
        data = resp.json()
        return data["choices"][0]["message"]["content"]
    except Exception:
        frappe.throw("AI 返回格式无法解析")
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests -p "test_ai_review.py" -v`
Expected: PASS（4 例）。

- [ ] **Step 5: 全量 + Commit**

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests`
Expected: PASS（98 + 4 = 102）。

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/ai_review.py apps/hb_attendance_app/tests/test_ai_review.py
git commit -m "feat: 新增 AI 复核纯函数模块 ai_review.py（prompt/解析/env，离线可测）"
```

---

### Task 2: 报表 enable_ai 过滤 + ai_review 列 + preview 端点

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/report/月度考勤汇总/月度考勤汇总.py`
- Modify: `apps/hb_attendance_app/tests/test_ai_review.py`（追加契约测试）

**Interfaces:**
- Consumes: Task 1 `ai_review.build_prompt` / `parse_review` / `call_llm` / `env_config`。
- Produces: `execute(filters)` 在 `filters["enable_ai"]` truthy 时给每个有异常的行补 `ai_review` 文本；`_columns(enable_ai)` 追加「AI复核」列；`@frappe.whitelist() ai_review_preview(month,year,employee,department,from_date,to_date) -> {employee_count, anomaly_count}`。

- [ ] **Step 1: 写失败测试（契约，文件内容校验）**

在 `apps/hb_attendance_app/tests/test_ai_review.py` 追加：

```python
from pathlib import Path

APP = Path(__file__).parents[1] / "hb_attendance_app"
REPORT_PY = APP / "hbos_attendance/report/月度考勤汇总/月度考勤汇总.py"
REPORT_JS = APP / "hbos_attendance/report/月度考勤汇总/月度考勤汇总.js"
EXPORT_PY = APP / "hbos_attendance/report/月度考勤汇总/export.py"


class AiMonthlyReportContractTest(unittest.TestCase):
    def test_report_enable_ai_gates_column_and_build(self):
        content = REPORT_PY.read_text()
        self.assertIn('enable_ai', content)
        self.assertIn("def _columns(enable_ai=False)", content)
        self.assertIn('"AI复核"', content)
        self.assertIn("ai_review", content)
        self.assertIn("build_prompt", content)
        self.assertIn("parse_review", content)
        self.assertIn("call_llm", content)
        self.assertIn("ai_review_preview", content)

    def test_report_skips_ai_when_disabled(self):
        content = REPORT_PY.read_text()
        self.assertIn("if enable_ai:", content)

    def test_export_passes_enable_ai(self):
        content = EXPORT_PY.read_text()
        self.assertIn("enable_ai", content)

    def test_js_has_ai_review_button_and_confirm_and_reset(self):
        content = REPORT_JS.read_text()
        self.assertIn("AI复核", content)
        self.assertIn("ai_review_preview", content)
        self.assertIn("enable_ai", content)
        self.assertIn("confirm", content)
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests -p "test_ai_review.py" -v`
Expected: FAIL —— 上述字符串不在文件中。

- [ ] **Step 3: 改造月度考勤汇总.py**

打开 `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/report/月度考勤汇总/月度考勤汇总.py`：

1) `execute()` 开头读取 enable_ai（在 `filters = filters or {}` 后）：
```python
    enable_ai = bool(filters.get("enable_ai"))
```
2) 在 data 构建处，把返回改为 `return _columns(enable_ai), data`（两处：`return _columns(), data` 与文件尾 `return _columns(), []` 改为 `return _columns(enable_ai), []`——注意尾部作用域无 enable_ai，可保持 `_columns()` 但需同步：把尾部的 `return _columns(), []` 改为 `return _columns(enable_ai), []` 前先给函数体定义 enable_ai 默认 `enable_ai = bool(filters.get("enable_ai")) if filters else False`。建议：两处都走同一分支——execute 末尾在 `if attendance:` 后返回，else 分支也需要 enable_ai。将两处 return 均改为带 enable_ai。
3) 在 `data` 列表已生成后、return 前，插入 AI 复核块（enable_ai 时）：
```python
        if enable_ai:
            _attach_ai_review(data, filters, emp_leave_dates)
```
并实现模块级函数 `_attach_ai_review(data, filters, emp_leave_dates)`（放 execute 之后、_columns 之前），内容如下（frappe 查询打卡 + 调 LLM，逐人失败隔离）：

```python
def _attach_ai_review(data, filters, emp_leave_dates):
    """enable_ai 时：为当月有异常的员工逐人调 AI，写 ai_review 文本。

    仅复核迟到/早退/缺勤任一 >0 的员工；逐人失败回落提示，不整体中断。
    """
    from hb_attendance_app.hbos_attendance.ai_review import (
        build_prompt, call_llm, env_config, parse_review,
    )
    cfg = env_config()
    if not (cfg["base_url"] and cfg["api_key"] and cfg["model"]):
        for r in data:
            if _row_anomaly(r):
                r["ai_review"] = "AI复核失败：未配置 AI（HBOS_AI_BASE_URL / HBOS_AI_API_KEY / HBOS_AI_MODEL）"
        return

    # 收集有异常员工的异常日期 + 考勤明细
    target = [(r, [d for d in _anomaly_dates(r)]) for r in data if _row_anomaly(r)]
    emp_ids = [r["employee"] for r, _ in target]
    if not emp_ids:
        return

    # 打卡流水（供 AI 判断；按员工拉全部当月卡，facts 内筛对应异常日）
    from_date, to_date = _range_from_filters(filters)
    checkin_map = {}
    if emp_ids:
        q = ",".join("'%s'" % e.replace("'", "") for e in emp_ids)
        rows = frappe.db.sql(
            f"SELECT employee, time, log_type, device_id FROM `tabEmployee Checkin` "
            f"WHERE employee IN ({q}) AND DATE(time) BETWEEN %(s)s AND %(e)s "
            f"ORDER BY employee, time",
            {"s": from_date, "e": to_date}, as_dict=True)
        for c in rows:
            checkin_map.setdefault(c.employee, []).append(c)

    for r, dates in target:
        emp_id = r["employee"]
        rule_line = _rule_line_for(r)
        cks = checkin_map.get(emp_id, [])
        ck_lines = "\n".join(
            f"{c.time.strftime('%m/%d %H:%M')} {c.log_type or ''} {c.device_id or ''}"
            for c in cks if str(c.time.date()) in dates
        ) or "无打卡记录"
        items = [(d, _type_for_date(r, d)) for d in dates]
        prompt = build_prompt(
            {"name": r["employee_name"], "num": r["employee_number"], "dept": r["department"]},
            items, ck_lines, rule_line,
        )
        try:
            text = call_llm(cfg, prompt)
            parsed = parse_review(text, dates)
            r["ai_review"] = "\n".join(f"{d}｜{parsed[d]}" for d in dates if d in parsed) or "AI复核失败：无有效返回"
        except Exception as e:
            r["ai_review"] = f"AI复核失败：{e}"
```

（配套小工具函数 `_row_anomaly(r)`、`_anomaly_dates(r)`、`_type_for_date(r, d)`、`_range_from_filters(filters)`、`_rule_line_for(r)` 一并实现——其中 `_row_anomaly` 判断 `r["late_count"]+r["early_count"]+r["absent_count"]>0`；`_anomaly_dates` 把 late_detail/early_detail/absent_detail 拆成 `date_str` 全集（需还原为 YYYY-MM-DD：从 filters 推年份月份）；`_type_for_date` 判断该日期属 late/early/absent；`_rule_line_for` 返回该员工班次判定文本，本期可简化为通用文本「按系统规则（部门-班次-人员 + 内置班次）判定」，并标注是否豁免/行政班名单。）

4) `_columns` 改签名：`def _columns(enable_ai=False):`；函数末尾追加：
```python
    if enable_ai:
        cols.append({"label": "AI复核", "fieldname": "ai_review",
                     "fieldtype": "Small Text", "width": 450})
    return cols
```
（把现有 `return [...]` 改为先 `cols = [...]` 再按 enable_ai 追加。）

5) 新增 `@frappe.whitelist() def ai_review_preview(month=None, year=None, employee=None, department=None, from_date=None, to_date=None):`：按同 execute 相同过滤（剔豁免）统计当月有异常员工数/异常条数，返回 `{"employee_count": N, "anomaly_count": M}`。实现：可直接调用 execute 的 SQL 查询段，为防重复实现，最简做法是复用 `execute({"month":month,"year":year,...,"enable_ai":0})[1]` 后对 data 统计 `_row_anomaly` 与异常条数。

- [ ] **Step 4: py_compile + 契约测试通过**

Run: `cd apps/hb_attendance_app && python3 -m py_compile hb_attendance_app/hbos_attendance/report/月度考勤汇总/月度考勤汇总.py`
Expected: 无输出。

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests -p "test_ai_review.py" -v`
Expected: PASS（4 + 4 = 8）。

- [ ] **Step 5: 全量 + Commit**

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests`
Expected: PASS（102）。

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/report/月度考勤汇总/月度考勤汇总.py apps/hb_attendance_app/tests/test_ai_review.py
git commit -m "feat: 月度考勤汇总新增 enable_ai 过滤与 AI复核列、ai_review_preview 端点"
```

---

### Task 3: 前端按钮 + 过滤复位 + 导出透传

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/report/月度考勤汇总/月度考勤汇总.js`
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/report/月度考勤汇总/export.py`
- Modify: `apps/hb_attendance_app/tests/test_ai_review.py`（契约断言已在 Task 2 写入，此 Task 使其通过）

**Interfaces:**
- Consumes: Task 2 `ai_review_preview`、`execute(enable_ai)`、`export_xlsx(enable_ai)`。

- [ ] **Step 1: JS 加隐藏过滤 + AI复核按钮 + 复位**

在 `月度考勤汇总.js`：
1) `filters` 数组末尾追加隐藏 Check：`{fieldname:"enable_ai",label:__("启用AI复核"),fieldtype:"Check",default:0,hidden:1}`。
2) `onload` 里加按钮（放在「异常考勤导出」按钮定义之后）：
```js
report.page.add_inner_button(__("AI复核"), function () {
	var args = {
		month: report.get_filter_value("month"),
		year: report.get_filter_value("year"),
		employee: report.get_filter_value("employee"),
		department: report.get_filter_value("department"),
		from_date: report.get_filter_value("from_date"),
		to_date: report.get_filter_value("to_date"),
	};
	frappe.call({
		method: "hb_attendance_app.hbos_attendance.report.月度考勤汇总.月度考勤汇总.ai_review_preview",
		args: args,
		callback: function (r) {
			var m = r.message || { employee_count: 0, anomaly_count: 0 };
			if (!m.employee_count) {
				frappe.msgprint(__("当前范围没有需复核的异常员工"));
				return;
			}
			frappe.confirm(
				__("将调用 AI 复核 ") + m.employee_count + __(" 名异常员工（约 ") + m.anomaly_count + __(" 条异常）。") +
				__("将调用外部大模型，请确认已配置 HBOS_AI_* 且接受相应费用。继续？"),
				function () {
					report.set_filter_value("enable_ai", 1);
					report.refresh();
				}
			);
		},
	});
});
```
3) 复位：给报表可见过滤加 onchange 复位（Frappe query report 无统一 filter onchange，但可监听 `report.page` 上的 filter 变更——用 `report.filters`? 兼容做法：在 onload 里对每个可见 filter field 追加 change 监听，把 enable_ai 置 0）。实现如下（放 onload 内、按钮绑定后）：
```js
["month","year","employee","department","from_date","to_date"].forEach(function (fn) {
	var f = report.get_filter(fn);
	if (f && f.$input) f.$input.on("change", function () {
		if (report.get_filter_value("enable_ai")) {
			report.set_filter_value("enable_ai", 0);
		}
	});
});
```
4) 两个导出按钮（导出 Excel / 异常考勤导出）的 args 对象各加一行 `enable_ai: report.get_filter_value("enable_ai"),`。

- [ ] **Step 2: export.py 透传 enable_ai**

`export.py` 的 `export_xlsx` 与 `export_exceptions` 签名各加 `enable_ai=None`，并在 filters dict 构建处加 `if enable_ai: filters["enable_ai"] = enable_ai`。这样 enable_ai=1 时 execute 返回带 AI 列的 data，Excel 导出含该列。

- [ ] **Step 3: 运行测试确认通过**

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests -p "test_ai_review.py" -v`
Expected: PASS（8，含 test_js_has_ai_review_button_and_confirm_and_reset / test_export_passes_enable_ai）。

Run: `cd apps/hb_attendance_app && node --check hb_attendance_app/hbos_attendance/report/月度考勤汇总/月度考勤汇总.js`
Expected: 无输出。

- [ ] **Step 4: 全量 + Commit**

Run: `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests`
Expected: PASS（102）。

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/report/月度考勤汇总/月度考勤汇总.js apps/hb_attendance_app/hb_attendance_app/hbos_attendance/report/月度考勤汇总/export.py apps/hb_attendance_app/tests/test_ai_review.py
git commit -m "feat: 月度汇总 AI复核按钮（确认人次）与过滤复位、导出透传 enable_ai"
```

---

### Task 4: 更新状态台账与主文档

**Files:**
- Modify: `docs/milestones/M1_FIX_B5_导入数据链路核查与报表口径收敛.md`
- Modify: `docs/PROJECT_STATUS.md` / `docs/CURRENT_MILESTONE.md` / `docs/milestones/M1_START_GATE.md`

**接口:** 无（纯文档）。

- [ ] **Step 1: B5 主文档追加「月度汇总 AI 复核」小节**（在「班次人员维护表导出」小节后）
- [ ] **Step 2: 三个台账 M1-FIX-B5 描述各追加一句**（AI 复核 enable_ai 临时列；公共入口只读核对）
- [ ] **Step 3: Commit**（中文描述）

---

## 验证清单

1. 全量 `cd apps/hb_attendance_app && PYTHONPATH=. python3 -m unittest discover -s tests` 通过（基线 98 + 本计划 ~4 = 102）。
2. **运行时需 Owner 配 env 后人工验证**（无密钥本环境无法调 LLM）：
   - backend 容器设 `HBOS_AI_BASE_URL / HBOS_AI_API_KEY / HBOS_AI_MODEL`（写入容器环境或 docker-compose env，不入 git）；`docker restart hbos-m0-r3a-backend-1`。
   - 打开 `/app/query-report/月度考勤汇总` → 选 2026-07 → 点「AI复核」→ 弹确认人次 → 确认后报表出现「AI复核」列并逐人复核。
   - 改部门/月份 → AI 列消失；再点 → 重新复核。「导出 Excel」在 AI 开时含 AI 列。
3. 无 env 时点按钮应给中文「未配置 AI」提示（通过 `ai_review_preview`/execute 文案）。

## Self-Review 记录

- **Spec 覆盖**：设计 §三 enable_ai 临时列/确认/重新复核 → Task 2（execute+_columns+preview）+ Task 3（按钮/复位/导出透传）；§四 ai_review 模块（pack/prompt/parse/call_llm/env）→ Task 1；§六 容错逐人失败隔离 + env 缺省 throw → Task 1 call_llm + Task 2 _attach_ai_review try/except；§七测试 → Task 1/2/3；§八台账 → Task 4。无缺口。
- **占位符扫描**：无 TBD；代码步骤含实现。个别辅助函数（`_row_anomaly/_anomaly_dates/_type_for_date/_range_from_filters/_rule_line_for`）在 Task 2 Step 3 有职责说明但未逐字给出——标注为需 Task 2 实现者按说明补齐并保契约测试绿；这是本计划唯一略缩点，实施时以职责说明为准自行实现（不改变公开接口）。
- **类型一致性**：`ai_review.env_config()/build_prompt/parse_review/call_llm` 签名 Task 1 定义、Task 2 调用一致；`execute(filters)` 与 `_columns(enable_ai)`、`ai_review_preview` 返回 `{employee_count, anomaly_count}`、JS 消费一致；`export_xlsx(..., enable_ai)` Task 3 与 JS 透传一致。
