# 考勤通知（每日 9:00 出站推送）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** HBOS 每天北京时间 09:00 自动算出当日各部门到岗情况，以出站 HTTP POST 推送给云端 OpenClaw，由它发到管理群。

**Architecture:** 统计口径收敛为后端纯模块 `board_stats.py`（无 frappe 依赖、可离线测试），看板与通知共用；通知模块 `attendance_notify.py` 复用看板数据层的取数结果渲染文本并 POST；定时由 Frappe scheduler 两条 cron + 任务内「北京时间 == 9 点」守卫触发，cache 幂等去重。HBOS 不存群 ID、不负责排版、不开入站端口。

**Tech Stack:** Python 3（Frappe 环境）、原生 JavaScript、frappe scheduler、requests、unittest。

## Global Constraints

- 通知**只含部门行，不含全厂汇总行**（Owner 2026-09-10 确认）。
- 统计口径**只有一处实现**（`board_stats.py`）；前端 JS 不得再定义自己的汇总逻辑。
- 恒等关系必须成立：`expected == present + noCard + absent`（逐部门与全厂）。
- `before_start`（班次尚未到上班时间）**不计入未打卡**——否则每天 9 点会把晚班/夜班人员误报为未到。
- 迟到按 **state == late 或 tags 含「迟到」** 计（覆盖回顾模式的 HRMS 迟到标签）。
- 时区：cron 不用于定时刻，任务内以北京时间小时 == 9 作守卫（容器可能为 UTC）。
- 幂等：`frappe.cache` 键 `hbos_notify_sent:<YYYY-MM-DD>`，同日只发一次。
- 配置经环境变量：`HBOS_NOTIFY_WEBHOOK_URL`（缺省则静默跳过发送）、`HBOS_NOTIFY_TOKEN`（可选，作 `Authorization: Bearer`）、`HBOS_NOTIFY_DRY_RUN`（=1 时只记日志不发送）。密钥/地址**不入仓库**（`.env` 已被 `.gitignore` 忽略）。
- 只做出站推送；不做入站接口、不做对话查询、不改任何考勤判定规则。
- 离线测试必须 frappe-free 且纳入 `apps/hb_attendance_app/tests/`（CI 用 `unittest discover`）。
- 提交信息用中文；不提交 `.env`、密钥、真实数据。

---

### Task 1: 统计口径纯模块 board_stats.py

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/board_stats.py`
- Test: `apps/hb_attendance_app/tests/test_board_stats.py`

**Interfaces:**
- Consumes: 无（纯 Python，不 import frappe）。
- Produces（后续任务依赖，签名锁定）：
  - `PRESENT_STATES: set[str]`
  - `summarize_rows(rows: list[dict]) -> dict` — 返回
    `{"total","expected","present","late","noCard","absent","leave","rest","exempt","attendance_rate"}`
    （`attendance_rate` 为 `present/expected*100` 保留 1 位，`expected == 0` 时为 `None`）
  - `group_by_dept(rows: list[dict]) -> dict[str, list[dict]]`（缺部门归 `"未分组"`）
  - `dept_summary(rows: list[dict]) -> list[dict]` — 每项
    `{"dept","total","expected","present","noCard","late","absent","leave"}`，
    按 `(noCard + late)` 降序、其次 `total` 降序；**不含 expected==0 且 total==0 的部门**（见测试）

row 契约：`{"dept","state","tags","kind"}`（tags 可为 None/缺失；kind ∈ {shift, rest, leave, exempt, unknown}）

- [ ] **Step 1: 写失败测试**

```python
import unittest

from hb_attendance_app.hbos_attendance.board_stats import (
    summarize_rows, group_by_dept, dept_summary,
)


def row(state, kind="shift", tags=None, dept="A", num="1"):
    return {"dept": dept, "state": state, "tags": tags or [], "kind": kind, "num": num}


class SummarizeRowsTest(unittest.TestCase):
    def test_present_and_late(self):
        rows = [row("present"), row("late"), row("fact_present"), row("out_day")]
        s = summarize_rows(rows)
        self.assertEqual(s["expected"], 4)
        self.assertEqual(s["present"], 4)      # 迟到也算已到岗
        self.assertEqual(s["late"], 1)         # state=late 计入迟到
        self.assertEqual(s["noCard"], 0)
        self.assertEqual(s["attendance_rate"], 100.0)

    def test_late_by_tag_counts_in_review_mode(self):
        # 回顾模式：HRMS 迟到以 out_day + tags 呈现，state 不是 late
        rows = [row("out_day", tags=["迟到"])]
        s = summarize_rows(rows)
        self.assertEqual(s["late"], 1)
        self.assertEqual(s["present"], 1)

    def test_before_start_not_counted_as_no_card(self):
        # 晚班 20:00 上班、当前 9 点 → 未到上班时间，不得计入未打卡
        rows = [row("before_start"), row("present")]
        s = summarize_rows(rows)
        self.assertEqual(s["expected"], 2)
        self.assertEqual(s["noCard"], 0)
        self.assertEqual(s["present"], 1)

    def test_no_card_and_absent_are_separate(self):
        rows = [row("absent_expected"), row("fact_none"), row("no_pair"), row("absent_day")]
        s = summarize_rows(rows)
        self.assertEqual(s["expected"], 4)
        self.assertEqual(s["noCard"], 3)       # absent_expected/fact_none/no_pair
        self.assertEqual(s["absent"], 1)       # 仅已定性的缺勤
        self.assertEqual(s["present"], 0)

    def test_identity_holds(self):
        rows = [row("present"), row("late"), row("absent_day"), row("fact_none"),
                row("before_start"), row("rest", kind="rest"), row("leave", kind="leave"),
                row("exempt", kind="exempt"), row("unknown", kind="unknown")]
        s = summarize_rows(rows)
        self.assertEqual(s["expected"], s["present"] + s["noCard"] + s["absent"])

    def test_non_shift_kinds_excluded_from_denominator(self):
        rows = [row("rest", kind="rest"), row("leave", kind="leave"),
                row("exempt", kind="exempt"), row("unknown", kind="unknown")]
        s = summarize_rows(rows)
        self.assertEqual(s["expected"], 0)
        self.assertIsNone(s["attendance_rate"])
        self.assertEqual(s["rest"], 1)
        self.assertEqual(s["leave"], 1)
        self.assertEqual(s["exempt"], 1)

    def test_out_only_counts_as_no_card(self):
        # 只有下班卡（跨天夜班次日）——未到岗、非 before_start → 未打卡
        rows = [row("out_only")]
        s = summarize_rows(rows)
        self.assertEqual(s["expected"], 1)
        self.assertEqual(s["noCard"], 1)

    def test_empty_rows(self):
        s = summarize_rows([])
        self.assertEqual(s["total"], 0)
        self.assertIsNone(s["attendance_rate"])


class GroupAndDeptSummaryTest(unittest.TestCase):
    def test_group_by_dept_buckets_missing_dept(self):
        g = group_by_dept([row("present", dept="A"), row("present", dept=""), row("present", dept="A")])
        self.assertEqual(len(g["A"]), 2)
        self.assertEqual(len(g["未分组"]), 1)

    def test_dept_summary_sorted_by_problem_first(self):
        rows = [
            row("present", dept="好部门"),
            row("fact_none", dept="差部门"),
            row("fact_none", dept="差部门"),
            row("late", dept="中部门"),
        ]
        out = dept_summary(rows)
        self.assertEqual([d["dept"] for d in out], ["差部门", "中部门", "好部门"])
        self.assertEqual(out[0]["noCard"], 2)
        self.assertEqual(out[0]["expected"], 2)

    def test_dept_summary_fields(self):
        out = dept_summary([row("present", dept="A"), row("absent_day", dept="A"),
                            row("leave", kind="leave", dept="A")])
        self.assertEqual(len(out), 1)
        d = out[0]
        self.assertEqual(d["dept"], "A")
        self.assertEqual(d["total"], 3)
        self.assertEqual(d["expected"], 2)
        self.assertEqual(d["present"], 1)
        self.assertEqual(d["absent"], 1)
        self.assertEqual(d["leave"], 1)
        self.assertEqual(d["noCard"], 0)

    def test_last_leave_survives_rest(self):
        out = dept_summary([row("rest", kind="rest", dept="A")])
        self.assertEqual(out[0]["expected"], 0)
        self.assertEqual(out[0]["total"], 1)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_board_stats -v
```
Expected: FAIL（`ModuleNotFoundError: board_stats`）

- [ ] **Step 3: 实现 board_stats.py**

```python
"""HBOS 看板/通知共用统计口径（纯模块，无 frappe 依赖，可离线测试）。

口径（Owner 2026-09-08 / 2026-09-10 确认）:
  应出勤 expected = kind == "shift" 的行（含缺勤行）
  已到岗 present  = 有卡/考勤出勤（迟到也算到岗）
  迟到   late     = state == "late" 或 tags 含「迟到」（覆盖回顾模式 HRMS 标签）
  缺勤   absent   = state == "absent_day"（仅已定性缺勤；计入 expected，不计入 noCard）
  无打卡 noCard   = 期望上班、未到岗、且 state 不属于 {before_start, absent_day}
  恒等: expected == present + noCard + absent
"""

PRESENT_STATES = {
    "present", "late", "fact_present", "present_offwindow", "out_day", "out_offwindow",
}

_EMPTY = {
    "total": 0, "expected": 0, "present": 0, "late": 0,
    "noCard": 0, "absent": 0, "leave": 0, "rest": 0, "exempt": 0,
    "attendance_rate": None,
}


def _tags(r):
    return r.get("tags") or []


def is_late(r):
    return r.get("state") == "late" or "迟到" in _tags(r)


def summarize_rows(rows):
    """按口径汇总一组行，返回计数与出勤率。"""
    s = dict(_EMPTY)
    s["total"] = len(rows)
    for r in rows:
        st = r.get("state")
        if st == "exempt":
            s["exempt"] += 1
            continue
        if st == "rest":
            s["rest"] += 1
            continue
        if st == "leave":
            s["leave"] += 1
            continue
        if st == "unknown":
            continue
        if r.get("kind") != "shift":
            continue
        s["expected"] += 1
        if is_late(r):
            s["late"] += 1
        if st == "absent_day":
            s["absent"] += 1
            continue
        if st in PRESENT_STATES:
            s["present"] += 1
        elif st != "before_start":
            s["noCard"] += 1
    s["attendance_rate"] = (
        round(s["present"] / s["expected"] * 100, 1) if s["expected"] else None
    )
    return s


def group_by_dept(rows):
    """按部门分组；无部门归「未分组」（保持首次出现顺序）。"""
    out = {}
    for r in rows:
        out.setdefault(r.get("dept") or "未分组", []).append(r)
    return out


def dept_summary(rows):
    """每部门一行汇总，按 (未打卡+迟到) 降序、其次在册人数降序。"""
    out = []
    for dept, drows in group_by_dept(rows).items():
        s = summarize_rows(drows)
        out.append({
            "dept": dept,
            "total": s["total"],
            "expected": s["expected"],
            "present": s["present"],
            "noCard": s["noCard"],
            "late": s["late"],
            "absent": s["absent"],
            "leave": s["leave"],
        })
    out.sort(key=lambda d: (-(d["noCard"] + d["late"]), -d["total"]))
    return out
```

- [ ] **Step 4: 运行确认通过**

Run: 同 Step 2
Expected: PASS（12 个用例）

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/board_stats.py apps/hb_attendance_app/tests/test_board_stats.py
git commit -m "feat: 新增看板/通知共用统计口径纯模块 board_stats"
```

---

### Task 2: 数据层改用 board_stats 并返回 dept_stats

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_department_board/department_board_data.py`
- Modify: `apps/hb_attendance_app/tests/test_department_board.py`（契约断言）

**Interfaces:**
- Consumes: Task 1 的 `summarize_rows` / `dept_summary`。
- Produces: `get_data()` 返回值新增两个键——
  - `stats`: 由 `summarize_rows(rows)` 生成（键名与 Task 1 一致：`noCard`/`absent`/`attendance_rate` 等）
  - `dept_stats`: 由 `dept_summary(rows)` 生成（每项键：`dept,total,expected,present,noCard,late,absent,leave`）
  删除原 `_aggregate` 与 `_empty_stats`。

- [ ] **Step 1: 先加契约断言（追加到 test_department_board.py 的 DataLayerContractTest 类内）**

```python
    def test_data_layer_uses_shared_board_stats(self):
        content = DATA_PY.read_text()
        self.assertIn("from hb_attendance_app.hbos_attendance.board_stats import", content)
        self.assertIn("dept_summary", content)
        self.assertIn('"dept_stats"', content)
        # 旧的自有聚合实现必须移除，口径只能有一处
        self.assertNotIn("def _aggregate(", content)
        self.assertNotIn("def _empty_stats(", content)
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_department_board.DataLayerContractTest -v
```
Expected: FAIL（缺少 `board_stats` 引用 / `_aggregate` 仍存在）

- [ ] **Step 3: 改数据层**

在 `department_board_data.py` 顶部 import 区加：

```python
from hb_attendance_app.hbos_attendance.board_stats import summarize_rows, dept_summary
```

把两处 `"stats": _empty_stats(), "rows": []}` 改为：

```python
                "stats": summarize_rows([]), "dept_stats": [], "rows": []}
```

把结尾：

```python
    stats = _aggregate(rows)
    return {"departments": _all_depts(), "meta": {"date": date_str, "mode": mode,
                                                  "now_hm": now.strftime("%H:%M"),
                                                  "scope": department or "全部部门"},
            "stats": stats, "rows": rows}
```

改为：

```python
    return {"departments": _all_depts(), "meta": {"date": date_str, "mode": mode,
                                                  "now_hm": now.strftime("%H:%M"),
                                                  "scope": department or "全部部门"},
            "stats": summarize_rows(rows), "dept_stats": dept_summary(rows), "rows": rows}
```

删除文件末尾的 `def _empty_stats()` 与 `def _aggregate(rows)` 两个函数（整段删除，含其文档字符串）。

- [ ] **Step 4: 运行全量离线测试**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m compileall -q apps/hb_attendance_app && python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -5
```
Expected: OK（无回归）

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_department_board/department_board_data.py apps/hb_attendance_app/tests/test_department_board.py
git commit -m "refactor: 看板数据层改用 board_stats 统一口径并返回 dept_stats"
```

---

### Task 3: 前端改用服务端 stats / dept_stats

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_department_board/hbos_department_board.js`
- Modify: `apps/hb_attendance_app/tests/test_department_board.py`（前端契约断言）

**Interfaces:**
- Consumes: Task 2 的 `stats`（键 `expected/present/late/noCard/absent/leave/rest/exempt/attendance_rate`）
  与 `dept_stats`（每项 `dept,total,expected,present,noCard,late,absent,leave`）。
- Produces: 前端不再有 `summarize()`；`render(data)` 用 `data.stats` 渲染 KPI、
  用 `data.dept_stats` 渲染部门表；**人员明细行仍由 `data.rows` 渲染**（未变）。

- [ ] **Step 1: 先加前端契约断言（追加到 FrontendContractTest 类内）**

```python
    def test_frontend_uses_server_stats(self):
        js = (Path(__file__).parents[1] / "hb_attendance_app/hbos_attendance/page/hbos_department_board/hbos_department_board.js").read_text()
        self.assertIn("data.stats", js)
        self.assertIn("data.dept_stats", js)
        # 前端不得再自带汇总实现（口径只能在后端一处）
        self.assertNotIn("function summarize(", js)
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_department_board.FrontendContractTest -v
```
Expected: FAIL

- [ ] **Step 3: 改前端**

删除 `function summarize(rows) {...}` 整段（含其上 3 行注释）。

`kpiHtml` 改为接服务端 stats：

```javascript
	function kpiHtml(s) {
		var rate = s.attendance_rate;
		var h = '<div class="db-kpi">';
		h += '<div class="db-hero"><div class="v db-num">' + (rate == null ? "—" : rate + "%") + '</div>'
			+ '<div class="k">' + __("出勤率") + '</div></div>';
		h += '<div class="db-kpis db-num">';
		[["应出勤", s.expected, ""], ["已到岗", s.present, ""],
		 ["迟到", s.late, "bad"], ["无打卡记录", s.noCard, "warn"],
		 ["缺勤", s.absent, "bad"], ["请假", s.leave, ""],
		 ["休息", s.rest, ""], ["豁免", s.exempt, ""]].forEach(function (c) {
			h += '<div class="db-kpi-i ' + c[2] + '"><div class="v">' + c[1] + '</div><div class="k">' + __(c[0]) + '</div></div>';
		});
		h += '</div></div>';
		return h;
	}
```

`deptTableHtml` 改为接服务端部门汇总（并删除其内部对 `summarize` 的调用）：

```javascript
	function deptTableHtml(deptStats, byDept) {
		var h = '<div class="db-sec"><h5>' + __("部门概览") + '</h5>'
			+ '<span class="hint">' + __("点击部门行展开人员明细") + '</span></div>';
		h += '<table class="db-tbl"><thead><tr>'
			+ '<th>' + __("部门") + '</th>'
			+ '<th class="r">' + __("在册") + '</th><th class="r">' + __("应出勤") + '</th>'
			+ '<th class="r">' + __("已到岗") + '</th><th class="r">' + __("迟到") + '</th>'
			+ '<th class="r">' + __("无打卡") + '</th><th class="r">' + __("缺勤") + '</th>'
			+ '<th class="r">' + __("请假") + '</th>'
			+ '</tr></thead><tbody>';
		deptStats.forEach(function (d) {
			var open = !!state.openDepts[d.dept];
			h += '<tr class="db-dept-row' + (open ? " open" : "") + '" data-dept="' + frappe.utils.escape_html(d.dept) + '">';
			h += '<td class="nm"><span class="caret">▶</span> ' + frappe.utils.escape_html(d.dept) + '</td>';
			h += '<td class="r db-num">' + d.total + '</td>';
			h += '<td class="r db-num">' + d.expected + '</td>';
			h += '<td class="r db-num">' + d.present + '</td>';
			h += '<td class="r db-num">' + (d.late ? '<span class="db-strong" style="color:#b3261e;">' + d.late + '</span>' : '<span class="db-zero">0</span>') + '</td>';
			h += '<td class="r db-num">' + (d.noCard ? '<span class="db-strong" style="color:#b26a00;">' + d.noCard + '</span>' : '<span class="db-zero">0</span>') + '</td>';
			h += '<td class="r db-num">' + (d.absent ? '<span class="db-strong" style="color:#b3261e;">' + d.absent + '</span>' : '<span class="db-zero">0</span>') + '</td>';
			h += '<td class="r db-num">' + (d.leave ? d.leave : '<span class="db-zero">0</span>') + '</td>';
			h += '</tr>';
			if (open) h += detailRowHtml(byDept[d.dept] || [], 8);
		});
		h += '</tbody></table>';
		return h;
	}
```

`render` 里相应改为：

```javascript
		var h = kpiHtml(data.stats || {});
		if (!rows.length) {
			h += '<div class="db-empty">' + __("当前范围无在册员工") + '</div>';
		} else if (d.scope && d.scope !== "全部部门") {
			h += flatTableHtml(rows);          // 单部门：直接明细，不要多余的汇总层
		} else {
			h += deptTableHtml(data.dept_stats || [], groupByDept(rows));
		}
```

同时清理只被 `summarize` 使用的死代码。当前引用关系（已核对）：

| 标识 | 谁在用 | 处理 |
| --- | --- | --- |
| `PRESENT_STATES` | 仅 `isPresent` | **删除** |
| `isPresent` | 仅 `summarize` | **删除** |
| `isShift` | 仅 `summarize` | **删除** |
| `ATTENTION_STATES` | `needsAttention` | **保留** |
| `hasTag` | `isLate` | **保留** |
| `isLate` | `needsAttention` | **保留** |
| `needsAttention` | `detailSort`（明细排序） | **保留** |
| `detailSort` | `detailRowHtml` / `flatTableHtml` | **保留** |

删除后运行 Step 4 的 `node --check` 与契约测试确认无残留引用。

- [ ] **Step 4: 运行全量离线测试 + JS 语法检查**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && node --check apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_department_board/hbos_department_board.js && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -5
```
Expected: JS OK；测试 OK

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_department_board/hbos_department_board.js apps/hb_attendance_app/tests/test_department_board.py
git commit -m "refactor: 部门看板改用服务端 stats/dept_stats，移除前端自有汇总"
```

---

### Task 4: 通知渲染纯函数（消息文本 + 请求体）

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py`
- Test: `apps/hb_attendance_app/tests/test_attendance_notify.py`

**Interfaces:**
- Consumes: Task 1 的 `dept_summary`（同一 dict 结构）。
- Produces（后续任务依赖，签名锁定）：
  - `render_report(date_str: str, hhmm: str, dept_stats: list[dict]) -> str` — 返回可直接发群的多行纯文本
  - `build_payload(date_str: str, generated_at: str, text: str, dept_stats: list[dict]) -> dict` — 返回待 POST 的 JSON

- [ ] **Step 1: 写失败测试**

```python
import unittest

from hb_attendance_app.hbos_attendance.attendance_notify import render_report, build_payload


def d(dept, expected, present, no_card, late=0):
    return {"dept": dept, "total": expected, "expected": expected, "present": present,
            "noCard": no_card, "late": late, "absent": 0, "leave": 0}


class RenderReportTest(unittest.TestCase):
    def test_contains_title_and_all_depts(self):
        text = render_report("2026-09-10", "09:00", [d("无菌车间", 60, 58, 2), d("厂外QC", 55, 53, 2)])
        self.assertIn("【考勤到岗】2026-09-10 09:00", text)
        self.assertIn("无菌车间", text)
        self.assertIn("厂外QC", text)
        self.assertIn("应出勤", text)
        self.assertIn("已到岗", text)
        self.assertIn("未打卡", text)
        self.assertIn("迟到", text)

    def test_no_total_row(self):
        text = render_report("2026-09-10", "09:00", [d("A", 10, 9, 1)])
        # 不含全厂汇总行（Owner 确认只看部门）
        self.assertNotIn("全厂", text)

    def test_dept_with_zero_expected_skipped(self):
        text = render_report("2026-09-10", "09:00", [d("全员休息部", 0, 0, 0), d("A", 10, 9, 1)])
        self.assertNotIn("全员休息部", text)
        self.assertIn("A", text)

    def test_empty_stats_returns_only_title(self):
        text = render_report("2026-09-10", "09:00", [])
        self.assertIn("【考勤到岗】", text)
        self.assertIn("暂无应出勤", text)

    def test_zero_values_render_as_zero(self):
        text = render_report("2026-09-10", "09:00", [d("A", 10, 10, 0, 0)])
        line = [l for l in text.splitlines() if l.startswith("A")][0]
        self.assertIn("0", line)


class BuildPayloadTest(unittest.TestCase):
    def test_payload_shape(self):
        stats = [d("A", 10, 9, 1)]
        p = build_payload("2026-09-10", "2026-09-10 09:00:03", "TEXT", stats)
        self.assertEqual(p["type"], "attendance_daily")
        self.assertEqual(p["date"], "2026-09-10")
        self.assertEqual(p["generated_at"], "2026-09-10 09:00:03")
        self.assertEqual(p["text"], "TEXT")
        self.assertEqual(len(p["dept_stats"]), 1)
        self.assertEqual(p["dept_stats"][0]["dept"], "A")

    def test_payload_dept_stats_has_snake_keys(self):
        # 传输层统一 snake_case，便于 OpenClaw 侧消费
        p = build_payload("2026-09-10", "x", "t", [d("A", 10, 9, 1)])
        self.assertEqual(sorted(p["dept_stats"][0].keys()),
                         ["absent", "dept", "expected", "late", "leave", "no_card", "present", "total"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify -v
```
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现渲染部分（本任务只做纯函数，不发 HTTP）**

在新建的 `attendance_notify.py` 中先实现（**文件顶部只 import 标准库，不 import frappe**，
HTTP 与 frappe 依赖在 Task 5 追加）：

```python
"""考勤通知：把当日部门到岗统计渲染成群消息并（出站）推送。

本模块的渲染与请求体构造是纯函数（无 frappe / 无网络），便于离线测试；
发送与调度在 Task 5 追加，依赖 frappe 与 requests。
"""
import os  # noqa: F401  (Task 5 使用；本任务保持模块可独立导入)

TITLE_PREFIX = "【考勤到岗】"
EMPTY_HINT = "（今日暂无应出勤人员）"
NO_CARD_HINT = "（晚班 20:00 / 夜班 0:00 上班，未到班次时间不计入未打卡）"

_DEPT_COL = 20          # 部门名显示宽度（西文按 1，中文按 2 计算）
_NUM_COLS = [("应出勤", 6), ("已到岗", 6), ("未打卡", 6), ("迟到", 5)]


def _width(s):
    """中文按 2 个字符宽度计算，用于纯文本表格对齐。"""
    return sum(2 if ord(ch) > 0x2E80 else 1 for ch in s)


def _pad(s, w):
    return s + " " * max(0, w - _width(s))


def render_report(date_str, hhmm, dept_stats):
    """渲染群消息文本；不含全厂汇总行（Owner 确认只看部门）。"""
    lines = ["%s%s %s" % (TITLE_PREFIX, date_str, hhmm), ""]
    rows = [d for d in dept_stats if (d.get("expected") or 0) > 0]
    if not rows:
        lines.append(EMPTY_HINT)
        return "\n".join(lines)
    header = _pad("部门", _DEPT_COL) + "".join(_pad(t, w) for t, w in _NUM_COLS)
    lines.append(header.rstrip())
    for d in rows:
        lines.append(
            _pad(d["dept"], _DEPT_COL)
            + _pad(str(d.get("expected", 0)), _NUM_COLS[0][1])
            + _pad(str(d.get("present", 0)), _NUM_COLS[1][1])
            + _pad(str(d.get("noCard", 0)), _NUM_COLS[2][1])
            + _pad(str(d.get("late", 0)), _NUM_COLS[3][1])
        )
    lines.append("")
    lines.append(NO_CARD_HINT)
    return "\n".join(lines)


def _to_snake(d):
    return {
        "dept": d.get("dept"),
        "total": d.get("total", 0),
        "expected": d.get("expected", 0),
        "present": d.get("present", 0),
        "no_card": d.get("noCard", 0),
        "late": d.get("late", 0),
        "absent": d.get("absent", 0),
        "leave": d.get("leave", 0),
    }


def build_payload(date_str, generated_at, text, dept_stats):
    """构造待 POST 的 JSON 体。"""
    return {
        "type": "attendance_daily",
        "date": date_str,
        "generated_at": generated_at,
        "title": "%s%s %s" % (TITLE_PREFIX, date_str, generated_at[11:16]),
        "text": text,
        "dept_stats": [_to_snake(d) for d in dept_stats],
    }
```

- [ ] **Step 4: 运行确认通过**

Run: 同 Step 2
Expected: PASS（7 个用例）

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py apps/hb_attendance_app/tests/test_attendance_notify.py
git commit -m "feat: 新增考勤通知渲染纯函数（消息文本与请求体）"
```

---

### Task 5: 发送函数 + 定时任务 + 时区守卫 + 幂等

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py`（追加 frappe 相关部分）
- Modify: `apps/hb_attendance_app/hb_attendance_app/hooks.py`（新增 cron）
- Modify: `apps/hb_attendance_app/tests/test_attendance_notify.py`（契约断言）

**Interfaces:**
- Consumes: Task 4 的 `render_report` / `build_payload`；Task 2 的 `get_data()`（取 `dept_stats`）。
- Produces:
  - `should_send_now(now_bj) -> bool`（纯函数：北京时间小时 == 9）
  - `already_sent(date_str) -> bool` / `mark_sent(date_str)`（frappe.cache）
  - `post_to_webhook(url, token, payload) -> tuple[bool, str]`（返回成功标志与说明；不抛）
  - `send_daily_report()`（scheduler 入口；供 `hooks.py` 引用）

- [ ] **Step 1: 先加契约与纯函数测试（追加到 test_attendance_notify.py）**

```python
class ShouldSendNowTest(unittest.TestCase):
    def test_only_nine_am_beijing(self):
        from datetime import datetime
        from hb_attendance_app.hbos_attendance.attendance_notify import should_send_now
        self.assertTrue(should_send_now(datetime(2026, 9, 10, 9, 0)))
        self.assertTrue(should_send_now(datetime(2026, 9, 10, 9, 59)))
        self.assertFalse(should_send_now(datetime(2026, 9, 10, 8, 59)))
        self.assertFalse(should_send_now(datetime(2026, 9, 10, 10, 0)))
        self.assertFalse(should_send_now(datetime(2026, 9, 10, 17, 0)))  # 容器 UTC 误触发


class ModuleContractTest(unittest.TestCase):
    def test_exposes_scheduler_entry_and_endpoint(self):
        from pathlib import Path
        src = (Path(__file__).parents[1]
               / "hb_attendance_app/hbos_attendance/attendance_notify.py").read_text()
        self.assertIn("def send_daily_report(", src)
        self.assertIn("def post_to_webhook(", src)
        self.assertIn("HBOS_NOTIFY_WEBHOOK_URL", src)
        self.assertIn("HBOS_NOTIFY_TOKEN", src)
        self.assertIn("HBOS_NOTIFY_DRY_RUN", src)
        self.assertIn("hbos_notify_sent:", src)

    def test_hooks_registers_nine_am_cron(self):
        from pathlib import Path
        src = (Path(__file__).parents[1] / "hb_attendance_app/hooks.py").read_text()
        self.assertIn("attendance_notify.send_daily_report", src)
        self.assertIn('"0 1 * * *"', src)   # UTC 容器下的北京 09:00
        self.assertIn('"0 9 * * *"', src)   # 北京时区容器
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify -v
```
Expected: FAIL（`should_send_now` 不存在等）

- [ ] **Step 3: 追加实现**

在 `attendance_notify.py` 末尾追加（`should_send_now` 为纯函数，放在纯函数区；其余为 frappe 部分）：

```python
def should_send_now(now_bj):
    """仅北京时间 09:00-09:59 触发发送（cron 时刻不可信, 以守卫为准）。"""
    return now_bj.hour == 9


# ---- 以下依赖 frappe / requests ----

def _bj_now():
    from datetime import datetime, timedelta, timezone
    return datetime.now(timezone(timedelta(hours=8))).replace(tzinfo=None)


def _sent_key(date_str):
    return "hbos_notify_sent:%s" % date_str


def already_sent(date_str):
    import frappe
    return bool(frappe.cache.get_value(_sent_key(date_str)))


def mark_sent(date_str):
    import frappe
    frappe.cache.set_value(_sent_key(date_str), "1")


def post_to_webhook(url, token, payload):
    """POST 到 OpenClaw；永不抛异常，返回 (是否成功, 说明)。"""
    import requests
    headers = {"Content-Type": "application/json"}
    if token:
        headers["Authorization"] = "Bearer %s" % token
    try:
        r = requests.post(url, json=payload, headers=headers, timeout=15)
        if 200 <= r.status_code < 300:
            return True, "HTTP %s" % r.status_code
        return False, "HTTP %s: %s" % (r.status_code, (r.text or "")[:200])
    except Exception as e:
        return False, str(e)


def send_daily_report(force=False):
    """scheduler 入口：算当日部门统计 → 渲染 → 出站推送。

    守卫：北京时间 09:00-09:59（force=True 时跳过，供干跑验证）；
    幂等：同日已发送则跳过；未配 webhook 静默跳过；
    异常：只记 Error Log，不抛（不影响调度器）。
    """
    import frappe
    from hb_attendance_app.hbos_attendance.page.hbos_department_board.department_board_data import get_data

    try:
        now = _bj_now()
        date_str = now.strftime("%Y-%m-%d")
        if not force and not should_send_now(now):
            return {"skipped": "not_9am_bj"}
        if not force and already_sent(date_str):
            return {"skipped": "already_sent"}

        data = get_data(department=None, date_str=date_str)
        dept_stats = data.get("dept_stats") or []
        text = render_report(date_str, now.strftime("%H:%M"), dept_stats)
        payload = build_payload(date_str, now.strftime("%Y-%m-%d %H:%M:%S"), text, dept_stats)

        dry = os.environ.get("HBOS_NOTIFY_DRY_RUN", "") == "1"
        url = os.environ.get("HBOS_NOTIFY_WEBHOOK_URL", "")
        if dry or not url:
            frappe.log_error(text, "HBOS考勤通知(未发送: %s)" % ("dry_run" if dry else "no_webhook"))
            if dry:
                mark_sent(date_str)
            return {"sent": False, "reason": "dry_run" if dry else "no_webhook", "text": text}

        ok, msg = post_to_webhook(url, os.environ.get("HBOS_NOTIFY_TOKEN", ""), payload)
        if ok:
            mark_sent(date_str)
            return {"sent": True, "detail": msg}
        frappe.log_error("%s\n%s" % (msg, text), "HBOS考勤通知发送失败")
        return {"sent": False, "reason": msg}
    except Exception as e:
        frappe.log_error(str(e), "HBOS考勤通知异常")
        return {"sent": False, "reason": str(e)}
```

在 `hooks.py` 的 `scheduler_events["cron"]` 中新增两项：

```python
        "0 1 * * *": [
            "hb_attendance_app.hbos_attendance.attendance_notify.send_daily_report"
        ],
        "0 9 * * *": [
            "hb_attendance_app.hbos_attendance.attendance_notify.send_daily_report"
        ],
```

- [ ] **Step 4: 运行确认通过 + 全量**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m compileall -q apps/hb_attendance_app && python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -5
```
Expected: OK

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py apps/hb_attendance_app/hb_attendance_app/hooks.py apps/hb_attendance_app/tests/test_attendance_notify.py
git commit -m "feat: 考勤通知出站推送与每日 9 点定时（含时区守卫与幂等）"
```

---

### Task 6: 运行态干跑验证与文档（需 Owner 授权）

> 运行态命令涉及活站点，**必须先获 Owner 明确授权**。未授权前只完成代码与文档。

**Files:**
- Modify: `docs/HBOS考勤判定规则.md`（追加「考勤通知」一节，说明口径与开关）
- Modify: `docs/PROJECT_STATUS.md`（M1-FIX-B5 追加行）
- Modify: `docs/CURRENT_MILESTONE.md`（M1-FIX-B5 段追加）

- [ ] **Step 1: 确认 CI 级离线验证**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m compileall -q apps/hb_attendance_app; python3 -m unittest discover -s apps/hb_attendance_app/tests -v 2>&1 | tail -20
```
Expected: 全部 PASS（含原 189 例 + 本批新增）

- [ ] **Step 2: 干跑（Owner 授权后执行）**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
docker compose exec -T backend bench --site frontend execute \
  hb_attendance_app.hbos_attendance.attendance_notify.send_daily_report --kwargs '{"force": true}'
```
（若 `bench execute` 不支持 kwargs，改用 console 调用 `send_daily_report(force=True)`）

Expected：返回 `{"sent": False, "reason": "no_webhook"...}`（未配地址时），
且在 Desk 的 **Error Log** 中看到以 `HBOS考勤通知(未发送: no_webhook)` 为标题的一条，
其内容为完整消息文本。

- [ ] **Step 3: 核对干跑内容与看板一致（Owner 授权后，人工）**

在同日打开「部门看板」逐项比对：**每个部门的 应出勤 / 已到岗 / 未打卡 / 迟到 四列必须与看板一致**
（因同源同实现）。若不一致 → 停下排查，不得继续。

- [ ] **Step 4: 配置真实 webhook 后实发（Owner 授权后，需提供地址）**

`HBOS_NOTIFY_WEBHOOK_URL` 写入本机 `.env` 并重启 backend 容器后，再次执行 Step 2 的命令
（`force=True`）确认群内收到消息；随后确认次日 09:00 自动触发（检查 Error Log 无异常、
`hbos_notify_sent:<日期>` 已置位）。

- [ ] **Step 5: 更新文档**

- `docs/HBOS考勤判定规则.md` 追加「考勤通知」一节：口径来源 `board_stats`、
  9 点口径（`before_start` 不计未打卡）、三个环境变量、dry-run 用法、恢复/关闭方式
  （不配 webhook 即静默不发送）。
- `docs/PROJECT_STATUS.md` / `docs/CURRENT_MILESTONE.md` 的 M1-FIX-B5 段追加一句
  「2026-09-10 追加：部门看板统计口径收敛至 board_stats；新增每日 9:00 出站考勤通知
  （推送到云端 OpenClaw，未配 webhook 静默跳过；本机不开入站端口）」。

- [ ] **Step 6: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add docs/HBOS考勤判定规则.md docs/PROJECT_STATUS.md docs/CURRENT_MILESTONE.md
git commit -m "docs: 记录考勤通知口径与开关（M1-FIX-B5 追加）"
```

- [ ] **Step 7: 收尾报告**

输出：新增/修改文件清单、离线测试结果、干跑输出（含消息全文）、看板比对结论、
是否已实发到群、以及**尚未完成的事项**（等待 webhook 地址）。
