# 调休模块（第一阶段：同步 + LLM 解析 + 核实 + 落库）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让飞书调休审批进入系统并完成「加班日提取 → 打卡核实」，产出一份可人工复核的清单；**本阶段不改变任何考勤结果**。

**Architecture:** 纯逻辑进 `rest_leave.py`（无 frappe 依赖，可离线单测）；IO 与落库进 `sync_rest_leave.py`。三段分离：同步（`*/30`）→ LLM 解析加班日并落库 → 核实（对比 `Attendance` 得出结论）。判定热路径不调用 LLM。

**Tech Stack:** Frappe v16 / Python 3.10+ / unittest（`python -m unittest`）/ 飞书多维表格 API / OpenAI 兼容 LLM（复用 `ai_review.py` 通道）

## Global Constraints

- 基准设计：`docs/superpowers/specs/2026-09-21-调休接入考勤判定设计.md`（本计划实现其 §3–§7、§10；**§8 判定豁免不在本阶段**）
- **不修改** `pairing.py`、`board_stats.py`、`api.py`、月度汇总报表、考勤结果报表
- **不修改** Frappe / ERPNext / HRMS 核心源码
- **不提交** 真实员工姓名、工号、`.env`、密钥、Excel/CSV、数据库、日志、运行时产物
- DocType 名沿用 `HBOS Rest Leave Record`（同名旧 DocType 从未 migrate，无历史数据）
- 飞书常量沿用：`app_token=XLD0bPiGXaP0JTsicHCcSLA4nlQ`、`table_id=tblYHBgNJdvVCiFM`
- 去重键前缀沿用：`feishu-bitable-restleave-`
- 调休日 = 日期字段「开始~结束」区间（**不使用** `调休天数` 字段做判定）
- 状态语义：`待解析 / 待核实 / 已核实 / 核实不通过 / 解析失败`
- 测试命令（仓库根目录执行）：
  ```bash
  PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py'
  ```
- 现有基线：311 个用例全绿。每个 Task 结束时该命令必须仍全绿。
- 提交信息用中文（可带 `feat:` / `fix:` / `docs:` 前缀）

---

### Task 1: 调休纯逻辑模块

新建 `rest_leave.py`，把调休的字段映射、日期展开、加班日 prompt/解析、核实判定全部收进来。这是后续所有任务的地基，且能在无 bench、无网络的 CI 里跑。

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave.py`
- Test: `apps/hb_attendance_app/tests/test_rest_leave.py`

**Interfaces:**
- Consumes: 无
- Produces:
  - 常量 `REST_LEAVE_APP_TOKEN`、`REST_LEAVE_TABLE_ID`、`ID_PREFIX`
  - 状态常量 `PARSE_PENDING="待解析"`、`VERIFY_PENDING="待核实"`、`VERIFY_OK="已核实"`、`VERIFY_FAIL="核实不通过"`、`VERIFY_PARSE_FAIL="解析失败"`
  - `ts_to_date(ms) -> str | None`
  - `rest_leave_fields(fields: dict) -> dict | None`，键为 `employee_number/employee_name/start_date/end_date/rest_days/remarks`
  - `expand_dates(start: str, end: str) -> list[str]`
  - `build_overtime_prompt(emp_name: str, emp_num: str, remarks: str, year: str) -> str`
  - `parse_overtime_dates(text: str, year: str) -> list[str]`
  - `verify_status_for(overtime_dates: list[str], paired_dates: set[str]) -> str`

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave.py`：

```python
import unittest

from hb_attendance_app.hbos_attendance.rest_leave import (
    VERIFY_FAIL, VERIFY_OK, VERIFY_PARSE_FAIL,
    build_overtime_prompt, expand_dates, parse_overtime_dates,
    rest_leave_fields, ts_to_date, verify_status_for,
)

MS_0901 = 1788192000000  # 2026-09-01 00:00:00 +08，与 test_swap_mapping 同源常量


class TsToDateTest(unittest.TestCase):
    def test_normal(self):
        self.assertEqual(ts_to_date(MS_0901), "2026-09-01")

    def test_empty_and_zero(self):
        self.assertIsNone(ts_to_date(0))
        self.assertIsNone(ts_to_date(None))
        self.assertIsNone(ts_to_date(""))

    def test_bad_value(self):
        self.assertIsNone(ts_to_date("not-a-ts"))


class RestLeaveFieldsTest(unittest.TestCase):
    def test_maps_all_columns(self):
        f = {"调休人员_姓名": "张三", "调休人员_工号": "10001",
             "调休人员_开始时间": MS_0901, "调休人员_结束时间": MS_0901,
             "调休人员_调休天数": "1", "说明": "加班换休"}
        out = rest_leave_fields(f)
        self.assertEqual(out["employee_number"], "10001")
        self.assertEqual(out["employee_name"], "张三")
        self.assertEqual(out["start_date"], "2026-09-01")
        self.assertEqual(out["end_date"], "2026-09-01")
        self.assertEqual(out["rest_days"], 1.0)
        self.assertEqual(out["remarks"], "加班换休")

    def test_missing_person_returns_none(self):
        self.assertIsNone(rest_leave_fields({"调休人员_开始时间": MS_0901}))
        self.assertIsNone(rest_leave_fields({"调休人员_工号": "1"}))

    def test_missing_dates_returns_none(self):
        self.assertIsNone(rest_leave_fields(
            {"调休人员_姓名": "张三", "调休人员_工号": "10001"}))

    def test_end_date_defaults_to_start(self):
        f = {"调休人员_姓名": "张三", "调休人员_工号": "10001",
             "调休人员_开始时间": MS_0901}
        self.assertEqual(rest_leave_fields(f)["end_date"], "2026-09-01")

    def test_days_dirty_value_falls_back_to_zero(self):
        """调休天数是 Text 字段，脏值不得抛异常（否则整次同步中断）。"""
        for bad in ("abc", "", None, " ", "一小时"):
            with self.subTest(bad=bad):
                f = {"调休人员_姓名": "张三", "调休人员_工号": "10001",
                     "调休人员_开始时间": MS_0901, "调休人员_调休天数": bad}
                self.assertEqual(rest_leave_fields(f)["rest_days"], 0.0)

    def test_phone_not_leaked(self):
        """联系电话与考勤无关，不得进入产出字段。"""
        f = {"调休人员_姓名": "张三", "调休人员_工号": "10001",
             "调休人员_开始时间": MS_0901, "调休人员_联系电话": "13800000000"}
        out = rest_leave_fields(f)
        self.assertNotIn("13800000000", str(out))


class ExpandDatesTest(unittest.TestCase):
    def test_single_day(self):
        self.assertEqual(expand_dates("2026-08-18", "2026-08-18"), ["2026-08-18"])

    def test_multi_day_inclusive(self):
        self.assertEqual(expand_dates("2026-08-13", "2026-08-14"),
                         ["2026-08-13", "2026-08-14"])

    def test_span_overrides_day_count(self):
        """09-06→09-11 跨 6 天只申报 1 天：仍按区间展开 6 天。"""
        out = expand_dates("2026-09-06", "2026-09-11")
        self.assertEqual(len(out), 6)
        self.assertEqual(out[0], "2026-09-06")
        self.assertEqual(out[-1], "2026-09-11")

    def test_end_defaults_to_start(self):
        self.assertEqual(expand_dates("2026-08-18", None), ["2026-08-18"])

    def test_bad_input_returns_empty(self):
        self.assertEqual(expand_dates(None, None), [])
        self.assertEqual(expand_dates("不是日期", "2026-08-18"), [])


class ParseOvertimeDatesTest(unittest.TestCase):
    def test_iso_format(self):
        self.assertEqual(parse_overtime_dates("2026-08-02\n2026-08-16", "2026"),
                         ["2026-08-02", "2026-08-16"])

    def test_chinese_format_uses_year_hint(self):
        self.assertEqual(parse_overtime_dates("8月2日", "2026"), ["2026-08-02"])

    def test_dedupes(self):
        self.assertEqual(parse_overtime_dates("2026-08-02\n2026-08-02", "2026"),
                         ["2026-08-02"])

    def test_no_date_returns_empty(self):
        self.assertEqual(parse_overtime_dates("无", "2026"), [])
        self.assertEqual(parse_overtime_dates("", "2026"), [])
        self.assertEqual(parse_overtime_dates(None, "2026"), [])


class BuildPromptTest(unittest.TestCase):
    def test_includes_identity_and_remarks(self):
        p = build_overtime_prompt("张三", "10001", "8月2日加班，调休到8月3日", "2026")
        self.assertIn("张三", p)
        self.assertIn("10001", p)
        self.assertIn("8月2日加班，调休到8月3日", p)
        self.assertIn("2026", p)


class VerifyStatusTest(unittest.TestCase):
    def test_all_paired_is_ok(self):
        self.assertEqual(verify_status_for(["2026-08-02"], {"2026-08-02"}), VERIFY_OK)

    def test_missing_pair_fails(self):
        self.assertEqual(verify_status_for(["2026-08-02"], set()), VERIFY_FAIL)

    def test_partial_fails(self):
        self.assertEqual(
            verify_status_for(["2026-08-02", "2026-08-16"], {"2026-08-02"}),
            VERIFY_FAIL)

    def test_no_overtime_dates_is_parse_failure(self):
        self.assertEqual(verify_status_for([], {"2026-08-02"}), VERIFY_PARSE_FAIL)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave.py'
```
Expected: FAIL —`ModuleNotFoundError: No module named 'hb_attendance_app.hbos_attendance.rest_leave'`

- [ ] **Step 3: 实现模块**

> 关于 `expand_dates`：本阶段**没有调用方**——调休日目前由 DocType 的 `start_date`/`end_date` 直接承载，豁免接入是下一阶段（第二阶段把它并入传给 `pair_employee_checkins()` 的豁免集合）。此处先实现并测试，是因为它是设计 §4 的**核心业务规则**，先固定语义可避免第二阶段各写一份。这是对 YAGNI 的一次有意偏离，评审时请知悉。

创建 `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave.py`：

```python
"""调休纯逻辑：飞书字段映射、调休日展开、加班日解析与核实判定。

纯标准库模块，无 frappe / requests 依赖，可离线单测（与 board_stats.py 同模式）。
判定规则见 docs/superpowers/specs/2026-09-21-调休接入考勤判定设计.md §4。
"""
import re
from datetime import datetime, timedelta, timezone

# 飞书毫秒时间戳按北京时间转日期；用固定 +8 而非容器本地时区，
# 避免容器为 UTC 时把「当日」算成前一天（与 api.py DELICLOUD_TZ 同基准）。
_TZ = timezone(timedelta(hours=8))

REST_LEAVE_APP_TOKEN = "XLD0bPiGXaP0JTsicHCcSLA4nlQ"
REST_LEAVE_TABLE_ID = "tblYHBgNJdvVCiFM"
ID_PREFIX = "feishu-bitable-restleave-"

# 状态机：待解析 → 待核实 → 已核实 / 核实不通过；解析不出加班日 → 解析失败
PARSE_PENDING = "待解析"
VERIFY_PENDING = "待核实"
VERIFY_OK = "已核实"
VERIFY_FAIL = "核实不通过"
VERIFY_PARSE_FAIL = "解析失败"
ALL_STATUSES = (PARSE_PENDING, VERIFY_PENDING, VERIFY_OK, VERIFY_FAIL, VERIFY_PARSE_FAIL)


def ts_to_date(ms):
    """飞书毫秒时间戳 → "YYYY-MM-DD"；空/0/异常返回 None。"""
    try:
        if not ms:
            return None
        return datetime.fromtimestamp(int(ms) / 1000, tz=_TZ).strftime("%Y-%m-%d")
    except Exception:
        return None


def _safe_float(v):
    """调休天数是 Text 字段（实测 115/115 返回 str），脏值回落 0 且不抛异常。

    原实现直接 float()，一条脏值即抛异常并中断整次同步（全批数据丢失）。
    """
    try:
        return float(str(v).strip())
    except (TypeError, ValueError):
        return 0.0


def rest_leave_fields(fields):
    """调休记录 → 待写入字段；缺人员或缺开始日期时返回 None。"""
    num = str(fields.get("调休人员_工号") or "").strip()
    name = str(fields.get("调休人员_姓名") or "").strip()
    start = ts_to_date(fields.get("调休人员_开始时间"))
    if not (num or name) or not start:
        return None
    return {
        "employee_number": num,
        "employee_name": name,
        "start_date": start,
        "end_date": ts_to_date(fields.get("调休人员_结束时间")) or start,
        "rest_days": _safe_float(fields.get("调休人员_调休天数")),
        "remarks": str(fields.get("说明") or ""),
    }
    # 注意：不返回「调休人员_联系电话」——与考勤无关的 PII，不抄第二份。


def expand_dates(start, end):
    """调休日 = [start, end] 闭区间逐日展开；异常输入返回 []。

    刻意不使用「调休天数」字段：实测 104 条中 16 条天数与日期跨度不符
    （如 09-06→09-11 跨 6 天只申报 1 天），用区间可解释、可复核。
    """
    try:
        d = datetime.strptime(str(start)[:10], "%Y-%m-%d").date()
        e = datetime.strptime(str(end or start)[:10], "%Y-%m-%d").date()
    except (TypeError, ValueError):
        return []
    if e < d:
        d, e = e, d
    out, cur = [], d
    while cur <= e:
        out.append(cur.strftime("%Y-%m-%d"))
        cur += timedelta(days=1)
    return out


_PROMPT_HEAD = (
    "你是海滨考勤助理。下面是一条【调休申请】的说明原文。\n"
    "请只提取「该员工本人」在这条申请里申报的【加班日期】。\n"
    "规则：\n"
    "1. 说明可能同时列出多个人的加班（含姓名），只取与【本人姓名】一致的那些；\n"
    "2. 若说明未出现任何姓名，视为本人；\n"
    "3. 日期可能写作「8月2日」「08.02」「8/2」等，一律换算成 YYYY-MM-DD；\n"
    "4. 年份以【申请年】为准；\n"
    "5. 找不到任何日期就只输出「无」；\n"
    "6. 只输出日期，一行一个，不要任何解释文字。\n"
    "注意：说明里提到「调休到 X 号」的日期是休息日，不是加班日，不要输出。\n"
)


def build_overtime_prompt(emp_name, emp_num, remarks, year):
    """构造提取加班日的 prompt。"""
    return (
        _PROMPT_HEAD
        + f"\n【本人姓名】{emp_name}\n【本人工号】{emp_num}\n【申请年】{year}\n"
        + f"【说明原文】\n{remarks}\n\n输出："
    )


def parse_overtime_dates(text, year):
    """解析 LLM 输出 → ["YYYY-MM-DD", ...]；无法解析返回 []。

    只认严格日期，不做模糊匹配——解析不出即「解析失败」，转人工，
    宁可漏判也不猜（猜错会把没加过的班认成加班）。
    """
    if not text:
        return []
    out = []
    for m in re.finditer(r"(\d{4})-(\d{2})-(\d{2})", str(text)):
        s = m.group(0)
        if s not in out:
            out.append(s)
    if out:
        return out
    # 容错：模型只回了 M月D日
    for m in re.finditer(r"(\d{1,2})\s*月\s*(\d{1,2})\s*日", str(text)):
        s = "%04d-%02d-%02d" % (int(year), int(m.group(1)), int(m.group(2)))
        if s not in out:
            out.append(s)
    return out


def verify_status_for(overtime_dates, paired_dates):
    """加班日是否都有完整配对 → 核实结论。

    paired_dates 由调用方提供（该员工当天存在完整上下班配对的日期集合，
    取自系统已生成的 Attendance）。任一加班日缺失即「核实不通过」。
    """
    if not overtime_dates:
        return VERIFY_PARSE_FAIL
    paired = paired_dates or set()
    return VERIFY_OK if all(d in paired for d in overtime_dates) else VERIFY_FAIL
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave.py'
```
Expected: `OK`（约 20 个用例）

- [ ] **Step 5: 跑全量，确认无回归**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`，用例总数 = 原 311 + 新增

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave.py apps/hb_attendance_app/tests/test_rest_leave.py
git commit -m "feat: 新增调休纯逻辑模块（日期展开、加班日解析、核实判定）"
```

---

### Task 2: 调休 DocType 增加自有字段

给 `HBOS Rest Leave Record` 加 4 个字段（3 个来自设计 §5，另加 `parsed_at` 用于防重复解析——见下）。

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_rest_leave_record/hbos_rest_leave_record.json`
- Test: `apps/hb_attendance_app/tests/test_rest_leave_doctype.py`（新建，取代 Task 6 将删除的 `test_rest_leave_swap_doctype.py`）

**Interfaces:**
- Consumes: Task 1 的状态常量值（`待解析/待核实/已核实/核实不通过/解析失败`）
- Produces: DocType `HBOS Rest Leave Record` 上的字段名 `overtime_dates`、`parsed_at`、`verify_status`、`verify_time`

**为何加 `parsed_at`（对设计 §5 的一处细化）**：设计列了 3 个字段。但「LLM 调用失败」与「尚未解析」必须区分，否则每次同步都会对同一条记录重复调 LLM（费用 + 延迟）。`parsed_at` 记录最后一次解析时刻，已解析过的记录不再重复调用。

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave_doctype.py`：

```python
import json
import unittest
from pathlib import Path

DT = (Path(__file__).parents[1]
      / "hb_attendance_app/hbos_attendance/doctype/hbos_rest_leave_record")


def load():
    return json.loads((DT / "hbos_rest_leave_record.json").read_text())


class RestLeaveDocTypeTest(unittest.TestCase):
    def test_identity(self):
        d = load()
        self.assertEqual(d["name"], "HBOS Rest Leave Record")
        self.assertEqual(d["module"], "HBOS Attendance")
        self.assertEqual(d["autoname"], "field:feishu_approval_id")

    def test_leave_template_fields(self):
        """模板与请假一致：这些字段一个都不能少。"""
        fields = [f["fieldname"] for f in load()["fields"]]
        for f in ("employee", "employee_name", "employee_number", "department",
                  "start_date", "end_date", "rest_days", "feishu_approval_id",
                  "approval_status", "feishu_sync_time", "remarks"):
            self.assertIn(f, fields, f"缺少模板字段 {f}")

    def test_own_fields(self):
        fields = [f["fieldname"] for f in load()["fields"]]
        for f in ("overtime_dates", "parsed_at", "verify_status", "verify_time"):
            self.assertIn(f, fields, f"缺少自有字段 {f}")

    def test_verify_status_options_match_pure_module(self):
        """Select 选项必须与 rest_leave.py 的常量逐字一致，否则落库会报错。"""
        from hb_attendance_app.hbos_attendance.rest_leave import ALL_STATUSES
        f = [x for x in load()["fields"] if x["fieldname"] == "verify_status"][0]
        self.assertEqual(f["fieldtype"], "Select")
        self.assertEqual(set(f["options"].split("\n")), set(ALL_STATUSES))

    def test_default_status_is_parse_pending(self):
        f = [x for x in load()["fields"] if x["fieldname"] == "verify_status"][0]
        self.assertEqual(f["default"], "待解析")

    def test_feishu_id_unique(self):
        f = [x for x in load()["fields"] if x["fieldname"] == "feishu_approval_id"][0]
        self.assertEqual(f.get("unique"), 1)

    def test_three_role_permissions(self):
        roles = sorted(p["role"] for p in load()["permissions"])
        self.assertEqual(roles, ["HR Manager", "HR User", "System Manager"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_doctype.py'
```
Expected: FAIL — `test_own_fields` 与 `test_verify_status_options_match_pure_module` 报缺少字段

- [ ] **Step 3: 改 DocType json**

编辑 `doctype/hbos_rest_leave_record/hbos_rest_leave_record.json`：

把 `field_order` 改为（在 `remarks` 之后追加核实区）：

```json
 "field_order": [
  "employee",
  "employee_name",
  "employee_number",
  "department",
  "column_break_1",
  "start_date",
  "end_date",
  "rest_days",
  "section_break_6",
  "feishu_approval_id",
  "approval_status",
  "feishu_sync_time",
  "remarks",
  "section_break_verify",
  "overtime_dates",
  "parsed_at",
  "verify_status",
  "verify_time"
 ],
```

在 `fields` 数组末尾（`remarks` 项之后）追加：

```json
  {"fieldname": "section_break_verify", "fieldtype": "Section Break", "label": "加班核实"},
  {"description": "由 LLM 从「说明」提取，逗号分隔的 YYYY-MM-DD；供人工核对", "fieldname": "overtime_dates",
   "fieldtype": "Small Text", "in_list_view": 1, "label": "加班日", "read_only": 1},
  {"fieldname": "parsed_at", "fieldtype": "Datetime", "label": "解析时间", "read_only": 1},
  {"default": "待解析", "fieldname": "verify_status", "fieldtype": "Select", "in_list_view": 1,
   "in_standard_filter": 1, "label": "核实状态",
   "options": "待解析\n待核实\n已核实\n核实不通过\n解析失败", "read_only": 1},
  {"fieldname": "verify_time", "fieldtype": "Datetime", "label": "核实时间", "read_only": 1}
```

同时把 `"modified"` 改为当前时间字符串，例如 `"2026-09-21 12:00:00.000000"`。

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_doctype.py'
```
Expected: `OK`

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_rest_leave_record/hbos_rest_leave_record.json apps/hb_attendance_app/tests/test_rest_leave_doctype.py
git commit -m "feat: 调休 DocType 增加加班日与核实状态字段"
```

---

### Task 3: 同步改造（写 DocType、容错、可见失败）

重写 `sync_rest_leave.py`：改用 Task 1 的纯模块、去掉 `frappe.throw`（调度任务里抛异常会打断调度链）、把匹配不到员工计入返回摘要（不再静默丢弃）。

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py`（整体重写）
- Modify: `apps/hb_attendance_app/tests/test_sync_rest_leave.py`（整体重写）

**Interfaces:**
- Consumes: Task 1 的 `rest_leave_fields`、`ID_PREFIX`、`REST_LEAVE_APP_TOKEN`、`REST_LEAVE_TABLE_ID`、`PARSE_PENDING`；`api.py` 的 `_get_token`、`_fetch_all_records_custom`、`STATUS_MAP`
- Produces:
  - `sync_rest_leave_from_bitable() -> dict`，返回 `{"total","created","updated","skipped","unmatched","error"}`
  - 模块级 `DOCTYPE = "HBOS Rest Leave Record"`

- [ ] **Step 1: 重写契约测试**

把 `apps/hb_attendance_app/tests/test_sync_rest_leave.py` 整体替换为：

```python
import unittest
from pathlib import Path

SRC = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/sync_rest_leave.py")


class SyncRestLeaveContractTest(unittest.TestCase):
    def setUp(self):
        self.src = SRC.read_text()

    def test_uses_pure_module(self):
        self.assertIn("from hb_attendance_app.hbos_attendance.rest_leave import", self.src)
        self.assertIn("rest_leave_fields", self.src)

    def test_writes_rest_leave_doctype(self):
        self.assertIn('"HBOS Rest Leave Record"', self.src)

    def test_reads_correct_table(self):
        self.assertIn("REST_LEAVE_APP_TOKEN", self.src)
        self.assertIn("REST_LEAVE_TABLE_ID", self.src)

    def test_unique_key_prefix(self):
        self.assertIn("ID_PREFIX", self.src)

    def test_is_whitelisted_entry(self):
        self.assertIn("@frappe.whitelist()", self.src)
        self.assertIn("def sync_rest_leave_from_bitable(", self.src)

    def test_no_throw_in_scheduler_path(self):
        """调度任务里抛异常会打断调度链；失败必须记日志并返回摘要。"""
        self.assertNotIn("frappe.throw(", self.src)

    def test_reports_unmatched_instead_of_silent_skip(self):
        self.assertIn("unmatched", self.src)

    def test_status_map_reused_from_api(self):
        self.assertIn("STATUS_MAP", self.src)

    def test_does_not_touch_attendance_generation(self):
        """本阶段不接判定：不得触发配对/重算。"""
        self.assertNotIn("regenerate_attendance", self.src)
        self.assertNotIn("pair_employee_checkins", self.src)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_sync_rest_leave.py'
```
Expected: FAIL — 多个用例失败（仍 import `swap_mapping`、仍 `frappe.throw`、无 `unmatched`）

- [ ] **Step 3: 重写实现**

把 `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py` 整体替换为：

```python
"""飞书「调休」表 → HBOS Rest Leave Record。

与请假同步（api.sync_from_bitable）同模板：取 token → 分页拉全量 → 状态映射
→ 以 feishu_approval_id 为唯一键 upsert。另有取舍不同：
  * 日期区间按天展开后由判定侧处理，本文件只落 start/end 原始区间；
  * 失败记 Error Log 并写进返回摘要，**不在调度任务里 throw**；
  * 匹配不到员工的记录计入 unmatched，不再静默丢弃。
加班日的解析与核实分别见 parse_pending_rest_leaves / verify_pending_rest_leaves。
"""
import frappe

from hb_attendance_app.hbos_attendance.api import (
    STATUS_MAP, _fetch_all_records_custom, _get_token,
)
from hb_attendance_app.hbos_attendance.rest_leave import (
    ID_PREFIX, PARSE_PENDING, REST_LEAVE_APP_TOKEN, REST_LEAVE_TABLE_ID,
    rest_leave_fields,
)

DOCTYPE = "HBOS Rest Leave Record"


def _match_employee(num, name):
    """工号优先匹配，其次姓名；都匹配不到返回 None。"""
    if num:
        emp = frappe.db.get_value("Employee", {"employee_number": num}, "name")
        if emp:
            return emp
    if name:
        return frappe.db.get_value("Employee", {"employee_name": name}, "name")
    return None


@frappe.whitelist()
def sync_rest_leave_from_bitable():
    """同步飞书调休表。任何失败都记日志并返回摘要，不抛异常。"""
    summary = {"total": 0, "created": 0, "updated": 0,
               "skipped": 0, "unmatched": 0, "error": ""}
    try:
        token = _get_token()
        records = _fetch_all_records_custom(
            token, REST_LEAVE_APP_TOKEN, REST_LEAVE_TABLE_ID)
    except Exception as e:
        frappe.log_error(str(e), "飞书调休同步")
        summary["error"] = str(e)
        return summary

    summary["total"] = len(records)
    for record in records:
        fields = record.get("fields", {}) or {}
        rid = record.get("id", "")
        if not rid:
            summary["skipped"] += 1
            continue
        mapped = rest_leave_fields(fields)
        if not mapped:
            summary["skipped"] += 1
            continue
        emp = _match_employee(mapped["employee_number"], mapped["employee_name"])
        if not emp:
            # 不静默丢弃：匹配不到必须可见，否则调休会无声消失
            summary["unmatched"] += 1
            frappe.log_error(
                "调休记录匹配不到员工: %s / %s"
                % (mapped["employee_number"], mapped["employee_name"]),
                "飞书调休同步")
            continue

        aid = ID_PREFIX + rid
        exists = frappe.db.exists(DOCTYPE, {"feishu_approval_id": aid})
        doc = (frappe.get_doc(DOCTYPE, {"feishu_approval_id": aid}) if exists
               else frappe.get_doc({"doctype": DOCTYPE, "feishu_approval_id": aid}))
        doc.update({
            "employee": emp,
            "start_date": mapped["start_date"],
            "end_date": mapped["end_date"],
            "rest_days": mapped["rest_days"],
            "remarks": mapped["remarks"],
            "approval_status": STATUS_MAP.get(fields.get("申请状态", ""), "审批中"),
            "feishu_sync_time": frappe.utils.now_datetime(),
        })
        if not exists:
            # 新记录等待 LLM 解析加班日
            doc.verify_status = PARSE_PENDING
        elif doc.remarks != mapped["remarks"]:
            # 说明被改过 → 加班日需重新解析
            doc.verify_status = PARSE_PENDING
        try:
            doc.save(ignore_permissions=True)
            frappe.db.commit()
            if exists:
                summary["updated"] += 1
            else:
                summary["created"] += 1
        except Exception as e:
            frappe.log_error(str(e), "飞书调休写入失败")
            summary["skipped"] += 1

    return summary
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_sync_rest_leave.py'
```
Expected: `OK`

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py apps/hb_attendance_app/tests/test_sync_rest_leave.py
git commit -m "fix: 调休同步改用纯模块并容错落库，失败可见不再静默丢弃"
```

---

### Task 4: LLM 解析加班日并落库

对 `verify_status=待解析` 的记录调 LLM 提取加班日。**只在同步后跑，判定路径永不调用。**

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py`（追加函数）
- Test: `apps/hb_attendance_app/tests/test_rest_leave_parse.py`（新建）

**Interfaces:**
- Consumes: Task 1 的 `build_overtime_prompt`、`parse_overtime_dates`、`PARSE_PENDING`、`VERIFY_PENDING`、`VERIFY_PARSE_FAIL`；`ai_review.py` 的 `call_llm`、`env_config`
- Produces: `parse_pending_rest_leaves(limit=200) -> dict`，返回 `{"parsed","failed","remaining","error"}`

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave_parse.py`：

```python
import unittest
from pathlib import Path

SRC = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/sync_rest_leave.py")


class ParseStageContractTest(unittest.TestCase):
    """解析段必须：只处理待解析、只调一次 LLM、结果落库、失败留痕。"""

    def setUp(self):
        self.src = SRC.read_text()

    def test_entry_exists(self):
        self.assertIn("def parse_pending_rest_leaves(", self.src)

    def test_only_pending_records(self):
        self.assertIn("PARSE_PENDING", self.src)

    def test_uses_shared_llm_channel(self):
        """复用 ai_review 的通道与配置，不另起一套。"""
        self.assertIn("from hb_attendance_app.hbos_attendance.ai_review import", self.src)
        self.assertIn("call_llm", self.src)
        self.assertIn("env_config", self.src)

    def test_never_called_from_attendance_path(self):
        """判定热路径不得调用 LLM。"""
        api = (Path(__file__).parents[1]
               / "hb_attendance_app/hbos_attendance/api.py").read_text()
        self.assertNotIn("parse_pending_rest_leaves", api)
        self.assertNotIn("call_llm", api)

    def test_failure_marks_parse_failed(self):
        self.assertIn("VERIFY_PARSE_FAIL", self.src)

    def test_records_parsed_at(self):
        self.assertIn("parsed_at", self.src)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_parse.py'
```
Expected: FAIL — `test_entry_exists` 等报函数不存在

- [ ] **Step 3: 追加实现**

在 `sync_rest_leave.py` 末尾追加（并把 import 补到文件顶部）：

顶部 import 增加：

```python
from hb_attendance_app.hbos_attendance.ai_review import call_llm, env_config
from hb_attendance_app.hbos_attendance.rest_leave import (
    ID_PREFIX, PARSE_PENDING, REST_LEAVE_APP_TOKEN, REST_LEAVE_TABLE_ID,
    VERIFY_PARSE_FAIL, VERIFY_PENDING,
    build_overtime_prompt, parse_overtime_dates, rest_leave_fields,
)
```

（即把原 Task 3 的 rest_leave import 扩展为上表；`VERIFY_*` 与 `build/parse` 一并引入。）

文件末尾追加：

```python
def parse_pending_rest_leaves(limit=200):
    """对 verify_status=待解析 的记录调 LLM 提取加班日并落库。

    仅在同步之后调用。判定每 10 分钟重算一次，**绝不能在判定路径调用 LLM**：
    成本与延迟不可接受，且同样输入未必同样输出，判定必须基于稳定的落库结论。
    """
    summary = {"parsed": 0, "failed": 0, "remaining": 0, "error": ""}
    try:
        cfg = env_config()
    except Exception as e:
        frappe.log_error(str(e), "调休加班日解析")
        summary["error"] = str(e)
        return summary
    if not (cfg["base_url"] and cfg["api_key"] and cfg["model"]):
        summary["error"] = "未配置 AI（HBOS_AI_BASE_URL / HBOS_AI_API_KEY / HBOS_AI_MODEL）"
        return summary

    pending = frappe.db.get_all(
        DOCTYPE,
        filters={"verify_status": PARSE_PENDING},
        fields=["name", "employee_name", "employee_number", "remarks", "start_date"],
        limit_page_length=limit,
        order_by="creation asc",
    )
    for row in pending:
        year = str(row.start_date or "")[:4] or "2026"
        prompt = build_overtime_prompt(
            row.employee_name or "", row.employee_number or "",
            row.remarks or "", year)
        try:
            text = call_llm(cfg, prompt)
        except Exception as e:
            # 调用失败不写 parsed_at：下次同步可重试（与「解析不出日期」区分开）
            frappe.log_error("%s: %s" % (row.name, e), "调休加班日解析")
            summary["failed"] += 1
            continue

        dates = parse_overtime_dates(text, year)
        doc = frappe.get_doc(DOCTYPE, row.name)
        doc.parsed_at = frappe.utils.now_datetime()
        doc.overtime_dates = ",".join(dates)
        # 解析不出日期 → 解析失败，转人工；不豁免、不重试
        doc.verify_status = VERIFY_PENDING if dates else VERIFY_PARSE_FAIL
        try:
            doc.save(ignore_permissions=True)
            frappe.db.commit()
            summary["parsed" if dates else "failed"] += 1
        except Exception as e:
            frappe.log_error(str(e), "调休加班日写入失败")
            summary["failed"] += 1

    summary["remaining"] = frappe.db.count(
        DOCTYPE, {"verify_status": PARSE_PENDING})
    return summary
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_parse.py'
```
Expected: `OK`

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py apps/hb_attendance_app/tests/test_rest_leave_parse.py
git commit -m "feat: 调休加班日由 LLM 解析并落库，判定路径不调用 LLM"
```

---

### Task 5: 核实加班日

对 `verify_status=待核实` 的记录，查加班日当天该员工是否有**完整的上下班配对**，回写结论。**本阶段只写结论，不接豁免。**

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py`（追加函数）
- Test: `apps/hb_attendance_app/tests/test_rest_leave_verify.py`（新建）

**Interfaces:**
- Consumes: Task 1 的 `verify_status_for`、`VERIFY_PENDING`；`Attendance` 表的既有 `status` / `working_hours` 字段
- Produces: `verify_pending_rest_leaves(limit=500) -> dict`，返回 `{"verified","failed","skipped","remaining","error"}`

**核实判据**：加班日在 `tabAttendance` 中存在该员工的记录，且 `status='Present'` 且 `working_hours >= 2`。这样复用系统**已经算好的完整配对结果**，不在调休模块里另写一套「什么算配对」——两套定义迟早会漂移。

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave_verify.py`：

```python
import unittest
from pathlib import Path

SRC = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/sync_rest_leave.py")


class VerifyStageContractTest(unittest.TestCase):
    def setUp(self):
        self.src = SRC.read_text()

    def test_entry_exists(self):
        self.assertIn("def verify_pending_rest_leaves(", self.src)

    def test_reuses_attendance_judgment(self):
        """核实复用系统已生成的 Attendance，不另写一套配对定义。"""
        self.assertIn("tabAttendance", self.src)
        self.assertIn("Present", self.src)

    def test_uses_pure_verdict(self):
        self.assertIn("verify_status_for", self.src)

    def test_does_not_hook_into_attendance_generation(self):
        """本阶段不接豁免：不得触发重算，也不得进入判定。"""
        self.assertNotIn("regenerate_attendance", self.src)
        self.assertNotIn("pair_employee_checkins", self.src)

    def test_records_verify_time(self):
        self.assertIn("verify_time", self.src)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_verify.py'
```
Expected: FAIL — `test_entry_exists` 报函数不存在

- [ ] **Step 3: 追加实现**

`sync_rest_leave.py` 顶部 import 再补 `verify_status_for` 与 `VERIFY_OK`：

```python
from hb_attendance_app.hbos_attendance.rest_leave import (
    ID_PREFIX, PARSE_PENDING, REST_LEAVE_APP_TOKEN, REST_LEAVE_TABLE_ID,
    VERIFY_OK, VERIFY_PARSE_FAIL, VERIFY_PENDING,
    build_overtime_prompt, parse_overtime_dates, rest_leave_fields,
    verify_status_for,
)
```

文件末尾追加：

```python
def _present_dates(employee, dates):
    """该员工在这些日期里，哪些天有完整上下班配对（取系统已生成的 Attendance）。"""
    if not dates or not employee:
        return set()
    rows = frappe.db.sql(
        """
        SELECT attendance_date FROM `tabAttendance`
        WHERE employee = %s
          AND attendance_date IN %(dates)s
          AND status = 'Present'
          AND working_hours >= 2
        """,
        {"dates": tuple(dates)},
        as_dict=True,
    )
    return {str(r["attendance_date"]) for r in rows}


def verify_pending_rest_leaves(limit=500):
    """对 verify_status=待核实 的记录核实加班日，回写结论。

    只写结论，**不改考勤结果**——豁免接入是下一阶段的事。
    """
    summary = {"verified": 0, "failed": 0, "skipped": 0,
               "remaining": 0, "error": ""}
    pending = frappe.db.get_all(
        DOCTYPE,
        filters={"verify_status": VERIFY_PENDING},
        fields=["name", "employee", "overtime_dates"],
        limit_page_length=limit,
        order_by="creation asc",
    )
    for row in pending:
        dates = [d.strip() for d in str(row.overtime_dates or "").split(",") if d.strip()]
        if not dates:
            summary["skipped"] += 1
            continue
        try:
            paired = _present_dates(row.employee, dates)
        except Exception as e:
            frappe.log_error("%s: %s" % (row.name, e), "调休加班核实")
            summary["failed"] += 1
            continue

        doc = frappe.get_doc(DOCTYPE, row.name)
        doc.verify_status = verify_status_for(dates, paired)
        doc.verify_time = frappe.utils.now_datetime()
        try:
            doc.save(ignore_permissions=True)
            frappe.db.commit()
            if doc.verify_status == VERIFY_OK:
                summary["verified"] += 1
            else:
                summary["failed"] += 1
        except Exception as e:
            frappe.log_error(str(e), "调休加班核实写入失败")
            summary["failed"] += 1

    summary["remaining"] = frappe.db.count(
        DOCTYPE, {"verify_status": VERIFY_PENDING})
    return summary
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_verify.py'
```
Expected: `OK`

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py apps/hb_attendance_app/tests/test_rest_leave_verify.py
git commit -m "feat: 新增调休加班日核实（复用已生成的考勤配对结果）"
```

---

### Task 6: 摘除换班代码，注册调休调度

换班（`HBOS Shift Swap Record` / `sync_shift_swap`）与调休是同一批提交引入的，都从未 migrate。本任务一并摘除，并把调休的三段（同步 → 解析 → 核实）挂上调度。

**Files:**
- Delete: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_shift_swap.py`
- Delete: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_shift_swap_record/`（整个目录）
- Delete: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/swap_mapping.py`
- Delete: `apps/hb_attendance_app/tests/test_swap_mapping.py`
- Delete: `apps/hb_attendance_app/tests/test_sync_shift_swap.py`
- Delete: `apps/hb_attendance_app/tests/test_rest_leave_swap_doctype.py`
- Delete: `apps/hb_attendance_app/tests/test_rest_leave_swap_entry.py`
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/hooks.py`
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/setup.py`
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/workspace/海滨考勤工作台/海滨考勤工作台.json`
- Test: `apps/hb_attendance_app/tests/test_rest_leave_entry.py`（新建，取代被删的 entry 测试）

**Interfaces:**
- Consumes: Task 4 `parse_pending_rest_leaves`、Task 5 `verify_pending_rest_leaves`
- Produces: 调度方法 `sync_rest_leave.sync_rest_leave_from_bitable`、`sync_rest_leave.parse_pending_rest_leaves`、`sync_rest_leave.verify_pending_rest_leaves`

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave_entry.py`：

```python
import json
import unittest
from pathlib import Path

APP = Path(__file__).parents[1] / "hb_attendance_app"
MOD = APP / "hbos_attendance"


class EntryContractTest(unittest.TestCase):
    def setUp(self):
        self.hooks = (MOD / "hooks.py").read_text()
        self.setup = (MOD / "setup.py").read_text()
        self.ws = json.loads(
            (MOD / "workspace/海滨考勤工作台/海滨考勤工作台.json").read_text())

    def test_hooks_registers_rest_leave_stages(self):
        self.assertIn("sync_rest_leave.sync_rest_leave_from_bitable", self.hooks)
        self.assertIn("sync_rest_leave.parse_pending_rest_leaves", self.hooks)
        self.assertIn("sync_rest_leave.verify_pending_rest_leaves", self.hooks)

    def test_hooks_no_longer_registers_swap(self):
        self.assertNotIn("sync_shift_swap", self.hooks)

    def test_sidebar_keeps_rest_leave_drops_swap(self):
        self.assertIn("HBOS Rest Leave Record", self.setup)
        self.assertNotIn("HBOS Shift Swap Record", self.setup)

    def test_workspace_keeps_rest_leave_drops_swap(self):
        labels = [l.get("label") for l in self.ws["links"]]
        self.assertIn("飞书调休记录", labels)
        self.assertNotIn("飞书换班记录", labels)

    def test_swap_artifacts_removed(self):
        self.assertFalse((MOD / "swap_mapping.py").exists())
        self.assertFalse((MOD / "sync_shift_swap.py").exists())
        self.assertFalse((MOD / "doctype/hbos_shift_swap_record").exists())


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_entry.py'
```
Expected: FAIL — hooks 未注册三段、swap 文件仍存在

- [ ] **Step 3: 挂调度**

编辑 `hooks.py` 的 `scheduler_events`，把 `*/30` 一段改为：

```python
        "*/30 * * * *": [
            "hb_attendance_app.hbos_attendance.api.sync_from_bitable",
            "hb_attendance_app.hbos_attendance.api.sync_overtime_from_bitable",
            # 调休三段：同步 → LLM 解析加班日 → 核实（顺序固定，解析依赖同步刚落的记录）
            "hb_attendance_app.hbos_attendance.sync_rest_leave.sync_rest_leave_from_bitable",
            "hb_attendance_app.hbos_attendance.sync_rest_leave.parse_pending_rest_leaves",
            "hb_attendance_app.hbos_attendance.sync_rest_leave.verify_pending_rest_leaves",
        ],
```

（删去 `sync_shift_swap.sync_shift_swap_from_bitable` 一行。）

- [ ] **Step 4: 摘侧边栏入口**

编辑 `setup.py`：删除 `PRIMARY_SIDEBAR_ITEMS` 中这一行：

```python
	{"label": "飞书换班记录", "link_type": "DocType", "link_to": "HBOS Shift Swap Record", "type": "Link", "icon": "refresh-cw"},
```

保留「飞书调休记录」那一行不动。

- [ ] **Step 5: 摘工作台链接**

编辑 `workspace/海滨考勤工作台/海滨考勤工作台.json`：从 `links` 数组中删除这一条：

```json
{"hidden": 0, "is_query_report": 0, "label": "飞书换班记录", "link_count": 0, "link_to": "HBOS Shift Swap Record", "link_type": "DocType", "onboard": 0, "type": "Link"}
```

保留「飞书调休记录」那条。删后 `links` 长度应为 18。同时把顶层 `"modified"` 改为当前时间字符串。

> 注：`apps/hb_stock_app/tests/oracle/海滨考勤工作台.json` 是这份产物的**顶层键**快照，`test_workspace_builder.py` 只比对顶层键集合、不比对 links 内容，因此本改动不会让它失败。

- [ ] **Step 6: 删除换班与旧映射文件**

```bash
git rm -r apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_shift_swap_record
git rm apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_shift_swap.py
git rm apps/hb_attendance_app/hb_attendance_app/hbos_attendance/swap_mapping.py
git rm apps/hb_attendance_app/tests/test_swap_mapping.py
git rm apps/hb_attendance_app/tests/test_sync_shift_swap.py
git rm apps/hb_attendance_app/tests/test_rest_leave_swap_doctype.py
git rm apps/hb_attendance_app/tests/test_rest_leave_swap_entry.py
```

- [ ] **Step 7: 确认无残留引用**

```bash
grep -rn "swap_mapping\|sync_shift_swap\|hbos_shift_swap\|HBOS Shift Swap" apps/ --include="*.py" --include="*.json" --include="*.md" || echo "无残留"
```
Expected: 输出 `无残留`（若有命中则逐个清理后重跑）

- [ ] **Step 8: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`

- [ ] **Step 9: 提交**

```bash
git add -A apps/hb_attendance_app
git commit -m "feat: 摘除换班代码并注册调休三段调度"
```

---

### Task 7: 上线与校验（需 Owner 授权后执行）

migrate 会建表、注册调度任务，并触发 `after_migrate`（工作台/侧栏同步，设计为幂等）。**这是对正在运行的生产系统动手，执行前必须得到 Owner 明确授权。**

**Files:**
- 无代码改动
- 记录：`docs/milestones/M1_FIX_C_调休模块落地记录.md`（新建）

**Interfaces:**
- Consumes: 前六个任务的产物
- Produces: 可复核的调休数据与核实结论

- [ ] **Step 1: 备份站点**

```bash
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend backup --with-files'
```
Expected: 输出备份文件路径（位于 `sites/frontend/private/backups/`）

- [ ] **Step 2: migrate**

```bash
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend migrate'
```
Expected: 无 traceback；末尾显示 migration 完成

- [ ] **Step 3: 重启 backend 与 scheduler**

```bash
docker compose restart backend scheduler
```
用 `restart` 而非 `up`：保持容器 IP，避免 nginx 连旧 IP 致 502（沿用飞书卡片那轮的验证结论）。

- [ ] **Step 4: 确认表与调度已建**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SHOW TABLES FROM \`_7aecc840db82aaec\` LIKE \"tabHBOS%\";
SELECT method, cron_format, stopped FROM \`_7aecc840db82aaec\`.\`tabScheduled Job Type\` WHERE method LIKE \"%rest_leave%\";
"'
```
Expected: 出现 `tabHBOS Rest Leave Record`；三条 rest_leave 调度，`cron_format = */30 * * * *`、`stopped = 0`；**不出现** shift_swap

- [ ] **Step 5: 触发同步并核对计数**

```bash
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute hb_attendance_app.hbos_attendance.sync_rest_leave.sync_rest_leave_from_bitable'
```
Expected: `created` 约 115、`unmatched` 为 1~2（实测 39 个工号中 1 个匹配不到）、`error` 为空

- [ ] **Step 6: 校验落库行数与状态分布**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT COUNT(*) AS total, SUM(approval_status=\"已通过\") AS approved FROM \`_7aecc840db82aaec\`.\`tabHBOS Rest Leave Record\`;
SELECT verify_status, COUNT(*) FROM \`_7aecc840db82aaec\`.\`tabHBOS Rest Leave Record\` GROUP BY verify_status;
"'
```
Expected: `total = 115`、`approved = 104`；状态全为 `待解析`

- [ ] **Step 7: 跑解析与核实三段**

```bash
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute hb_attendance_app.hbos_attendance.sync_rest_leave.parse_pending_rest_leaves'
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute hb_attendance_app.hbos_attendance.sync_rest_leave.verify_pending_rest_leaves'
```
Expected: 解析返回 `parsed` + `failed` 合计约 115、`remaining = 0`；核实返回 `remaining = 0`

- [ ] **Step 8: 人工逐条核对解析结果（关键验收）**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT name, employee_name, start_date, end_date, overtime_dates, verify_status
FROM \`_7aecc840db82aaec\`.\`tabHBOS Rest Leave Record\`
WHERE approval_status=\"已通过\" AND verify_status=\"已核实\"
ORDER BY start_date LIMIT 20;
"'
```
Expected: 抽查的 `overtime_dates` 与「说明」原文一致；不一致的记录下来。**这是 LLM 首次参与本项目判定链路，必须逐条人工过一遍，不能只看统计数字。**

- [ ] **Step 9: 统计核实分布并写落地记录**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT verify_status, COUNT(*) FROM \`_7aecc840db82aaec\`.\`tabHBOS Rest Leave Record\` WHERE approval_status=\"已通过\" GROUP BY verify_status;
"'
```
Expected: 得到 `已核实 / 核实不通过 / 解析失败` 三个数字

**另需回答一个问题（Task 5 审查提出，实测后圈定的唯一敞口）**：核实判据是 `status='Present' AND working_hours >= 2`，未设上限。实测 `HBOS-ATT-*` 的 Present 记录全部 `>= 2` 且无一条 `> 18h`，所以对现行引擎产出的记录，判据是精确的；**全部宽松量集中在 40 条 legacy `DELI-ATT-*` 记录，日期只落在 2026-07-28、07-29、07-30 三天**。

这三天两套引擎结论相反（`HBOS-ATT` 几乎全判 Absent，`DELI-ATT` 报大量 Present），所以不能靠 `name LIKE 'HBOS-ATT-%'` 过滤——那会把真实主张翻成「核实不通过」。需要人工判断的是：**是否有真实主张的加班日落在这三天**。

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT name, start_date, overtime_dates, verify_status
FROM \`_7aecc840db82aaec\`.\`tabHBOS Rest Leave Record\`
WHERE approval_status=\"已通过\"
  AND overtime_dates REGEXP \"2026-07-(28|29|30)\";
"'
```
Expected: 若返回空，则本次核实的全部结论都建立在精确判据上，敞口为零。若返回记录，逐条人工判断该加班日是否真实发生，并在落地记录中写明结论。

把 Step 1–8 的命令与实测输出（脱敏，勿含真实姓名工号）写入 `docs/milestones/M1_FIX_C_调休模块落地记录.md`。

- [ ] **Step 10: 确认考勤结果未被改变**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT status, COUNT(*) FROM \`_7aecc840db82aaec\`.\`tabAttendance\` WHERE attendance_date >= \"2026-08-01\" GROUP BY status;
"'
```
Expected: 与 migrate 之前的分布一致（本阶段不接豁免，**任何变化都说明有未预期的副作用，必须查清**）。若无法比对，先记录当前分布作为基线。

- [ ] **Step 11: 提交落地记录**

```bash
git add docs/milestones/M1_FIX_C_调休模块落地记录.md
git commit -m "docs: 记录调休模块第一阶段落地与核实结果"
```

---

## 本阶段结束后

**已完成**：飞书调休 → 落库 → LLM 提取加班日 → 打卡核实 → 可人工复核的结论。

**刻意未做（下一阶段）**：
- **§8 判定豁免接入**：把 `verify_status=已核实` 的调休日并入传给 `pair_employee_checkins()` 的豁免集合。等 Owner 看过 Step 8 的人工核对结果、确认解析可靠后再开。
- 看板 / 9:00 飞书卡片的调休口径（`department_board_data._load_leave_records` 增加调休来源）。
- 月度考勤汇总的调休呈现。
- 加班模块本体。

**继续前需 Owner 回应的开放项**（设计 §13）：
1. 调休已豁免后加班被撤销，是否追溯收回？
2. 「调休待核实 / 核实不通过」的可见位置：进异常名单即可，还是需要专门的列表页？
