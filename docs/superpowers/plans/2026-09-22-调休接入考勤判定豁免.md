# 调休接入考勤判定豁免（第二阶段）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「已核实」的调休日接入考勤判定与看板，使其不再判缺勤、不再被列为「未打卡」，并在考勤记录上标记为「调休」。

**Architecture:** 日期展开逻辑进纯模块 `rest_leave.py`（可离线单测）；新增唯一查询入口 `rest_leave_apply.py`，使「已通过 + 已核实」两个过滤条件只有一份；`api.py` 与部门看板各调用该入口一次。

**Tech Stack:** Frappe v16 / Python 3.10+ / unittest（`python -m unittest`）

## Global Constraints

- 基准设计：`docs/superpowers/specs/2026-09-22-调休接入考勤判定豁免设计.md`
- 前置交付：`docs/milestones/M1_FIX_F_调休模块第一阶段落地记录.md`（第一阶段已上线并通过验收）
- **不修改** `pairing.py`、`board_stats.py`、月度汇总报表、考勤结果报表
- **不修改** Frappe / ERPNext / HRMS 核心源码
- **不提交** 真实员工姓名、工号、`.env`、密钥、Excel/CSV、数据库、日志、运行时产物
- 查询口径固定为两个条件**同时**成立：`approval_status='已通过'` **且** `verify_status='已核实'`
- 新集合命名 **`emp_rest_leave_dates`**；**不得**与 `api.py` 既有的 `emp_rest_dates`（排班休息日，语义相反）混用
- 调休日生成的记录，班次列写 `调休`；请假记录班次列仍为空字符串
- 测试命令（仓库根目录执行）：
  ```bash
  PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py'
  ```
- 现有基线：396 个用例全绿。每个 Task 结束时该命令必须仍全绿。
- 提交信息用中文（可带 `feat:` / `fix:` 前缀）
- **禁止** `git add -A` / `git add .`——工作区有三个属他人并发改动的文件（`daily_feishu_sync.py`、`pair_checkins.py`、`rule_lists.py`），必须逐一写明路径

---

### Task 1: 日期展开纯函数

在既有纯模块 `rest_leave.py` 增加 `expand_verified_records`：把「已核实的调休记录」展开成 `{employee: {日期}}`。这是判定与看板共用的数据形态。

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave.py`（追加函数）
- Test: `apps/hb_attendance_app/tests/test_rest_leave_expand_verified.py`（新建）

**Interfaces:**
- Consumes: 无（纯标准库）
- Produces: `expand_verified_records(rows) -> dict[str, set[str]]`
  - `rows`：含 `employee` / `start_date` / `end_date` 三个键的 dict 列表（`end_date` 可为 `None`）
  - 返回：`{"<employee>": {"YYYY-MM-DD", ...}}`；无有效记录时返回 `{}`

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave_expand_verified.py`：

```python
import unittest

from hb_attendance_app.hbos_attendance.rest_leave import expand_verified_records


class ExpandVerifiedRecordsTest(unittest.TestCase):
    def test_single_day(self):
        rows = [{"employee": "E1", "start_date": "2026-08-18", "end_date": "2026-08-18"}]
        self.assertEqual(expand_verified_records(rows), {"E1": {"2026-08-18"}})

    def test_multi_day_inclusive(self):
        rows = [{"employee": "E1", "start_date": "2026-08-13", "end_date": "2026-08-14"}]
        self.assertEqual(expand_verified_records(rows),
                         {"E1": {"2026-08-13", "2026-08-14"}})

    def test_end_none_falls_back_to_start(self):
        rows = [{"employee": "E1", "start_date": "2026-09-01", "end_date": None}]
        self.assertEqual(expand_verified_records(rows), {"E1": {"2026-09-01"}})

    def test_same_employee_multiple_records_merge(self):
        rows = [
            {"employee": "E1", "start_date": "2026-08-18", "end_date": "2026-08-18"},
            {"employee": "E1", "start_date": "2026-08-20", "end_date": "2026-08-20"},
        ]
        self.assertEqual(expand_verified_records(rows),
                         {"E1": {"2026-08-18", "2026-08-20"}})

    def test_reverse_range_still_expands(self):
        rows = [{"employee": "E1", "start_date": "2026-08-18", "end_date": "2026-08-16"}]
        got = expand_verified_records(rows)["E1"]
        self.assertEqual(len(got), 3)

    def test_bad_rows_are_skipped_not_raised(self):
        rows = [
            {"employee": "E1", "start_date": "不是日期", "end_date": "2026-08-18"},
            {"employee": "", "start_date": "2026-08-18", "end_date": "2026-08-18"},
            {"employee": "E2", "start_date": None, "end_date": None},
            {"employee": "E3", "start_date": "2026-08-18", "end_date": "2026-08-18"},
        ]
        self.assertEqual(expand_verified_records(rows), {"E3": {"2026-08-18"}})

    def test_empty_input(self):
        self.assertEqual(expand_verified_records([]), {})
        self.assertEqual(expand_verified_records(None), {})

    def test_missing_keys_do_not_raise(self):
        self.assertEqual(expand_verified_records([{"employee": "E1"}]), {})


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_expand_verified.py'
```
Expected: FAIL — `ImportError: cannot import name 'expand_verified_records'`

- [ ] **Step 3: 实现函数**

在 `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave.py` **末尾追加**（不要改动文件中的任何既有函数）：

```python
def expand_verified_records(rows):
    """把「已核实的调休记录」展开成 {employee: {日期}}。

    与 `expand_dates` 职责不同：`expand_dates` 只展开一段区间，本函数负责
    遍历多行记录、按人合并、并跳过脏数据。两条路径共用同一套「区间按天展开」
    语义，故内部直接调用 `expand_dates`，不另写一份。

    任一字段缺失或日期不可解析的记录**整行跳过**（不抛异常）——上游是飞书
    同步来的数据，脏值不得让考勤重算中断（第一阶段已因同类问题吃过亏）。
    """
    out = {}
    for row in rows or []:
        if not isinstance(row, dict):
            continue
        emp = (row.get("employee") or "").strip()
        start = row.get("start_date")
        if not emp or not start:
            continue
        days = expand_dates(start, row.get("end_date"))
        if not days:
            continue
        out.setdefault(emp, set()).update(days)
    return out
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_expand_verified.py'
```
Expected: `OK`（8 个用例）

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`，总数 = 396 + 8 = 404

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave.py apps/hb_attendance_app/tests/test_rest_leave_expand_verified.py
git commit -m "feat: 新增已核实调休记录的日期展开纯函数"
```

---

### Task 2: 唯一查询入口

新建 `rest_leave_apply.py`。它是「已通过 + 已核实」这两个过滤条件在代码库中的**唯一**出处，考勤判定与部门看板都从这里取数。

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave_apply.py`
- Test: `apps/hb_attendance_app/tests/test_rest_leave_apply.py`（新建）

**Interfaces:**
- Consumes: Task 1 的 `expand_verified_records`
- Produces: `verified_rest_dates() -> dict[str, set[str]]`；模块常量 `DOCTYPE`、`FILTERS`

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave_apply.py`：

```python
import unittest
from pathlib import Path

SRC = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/rest_leave_apply.py")


class VerifiedRestDatesContractTest(unittest.TestCase):
    """口径契约：两个过滤条件缺一不可，且不得在别处出现第二份。"""

    def setUp(self):
        self.src = SRC.read_text()

    def test_module_exists_with_entry(self):
        self.assertIn("def verified_rest_dates(", self.src)

    def test_reads_rest_leave_doctype(self):
        self.assertIn('"HBOS Rest Leave Record"', self.src)

    def test_requires_approved(self):
        """少了「已通过」，被驳回/撤回的申请也会发豁免。"""
        self.assertIn('"approval_status": "已通过"', self.src)

    def test_requires_verified(self):
        """少了「已核实」，未核实/核实不通过的调休日也会发豁免。"""
        self.assertIn('"verify_status": "已核实"', self.src)

    def test_filters_exist_as_module_constant(self):
        """两个条件必须来自同一个常量，避免被复制成第二份。"""
        self.assertIn("FILTERS", self.src)
        self.assertIn("FILTERS", self.src.split("def verified_rest_dates")[0])

    def test_delegates_expansion_to_pure_module(self):
        self.assertIn("expand_verified_records", self.src)

    def test_is_not_whitelisted(self):
        """内部查询入口，不应暴露为接口。"""
        self.assertNotIn("@frappe.whitelist", self.src)


class SingleSourceOfTruthTest(unittest.TestCase):
    """全库只能有一处写死「已核实」的查询条件，防止口径分叉。"""

    def test_only_one_module_filters_on_verified(self):
        pkg = SRC.parent
        hits = []
        for p in sorted(pkg.rglob("*.py")):
            if p.name == "rest_leave_apply.py":
                continue
            text = p.read_text()
            if '"verify_status"' in text and '"已核实"' in text:
                hits.append(p.name)
        self.assertEqual(hits, [],
                         "「已核实」过滤条件只能出现在 rest_leave_apply.py，"
                         "否则两处口径会漂移：%s" % hits)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_apply.py'
```
Expected: FAIL — `FileNotFoundError`（`rest_leave_apply.py` 尚不存在）

- [ ] **Step 3: 实现模块**

创建 `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave_apply.py`：

```python
"""已核实调休日的**唯一**查询入口。

考勤判定（api.py）与部门看板（department_board_data.py）都从这里取数。
两个过滤条件必须同时成立：

    approval_status = 已通过    —— 否则被驳回/撤回的申请也会发放豁免
    verify_status  = 已核实     —— 否则未核实或核实不通过的调休日也会发放豁免

把条件收敛在此处（而非各调用方各写一份）是为了让口径无法漂移：本项目已有
两份分叉的行政班名单作为前车之鉴（daily_feishu_sync.py 与 rule_lists.py）。

本模块只负责取数，不参与判定，也不暴露为接口。
"""
import frappe

from hb_attendance_app.hbos_attendance.rest_leave import expand_verified_records

DOCTYPE = "HBOS Rest Leave Record"

# 唯一的过滤口径。任何调用方都不得自建第二份。
FILTERS = {
    "approval_status": "已通过",
    "verify_status": "已核实",
}


def verified_rest_dates():
    """返回 {employee: {日期}}，仅含已通过且已核实的调休日。

    查不到任何记录时返回 {}——调用方按「无豁免」处理即可。
    """
    rows = frappe.db.get_all(
        DOCTYPE,
        filters=FILTERS,
        fields=["employee", "start_date", "end_date"],
    )
    return expand_verified_records(rows)
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_apply.py'
```
Expected: `OK`（8 个用例）

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`，总数 = 404 + 8 = 412

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/rest_leave_apply.py apps/hb_attendance_app/tests/test_rest_leave_apply.py
git commit -m "feat: 新增已核实调休日唯一查询入口"
```

---

### Task 3: 接入考勤判定

把已核实的调休日并入 `api.py` 的豁免集合，并在生成记录时标记调休。**这是本阶段唯一改变考勤结果的任务。**

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/api.py`（3 处）
- Test: `apps/hb_attendance_app/tests/test_api_rest_leave_exemption.py`（新建）

**Interfaces:**
- Consumes: Task 2 的 `verified_rest_dates()`
- Produces: 无对外接口；改变 `regenerate_attendance` 的行为

**三处改动（都在 `regenerate_attendance` 内）**

| 处 | 位置 | 改动 |
| --- | --- | --- |
| A | `emp_rest_dates` 定义之后（约 `api.py:376`） | 新增 `emp_rest_leave_dates`，并**并入** `emp_leave_dates` |
| B | 零打卡分支生成 `On Leave`（约 `api.py:637`） | 若该日属调休，班次列写 `调休`；否则保持空字符串（请假） |
| C | 该分支之后的请假记录写入 | 无需改动（班次值已由 B 决定） |

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_api_rest_leave_exemption.py`：

```python
import re
import unittest
from pathlib import Path

API = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/api.py")


class RestLeaveExemptionContractTest(unittest.TestCase):
    def setUp(self):
        self.src = API.read_text()

    def _fn(self):
        """切出 regenerate_attendance 函数体，避免误判同文件其它函数。"""
        i = self.src.index("def regenerate_attendance(")
        j = self.src.find("\ndef ", i + 10)
        return self.src[i:j if j > 0 else len(self.src)]

    def test_fetches_verified_rest_dates(self):
        self.assertIn("from hb_attendance_app.hbos_attendance.rest_leave_apply import",
                      self.src)
        self.assertIn("verified_rest_dates()", self._fn())

    def test_merges_into_leave_dates(self):
        """必须并入 emp_leave_dates——pairing 只看这一个集合来抑制缺勤。"""
        fn = self._fn()
        self.assertIn("emp_leave_dates", fn)
        self.assertRegex(fn, r"emp_leave_dates\[[^\]]+\]\s*\.update\(|"
                             r"emp_leave_dates\.setdefault\(")

    def test_keeps_separate_rest_leave_set(self):
        """另存一个集合用于区分「调休」与「请假」。"""
        fn = self._fn()
        self.assertIn("emp_rest_leave_dates", fn)

    def test_does_not_reuse_schedule_rest_set(self):
        """不得复用 emp_rest_dates——那是排班休息日，语义相反（不生成记录）。"""
        fn = self._fn()
        self.assertNotRegex(fn, r"emp_rest_dates\s*=\s*verified_rest_dates")

    def test_marks_rest_leave_shift(self):
        """调休日生成的 On Leave 记录，班次列写「调休」。"""
        fn = self._fn()
        self.assertIn('"调休"', fn)

    def test_leave_records_keep_empty_shift(self):
        """请假仍写空班次——不得因为加了调休而把请假也标成调休。"""
        fn = self._fn()
        self.assertRegex(fn, r'"On Leave",\s*(""|shift)')

    def test_does_not_import_from_pairing(self):
        """本阶段靠并入豁免集合实现，不改判定核心。"""
        self.assertNotIn("pair_employee_checkins(", self.src.split("import")[0])

    def test_no_new_sql_for_rest_leave(self):
        """查询收敛在 rest_leave_apply，api.py 不得自建第二份查询。"""
        fn = self._fn()
        self.assertNotIn("tabHBOS Rest Leave Record", fn)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_api_rest_leave_exemption.py'
```
Expected: FAIL — `test_fetches_verified_rest_dates` 等报找不到（api.py 尚无改动）

- [ ] **Step 3: 改动 A —— 取数与并入豁免集合**

在 `api.py` 的 `emp_rest_dates` 两个循环之后（即 `for s in frappe.db.get_all("HBOS Employee Schedule", filters={"shift_type": "休息"}, ...)` 那个循环闭合之后）、`# 清空生成范围内已有...` 注释之前，插入：

```python
    # 已核实的调休日：并入请假豁免集合（pairing 只看它抑制缺勤），
    # 同时另存一份用于区分「调休」与「请假」的记录标记。
    # 口径（已通过 + 已核实）由 rest_leave_apply 独占，此处不自建查询。
    from hb_attendance_app.hbos_attendance.rest_leave_apply import verified_rest_dates
    emp_rest_leave_dates = verified_rest_dates()
    for _eid, _days in emp_rest_leave_dates.items():
        emp_leave_dates[_eid].update(_days)
```

并在文件顶部 import 区（与其它 `from hb_attendance_app...` 同处）加入：

```python
from hb_attendance_app.hbos_attendance.rest_leave_apply import verified_rest_dates
```

（若已按上面在函数内 import，可省去顶部这一行；二选一，不要同时存在两份 import。）

- [ ] **Step 4: 改动 B —— 生成记录时标记调休**

把零打卡分支中生成 `On Leave` 的那一行，由：

```python
                    leave_absent.append(("HBOS-ATT-" + eid + "-" + ds, eid, ds, "On Leave", "", 0, ds + " 00:00:00", 0, 0))
```

改为：

```python
                    # 调休日标「调休」，请假仍留空班次——两者在报表里需可区分
                    _shift = "调休" if ds in emp_rest_leave_dates.get(eid, set()) else ""
                    leave_absent.append(("HBOS-ATT-" + eid + "-" + ds, eid, ds, "On Leave", _shift, 0, ds + " 00:00:00", 0, 0))
```

- [ ] **Step 5: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_api_rest_leave_exemption.py'
```
Expected: `OK`（8 个用例）

- [ ] **Step 6: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`，总数 = 412 + 8 = 420

- [ ] **Step 7: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/api.py apps/hb_attendance_app/tests/test_api_rest_leave_exemption.py
git commit -m "feat: 已核实调休日接入考勤豁免并标记调休"
```

---

### Task 4: 接入部门看板与 9 点卡片

看板与 9 点飞书卡片共用 `_load_leave_records()`。不接入的话，调休人员会被列为「未打卡」——群里天天点名一批实际在调休的人。

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_department_board/department_board_data.py`（1 处）
- Test: `apps/hb_attendance_app/tests/test_board_rest_leave.py`（新建）

**Interfaces:**
- Consumes: Task 2 的 `verified_rest_dates()`
- Produces: `_load_leave_records(date_str, emp_names)` 的返回值新增调休来源

**注意**：`_load_leave_records` 返回 `{employee: 请假类型}`。调休人员应返回类型 `调休`，使看板标签显示为「请假（调休）」而非空。

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_board_rest_leave.py`：

```python
import unittest
from pathlib import Path

SRC = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/page/hbos_department_board/department_board_data.py")


class BoardRestLeaveContractTest(unittest.TestCase):
    def setUp(self):
        self.src = SRC.read_text()

    def test_loads_verified_rest_dates(self):
        self.assertIn("verified_rest_dates", self.src)

    def test_uses_shared_entry_not_own_query(self):
        """不得在看板里自建第二份「已通过 + 已核实」查询。"""
        i = self.src.index("def _load_leave_records(")
        j = self.src.find("\ndef ", i + 10)
        body = self.src[i:j if j > 0 else len(self.src)]
        self.assertNotIn("tabHBOS Rest Leave Record", body)
        self.assertNotIn("verify_status", body)

    def test_labels_rest_leave_distinctly(self):
        self.assertIn("调休", self.src)

    def test_still_reads_native_leave_records(self):
        """原有的 HBOS Leave Record 来源不得被替换掉。"""
        self.assertIn('"HBOS Leave Record"', self.src)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_board_rest_leave.py'
```
Expected: FAIL — `test_loads_verified_rest_dates` 报未找到

- [ ] **Step 3: 实现**

在 `department_board_data.py` 顶部 import 区加入：

```python
from hb_attendance_app.hbos_attendance.rest_leave_apply import verified_rest_dates
```

把 `_load_leave_records` 整体替换为：

```python
def _load_leave_records(date_str, emp_names):
    """返回 {employee: 类型}。含飞书请假与**已核实的调休**两个来源。

    调休单列一个来源而非并入请假，是为了让看板标签能显示「调休」——
    Owner 明确要求两者在报表里可区分（调休是调休，请假是请假）。
    口径由 rest_leave_apply 独占，此处不自建查询。
    """
    if not emp_names:
        return {}
    out = {}
    rows = frappe.db.get_all(
        "HBOS Leave Record",
        filters={"approval_status": "已通过",
                 "start_date": ["<=", date_str], "end_date": [">=", date_str],
                 "employee": ["in", emp_names]},
        fields=["employee", "leave_type"],
    )
    for r in rows:
        out.setdefault(r.employee, r.leave_type)
    # 已核实的调休：仅覆盖当日、且属于本次查询人员的
    wanted = set(emp_names)
    for emp, days in verified_rest_dates().items():
        if emp in wanted and date_str in days:
            out.setdefault(emp, "调休")
    return out
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_board_rest_leave.py'
```
Expected: `OK`（4 个用例）

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`，总数 = 420 + 4 = 424

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_department_board/department_board_data.py apps/hb_attendance_app/tests/test_board_rest_leave.py
git commit -m "feat: 部门看板与 9 点卡片接入已核实调休日"
```

---

### Task 5: 修复 F3——重新解析失效键含 employee

`sync_rest_leave.py` 判断「飞书记录是否需重新解析」时只比较 `remarks`。若有人改了「调休人员_工号」，记录会重新匹配到**另一个人**，但 `remarks` 未变 → 结论不重置 → **仍用原那人的加班证据**。

第一阶段仅出结论，后果是观感问题；接入豁免后，后果是「用甲的加班给乙换休」。

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py`（约 2 行）
- Test: `apps/hb_attendance_app/tests/test_rest_leave_reparse_key.py`（新建）

**Interfaces:**
- Consumes: 无
- Produces: 改变 `sync_rest_leave_from_bitable` 的重解析触发条件

- [ ] **Step 1: 写失败测试**

创建 `apps/hb_attendance_app/tests/test_rest_leave_reparse_key.py`：

```python
import unittest
from pathlib import Path

SRC = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/sync_rest_leave.py")


class ReparseKeyContractTest(unittest.TestCase):
    """重解析的失效键必须同时覆盖 说明 与 员工。

    只比 remarks：改了工号 → 换了人却不重算 → 用甲的加班给乙换休。
    """

    def setUp(self):
        self.src = SRC.read_text()

    def _sync_fn(self):
        i = self.src.index("def sync_rest_leave_from_bitable(")
        j = self.src.find("\ndef ", i + 10)
        return self.src[i:j if j > 0 else len(self.src)]

    def test_captures_old_employee(self):
        self.assertIn("old_employee", self._sync_fn())

    def test_compares_both_keys(self):
        fn = self._sync_fn()
        self.assertIn("old_remarks", fn)
        self.assertRegex(fn, r"old_employee[^\n]*!=|!=[^\n]*old_employee",
                         "重解析判定必须同时比较 employee")

    def test_old_employee_captured_before_update(self):
        """必须在 doc.update 之前取旧值，否则比较恒为假（第一阶段踩过）。"""
        fn = self._sync_fn()
        cap = fn.index("old_employee")
        upd = fn.index("doc.update(")
        self.assertLess(cap, upd, "old_employee 必须在 doc.update 之前捕获")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_reparse_key.py'
```
Expected: FAIL — `test_captures_old_employee` 报未找到

- [ ] **Step 3: 实现**

在 `sync_rest_leave.py` 的 `sync_rest_leave_from_bitable` 内，把捕获旧值的两行：

```python
            # 必须在校验前暂存旧说明：doc.update 会就地覆盖 doc.remarks，
            # 之后再比较恒为假，「说明被改过 → 重新解析」将永远不触发。
            old_remarks = doc.remarks if exists else None
```

改为：

```python
            # 必须在校验前暂存旧值：doc.update 会就地覆盖它们，之后再比较
            # 恒为假，「被改过 → 重新解析」将永远不触发（第一阶段踩过此坑）。
            # 同时覆盖 employee：飞书改了工号会换人，只比 remarks 会沿用
            # 原那人的加班证据（接入豁免后即「用甲的加班给乙换休」）。
            old_remarks = doc.remarks if exists else None
            old_employee = doc.employee if exists else None
```

并把判定分支：

```python
            elif old_remarks != mapped["remarks"]:
                # 说明被改过 → 加班日需重新解析
                doc.verify_status = PARSE_PENDING
```

改为：

```python
            elif old_remarks != mapped["remarks"] or old_employee != emp:
                # 说明或员工被改过 → 加班日需重新解析
                doc.verify_status = PARSE_PENDING
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_rest_leave_reparse_key.py'
```
Expected: `OK`（3 个用例）

- [ ] **Step 5: 跑全量**

```bash
PYTHONPATH="apps/hb_attendance_app:apps/hb_attendance_app/tests" python3 -m unittest discover -s apps/hb_attendance_app/tests -p 'test_*.py' 2>&1 | tail -5
```
Expected: `OK`，总数 = 424 + 3 = 427

- [ ] **Step 6: 提交**

```bash
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py apps/hb_attendance_app/tests/test_rest_leave_reparse_key.py
git commit -m "fix: 调休重解析失效键补入 employee，避免换人后沿用旧证据"
```

---

### Task 6: 生效验收（代码落盘即生效，需 Owner 在场）

工作副本直接挂载进容器，**代码一提交，下一次 10 分钟考勤重算即应用新逻辑**。没有单独的发布步骤。

**Files:**
- 无代码改动
- 记录：更新 `docs/milestones/M1_FIX_F_调休模块第一阶段落地记录.md`（追加第二阶段节）

**Interfaces:**
- Consumes: Task 1–5 全部产物
- Produces: 可复核的验收证据

- [ ] **Step 1: 确认前置状态**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT status, COUNT(*) FROM \`_7aecc840db82aaec\`.\`tabAttendance\` GROUP BY status;
SELECT verify_status, COUNT(*) FROM \`_7aecc840db82aaec\`.\`tabHBOS Rest Leave Record\` WHERE approval_status=\"已通过\" GROUP BY verify_status;
"'
```
Expected: 已核实 40；`Attendance` 分布作为改动前基线记录下来

- [ ] **Step 2: 触发一次重算**

```bash
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute hb_attendance_app.hbos_attendance.api.regenerate_attendance --kwargs "{\"range_start\": \"2026-08-01\", \"range_end\": \"2026-09-21\"}"'
```
Expected: 无 traceback

- [ ] **Step 3: 与干跑预测逐值对账（本任务最关键的一步）**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT status, shift, COUNT(*) FROM \`_7aecc840db82aaec\`.\`tabAttendance\`
WHERE attendance_date >= \"2026-08-01\" GROUP BY status, shift ORDER BY 3 DESC LIMIT 20;
"'
```

预测值（接入前只读干跑所得）：

| 项 | 预测 |
| --- | --- |
| 新增 `On Leave`（该日原无记录） | **26** |
| `Absent` → `On Leave` | **3** |
| 调休日原为 `Present` → 不变 | **11** |
| 合计调休日 | **40** |

**校验方式**：查 `status='On Leave' AND shift='调休'` 的行数，应等于 **40**。若为其他数字，逐条查因后再继续，**不得直接收工**。

- [ ] **Step 4: 确认请假记录未被误标**

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT IFNULL(shift,\"\"), COUNT(*) FROM \`_7aecc840db82aaec\`.\`tabAttendance\`
WHERE status=\"On Leave\" GROUP BY shift;
"'
```
Expected: 只出现 `调休`（40）与空串（原有请假）。**若出现第三种值，说明标记逻辑写错了。**

- [ ] **Step 5: 看板口径**

`send_daily_report(force=False)` **不接受 dry_run 参数**——干跑由环境变量 `HBOS_NOTIFY_DRY_RUN=1` 控制（见 `attendance_notify.py:325`）。因此直接传入 `dry_run` 会 `TypeError`。正确做法是临时设该环境变量再调用：

```bash
docker compose exec -T -e HBOS_NOTIFY_DRY_RUN=1 backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute hb_attendance_app.hbos_attendance.attendance_notify.send_daily_report --kwargs "{\"force\": true}"'
```
Expected: `{"sent": false, "reason": "dry_run", ...}`；干跑正文里调休人员**不出现**在「未打卡」名录；日志无 traceback。

**回看干跑内容**（正文写在 Error Log 里）：

```bash
docker compose exec -T db sh -c 'mariadb -uroot -p"$MYSQL_ROOT_PASSWORD" -N -e "
SELECT LEFT(error, 1200) FROM \`_7aecc840db82aaec\`.\`tabError Log\`
WHERE method LIKE \"%HBOS考勤通知%\" ORDER BY creation DESC LIMIT 1\G
"'
```
Expected: 正文中「未打卡」的人名不含本次接入的调休人员。**注意**：干跑会写下当日幂等位，故同日稍后如要真实发送，需 `force=true`。

- [ ] **Step 6: 写验收记录**

把 Step 1–5 的命令与实测输出（脱敏，勿含真实姓名工号）追加到 `docs/milestones/M1_FIX_F_调休模块第一阶段落地记录.md` 的新节，并把该文档标题所指范围更新为「第一、二阶段」。

- [ ] **Step 7: 提交**

```bash
git add docs/milestones/M1_FIX_F_调休模块第一阶段落地记录.md
git commit -m "docs: 记录调休第二阶段接入豁免与验收结果"
```

---

## 本阶段结束后

**已完成**：已核实的调休日接入考勤判定与看板，调休人员不再被判缺勤、不再被列为「未打卡」，记录标记为「调休」。

**明确未做（记录在案）**：

- **F4 终态重新核实**：`已核实` / `核实不通过` 是终态。若事后考勤数据变化导致旧结论失效，只能人工处理。Owner 2026-09-22 决定先观察。
- 加班模块本体、额度校验、按小时调休。
- `_safe_log` 收敛与测试助手重构（第一阶段遗留的后续工作）。
