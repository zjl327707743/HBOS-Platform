# 考勤提醒卡片化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把每日 09:00 发到飞书群的考勤提醒从「空格补位纯文本表格」改为「飞书卡片」——只列异常部门并带人名，其余部门汇总一行。

**Architecture:** 只改 `attendance_notify.py` 一个文件的呈现层。新增两个纯函数（`collect_exceptions` 取异常名录、`render_card` 渲染卡片 JSON），把 `build_feishu_payload` 的报文类型由 `text` 改为 `interactive`，卡片渲染失败时自动降级回纯文本。统计口径（`board_stats.py`）、调度、幂等位、判定规则**一行不动**。

**Tech Stack:** Python 3（仅标准库 + 既有 frappe/requests 依赖）、飞书群自定义机器人 webhook（interactive 卡片）、`unittest` 离线测试。

## Global Constraints

- **只改一个源文件**：`apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py`；`board_stats.py` 与 `hooks.py` 一行不改。
- **口径零改动**：异常判定必须复用 `board_stats` 的既有谓词与汇总（`is_late` / `summarize_rows` / `dept_summary`），**不得重写判定规则、不得复制归桶分支**。
- **纯函数无 frappe**：`collect_exceptions` 与 `render_card` 不得 import frappe、不得发网络请求，必须可离线测试。
- **人名只放姓名**（`name`），不放工号；重名问题本轮不处理。
- **截断必须显式**：人名上限 6 人、异常部门上限 10 个，超出时必须在卡片上写明省略数量，不得静默截断。
- **降级优先于失败**：卡片 JSON 构造抛异常时必须降级为纯文本发出，不得因渲染失败而漏发。
- **未经 Owner 明确授权，不得执行任何飞书真实写入**（Task 4 的全部运行态步骤）。
- 测试命令统一为：
  ```bash
  cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify -v
  ```
- 提交信息用中文描述（可保留 `feat` / `test` / `docs` 前缀）。

---

## 文件结构

| 文件 | 责任 | 本计划中的改动 |
| --- | --- | --- |
| `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py` | 考勤通知的渲染与出站推送 | 新增 2 个纯函数 + 2 个常量块 + 2 个报文构造 + 降级路径；`build_feishu_payload` 改报文类型 |
| `apps/hb_attendance_app/tests/test_attendance_notify.py` | 上者的离线测试 | 新增 3 个测试类/方法组；更新 2 处既有断言 |
| `docs/HBOS考勤判定规则.md` | 考勤口径与人读文档 | 追加卡片版式与本轮改动说明 |
| `docs/PROJECT_STATUS.md`、`docs/CURRENT_MILESTONE.md` | 状态台账 | M1-FIX-B5 段追加一行 |

**任务边界说明**：Task 1 与 Task 2 都是同一文件里的纯函数，但分属「数据层（谁算异常）」与「呈现层（怎么排版）」两个关注点，测试套件也不同，评审可以独立否决其中之一，故分开。

---

### Task 1: `collect_exceptions` —— 从明细行取异常名录

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py`（在 `# ---- 以下依赖 frappe / requests ----` 分隔线**之前**新增；顶部常量区新增两个常量）
- Test: `apps/hb_attendance_app/tests/test_attendance_notify.py`

**Interfaces:**
- Consumes: `board_stats.is_late(r)`、`board_stats.summarize_rows(rows)`、`board_stats.dept_summary(rows)`；`get_data()["rows"]` 的行结构（`dept / num / name / state / kind / tags / anomaly_hidden`）
- Produces: `collect_exceptions(rows) -> [{"dept": str, "noCard": [{"name": str, "num": str}], "late": [...]}]`，只含至少一项异常的部门，部门顺序与 `dept_summary` 一致，部门内按 `num` 升序

- [ ] **Step 1: 写失败测试**

在 `apps/hb_attendance_app/tests/test_attendance_notify.py` 顶部的 import 区改成（**新增 `board_stats` 与 `collect_exceptions`**）：

```python
from hb_attendance_app.hbos_attendance import attendance_notify, board_stats
from hb_attendance_app.hbos_attendance.attendance_notify import (
    render_report, build_feishu_payload, feishu_result, collect_exceptions,
    render_card, build_text_payload,
)
```

在既有 `def d(dept, expected, present, no_card, late=0):` 辅助函数下方，新增一个针对**明细行**的辅助函数：

```python
def row(dept, num, name, state, kind="shift", tags=None, anomaly_hidden=False):
    """明细行（get_data()['rows'] 的元素）测试构造器。"""
    return {"dept": dept, "num": num, "name": name, "state": state, "kind": kind,
            "tags": tags or [], "anomaly_hidden": anomaly_hidden}
```

在文件末尾（`if __name__ == "__main__": unittest.main()` **之前**）新增整个测试类：

```python
class CollectExceptionsTest(unittest.TestCase):
    """异常名录的取数与 board_stats 同一口径（不重写判定规则）。"""

    def test_no_card_and_late_split_by_type(self):
        rows = [
            row("六车间", "0002", "李四", "absent_expected"),
            row("六车间", "0001", "张三", "absent_expected"),
            row("六车间", "0003", "王五", "late"),
        ]
        out = collect_exceptions(rows)
        self.assertEqual(len(out), 1)
        self.assertEqual(out[0]["dept"], "六车间")
        self.assertEqual([p["name"] for p in out[0]["noCard"]], ["张三", "李四"])  # 按工号升序
        self.assertEqual([p["name"] for p in out[0]["late"]], ["王五"])

    def test_skips_non_alert_states(self):
        rows = [row("A", "1", "甲", "present"), row("A", "2", "乙", "rest"),
                row("A", "3", "丙", "leave"), row("A", "4", "丁", "exempt"),
                row("A", "5", "戊", "unknown"), row("A", "6", "己", "before_start"),
                row("A", "7", "庚", "out_only"), row("A", "8", "辛", "fact_none")]
        self.assertEqual(collect_exceptions(rows), [])

    def test_skips_anomaly_hidden_and_non_shift(self):
        rows = [row("A", "1", "甲", "absent_expected", anomaly_hidden=True),
                row("A", "2", "乙", "absent_expected", kind="holiday")]
        self.assertEqual(collect_exceptions(rows), [])

    def test_late_via_tag_is_caught(self):
        # 回顾模式：HRMS 打「迟到」标签而 state 非 late
        rows = [row("A", "1", "甲", "present", tags=["迟到"])]
        out = collect_exceptions(rows)
        self.assertEqual([p["name"] for p in out[0]["late"]], ["甲"])

    def test_late_tag_on_non_expected_row_is_ignored(self):
        # 关键回归：非应出勤行（休假/休息/豁免/无排班/非班次）即使带「迟到」标签也不算异常——
        # summarize_rows 不计它，卡片若计入就会与看板数字漂移。
        # 注意 fact_none（班次未定）不属此类：board_stats 对 kind="shift" 的 fact_none 仍计入
        # expected，其「迟到」标签也计入 late（实测单行 expected=1/late=1），
        # 因此它归入「应出勤且迟到」，由 test_matches_summarize_rows_over_all_states 覆盖。
        rows = [row("A", "1", "甲", "leave", tags=["迟到"]),
                row("A", "2", "乙", "rest", tags=["迟到"]),
                row("A", "3", "丙", "exempt", tags=["迟到"]),
                row("A", "4", "丁", "unknown", tags=["迟到"]),
                row("A", "6", "己", "present", tags=["迟到"], kind="holiday")]
        self.assertEqual(collect_exceptions(rows), [])

    def test_matches_summarize_rows_over_all_states(self):
        # 逐 state × kind × 迟到标签 全枚举，断言卡片名单与统计口径逐项一致
        states = ["present", "late", "fact_present", "present_offwindow", "out_day",
                  "out_offwindow", "out_only", "fact_none", "before_start",
                  "absent_expected", "no_pair", "pending", "absent_day",
                  "leave", "rest", "exempt", "unknown", "unexpected_state"]
        for st in states:
            for kind in ("shift", "holiday"):
                for tags in ([], ["迟到"]):
                    r = row("D", "1", "甲", st, kind=kind, tags=tags)
                    s = board_stats.summarize_rows([r])
                    out = collect_exceptions([r])
                    got_nc = bool(out and out[0]["noCard"])
                    got_lt = bool(out and out[0]["late"])
                    self.assertEqual(got_nc, bool(s["noCard"]), "%s/%s/%s" % (st, kind, tags))
                    self.assertEqual(got_lt, bool(s["late"]), "%s/%s/%s" % (st, kind, tags))
                    if not s["expected"]:
                        self.assertEqual(out, [], "%s/%s" % (st, kind))

    def test_anomaly_hidden_never_listed(self):
        rows = [row("A", "1", "甲", "absent_expected", anomaly_hidden=True),
                row("A", "2", "乙", "late", anomaly_hidden=True)]
        self.assertEqual(collect_exceptions(rows), [])

    def test_counts_match_summarize_rows(self):
        rows = [row("六车间", "1", "甲", "absent_expected"),
                row("六车间", "2", "乙", "no_pair"),
                row("六车间", "3", "丙", "late"),
                row("仓储部", "4", "丁", "pending"),
                row("仓储部", "5", "戊", "present")]
        out = {e["dept"]: e for e in collect_exceptions(rows)}
        for dept in ("六车间", "仓储部"):
            s = board_stats.summarize_rows([r for r in rows if r["dept"] == dept])
            self.assertEqual(len(out[dept]["noCard"]), s["noCard"], dept)
            self.assertEqual(len(out[dept]["late"]), s["late"], dept)

    def test_dept_order_matches_dept_summary(self):
        rows = [row("A", "1", "甲", "absent_expected"),
                row("B", "2", "乙", "absent_expected"),
                row("B", "3", "丙", "absent_expected")]
        out = [e["dept"] for e in collect_exceptions(rows)]
        ordered = [d_["dept"] for d_ in board_stats.dept_summary(rows) if d_["dept"] in out]
        self.assertEqual(out, ordered)
        self.assertEqual(out, ["B", "A"])          # 异常多的部门在前

    def test_depts_without_exception_omitted(self):
        rows = [row("正常部", "1", "甲", "present"),
                row("异常部", "2", "乙", "absent_expected")]
        self.assertEqual([e["dept"] for e in collect_exceptions(rows)], ["异常部"])

    def test_empty_rows(self):
        self.assertEqual(collect_exceptions([]), [])
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify -v
```
Expected: FAIL —— `ImportError: cannot import name 'collect_exceptions'`（同时 `render_card` / `build_text_payload` 也尚未存在）

- [ ] **Step 3: 实现**

在 `attendance_notify.py` 顶部常量区追加（放在 `NO_CARD_HINT` 之后）：

```python
MAX_NAMES_PER_DEPT = 6      # 单个部门最多显示的人名数，超出收成「等 N 人」
MAX_EXCEPTION_DEPTS = 10    # 卡片最多列出的异常部门数，超出收成「另有 N 个部门」
```

在 `# ---- 以下依赖 frappe / requests ----` 分隔线**之前**（即紧随 `should_send_now` 之后）新增：

```python
def collect_exceptions(rows):
    """挑出未打卡 / 迟到的人，按部门聚合。

    归桶不重写规则：对每行调一次 board_stats.summarize_rows([r])，直接取它的
    late / noCard 计数作为该行的归桶判定 —— 与统计口径字面同源，board_stats.py 不改。
    返回只含至少一项异常的部门，部门顺序与 dept_summary 一致（未打卡+迟到降序）。
    """
    from hb_attendance_app.hbos_attendance import board_stats as bs

    buckets = {}
    for r in rows:
        # anomaly_hidden 的行不进任何桶（与看板、统计一致，如设备动力部）
        if r.get("anomaly_hidden"):
            continue
        # 单行统计即该行的归桶判定。expected 为假的行（豁免/休息/请假/班次未定/非班次）
        # 一律不算异常——注意这里不能改用 bs.is_late(r) 单独判迟到，
        # 那会把「非应出勤行 + 迟到标签」也算进来，而 summarize_rows 不计，两处数字就会漂移。
        s = bs.summarize_rows([r])
        if not s["expected"] or not (s["late"] or s["noCard"]):
            continue
        dept = r.get("dept") or "未分组"
        b = buckets.setdefault(dept, {"dept": dept, "noCard": [], "late": []})
        person = {"name": r.get("name") or "", "num": r.get("num") or ""}
        if s["late"]:
            b["late"].append(person)
        if s["noCard"]:
            b["noCard"].append(person)

    order = {s["dept"]: i for i, s in enumerate(bs.dept_summary(rows))}
    out = sorted(buckets.values(), key=lambda b: order.get(b["dept"], len(order)))
    for b in out:
        b["noCard"].sort(key=lambda p: p["num"])
        b["late"].sort(key=lambda p: p["num"])
    return out
```

> `dept_summary` 内用 `group_by_dept`，把空部门名映射成「未分组」，与本函数一致，故 `order` 查得到。
> 逐行调 `summarize_rows` 是 O(n)·O(1)，全厂约 600 行，开销可忽略；换来的是归桶逻辑零重复、改口径时不会两处漂移。

- [ ] **Step 4: 运行确认通过**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify.CollectExceptionsTest -v
```
Expected: PASS（11 例）。注意此时整个测试文件仍会因 `render_card` / `build_text_payload` 尚未定义而 import 失败——若出现 `ImportError`，把这两个名字**暂时从测试 import 行移除**，Task 2 完成后加回。

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py apps/hb_attendance_app/tests/test_attendance_notify.py
git commit -m "feat: 新增考勤异常名录提取纯函数（复用 board_stats 口径，不重写判定）"
```

---

### Task 2: `render_card` —— 渲染飞书卡片 JSON

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py`（继续在 frappe 分隔线之前新增）
- Test: `apps/hb_attendance_app/tests/test_attendance_notify.py`

**Interfaces:**
- Consumes: `collect_exceptions` 的返回结构（Task 1）；`render_report` 用的 `dept_stats` 结构；`get_data()["stats"]`（`summarize_rows` 的输出）；`EMPTY_HINT`、`NO_CARD_HINT`（模块既有常量）
- Produces: `render_card(date_str, hhmm, stats, dept_stats, exceptions) -> dict`，返回飞书卡片 JSON（`config` / `header` / `elements`），**不含 `msg_type`**

- [ ] **Step 1: 写失败测试**

把 Task 1 的 Step 1 中曾暂时移除的 `render_card` 名字加回 import 行。在文件末尾（`unittest.main()` 之前）新增：

```python
def card_text(card):
    """把卡片里所有文本元素拍平，便于断言（分隔线等无文本元素自动跳过）。"""
    parts = []
    for el in card["elements"]:
        t = (el.get("text") or {}).get("content")
        if t:
            parts.append(t)
    return "\n".join(parts)


class RenderCardTest(unittest.TestCase):
    STATS = {"expected": 696, "present": 651, "noCard": 12, "late": 3,
             "notStarted": 40, "outOnly": 8, "unknownTime": 5}

    def _card(self, exceptions, dept_stats=None, stats=None):
        return render_card("2026-09-11", "09:00", stats or self.STATS,
                           dept_stats if dept_stats is not None else [d("六车间", 40, 36, 3)],
                           exceptions)

    def test_header_orange_when_alert(self):
        card = self._card([{"dept": "六车间", "noCard": [{"name": "张三", "num": "1"}], "late": []}])
        self.assertEqual(card["header"]["template"], "orange")
        self.assertEqual(card["header"]["title"]["content"], "考勤到岗 · 2026-09-11 09:00")

    def test_header_green_and_hint_when_no_alert(self):
        card = self._card([], dept_stats=[d("六车间", 40, 36, 0)],
                          stats=dict(self.STATS, noCard=0, late=0))
        self.assertEqual(card["header"]["template"], "green")
        self.assertIn("全部正常", card_text(card))

    def test_factory_line_shows_expected_and_present_always(self):
        card = self._card([], dept_stats=[d("六车间", 40, 36, 0)],
                          stats=dict(self.STATS, noCard=0, late=0))
        txt = card_text(card)
        self.assertIn("应出勤 696", txt)
        self.assertIn("已到岗 651", txt)

    def test_factory_line_omits_zero_fields(self):
        card = self._card([], dept_stats=[d("六车间", 40, 36, 0)],
                          stats=dict(self.STATS, noCard=0, late=0))
        txt = card_text(card)
        self.assertNotIn("未打卡 0", txt)
        self.assertNotIn("迟到 0", txt)

    def test_dept_line_shows_only_nonzero_counts(self):
        card = self._card([{"dept": "仓储部", "noCard": [{"name": "赵六", "num": "1"}], "late": []}],
                          dept_stats=[d("仓储部", 17, 15, 1)])
        txt = card_text(card)
        self.assertIn("未打卡 1", txt)
        self.assertNotIn("迟到 0", txt)

    def test_single_type_omits_name_label(self):
        txt = card_text(self._card(
            [{"dept": "六车间", "noCard": [{"name": "张三", "num": "1"}], "late": []}]))
        self.assertIn("张三", txt)
        self.assertNotIn("未打卡：张三", txt)

    def test_both_types_get_labels(self):
        txt = card_text(self._card(
            [{"dept": "六车间", "noCard": [{"name": "张三", "num": "1"}],
              "late": [{"name": "李四", "num": "2"}]}],
            dept_stats=[d("六车间", 40, 36, 1, 1)]))
        self.assertIn("未打卡：张三", txt)
        self.assertIn("迟到：李四", txt)

    def test_names_truncated_at_six(self):
        people = [{"name": "人%d" % i, "num": "%03d" % i} for i in range(1, 10)]   # 9 人
        txt = card_text(self._card([{"dept": "六车间", "noCard": people, "late": []}],
                                   dept_stats=[d("六车间", 40, 36, 9)]))
        self.assertIn("等 3 人", txt)
        self.assertNotIn("人9", txt)

    def test_depts_truncated_at_ten(self):
        dept_stats = [d("D%02d" % i, 10, 9, 1) for i in range(1, 13)]             # 12 个部门
        exc = [{"dept": "D%02d" % i, "noCard": [{"name": "张三", "num": "1"}], "late": []}
               for i in range(1, 13)]
        txt = card_text(self._card(exc, dept_stats=dept_stats))
        self.assertIn("另有 2 个部门存在异常", txt)
        self.assertNotIn("D11", txt)
        self.assertNotIn("D12", txt)

    def test_normal_dept_count(self):
        dept_stats = [d("A", 10, 9, 1), d("B", 10, 10, 0), d("C", 10, 10, 0)]
        txt = card_text(self._card(
            [{"dept": "A", "noCard": [{"name": "张三", "num": "1"}], "late": []}],
            dept_stats=dept_stats))
        self.assertIn("其余 2 个部门全部正常", txt)

    def test_no_normal_dept_line_when_all_alert(self):
        dept_stats = [d("A", 10, 9, 1)]
        txt = card_text(self._card(
            [{"dept": "A", "noCard": [{"name": "张三", "num": "1"}], "late": []}],
            dept_stats=dept_stats))
        self.assertIn("⚠ 需处理（1 个部门）", txt)
        self.assertNotIn("其余", txt)

    def test_zero_expected_dept_not_counted_as_normal(self):
        dept_stats = [d("A", 10, 9, 1), d("全员休息", 0, 0, 0)]
        txt = card_text(self._card(
            [{"dept": "A", "noCard": [{"name": "张三", "num": "1"}], "late": []}],
            dept_stats=dept_stats))
        self.assertNotIn("其余 1 个部门全部正常", txt)

    def test_note_present_when_not_started_group_exists(self):
        txt = card_text(self._card([], dept_stats=[d("A", 10, 9, 0)],
                                   stats=dict(self.STATS, noCard=0, late=0)))
        self.assertIn(NO_CARD_HINT, txt)

    def test_note_absent_when_no_not_started_group(self):
        txt = card_text(self._card([], dept_stats=[d("A", 10, 9, 0)],
                                   stats=dict(self.STATS, noCard=0, late=0,
                                              notStarted=0, outOnly=0, unknownTime=0)))
        self.assertNotIn(NO_CARD_HINT, txt)

    def test_no_expected_dept_shows_empty_hint_only(self):
        card = render_card("2026-09-11", "09:00",
                           dict(self.STATS, expected=0, present=0, noCard=0, late=0), [], [])
        self.assertEqual(card["header"]["template"], "green")
        txt = card_text(card)
        self.assertIn(EMPTY_HINT, txt)
        self.assertNotIn(NO_CARD_HINT, txt)

    def test_serializable(self):
        import json
        card = self._card([{"dept": "六车间", "noCard": [{"name": "张三", "num": "1"}], "late": []}])
        self.assertIsInstance(json.dumps(card, ensure_ascii=False), str)

    def test_every_element_is_renderable(self):
        # 卡片元素只允许 div / hr（最小能力集，避免依赖表格组件的版本兼容性）
        card = self._card([{"dept": "六车间", "noCard": [{"name": "张三", "num": "1"}], "late": []}])
        self.assertEqual(card["config"], {"wide_screen_mode": True})
        self.assertIn("header", card)
        for el in card["elements"]:
            self.assertIn(el["tag"], ("div", "hr"))
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify.RenderCardTest -v
```
Expected: FAIL —— `ImportError: cannot import name 'render_card'`

- [ ] **Step 3: 实现**

在 `attendance_notify.py` 中 `collect_exceptions` 之后、frappe 分隔线之前新增：

```python
CARD_TITLE_PREFIX = "考勤到岗 · "


def _md(content):
    """卡片文本元素（lark_md 支持 **粗体** 与换行）。"""
    return {"tag": "div", "text": {"tag": "lark_md", "content": content}}


def _fmt_names(people):
    """人名串；超过 MAX_NAMES_PER_DEPT 人时收成「等 N 人」，N 必须显式写出。"""
    names = [p["name"] for p in people]
    if len(names) > MAX_NAMES_PER_DEPT:
        return " · ".join(names[:MAX_NAMES_PER_DEPT]) + " 等 %d 人" % (
            len(names) - MAX_NAMES_PER_DEPT)
    return " · ".join(names)


def _counts_text(exc):
    """部门行的计数：只写非零项，不出现「迟到 0」。"""
    parts = []
    if exc["noCard"]:
        parts.append("未打卡 %d" % len(exc["noCard"]))
    if exc["late"]:
        parts.append("迟到 %d" % len(exc["late"]))
    return "　".join(parts)


def _dept_block(exc):
    """一个异常部门：标题行 + 人名行。两类都有时给人名加标签，只有一类时省略标签。"""
    lines = ["**%s**　%s" % (exc["dept"], _counts_text(exc))]
    no_card, late = exc["noCard"], exc["late"]
    if no_card and late:
        lines.append("未打卡：%s" % _fmt_names(no_card))
        lines.append("迟到：%s" % _fmt_names(late))
    elif no_card:
        lines.append(_fmt_names(no_card))
    elif late:
        lines.append(_fmt_names(late))
    return "\n".join(lines)


def _factory_line(stats):
    """全厂汇总：应出勤与已到岗恒显示，未打卡 / 迟到为 0 时省略。"""
    parts = ["应出勤 %d" % stats.get("expected", 0),
             "已到岗 %d" % stats.get("present", 0)]
    if stats.get("noCard"):
        parts.append("未打卡 %d" % stats["noCard"])
    if stats.get("late"):
        parts.append("迟到 %d" % stats["late"])
    return "**全厂**　" + " · ".join(parts)


def _needs_note(stats):
    """有「未到上班时间 / 仅下班卡 / 班次未定」的人时，才需要附口径说明。"""
    return bool(stats.get("notStarted") or stats.get("outOnly") or stats.get("unknownTime"))


def render_card(date_str, hhmm, stats, dept_stats, exceptions):
    """渲染飞书卡片 JSON（纯函数，不涉及网络，不含 msg_type）。

    有异常 → 橙色标题；无异常 → 绿色标题。只列异常部门，正常部门收成一行。
    """
    expected_depts = [x for x in dept_stats if (x.get("expected") or 0) > 0]
    has_alert = bool(exceptions)
    elements = []

    if not expected_depts:
        elements.append(_md(EMPTY_HINT))
    else:
        elements.append(_md(_factory_line(stats)))
        if has_alert:
            elements.append({"tag": "hr"})
            elements.append(_md("⚠ 需处理（%d 个部门）" % len(exceptions)))
            shown = exceptions[:MAX_EXCEPTION_DEPTS]
            for exc in shown:
                elements.append(_md(_dept_block(exc)))
            omitted = len(exceptions) - len(shown)
            if omitted > 0:
                elements.append(_md("另有 %d 个部门存在异常（详见部门看板）" % omitted))
            normal = len(expected_depts) - len(exceptions)
            if normal > 0:
                elements.append({"tag": "hr"})
                elements.append(_md("其余 %d 个部门全部正常" % normal))
        else:
            elements.append(_md("✅ 各部门全部正常，无未打卡、无迟到"))
        if _needs_note(stats):
            elements.append({"tag": "hr"})
            elements.append(_md(NO_CARD_HINT))

    return {
        "config": {"wide_screen_mode": True},
        "header": {
            "template": "orange" if has_alert else "green",
            "title": {"tag": "plain_text",
                      "content": "%s%s %s" % (CARD_TITLE_PREFIX, date_str, hhmm)},
        },
        "elements": elements,
    }
```

- [ ] **Step 4: 运行确认通过**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify -v
```
Expected: PASS（Task 1 + Task 2 全部新例通过；既有 20 例仍通过——本任务未改动既有函数）

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py apps/hb_attendance_app/tests/test_attendance_notify.py
git commit -m "feat: 新增考勤卡片渲染纯函数（只列异常部门+人名，超限显式截断）"
```

---

### Task 3: 报文切为卡片 + 降级路径 + 更新既有断言

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py`（`build_feishu_payload`、`send_daily_report`，新增 `build_text_payload` / `_build_payload` / `_dry_run_detail`）
- Test: `apps/hb_attendance_app/tests/test_attendance_notify.py`

**Interfaces:**
- Consumes: `collect_exceptions`（Task 1）、`render_card`（Task 2）、既有 `render_report`
- Produces: `build_feishu_payload(card) -> {"msg_type": "interactive", "content": card}`；`build_text_payload(text) -> {"msg_type": "text", "content": {"text": text}}`

- [ ] **Step 1: 写失败测试**

**先更新两处既有断言**（它们现在断言的是纯文本报文）：

把 `FeishuPayloadTest` 整个类替换为：

```python
class FeishuPayloadTest(unittest.TestCase):
    """飞书报文构造（Owner 2026-09-11：纯文本改为 interactive 卡片，纯文本仅作降级）。"""

    def test_interactive_message_shape(self):
        card = {"header": {"template": "green"}, "elements": []}
        p = build_feishu_payload(card)
        self.assertEqual(p, {"msg_type": "interactive", "content": card})

    def test_payload_has_only_expected_keys(self):
        p = build_feishu_payload({"elements": []})
        self.assertEqual(sorted(p.keys()), ["content", "msg_type"])

    def test_text_payload_shape(self):
        p = build_text_payload("【考勤到岗】2026-09-11 09:00\n部门 ...")
        self.assertEqual(p, {"msg_type": "text",
                             "content": {"text": "【考勤到岗】2026-09-11 09:00\n部门 ..."}})
```

把 `HeaderAlignmentTest` 里的 `test_feishu_payload_carries_rendered_title` 替换为：

```python
    def test_text_payload_carries_rendered_title(self):
        text = render_report("2026-09-10", "09:00", [])
        p = build_text_payload(text)
        self.assertIn("【考勤到岗】2026-09-10 09:00", p["content"]["text"])
```

**再让假取数返回明细行**，并新增降级测试。把 `SendDailyReportDispatchTest._default_get_data` 替换为：

```python
    def _default_get_data(self, department=None, date_str=None):
        self.get_data_calls.append((department, date_str))
        rows = [row("无菌车间", "0001", "张三", "absent_expected"),
                row("无菌车间", "0002", "李四", "present")]
        return {"stats": board_stats.summarize_rows(rows),
                "dept_stats": board_stats.dept_summary(rows),
                "rows": rows}
```

在 `SendDailyReportDispatchTest` 末尾追加：

```python
    def test_card_path_sends_interactive_payload(self):
        captured = {}

        def fake_post(url, token, payload):
            captured["payload"] = payload
            return True, "HTTP 200"

        with mock.patch.dict(os.environ, {
                "HBOS_NOTIFY_DRY_RUN": "",
                "HBOS_NOTIFY_WEBHOOK_URL": "https://example.invalid/hook"}, clear=False), \
                mock.patch.object(attendance_notify, "post_to_webhook", fake_post):
            result = attendance_notify.send_daily_report(force=True)
        self.assertTrue(result["sent"])
        payload = captured["payload"]
        self.assertEqual(payload["msg_type"], "interactive")
        self.assertEqual(payload["content"]["header"]["template"], "orange")   # 有未打卡 → 橙
        body = "\n".join(el.get("text", {}).get("content", "")
                         for el in payload["content"]["elements"])
        self.assertIn("张三", body)

    def test_card_render_failure_degrades_to_text_and_still_sends(self):
        captured = {}

        def fake_post(url, token, payload):
            captured["payload"] = payload
            return True, "HTTP 200"

        with mock.patch.dict(os.environ, {
                "HBOS_NOTIFY_DRY_RUN": "",
                "HBOS_NOTIFY_WEBHOOK_URL": "https://example.invalid/hook"}, clear=False), \
                mock.patch.object(attendance_notify, "render_card",
                                  side_effect=RuntimeError("card-boom")), \
                mock.patch.object(attendance_notify, "post_to_webhook", fake_post):
            result = attendance_notify.send_daily_report(force=True)
        self.assertTrue(result["sent"])                      # 降级后仍发出，不漏发
        self.assertEqual(captured["payload"]["msg_type"], "text")
        self.assertIn("考勤到岗", captured["payload"]["content"]["text"])
        self.assertTrue(self.log_calls)                      # 降级有留痕

    def test_dry_run_logs_both_card_and_text(self):
        with mock.patch.dict(os.environ, {
                "HBOS_NOTIFY_DRY_RUN": "1",
                "HBOS_NOTIFY_WEBHOOK_URL": ""}, clear=False):
            result = attendance_notify.send_daily_report(force=True)
        self.assertEqual(result["reason"], "dry_run")
        logged = "\n".join(str(c) for c in self.log_calls)
        self.assertIn("interactive", logged)                 # 卡片 JSON 留档
        self.assertIn("纯文本", logged)                       # 纯文本留档
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify -v
```
Expected: FAIL —— `ImportError: cannot import name 'build_text_payload'`；即便注释掉该 import，`test_interactive_message_shape` 也会因 `msg_type` 仍为 `text` 而失败

- [ ] **Step 3: 实现**

**(a)** 替换 `build_feishu_payload` 整个函数：

```python
def build_feishu_payload(card):
    """飞书卡片报文（msg_type=interactive）。

    Owner 2026-09-11：纯文本 + 空格补位在飞书比例字体下必然错列，改发卡片由组件负责对齐。
    """
    return {"msg_type": "interactive", "content": card}


def build_text_payload(text):
    """纯文本报文；卡片渲染失败时降级使用。"""
    return {"msg_type": "text", "content": {"text": text}}
```

**(b)** 在 `_mark_sent_best_effort` 之后、`send_daily_report` 之前新增：

```python
def _build_payload(data, date_str, hhmm, dept_stats, text):
    """构造出站报文；卡片渲染任何异常都降级为纯文本（宁可丑，不能不发）。"""
    import frappe
    try:
        exceptions = collect_exceptions(data.get("rows") or [])
        card = render_card(date_str, hhmm, data.get("stats") or {}, dept_stats, exceptions)
        return build_feishu_payload(card)
    except Exception as e:
        frappe.log_error("%s\n%s" % (e, text), "HBOS考勤通知卡片渲染失败(降级为纯文本)")
        return build_text_payload(text)


def _dry_run_detail(payload, text):
    """干跑留档：卡片 JSON 与纯文本都留，便于先核对版式再配地址。"""
    import json
    return "纯文本:\n%s\n\n报文JSON:\n%s" % (
        text, json.dumps(payload, ensure_ascii=False, indent=2))
```

**(c)** 修改 `send_daily_report` 中取数到干跑之间的四行。把：

```python
        data = get_data(department=None, date_str=date_str)
        dept_stats = data.get("dept_stats") or []
        text = render_report(date_str, now.strftime("%H:%M"), dept_stats)
        payload = build_feishu_payload(text)

        dry = os.environ.get("HBOS_NOTIFY_DRY_RUN", "") == "1"
        url = os.environ.get("HBOS_NOTIFY_WEBHOOK_URL", "")
        if dry or not url:
            frappe.log_error(text, "HBOS考勤通知(未发送: %s)" % ("dry_run" if dry else "no_webhook"))
```

替换为：

```python
        data = get_data(department=None, date_str=date_str)
        dept_stats = data.get("dept_stats") or []
        hhmm = now.strftime("%H:%M")
        text = render_report(date_str, hhmm, dept_stats)      # 纯文本：降级与留档用
        payload = _build_payload(data, date_str, hhmm, dept_stats, text)

        dry = os.environ.get("HBOS_NOTIFY_DRY_RUN", "") == "1"
        url = os.environ.get("HBOS_NOTIFY_WEBHOOK_URL", "")
        if dry or not url:
            frappe.log_error(_dry_run_detail(payload, text),
                             "HBOS考勤通知(未发送: %s)" % ("dry_run" if dry else "no_webhook"))
```

发送失败处的日志保持原样（`"%s\n%s" % (msg, text)`）——人排障时读文本，不读 JSON。

- [ ] **Step 4: 运行确认通过**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_attendance_notify -v
```
Expected: PASS（Task 1 + 2 + 3 全部；既有 `ModuleContractTest` / `ShouldSendNowTest` / `RenderReportTest` 仍通过）

- [ ] **Step 5: 全量离线回归**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m compileall -q apps/hb_attendance_app && python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -5
```
Expected: `OK`，无 FAIL / ERROR（确认未波及其它模块）

- [ ] **Step 6: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/attendance_notify.py apps/hb_attendance_app/tests/test_attendance_notify.py
git commit -m "feat: 考勤通知改发飞书卡片（渲染失败自动降级纯文本）"
```

---

### Task 4: 运行态验证与文档（需 Owner 授权）

> 本任务全部运行态步骤涉及飞书真实写入，**必须先获 Owner 明确授权**。未授权前只做 Step 5 的文档与 Step 6 的提交。

**Files:**
- Modify: `docs/HBOS考勤判定规则.md`
- Modify: `docs/PROJECT_STATUS.md`
- Modify: `docs/CURRENT_MILESTONE.md`

**Interfaces:**
- Consumes: Task 1–3 的全部产出
- Produces: 经群里确认可渲染的卡片；更新后的状态台账

- [ ] **Step 1: 最小卡片通路验证（需 Owner 授权）**

卡片组件在飞书群机器人 webhook 上的兼容性无法离线验证，故先发一张最小卡片。
把下面这段写进容器可执行的位置（或直接在 console 里粘贴执行）：

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
docker compose exec -T backend bench --site frontend console <<'PY'
import os, json
from hb_attendance_app.hbos_attendance.attendance_notify import (
    build_feishu_payload, post_to_webhook)
card = {
    "config": {"wide_screen_mode": True},
    "header": {"template": "orange",
               "title": {"tag": "plain_text", "content": "考勤到岗 · 卡片通路测试"}},
    "elements": [{"tag": "div",
                  "text": {"tag": "lark_md",
                           "content": "最小卡片渲染测试，收到即表示卡片通路可用。"}}],
}
print(post_to_webhook(os.environ.get("HBOS_NOTIFY_WEBHOOK_URL", ""), "", build_feishu_payload(card)))
PY
```

Expected: 返回 `(True, 'HTTP 200')`，且群里出现一张橙边卡片。
**若失败**：先试把 `header.title.tag` 与文本元素的 `tag` 降级为卡片 v1 结构重试；
仍失败则执行保底——保留纯文本路线（`build_text_payload`），并向 Owner 说明卡片路线不可行、停止后续步骤。

- [ ] **Step 2: 干跑核对卡片版式与数字（需 Owner 授权）**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
docker compose exec -T backend bench --site frontend execute \
  hb_attendance_app.hbos_attendance.attendance_notify.send_daily_report --kwargs '{"force": true}'
```
（若 `bench execute` 不支持 kwargs，改用 console 调用 `send_daily_report(force=True)`）

Expected: 返回 `{"sent": false, "reason": "dry_run", ...}`；Desk → **Error Log** 中出现标题为
`HBOS考勤通知(未发送: dry_run)` 的一条，内容含「纯文本」与「报文JSON」两段。

- [ ] **Step 3: 逐项比对数字（人工）**

同日在「部门看板」核对：卡片里每个异常部门的「未打卡 / 迟到」人数、人名，必须与看板展开后
逐条一致；全厂行的四个数字必须等于看板全厂汇总。
**若不一致** → 停下排查，不得继续。口径未改动，出现不一致即为实现缺陷。

- [ ] **Step 4: 实发完整卡片（需 Owner 授权）**

确认 `.env` 中 `HBOS_NOTIFY_DRY_RUN=1` 已改回 `0` 或空，重启 backend 容器后再次执行 Step 2 的命令
（`force=True`）。Expected: 群里收到完整卡片；Error Log 无异常；
`hbos_notify_sent:<日期>` 幂等位已置位（可用 console 查 `frappe.cache.get_value`）。

- [ ] **Step 5: 更新文档**

- `docs/HBOS考勤判定规则.md` 的「考勤通知」一节追加：消息形态为飞书 interactive 卡片；
  只列异常部门（未打卡/迟到）并带人名，正常部门收成一行；人名上限 6 人、异常部门上限 10 个，
  超出显式标注；卡片渲染失败自动降级为纯文本；口径仍来自 `board_stats`（本轮未改判定）。
- `docs/PROJECT_STATUS.md` 与 `docs/CURRENT_MILESTONE.md` 的 M1-FIX-B5 段各追加一句：
  「2026-09-11 追加：考勤提醒改发飞书卡片（只列异常部门+人名，正常部门汇总一行；
  渲染失败自动降级纯文本；判定口径与调度不变）。」

- [ ] **Step 6: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add docs/HBOS考勤判定规则.md docs/PROJECT_STATUS.md docs/CURRENT_MILESTONE.md
git commit -m "docs: 记录考勤提醒卡片版式与降级行为（M1-FIX-B5 追加）"
```

- [ ] **Step 7: 收尾报告**

按 `CLAUDE.md` 要求输出：新增/修改文件清单、离线测试结果、最小卡片验证结论、
干跑输出（含卡片 JSON）、看板比对结论、是否已实发到群、**尚未完成的事项**。
并说明状态台账与公共入口文件（`README.md` / `CLAUDE.md` / `AGENTS.md` /
`docs/AI_CONTEXT.md` / `docs/READING_GUIDE.md`）哪些已更新、哪些无需更新及原因。

---

## 自检记录

**1. Spec 覆盖**：§4 卡片结构 → Task 2；§5 截断规则 → Task 2（`MAX_NAMES_PER_DEPT` / `MAX_EXCEPTION_DEPTS`）；§6.1 `collect_exceptions` → Task 1；§6.2/6.3 报文 → Task 3；§7 兜底 → Task 3（降级）+ Task 4 Step 1（通路验证）；§8 测试 → Task 1–3 各 Step 1；§9 验收 → Task 3 Step 5 + Task 4。§3 的「纯文本加强版」保底写在 Task 4 Step 1 的失败分支。无遗漏。

**2. 占位符扫描**：无 TBD / TODO；每个代码步骤都给了完整可粘贴的代码；每个测试步骤都给了具体命令与预期输出。

**3. 类型一致性**：`collect_exceptions` 返回 `[{"dept", "noCard", "late"}]`，Task 2 的 `render_card` 与测试构造的 exceptions 字面量一致；`_counts_text` / `_fmt_names` / `_dept_block` 均以该结构为入参；`build_feishu_payload(card)` 与 `build_text_payload(text)` 的调用点在 Task 3 的 `_build_payload` / 降级分支中与定义一致。`render_card` 签名在 spec §6.2 与本计划中均为 `(date_str, hhmm, stats, dept_stats, exceptions)`（spec 已同步修正加入 `stats`）。

**已发现的 spec 缺陷并就地修正**：spec §6 原签名漏了 `stats`（全厂汇总行需要它），已改为 `render_card(date_str, hhmm, stats, dept_stats, exceptions)`，并补上 `build_text_payload`。

**归桶实现方式（Owner 2026-09-11 裁决）**：`collect_exceptions` 初稿把 `summarize_rows` 的 `if/elif` 骨架在 `attendance_notify` 里重写了一遍（分支体为空），属重复逻辑块，会被代码审查判为缺陷。改为**单行委派**——对每行调一次 `bs.summarize_rows([r])`，**`late` 与 `noCard` 都取它的结果**，归桶判定零重复、与统计口径字面同源，且 `board_stats.py` 仍一行不改。逐行 O(1)，全厂约 600 行开销可忽略。

**单行委派的第一版引入过一个真实缺陷（已修，并已固化为回归测试）**：第一版只用单行委派判「未打卡」，迟到仍单独调 `bs.is_late(r)`。但 `summarize_rows` 对**非应出勤行**（`leave` / `rest` / `exempt` / `unknown` / `kind != "shift"`）直接跳过、不计任何桶，而 `is_late` 只看 `state` 与 tags，故一行「休假 + 迟到标签」会被 `is_late` 判为迟到、却被统计忽略 → 卡片会列出看板不认的人，正是验收标准 4 明令禁止的「数字漂移」。
修法：`late` 也取单行统计结果，并以 `s["expected"]` 为前置门槛。已用 state × kind × 标签 × `anomaly_hidden` 共 144 组合全枚举验证，并加全厂/逐部门聚合一致性断言，全部通过。计划中已加入对应回归测试（`test_late_tag_on_non_expected_row_is_ignored`、`test_matches_summarize_rows_over_all_states`）。
