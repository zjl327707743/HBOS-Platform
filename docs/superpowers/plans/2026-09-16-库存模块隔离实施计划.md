# M2-STOCK-R1 库存模块隔离实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在同一条 Docker 栈内新建独立站点 `stock`，配合轻量 `hb_stock_app` 挂载「海滨库存」工作台，使海滨拥有一套与考勤站点数据完全隔离的 ERPNext 原生库存，并可通过 `http://localhost:8082` 访问。

**Architecture:** 不复制 ERPNext 代码，也不修改其源码。隔离靠 Frappe 原生的「一栈多站点」实现：新站点有独立数据库 schema、独立用户与权限；`hb_stock_app` 只承载一个 Workspace 定义和 `after_migrate` 幂等同步逻辑，不新建任何 DocType、不重写任何库存逻辑。现有 `frontend` 站点通过新增一个前端容器（注入不同的 `X-Frappe-Site-Name`）保持零改动。

**Tech Stack:** Frappe Framework 16、ERPNext 16、MariaDB 11.8、Docker Compose、Python 3.14、unittest（离线纯函数测试）

## Global Constraints

以下为 spec 定稿的项目级要求，每个任务的要求都隐含包含本节。

- **不修改 Frappe / ERPNext / HRMS 核心源码。**
- **不执行 `docker compose down -v`，不删除 Docker volume，不重建 `frontend` site。**
- **不提交 `.env`、App Secret、密钥、token、真实员工数据、Excel/CSV 数据文件。**
- 站点名固定为 `stock`；访问端口固定为 `8082`。
- 公司主体固定为 `HAIBIN`（abbr `H`，country `China`，default_currency `CNY`），与 `frontend` 站点口径一致。
- `stock` 站点只装 `frappe`、`erpnext`、`hb_stock_app`；**不装** `hrms`、`hb_attendance_app`。
- `hb_stock_app` **不装到 `frontend` 站点**，考勤站点零污染。
- `hb_stock_app` 内**不新建任何 DocType**、不重写库存逻辑。
- 所有 Git commit 描述使用中文，可保留 `feat:` / `docs:` / `chore:` 前缀。
- 每个任务结束必须让 `frontend` 站点保持可用（`http://localhost:8080/login` 返回 200）。

## File Structure

| 文件 | 职责 |
| --- | --- |
| `apps/hb_stock_app/pyproject.toml` | App 打包元数据，声明 frappe/erpnext 依赖 |
| `apps/hb_stock_app/README.md` | App 用途说明 |
| `apps/hb_stock_app/modules.txt` | App 级模块登记，内容 `HBOS Stock` |
| `apps/hb_stock_app/__init__.py` | 空文件，使 App 根目录成为包 |
| `apps/hb_stock_app/hb_stock_app/__init__.py` | 包版本号 |
| `apps/hb_stock_app/hb_stock_app/modules.txt` | 包级模块登记，内容 `HBOS Stock` |
| `apps/hb_stock_app/hb_stock_app/hooks.py` | `after_migrate` 钩子注册 |
| `apps/hb_stock_app/hb_stock_app/hbos_stock/__init__.py` | 模块目录标记 |
| `apps/hb_stock_app/hb_stock_app/hbos_stock/workspace_builder.py` | **纯函数**：Stock 工作台 payload → 海滨库存 payload |
| `apps/hb_stock_app/hb_stock_app/hbos_stock/setup.py` | `after_migrate` 入口 + 幂等同步 Workspace |
| `apps/hb_stock_app/hb_stock_app/hbos_stock/init_site.py` | 站点初始化：Company `HAIBIN` + 默认仓库，幂等 |
| `apps/hb_stock_app/hb_stock_app/hbos_stock/workspace/海滨库存/海滨库存.json` | 生成的工作台 fixture，进 Git |
| `apps/hb_stock_app/tests/test_workspace_builder.py` | `workspace_builder` 的离线单测 |
| `tools/gen_stock_workspace_fixture.py` | 从运行态 Stock 工作台生成 fixture 的开发工具 |
| `docker-compose.yml` | 新增 App 卷挂载与 PYTHONPATH；新增 `frontend-stock` 服务 |
| `.env` / `.env.example` | 新增 `STOCK_SITE_NAME` / `STOCK_HTTP_PORT` / `STOCK_ADMIN_PASSWORD` |
| `docs/milestones/M2_START_GATE.md` | M2 阶段门禁文档（新建） |
| `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md` | 本轮主文档（新建） |

**关键设计决定（`workspace_builder` 必须剥离子表行身份字段）**

实测：运行态 `Stock` 工作台的 72 条 links、1 条 charts、3 条 number_cards **每一条都带 `name`**（如 `si4ekhi8tq`），以及 `parent` / `owner` / `creation` / `modified`。子表 docname 在各自 child doctype 内**全局唯一**。若原样带进新工作台，frappe 会复use 同名子文档，等于把原生 `Stock` 工作台的子表行搬走或改写。因此转换时必须剥离这些字段，由 frappe 重新分配名字。这是本计划必须用测试守住的一条不变量。

---

### Task 1: hb_stock_app 骨架与工作台 fixture 生成器

**Files:**
- Create: `apps/hb_stock_app/pyproject.toml`
- Create: `apps/hb_stock_app/README.md`
- Create: `apps/hb_stock_app/modules.txt`
- Create: `apps/hb_stock_app/__init__.py`
- Create: `apps/hb_stock_app/hb_stock_app/__init__.py`
- Create: `apps/hb_stock_app/hb_stock_app/modules.txt`
- Create: `apps/hb_stock_app/hb_stock_app/hooks.py`
- Create: `apps/hb_stock_app/hb_stock_app/hbos_stock/__init__.py`
- Create: `apps/hb_stock_app/hb_stock_app/hbos_stock/workspace_builder.py`
- Create: `apps/hb_stock_app/hb_stock_app/hbos_stock/setup.py`
- Create: `apps/hb_stock_app/tests/test_workspace_builder.py`
- Create: `tools/gen_stock_workspace_fixture.py`
- Generate: `apps/hb_stock_app/hb_stock_app/hbos_stock/workspace/海滨库存/海滨库存.json`

**Interfaces:**
- Consumes: 无（首个任务）
- Produces:
  - `hb_stock_app.hbos_stock.workspace_builder.build_workspace_payload(source: dict) -> dict`
  - `hb_stock_app.hbos_stock.workspace_builder.TARGET_WORKSPACE: str`（值 `海滨库存`）
  - `hb_stock_app.hbos_stock.setup.after_migrate() -> None`
  - `hb_stock_app.hbos_stock.setup.load_workspace_fixture() -> dict`
  - fixture 文件路径约定：`hb_stock_app/hbos_stock/workspace/海滨库存/海滨库存.json`

- [ ] **Step 1: 写失败的测试**

创建 `apps/hb_stock_app/tests/test_workspace_builder.py`：

```python
import copy
import json
import unittest
from pathlib import Path

from hb_stock_app.hbos_stock.workspace_builder import (
    TARGET_APP,
    TARGET_MODULE,
    TARGET_WORKSPACE,
    build_workspace_payload,
)

FIXTURE_PATH = (
    Path(__file__).resolve().parent.parent
    / "hb_stock_app" / "hbos_stock" / "workspace" / TARGET_WORKSPACE
    / f"{TARGET_WORKSPACE}.json"
)


def synthetic_source():
    """最小化的 Stock 工作台 payload，字段名与结构照抄运行态实测结果。"""
    return {
        "name": "Stock",
        "label": "Stock",
        "title": "Stock",
        "module": "Stock",
        "app": "erpnext",
        "icon": "stock",
        "is_hidden": 0,
        "public": 1,
        "sequence_id": 7.0,
        "type": "Workspace",
        "content": json.dumps([
            {"id": "abc123", "type": "header", "data": {"text": "<b>Masters</b>", "col": 12}},
            {"id": "def456", "type": "card", "data": {"card_name": "Items Catalogue", "col": 4}},
        ]),
        "links": [
            {
                "name": "si4ekhi8tq", "owner": "Administrator",
                "creation": "2020-03-02 15:43:10.096528",
                "modified": "2026-07-21 12:26:58.048794", "modified_by": "Administrator",
                "docstatus": 0, "idx": 1, "type": "Link", "label": "Item", "icon": None,
                "description": None, "hidden": 0, "link_type": "DocType", "link_to": "Item",
                "report_ref_doctype": None, "dependencies": "", "only_for": None,
                "onboard": 1, "is_query_report": 0, "link_count": 0,
                "parent": "Stock", "parentfield": "links", "parenttype": "Workspace",
                "doctype": "Workspace Link",
            },
            {
                "name": "si4po43tnv", "owner": "Administrator",
                "creation": "2020-03-02 15:43:10.096528",
                "modified": "2026-07-21 12:26:58.048794", "modified_by": "Administrator",
                "docstatus": 0, "idx": 11, "type": "Card Break",
                "label": "Stock Transactions", "icon": None, "description": None,
                "hidden": 0, "link_type": None, "link_to": None, "report_ref_doctype": None,
                "dependencies": None, "only_for": None, "onboard": 0,
                "is_query_report": 0, "link_count": 0,
                "parent": "Stock", "parentfield": "links", "parenttype": "Workspace",
                "doctype": "Workspace Link",
            },
        ],
        "charts": [{
            "name": "si43jqfj07", "owner": "Administrator", "docstatus": 0, "idx": 1,
            "chart_name": "Stock Value by Item Group", "label": "Stock Value by Item Group",
            "parent": "Stock", "parentfield": "charts", "parenttype": "Workspace",
            "doctype": "Workspace Chart",
        }],
        "number_cards": [{
            "name": "si48oamd8m", "owner": "Administrator", "docstatus": 0, "idx": 1,
            "number_card_name": "Total Warehouses", "label": "Total Warehouses",
            "parent": "Stock", "parentfield": "number_cards", "parenttype": "Workspace",
            "doctype": "Workspace Number Card",
        }],
        "roles": [],
        "shortcuts": [],
        "custom_blocks": [],
        "quick_lists": [],
    }


IDENTITY_FIELDS = ("name", "owner", "creation", "modified", "modified_by",
                   "docstatus", "idx", "parent", "parentfield", "parenttype", "doctype")


class BuildPayloadTest(unittest.TestCase):
    def test_target_identity(self):
        p = build_workspace_payload(synthetic_source())
        self.assertEqual(p["name"], "海滨库存")
        self.assertEqual(p["label"], "海滨库存")
        self.assertEqual(p["title"], "海滨库存")
        self.assertEqual(p["module"], "HBOS Stock")
        self.assertEqual(p["app"], "hb_stock_app")

    def test_top_level_fields_carried_over(self):
        p = build_workspace_payload(synthetic_source())
        self.assertEqual(p["icon"], "stock")
        self.assertEqual(p["type"], "Workspace")
        self.assertEqual(p["public"], 1)
        self.assertEqual(p["sequence_id"], 7.0)

    def test_link_rows_drop_child_identity(self):
        p = build_workspace_payload(synthetic_source())
        self.assertEqual(len(p["links"]), 2)
        for row in p["links"]:
            for field in IDENTITY_FIELDS:
                self.assertNotIn(field, row,
                                 f"子表行不应带 {field}，否则会复用原生 Stock 的子文档名")

    def test_charts_and_number_cards_drop_child_identity(self):
        p = build_workspace_payload(synthetic_source())
        self.assertEqual(p["charts"], [{"chart_name": "Stock Value by Item Group",
                                        "label": "Stock Value by Item Group"}])
        self.assertEqual(p["number_cards"], [{"number_card_name": "Total Warehouses",
                                              "label": "Total Warehouses"}])

    def test_link_semantics_preserved(self):
        p = build_workspace_payload(synthetic_source())
        link, card_break = p["links"]
        self.assertEqual(link["type"], "Link")
        self.assertEqual(link["label"], "Item")
        self.assertEqual(link["link_type"], "DocType")
        self.assertEqual(link["link_to"], "Item")
        self.assertEqual(link["onboard"], 1)
        self.assertEqual(card_break["type"], "Card Break")
        self.assertEqual(card_break["label"], "Stock Transactions")

    def test_source_not_mutated(self):
        source = synthetic_source()
        before = copy.deepcopy(source)
        build_workspace_payload(source)
        self.assertEqual(source, before, "build_workspace_payload 不得修改入参")

    def test_content_prepends_title_header_once(self):
        p = build_workspace_payload(synthetic_source())
        blocks = json.loads(p["content"])
        self.assertEqual(len(blocks), 3)
        self.assertEqual(blocks[0]["id"], "hbos-stock-title")
        self.assertEqual(blocks[0]["type"], "header")
        self.assertIn("海滨库存", blocks[0]["data"]["text"])
        # 原有块逐字保留
        self.assertEqual(blocks[1]["id"], "abc123")
        self.assertEqual(blocks[2]["id"], "def456")

    def test_empty_source_tolerated(self):
        p = build_workspace_payload({})
        self.assertEqual(p["links"], [])
        self.assertEqual(p["charts"], [])
        self.assertEqual(p["number_cards"], [])
        self.assertEqual(p["roles"], [])
        self.assertEqual(p["shortcuts"], [])


@unittest.skipUnless(FIXTURE_PATH.exists(), "fixture 尚未生成")
class GeneratedFixtureTest(unittest.TestCase):
    def setUp(self):
        self.payload = json.loads(FIXTURE_PATH.read_text())

    def test_real_stock_scale(self):
        self.assertEqual(len(self.payload["links"]), 72)
        self.assertEqual(len(self.payload["charts"]), 1)
        self.assertEqual(len(self.payload["number_cards"]), 3)

    def test_real_card_break_count(self):
        breaks = [r for r in self.payload["links"] if r["type"] == "Card Break"]
        self.assertEqual(len(breaks), 8)

    def test_no_child_row_carries_identity(self):
        for table in ("links", "charts", "number_cards"):
            for row in self.payload[table]:
                for field in IDENTITY_FIELDS:
                    self.assertNotIn(field, row)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```bash
PYTHONPATH=apps/hb_stock_app python3 -m unittest discover -s apps/hb_stock_app/tests -v
```
Expected: FAIL，报 `ModuleNotFoundError: No module named 'hb_stock_app'`

- [ ] **Step 3: 建 App 骨架文件**

`apps/hb_stock_app/__init__.py`（空文件，0 字节）

`apps/hb_stock_app/modules.txt`：
```
HBOS Stock
```

`apps/hb_stock_app/hb_stock_app/__init__.py`：
```python
__version__ = "0.0.1"
```

`apps/hb_stock_app/hb_stock_app/modules.txt`：
```
HBOS Stock
```

`apps/hb_stock_app/hb_stock_app/hbos_stock/__init__.py`：
```python
# HBOS Stock module
```

`apps/hb_stock_app/pyproject.toml`：
```toml
[project]
name = "hb_stock_app"
authors = [
    { name = "HBOS", email = "admin@example.com" },
]
description = "Lightweight HBOS stock workspace app"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"
erpnext = ">=16.0.0,<17.0.0"
```

`apps/hb_stock_app/README.md`：
```markdown
# hb_stock_app

海滨库存工作台 App。

只承载 `海滨库存` Workspace 定义与 `after_migrate` 幂等同步，不新建 DocType、不重写库存逻辑。
库存单据、报表、估价与会计联动全部复用 ERPNext 原生能力。

安装站点：`stock`（独立站点）。**不要**安装到 `frontend` 考勤站点。
```

- [ ] **Step 4: 写 `workspace_builder.py` 实现**

创建 `apps/hb_stock_app/hb_stock_app/hbos_stock/workspace_builder.py`：

```python
"""把 ERPNext 原生 Stock 工作台的 payload 转换为海滨库存工作台 payload。

纯函数模块：不导入 frappe，不访问数据库，可离线测试。
"""

import json

TARGET_WORKSPACE = "海滨库存"
TARGET_MODULE = "HBOS Stock"
TARGET_APP = "hb_stock_app"

# 子表行上的身份/父级/审计字段：复制时必须剥离。
# 子表 docname（如实测到的 si4ekhi8tq）在各自 child doctype 内全局唯一，
# 原样带过去会让 frappe 复用同名子文档，等于把原生 Stock 工作台的子表行搬走。
ROW_DROP_FIELDS = (
	"name", "owner", "creation", "modified", "modified_by",
	"docstatus", "idx", "parent", "parentfield", "parenttype", "doctype",
)

TOP_LEVEL_KEEP_FIELDS = (
	"app", "content", "icon", "is_hidden", "label", "module",
	"public", "sequence_id", "title", "type",
)
LINK_KEEP_FIELDS = (
	"type", "label", "icon", "description", "hidden", "link_type", "link_to",
	"report_ref_doctype", "dependencies", "only_for", "onboard",
	"is_query_report", "link_count",
)
CHART_KEEP_FIELDS = ("chart_name", "label")
NUMBER_CARD_KEEP_FIELDS = ("number_card_name", "label")
SHORTCUT_KEEP_FIELDS = ("color", "doc_view", "label", "link_to", "stats_filter", "type")

TITLE_HEADER_ID = "hbos-stock-title"
TITLE_HEADER_TEXT = (
	'<span class="h4"><b>海滨库存</b></span><br>'
	'<span class="text-muted">本工作台承载海滨独立库存数据，复用 ERPNext 原生库存单据与报表；'
	'与考勤站点数据完全隔离。</span>'
)


def build_workspace_payload(source):
	"""把 Stock 工作台 payload 转成海滨库存工作台 payload。

	纯函数：返回全新 dict，不修改入参，不访问数据库。
	"""
	payload = _pick(source, TOP_LEVEL_KEEP_FIELDS)
	payload["name"] = TARGET_WORKSPACE
	payload["label"] = TARGET_WORKSPACE
	payload["title"] = TARGET_WORKSPACE
	payload["module"] = TARGET_MODULE
	payload["app"] = TARGET_APP

	payload["links"] = [_pick(r, LINK_KEEP_FIELDS) for r in source.get("links") or []]
	payload["charts"] = [_pick(r, CHART_KEEP_FIELDS) for r in source.get("charts") or []]
	payload["number_cards"] = [
		_pick(r, NUMBER_CARD_KEEP_FIELDS) for r in source.get("number_cards") or []
	]
	payload["roles"] = [_pick(r, ("role",)) for r in source.get("roles") or []]
	payload["shortcuts"] = [
		_pick(r, SHORTCUT_KEEP_FIELDS) for r in source.get("shortcuts") or []
	]

	payload["content"] = _prepend_title_header(source.get("content"))
	return payload


def _pick(row, fields):
	return {f: row.get(f) for f in fields if f in row}


def _prepend_title_header(content):
	"""在 content 顶部插入海滨库存标题块，其余块逐字保留；已插入则不重复。"""
	blocks = json.loads(content) if content else []
	if any(b.get("id") == TITLE_HEADER_ID for b in blocks):
		return content
	header = {"id": TITLE_HEADER_ID, "type": "header",
	          "data": {"text": TITLE_HEADER_TEXT, "col": 12}}
	return json.dumps([header, *blocks], ensure_ascii=False)
```

- [ ] **Step 5: 运行测试确认通过**

Run:
```bash
PYTHONPATH=apps/hb_stock_app python3 -m unittest discover -s apps/hb_stock_app/tests -v
```
Expected: PASS —— `BuildPayloadTest` 8 个测试通过，`GeneratedFixtureTest` 3 个 skipped（fixture 尚未生成）

- [ ] **Step 6: 写 `setup.py` 与 `hooks.py`**

创建 `apps/hb_stock_app/hb_stock_app/hbos_stock/setup.py`：

```python
"""海滨库存工作台的幂等同步。

模式对齐 hb_attendance_app.hbos_attendance.setup 的 after_migrate 同步。
"""

import json
from pathlib import Path

import frappe

MODULE = "HBOS Stock"
WORKSPACE_TITLE = "海滨库存"

TOP_LEVEL_FIELDS = (
	"app", "content", "icon", "is_hidden", "label", "module",
	"public", "sequence_id", "title", "type",
)
CHILD_TABLES = ("links", "charts", "number_cards", "roles", "shortcuts")


def after_migrate():
	sync_stock_workspace()


def load_workspace_fixture():
	path = (Path(__file__).parent / "workspace" / WORKSPACE_TITLE
	        / f"{WORKSPACE_TITLE}.json")
	return json.loads(path.read_text())


def sync_stock_workspace():
	"""以版本化 fixture 幂等同步「海滨库存」工作台（新建或更新）。"""
	payload = load_workspace_fixture()
	exists = frappe.db.exists("Workspace", payload["name"])
	workspace = (frappe.get_doc("Workspace", payload["name"]) if exists
	             else frappe.new_doc("Workspace"))
	for field in TOP_LEVEL_FIELDS:
		workspace.set(field, payload.get(field))
	for table in CHILD_TABLES:
		workspace.set(table, payload.get(table) or [])
	workspace.save(ignore_permissions=True)
```

创建 `apps/hb_stock_app/hb_stock_app/hooks.py`：

```python
app_name = "hb_stock_app"
app_title = "海滨库存"
app_publisher = "HBOS"
app_description = "海滨独立库存：复用 ERPNext 原生库存单据与报表，与考勤站点数据隔离"
app_email = "admin@example.com"
app_license = "MIT"
required_apps = ["frappe", "erpnext"]

after_migrate = "hb_stock_app.hbos_stock.setup.after_migrate"
```

- [ ] **Step 7: 写 fixture 生成器工具**

创建 `tools/gen_stock_workspace_fixture.py`：

```python
"""从运行态 Stock 工作台生成 hb_stock_app 的「海滨库存」工作台 fixture。

用法：
    # 1. 在 backend 容器内导出原生 Stock 工作台
    docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && \
      bench --site frontend execute frappe.client.get \
      --kwargs "{\\"doctype\\":\\"Workspace\\",\\"name\\":\\"Stock\\"}" > /tmp/stock_ws.json'
    # 2. 拷回宿主机
    docker cp hbos-m0-r3a-backend-1:/tmp/stock_ws.json /tmp/stock_ws.json
    # 3. 生成 fixture
    PYTHONPATH=apps/hb_stock_app python3 tools/gen_stock_workspace_fixture.py /tmp/stock_ws.json
"""

import json
import sys
from pathlib import Path

from hb_stock_app.hbos_stock.workspace_builder import (
	TARGET_WORKSPACE,
	build_workspace_payload,
)

FIXTURE = (Path(__file__).resolve().parent.parent / "apps" / "hb_stock_app"
           / "hb_stock_app" / "hbos_stock" / "workspace" / TARGET_WORKSPACE
           / f"{TARGET_WORKSPACE}.json")


def extract_json(raw):
	"""bench execute 输出会夹带日志行，截取首个 { 到末个 }。"""
	start, end = raw.find("{"), raw.rfind("}")
	if start == -1 or end == -1:
		raise SystemExit("未在输入中找到 JSON 对象")
	return json.loads(raw[start:end + 1])


def main():
	if len(sys.argv) != 2:
		raise SystemExit("用法: python3 tools/gen_stock_workspace_fixture.py <stock_ws.json>")
	source = extract_json(Path(sys.argv[1]).read_text())
	payload = build_workspace_payload(source)
	FIXTURE.parent.mkdir(parents=True, exist_ok=True)
	FIXTURE.write_text(json.dumps(payload, ensure_ascii=False, indent=1, sort_keys=True))
	print(f"已写入 {FIXTURE}")
	print(f"links={len(payload['links'])} charts={len(payload['charts'])} "
	      f"number_cards={len(payload['number_cards'])}")


if __name__ == "__main__":
	main()
```

- [ ] **Step 8: 生成 fixture**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute frappe.client.get --kwargs "{\"doctype\":\"Workspace\",\"name\":\"Stock\"}" > /tmp/stock_ws.json'
```
```bash
docker cp hbos-m0-r3a-backend-1:/tmp/stock_ws.json /tmp/stock_ws.json
```
```bash
PYTHONPATH=apps/hb_stock_app python3 tools/gen_stock_workspace_fixture.py /tmp/stock_ws.json
```
Expected 输出：
```
已写入 /Users/.../Vibe Coding/apps/hb_stock_app/hb_stock_app/hbos_stock/workspace/海滨库存/海滨库存.json
links=72 charts=1 number_cards=3
```

- [ ] **Step 9: 运行测试确认全部通过（含真实 fixture 断言）**

Run:
```bash
PYTHONPATH=apps/hb_stock_app python3 -m unittest discover -s apps/hb_stock_app/tests -v
```
Expected: PASS —— 11 个测试全部通过，0 skipped

- [ ] **Step 10: 确认未引入回归**

Run:
```bash
PYTHONPATH=apps/hb_attendance_app python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -4
```
Expected: `Ran 311 tests ... OK`

- [ ] **Step 11: Commit**

```bash
git add apps/hb_stock_app tools/gen_stock_workspace_fixture.py
git commit -m "feat: 新增 hb_stock_app 骨架与海滨库存工作台 fixture 生成器"
```

---

### Task 2: compose 挂载 hb_stock_app（卷 + PYTHONPATH）

**Files:**
- Modify: `docker-compose.yml`（`backend`、`configurator`、`create-site`、`frontend`、`queue-long`、`queue-short`、`scheduler`、`websocket` 八个服务的 `volumes`；七个服务的 `PYTHONPATH`）

**Interfaces:**
- Consumes: Task 1 的 `apps/hb_stock_app/` 目录
- Produces: 容器内 `/home/frappe/frappe-bench/apps/hb_stock_app` 可导入；`sites/apps.txt` 自动含 `hb_stock_app`

- [ ] **Step 1: 在八个 frappe 服务的 volumes 追加挂载**

在 `docker-compose.yml` 中，把每一处

```yaml
      - ./apps/hb_attendance_app:/home/frappe/frappe-bench/apps/hb_attendance_app
```

后面追加一行（缩进一致）：

```yaml
      - ./apps/hb_stock_app:/home/frappe/frappe-bench/apps/hb_stock_app
```

涉及出现该行的全部服务：`backend`、`configurator`、`create-site`、`frontend`、`queue-long`、`queue-short`、`scheduler`、`websocket`。

- [ ] **Step 2: 在六个服务的 PYTHONPATH 末尾追加新 App**

把每一处

```yaml
      PYTHONPATH: /home/frappe/frappe-bench/apps/hrms:/home/frappe/frappe-bench/apps/hb_attendance_app
```

改为

```yaml
      PYTHONPATH: /home/frappe/frappe-bench/apps/hrms:/home/frappe/frappe-bench/apps/hb_attendance_app:/home/frappe/frappe-bench/apps/hb_stock_app
```

涉及服务：`backend`、`configurator`、`create-site`、`queue-long`、`queue-short`、`scheduler`。

**注意（2026-09-16 实测修正）**：`frontend` 与 `frontend-stock` 是 nginx，无 PYTHONPATH；`websocket` 跑的是 `node socketio.js`，**本身没有 PYTHONPATH 行**——它只有卷挂载。因此实际是 **8 处卷挂载、6 处 PYTHONPATH**，不是 7 处。校验时用 `grep -c "PYTHONPATH: /home/frappe/frappe-bench/apps/hrms" docker-compose.yml` 应得 `6`（改前）与 `6`（改后，行数不变，只加长）。

- [ ] **Step 3: 校验 compose 语法**

Run:
```bash
docker compose config --quiet && echo "compose 配置合法"
```
Expected: `compose 配置合法`

- [ ] **Step 4: 重建受影响服务**

Run:
```bash
docker compose up -d
```
Expected: 所有服务 Up；`configurator`、`create-site` 为 `Exited (0)`

- [ ] **Step 5: 确认新 App 可被容器导入**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && python3 -c "import hb_stock_app.hbos_stock.workspace_builder as m; print(m.TARGET_WORKSPACE)"'
```
Expected: `海滨库存`

- [ ] **Step 6: 确认 apps.txt 已含新 App，且 frontend 站点未受影响**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cat /home/frappe/frappe-bench/sites/apps.txt'
```
Expected:
```
erpnext
frappe
hb_attendance_app
hb_stock_app
hrms
```

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend list-apps'
```
Expected: 仍只有 `frappe`、`erpnext`、`hrms`、`hb_attendance_app` —— **不含** `hb_stock_app`

- [ ] **Step 7: 确认考勤站点零回归**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/login
```
Expected: `200`

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute frappe.client.get_count --kwargs "{\"doctype\":\"Employee Checkin\"}"'
```
Expected: 返回一个数字（与改动前一致；若改前未记录，此处记为基线值并写入本轮主文档）

- [ ] **Step 8: Commit**

```bash
git add docker-compose.yml
git commit -m "chore: compose 挂载 hb_stock_app 卷与 PYTHONPATH（不改动现有站点）"
```

---

### Task 3: 备份 frontend 并创建 stock 站点

**Files:**
- 无仓库文件改动（运行态操作）

**Interfaces:**
- Consumes: Task 2 的容器环境
- Produces: 站点 `stock`，已装 `frappe` + `erpnext`；`common_site_config.json` 的 `default_site` 仍为 `frontend`

- [ ] **Step 1: 备份 frontend 站点（含文件）**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend backup --with-files'
```
Expected: 输出两行 `Backup for frontend ... created at ...`（数据库 + 文件）。记下路径，写入本轮主文档。

- [ ] **Step 2: 记录改动前基线**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cat /home/frappe/frappe-bench/sites/common_site_config.json'
```
Expected: `"default_site": "frontend"`（记为基线）

- [ ] **Step 3: 确认 hosts 中尚无 stock 站点**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'ls /home/frappe/frappe-bench/sites/'
```
Expected: 列表**不含** `stock`

- [ ] **Step 4: 创建 stock 站点**

先在宿主机 `.env` 末尾追加（只加这一行即可，Task 6 会补齐其余两个变量；密码用本机强随机值，不要提交）：

```
STOCK_ADMIN_PASSWORD=<LOCAL_STOCK_ADMIN_PASSWORD>
```

Run:
```bash
docker exec -e STOCK_ADMIN_PASSWORD="$(grep -E '^STOCK_ADMIN_PASSWORD=' .env | cut -d= -f2-)" hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench new-site stock --mariadb-user-host-login-scope="%" --admin-password="$STOCK_ADMIN_PASSWORD" --db-root-username=root --db-root-password="$MARIADB_ROOT_PASSWORD" --install-app erpnext'
```

说明：
- **不传 `--set-default`**（该参数是布尔开关，不是键值对），避免抢走 `frontend` 的默认站点身份。
- 用 `docker exec -e` + 命令替换传密码，密码不出现在命令行字面量里。
- `MARIADB_ROOT_PASSWORD` 已由 compose 注入 backend 容器。

Expected: 末尾输出 `Site stock created` 之类成功信息，无 traceback

- [ ] **Step 5: 确认 default_site 未被抢走**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cat /home/frappe/frappe-bench/sites/common_site_config.json'
```
Expected: `"default_site": "frontend"` —— **与 Step 2 基线一致**

- [ ] **Step 6: 确认新站点已装 erpnext，且未装考勤类 App**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock list-apps'
```
Expected: 只有 `frappe`、`erpnext`（**不含** `hrms`、`hb_attendance_app`、`hb_stock_app`）

- [ ] **Step 7: 确认考勤站点仍可用**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/login
```
Expected: `200`

- [ ] **Step 8: 记录本轮运行态操作到主文档草稿**

创建 `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md`，写入备份路径、基线计数与建站命令。完整章节在 Task 8 补齐。

- [ ] **Step 9: Commit**

```bash
git add docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md
git commit -m "docs: 新增 M2-STOCK-R1 实施记录（建站与备份基线）"
```

---

### Task 4: 在 stock 站点安装 hb_stock_app 并生成海滨库存工作台

**Files:**
- 无仓库文件改动（运行态操作；fixture 已在 Task 1 入库）

**Interfaces:**
- Consumes: Task 1 的 `hb_stock_app.hbos_stock.setup.after_migrate`；Task 3 的 `stock` 站点
- Produces: `stock` 站点内存在 Workspace `海滨库存`（72 links / 1 chart / 3 number cards），Module Def `HBOS Stock`

- [ ] **Step 1: 安装 App**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock install-app hb_stock_app'
```
Expected: 无 traceback，输出安装成功信息

- [ ] **Step 2: 执行 migrate 触发 after_migrate**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock migrate'
```
Expected: 无 traceback

- [ ] **Step 3: 确认工作台已生成且规模正确**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock execute frappe.client.get --kwargs "{\"doctype\":\"Workspace\",\"name\":\"海滨库存\"}"' 2>&1 | python3 -c "
import sys, json
raw = sys.stdin.read()
d = json.loads(raw[raw.find('{'):raw.rfind('}') + 1])
print('name:', d['name'], '| label:', d['label'], '| module:', d['module'], '| app:', d['app'])
print('links:', len(d['links']), '| charts:', len(d['charts']), '| number_cards:', len(d['number_cards']))
print('card_breaks:', sum(1 for l in d['links'] if l['type'] == 'Card Break'))
"
```
Expected:
```
name: 海滨库存 | label: 海滨库存 | module: HBOS Stock | app: hb_stock_app
links: 72 | charts: 1 | number_cards: 3
card_breaks: 8
```

- [ ] **Step 4: 确认原生 Stock 工作台未被破坏**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock execute frappe.client.get --kwargs "{\"doctype\":\"Workspace\",\"name\":\"Stock\"}"' 2>&1 | python3 -c "
import sys, json
raw = sys.stdin.read()
d = json.loads(raw[raw.find('{'):raw.rfind('}') + 1])
print('Stock links:', len(d['links']), '| charts:', len(d['charts']))
"
```
Expected: `Stock links: 72 | charts: 1` —— 原生工作台**未被搬空**（这是子表行身份字段剥离是否正确的关键证据）

- [ ] **Step 5: 确认考勤站点未被装上 hb_stock_app**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend list-apps'
```
Expected: **不含** `hb_stock_app`

- [ ] **Step 6: 确认考勤站点零回归**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/login
```
Expected: `200`

- [ ] **Step 7: 补记主文档**

在 `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md` 追加：安装与 migrate 命令、工作台实测计数、原生 Stock 工作台完好的证据。

- [ ] **Step 8: Commit**

```bash
git add docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md
git commit -m "docs: 记录 hb_stock_app 安装与海滨库存工作台生成结果"
```

---

### Task 5: 初始化 stock 站点（Company HAIBIN + 默认仓库）

**Files:**
- Create: `apps/hb_stock_app/hb_stock_app/hbos_stock/init_site.py`
- Modify: `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md`

**Interfaces:**
- Consumes: Task 3 的 `stock` 站点
- Produces: `hb_stock_app.hbos_stock.init_site.run() -> dict`，返回 `{"company": "HAIBIN", "warehouses": [...]}`；可重复执行（幂等）

- [ ] **Step 1: 写 `init_site.py`**

创建 `apps/hb_stock_app/hb_stock_app/hbos_stock/init_site.py`：

```python
"""stock 站点初始化：创建公司主体与默认仓库。

幂等：已存在则跳过。通过
    bench --site stock execute hb_stock_app.hbos_stock.init_site.run
调用。
"""

import frappe

COMPANY = "HAIBIN"
ABBR = "H"
COUNTRY = "China"
CURRENCY = "CNY"


def run():
	"""创建 HAIBIN 公司；ERPNext 会随之自动建默认仓库与会计科目。"""
	created = False
	if not frappe.db.exists("Company", COMPANY):
		company = frappe.new_doc("Company")
		company.company_name = COMPANY
		company.abbr = ABBR
		company.country = COUNTRY
		company.default_currency = CURRENCY
		company.insert(ignore_permissions=True)
		created = True

	frappe.db.commit()

	warehouses = [w.name for w in frappe.get_all(
		"Warehouse", filters={"company": COMPANY}, fields=["name"], order_by="name")]
	accounts = frappe.db.count("Account", {"company": COMPANY})
	print(f"company={COMPANY} created={created} warehouses={len(warehouses)} accounts={accounts}")
	print(f"warehouses={warehouses}")
	return {"company": COMPANY, "warehouses": warehouses}
```

- [ ] **Step 2: 执行初始化**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock execute hb_stock_app.hbos_stock.init_site.run'
```
Expected: 输出 `company=HAIBIN created=True warehouses=5 accounts=<正整数>` 以及 5 个仓库名

- [ ] **Step 3: 确认仓库口径与考勤站点一致**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock execute frappe.client.get_list --kwargs "{\"doctype\":\"Warehouse\",\"fields\":[\"name\",\"company\"],\"limit_page_length\":0}"' 2>&1 | tail -1
```
Expected: 含 `All Warehouses - H`、`Finished Goods - H`、`Goods In Transit - H`、`Stores - H`、`Work In Progress - H`，全部 `company: HAIBIN`

- [ ] **Step 4: 确认幂等（重复执行不报错、不重复建）**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock execute hb_stock_app.hbos_stock.init_site.run'
```
Expected: 输出 `created=False`，仓库仍为 5

- [ ] **Step 5: 确认库存数据为空（无物料，符合预期）**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock execute frappe.client.get_count --kwargs "{\"doctype\":\"Item\"}"'
```
Expected: `0`

- [ ] **Step 6: 补记主文档并提交**

在 `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md` 追加：初始化脚本路径、公司/仓库/科目实测结果、Item=0。

```bash
git add apps/hb_stock_app/hb_stock_app/hbos_stock/init_site.py docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md
git commit -m "feat: 新增 stock 站点初始化脚本并完成 HAIBIN 公司与默认仓库创建"
```

---

### Task 6: 暴露 stock 站点（frontend-stock 服务 + 端口）

**Files:**
- Modify: `docker-compose.yml`（新增 `frontend-stock` 服务）
- Modify: `.env`（追加库存站点变量；`.env` 不进 Git）
- Modify: `.env.example`（追加同名占位变量；进 Git）

**Interfaces:**
- Consumes: Task 3 的 `stock` 站点
- Produces: `http://localhost:8082` 指向 `stock` 站点；`http://localhost:8080` 仍指向 `frontend`

- [ ] **Step 1: 在宿主机 `.env` 追加库存站点变量**

在 `.env` 末尾追加（密码用本机强随机值，不要提交）：

```
# 库存站点（M2-STOCK-R1）：与 frontend 同栈但独立站点，经 frontend-stock 容器暴露。
STOCK_SITE_NAME=stock
STOCK_HTTP_PORT=8082
STOCK_ADMIN_PASSWORD=<LOCAL_STOCK_ADMIN_PASSWORD>
```

- [ ] **Step 2: 在 `.env.example` 追加同名占位变量**

在 `.env.example` 末尾追加：

```
# 库存站点（M2-STOCK-R1）。独立站点 stock，经 frontend-stock 容器暴露。
STOCK_SITE_NAME=stock
STOCK_HTTP_PORT=8082
STOCK_ADMIN_PASSWORD=<LOCAL_STOCK_ADMIN_PASSWORD>
```

（若 Task 3 Step 4 已提前写入 `.env`，此步只需确认存在。）

- [ ] **Step 3: 在 `docker-compose.yml` 新增 `frontend-stock` 服务**

在 `frontend` 服务定义之后、`queue-long` 之前插入：

```yaml
  frontend-stock:
    image: frappe/erpnext:${ERPNEXT_VERSION}
    networks:
      - frappe_network
    depends_on:
      - websocket
    restart: on-failure
    command:
      - nginx-entrypoint.sh
    environment:
      BACKEND: backend:8000
      FRAPPE_SITE_NAME_HEADER: ${STOCK_SITE_NAME}
      SOCKETIO: websocket:9000
      UPSTREAM_REAL_IP_ADDRESS: ${UPSTREAM_REAL_IP_ADDRESS}
      UPSTREAM_REAL_IP_HEADER: ${UPSTREAM_REAL_IP_HEADER}
      UPSTREAM_REAL_IP_RECURSIVE: "${UPSTREAM_REAL_IP_RECURSIVE}"
      PROXY_READ_TIMEOUT: "${PROXY_READ_TIMEOUT}"
      CLIENT_MAX_BODY_SIZE: ${CLIENT_MAX_BODY_SIZE}
    volumes:
      - ./runtime/apps/hrms:/home/frappe/frappe-bench/apps/hrms
      - ./apps/hb_attendance_app:/home/frappe/frappe-bench/apps/hb_attendance_app
      - ./apps/hb_stock_app:/home/frappe/frappe-bench/apps/hb_stock_app
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - assets-data:/home/frappe/frappe-bench/assets
    ports:
      - "${STOCK_HTTP_PORT}:8080"
```

要点：`FRAPPE_SITE_NAME_HEADER` 为 `${STOCK_SITE_NAME}`（即 `stock`），这是该容器与 `frontend` 容器**唯一**的语义差别。路由依据：`frappe/app.py:178` 中 `X-Frappe-Site-Name` 请求头优先于主机名。

- [ ] **Step 4: 校验 compose 并启动新服务**

Run:
```bash
docker compose config --quiet && echo "compose 配置合法"
```
Expected: `compose 配置合法`

Run:
```bash
docker compose up -d
```
Expected: 新增容器 `hbos-m0-r3a-frontend-stock-1` 状态 Up

- [ ] **Step 5: 确认 8082 指向 stock 站点**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8082/login
```
Expected: `200`

- [ ] **Step 6: 确认两个端口指向不同站点**

Run:
```bash
curl -s -o /dev/null -w "8080=%{http_code} " http://localhost:8080/login; curl -s -o /dev/null -w "8082=%{http_code}\n" http://localhost:8082/login
```
Expected: `8080=200 8082=200`

再取两站各自 `X-Frappe-Site-Name` 证据（8082 应命中 stock）：

Run:
```bash
docker exec hbos-m0-r3a-frontend-stock-1 bash -lc 'grep -c "proxy_set_header X-Frappe-Site-Name stock" /etc/nginx/conf.d/frappe.conf'
```
Expected: `2`（与 frontend 容器硬写 `frontend` 的两处同构）

- [ ] **Step 7: 确认考勤站点零回归**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute frappe.client.get_count --kwargs "{\"doctype\":\"Employee Checkin\"}"'
```
Expected: 与 Task 2 Step 7 记录的基线一致

- [ ] **Step 8: Commit**

```bash
git add docker-compose.yml .env.example
git commit -m "feat: 新增 frontend-stock 入口容器，库存站点经 8082 对外暴露"
```

---

### Task 7: 端到端验收

**Files:**
- Modify: `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md`

**Interfaces:**
- Consumes: Task 1–6 的全部产物
- Produces: 验收结论（通过/不通过）+ 证据，写入主文档

- [ ] **Step 1: 核验站点隔离 —— App 清单**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && echo "--- frontend ---" && bench --site frontend list-apps && echo "--- stock ---" && bench --site stock list-apps'
```
Expected:
- `frontend`：`frappe`、`erpnext`、`hrms`、`hb_attendance_app`（无 `hb_stock_app`）
- `stock`：`frappe`、`erpnext`、`hb_stock_app`（无 `hrms`、`hb_attendance_app`）

- [ ] **Step 2: 核验数据隔离 —— 库存数据互不可见**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && echo "frontend warehouses:" && bench --site frontend execute frappe.client.get_count --kwargs "{\"doctype\":\"Warehouse\"}" && echo "stock warehouses:" && bench --site stock execute frappe.client.get_count --kwargs "{\"doctype\":\"Warehouse\"}"'
```
Expected: 两者各为 5（同一口径各自独立）

- [ ] **Step 3: 核验数据隔离 —— 考勤数据不出现在库存站点**

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site stock execute frappe.client.get_count --kwargs "{\"doctype\":\"Employee Checkin\"}"'
```
Expected: 报错或 `0`（`stock` 站点未装 hrms，`Employee Checkin` doctype 不存在）—— 任一结果都证明考勤数据不在库存站点

- [ ] **Step 4: 核验工作台入口可访问**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8082/app
```
Expected: `200`，或 `301` / `302`（未登录时重定向到登录页，属正常）

工作台本身的完整性以 Task 4 Step 3 的实测计数为准；此步只验证入口通。

- [ ] **Step 5: 浏览器人工验收（Owner 执行）**

在浏览器中：
1. 打开 `http://localhost:8082/login`，用 `stock` 站点 Administrator 登录；
2. 进入 `http://localhost:8082/app`，确认左侧/图标区出现「海滨库存」；
3. 点击进入，确认卡片分组齐全，抽查若干链接可打开（如 `Item`、`Stock Entry`、`Warehouse`）；
4. 打开 `http://localhost:8080/app`，确认考勤站点「海滨考勤」一切如常。

Expected: 4 项全部符合

- [ ] **Step 6: 把验收结果写入主文档**

在 `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md` 追加「验收结果」节，逐条记录 Step 1–5 的实际输出与结论。

- [ ] **Step 7: Commit**

```bash
git add docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md
git commit -m "docs: 记录 M2-STOCK-R1 端到端验收结果"
```

---

### Task 8: 文档与状态台账收口

**Files:**
- Create: `docs/milestones/M2_START_GATE.md`
- Modify: `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md`
- Modify: `docs/PROJECT_STATUS.md`
- Modify: `docs/CURRENT_MILESTONE.md`
- Modify: `docs/AI_CONTEXT.md`
- Check: `README.md`、`CLAUDE.md`、`AGENTS.md`、`docs/READING_GUIDE.md`

**Interfaces:**
- Consumes: Task 1–7 的全部产物与验收结论
- Produces: 三处状态台账一致；M2 阶段门禁文档就位

- [ ] **Step 1: 新建 M2 阶段门禁文档**

创建 `docs/milestones/M2_START_GATE.md`，结构对齐 `docs/milestones/M1_START_GATE.md`，至少包含以下章节：

```markdown
# M2 启动门禁

## 文件定位

M2 阶段（库存及其后续子轮次）的阶段门禁文档。记录 M2 的目标、边界、门禁条件、子轮次状态与下一步路线。

## 必须满足的前置条件

- M1-FIX 当前状态与本轮关系已明确（不因启动 M2 而改变 M1-FIX 各轮状态）。
- 新站点 `stock` 与现有 `frontend` 站点数据隔离已验证。
- 现有 `frontend` 站点零回归。

## M2 初期范围

- M2-STOCK-R1：库存模块隔离（独立站点 `stock` + `hb_stock_app` + 海滨库存工作台）。

## 环境保护规则

- 不执行 `docker compose down -v`，不删除 Docker volume，不重建 `frontend` site。
- 不修改 Frappe / ERPNext / HRMS 核心源码。
- `hb_stock_app` 不新建 DocType、不重写库存逻辑，且不安装到 `frontend` 站点。
- `stock` 站点不安装 `hrms`、`hb_attendance_app`。

## 子轮次状态

| 轮次 | 名称 | 状态 |
| --- | --- | --- |
| M2-STOCK-R1 | 库存模块隔离 | 见 `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md` |

## 启动顺序

见 `docs/superpowers/plans/2026-09-16-库存模块隔离实施计划.md`。
```

- [ ] **Step 2: 更新 `docs/PROJECT_STATUS.md`**

在「当前状态」节更新：新增 M2-STOCK-R1 条目，说明已创建独立站点 `stock`、`hb_stock_app`、海滨库存工作台，`http://localhost:8082` 可访问，`frontend` 站点零回归。同步更新状态口径块。

- [ ] **Step 3: 更新 `docs/CURRENT_MILESTONE.md`**

新增 M2 章节与 M2-STOCK-R1 条目；在状态口径块追加 `M2 = IN_PROGRESS (M2-STOCK-R1)`。**不改变 M1-FIX 各轮既有状态。**

- [ ] **Step 4: 更新 `docs/AI_CONTEXT.md`**

在「当前上下文」补记：M2 已启动，M2-STOCK-R1 库存模块隔离完成，独立站点 `stock` 位于 `http://localhost:8082`，`hb_stock_app` 承载海滨库存工作台。

- [ ] **Step 5: 修正 2026-09-16 自审发现的 3 处台账过期项**

这三项与库存无关，但属 spec 第 8 节明确列出的收口内容。

**5a. 版本号过期。** 台账写 Frappe `16.25.0` / HRMS `16.12.0`，运行态实测为 Frappe `16.26.3` / ERPNext `16.26.2` / HRMS `16.13.0 version-16`。在 `docs/PROJECT_STATUS.md`、`docs/CURRENT_MILESTONE.md`、`docs/AI_CONTEXT.md` 中改为实测值。

**5b. 测试基线过期。** 台账写「全量 144 通过」，实测为 311。更新为：

Run:
```bash
PYTHONPATH=apps/hb_attendance_app python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -3
```
把实际数字写入 `docs/PROJECT_STATUS.md`、`docs/AI_CONTEXT.md`，并删除「全量 144 通过」的旧表述。

**5c. 部门看板注册口径过期。** 台账写「运行态 migrate 注册待 Owner 授权」，但运行态已注册。改为已完成：

Run:
```bash
docker exec hbos-m0-r3a-backend-1 bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend execute frappe.client.get_list --kwargs "{\"doctype\":\"Page\",\"filters\":{\"module\":\"HBOS Attendance\"},\"fields\":[\"name\"],\"limit_page_length\":0}"'
```
Expected: 含 `hbos-department-board`。据此把 3 处「待 Owner 授权」表述改为「已注册」。

- [ ] **Step 6: 检查公共入口文件是否存在过期阶段描述**

逐个人工检查 `README.md`、`CLAUDE.md`、`AGENTS.md`、`docs/READING_GUIDE.md`。

判定标准：
- 若其中写有「M2 未启动」「M2 待 Owner 授权」之类描述 → **需更新**；
- 若只描述协作规则与架构，不含阶段进度 → **无需更新**。

把检查结果（哪些需更新、哪些无需更新及原因）写入主文档收尾节。

- [ ] **Step 7: 补完主文档收尾节**

在 `docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md` 追加：
- 状态台账是否已更新；
- 公共入口文件检查结果；
- 回滚方法（`bench drop-site stock` + 移除 `frontend-stock` 服务与端口）；
- 遗留事项（例如 `stock` 站点尚无备份习惯、物料档案为空待后续录入）。

- [ ] **Step 8: 确认全部测试通过**

Run:
```bash
PYTHONPATH=apps/hb_stock_app python3 -m unittest discover -s apps/hb_stock_app/tests 2>&1 | tail -4
```
Expected: `OK`

Run:
```bash
PYTHONPATH=apps/hb_attendance_app python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -4
```
Expected: `Ran 311 tests ... OK`

- [ ] **Step 9: Commit**

```bash
git add docs/milestones/M2_START_GATE.md docs/milestones/M2_STOCK_R1_库存模块隔离实施记录.md docs/PROJECT_STATUS.md docs/CURRENT_MILESTONE.md docs/AI_CONTEXT.md
git commit -m "docs: 收口 M2-STOCK-R1 状态台账、修正过期口径并新增 M2 阶段门禁文档"
```

---

## 回滚总表

| 阶段 | 回滚动作 |
| --- | --- |
| Task 1–2 之后 | `git revert` 对应提交；`docker compose up -d` 恢复 |
| Task 3 之后（未装 App） | `bench drop-site stock`；`git revert` Task 2 提交 |
| Task 4–7 之后（已有数据） | 导出 `stock` 数据 → `bench drop-site stock` → 移除 `frontend-stock` 服务与 `STOCK_HTTP_PORT` → 恢复 Task 3 备份的 `frontend` |

## 明确不做

- 不复制 ERPNext 库存 DocType 定义（75 个），不 fork ERPNext。
- 不做物料档案迁移（`frontend` 的 `Item` 为 0 条）。
- 不新增库存业务逻辑、不自建库存报表。
- 不动 M1-FIX 各轮既有状态（B3/B4/B5 保持原状）。
- 不执行 `docker compose down -v`，不删除 volume，不重建 `frontend` site。
