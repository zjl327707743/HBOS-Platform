# 调休与换班记录界面 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增「飞书调休记录」与「飞书换班记录」两个只读 DocType 与对应飞书同步，供 HR 查看筛选；**不改动任何考勤判定**。

**Architecture:** 两个 DocType（各以 `feishu_approval_id` 为唯一键）+ 一个共享的字段映射纯模块（`swap_mapping.py`，无 frappe 依赖、可离线测试）+ 两个同步脚本（对齐既有 `api.sync_overtime_from_bitable` 写法：取 token → 分页拉取 → 只取「已通过」→ upsert）+ 入口接入既有侧栏与工作台。

**Tech Stack:** Frappe DocType(JSON)、Python 3、requests、unittest（离线）。

## Global Constraints

- **不影响考勤判定**：不 import / 不调用 pairing、api.regenerate_attendance；不重算任何历史。
- **不与请假表合并**：`HBOS Leave Record` 中「请假类型=调休」的记录与本表**各记各的**（Owner 2026-09-11 确认）。
- **只读飞书**：只 GET，不写回飞书。
- 两个 DocType 的 `autoname = field:feishu_approval_id`，唯一键格式：
  - 调休 `feishu-bitable-restleave-<SourceID>`
  - 换班 `feishu-bitable-swap-<SourceID>`
- DocType 权限与既有加班记录一致：System Manager / HR Manager / HR User 读写。
- 同步只处理 `申请状态 == "已通过"`；工号优先匹配 Employee，缺失才回退姓名；两列都取不到的人员跳过并计数。
- 飞书列名（已实读确认，改名前不得臆造）：
  - 调休：`申请状态`、`调休人员_姓名`、`调休人员_工号`、`调休人员_开始时间`、`调休人员_结束时间`、`调休人员_调休天数`、`说明`、`SourceID`
  - 换班：`申请状态`、`明细_申请人`、`明细_申请人工号`、`明细_替班人`、`明细_替班人工号`、`明细_换班日期`、`明细_还班日期`、`说明`、`SourceID`
- 飞书表标识：调休 `app_token=XLD0bPiGXaP0JTsicHCcSLA4nlQ` `table_id=tblYHBgNJdvVCiFM`；
  换班 `wiki_node=QVUMwYsggiYGK8kdt0CcJifmnvf` `table_id=tbltVlpHx6NzG6WG`（**换班是 wiki 链接，必须先解析 obj_token**）。
- 离线测试必须 frappe-free、纳入 `apps/hb_attendance_app/tests/`（CI 用 `unittest discover`）。
- 提交信息用中文；不提交任何飞书凭据、真实数据。
- 运行态命令（migrate / 真实同步）**需 Owner 授权**后才执行；未授权时只交付代码与离线测试。

---

### Task 1: 换班日期解析与字段映射纯模块

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/swap_mapping.py`
- Test: `apps/hb_attendance_app/tests/test_swap_mapping.py`

**Interfaces:**
- Consumes: 无（纯 Python，仅标准库）。
- Produces（后续任务依赖，签名锁定）：
  - `ts_to_date(ms) -> str|None`：毫秒时间戳 → `"YYYY-MM-DD"`；空/0/异常 → `None`
  - `rest_leave_fields(fields: dict) -> dict|None`：飞书调休记录的 fields → 待写入字段 dict；
    无法使用（缺工号姓名或缺开始日期）返回 `None`
  - `swap_fields(fields: dict) -> dict|None`：飞书换班记录的 fields → 待写入字段 dict；
    无法使用返回 `None`

返回 dict 的键（大写驼峰外的 snake 与 DocType 字段名一致）：

```
rest_leave_fields → {"employee_number","employee_name","start_date","end_date","rest_days","remarks"}
swap_fields       → {"applicant_number","applicant_name","substitute_number","substitute_name",
                     "swap_date","repay_date","remarks"}
```

- [ ] **Step 1: 写失败测试**

```python
import unittest

from hb_attendance_app.hbos_attendance.swap_mapping import (
    ts_to_date, rest_leave_fields, swap_fields,
)

MS_0901 = 1788192000000   # 2026-09-01 00:00:00 +08（已用 python 反算核对）


class TsToDateTest(unittest.TestCase):
    def test_normal(self):
        self.assertEqual(ts_to_date(MS_0901), "2026-09-01")

    def test_empty_and_zero(self):
        self.assertIsNone(ts_to_date(0))
        self.assertIsNone(ts_to_date(None))
        self.assertIsNone(ts_to_date(""))

    def test_bad_value(self):
        self.assertIsNone(ts_to_date("not-a-ts"))


class RestLeaveFieldsTest(unittest.TestCase):
    def test_maps_all_columns(self):
        f = {"调休人员_姓名": "张三", "调休人员_工号": "10001",
             "调休人员_开始时间": MS_0901, "调休人员_结束时间": MS_0901,
             "调休人员_调休天数": 1, "说明": "加班换休"}
        out = rest_leave_fields(f)
        self.assertEqual(out["employee_number"], "10001")
        self.assertEqual(out["employee_name"], "张三")
        self.assertEqual(out["start_date"], "2026-09-01")
        self.assertEqual(out["end_date"], "2026-09-01")
        self.assertEqual(out["rest_days"], 1.0)
        self.assertEqual(out["remarks"], "加班换休")

    def test_missing_person_returns_none(self):
        self.assertIsNone(rest_leave_fields({"调休人员_开始时间": MS_0901}))
        self.assertIsNone(rest_leave_fields({"调休人员_工号": "1"}))

    def test_missing_dates_returns_none(self):
        f = {"调休人员_姓名": "张三", "调休人员_工号": "10001"}
        self.assertIsNone(rest_leave_fields(f))

    def test_end_date_defaults_to_start(self):
        f = {"调休人员_姓名": "张三", "调休人员_工号": "10001",
             "调休人员_开始时间": MS_0901}
        self.assertEqual(rest_leave_fields(f)["end_date"], "2026-09-01")

    def test_days_defaults_zero(self):
        f = {"调休人员_姓名": "张三", "调休人员_工号": "10001",
             "调休人员_开始时间": MS_0901}
        self.assertEqual(rest_leave_fields(f)["rest_days"], 0.0)


class SwapFieldsTest(unittest.TestCase):
    def test_maps_all_columns(self):
        f = {"明细_申请人": "甲", "明细_申请人工号": "10001",
             "明细_替班人": "乙", "明细_替班人工号": "10002",
             "明细_换班日期": MS_0901, "明细_还班日期": MS_0901, "说明": "调班"}
        out = swap_fields(f)
        self.assertEqual(out["applicant_number"], "10001")
        self.assertEqual(out["applicant_name"], "甲")
        self.assertEqual(out["substitute_number"], "10002")
        self.assertEqual(out["substitute_name"], "乙")
        self.assertEqual(out["swap_date"], "2026-09-01")
        self.assertEqual(out["repay_date"], "2026-09-01")
        self.assertEqual(out["remarks"], "调班")

    def test_requires_both_people(self):
        # 缺替班人 → None
        self.assertIsNone(swap_fields({"明细_申请人工号": "10001",
                                       "明细_换班日期": MS_0901}))
        # 缺申请人 → None
        self.assertIsNone(swap_fields({"明细_替班人工号": "10002",
                                       "明细_换班日期": MS_0901}))

    def test_requires_swap_date(self):
        f = {"明细_申请人": "甲", "明细_申请人工号": "10001",
             "明细_替班人": "乙", "明细_替班人工号": "10002"}
        self.assertIsNone(swap_fields(f))

    def test_repay_date_optional(self):
        f = {"明细_申请人": "甲", "明细_申请人工号": "10001",
             "明细_替班人": "乙", "明细_替班人工号": "10002",
             "明细_换班日期": MS_0901}
        self.assertIsNone(swap_fields(f)["repay_date"])
        self.assertEqual(swap_fields(f)["swap_date"], "2026-09-01")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_swap_mapping -v
```
Expected: FAIL（`ModuleNotFoundError: swap_mapping`）

- [ ] **Step 3: 实现 swap_mapping.py**

```python
"""调休/换班飞书记录 → HBOS 字段的映射（纯模块，无 frappe 依赖，可离线测试）。

列名以飞书表实读结果为准（Owner 2026-09-11）：
  调休: 调休人员_姓名/工号/开始时间/结束时间/调休天数, 说明
  换班: 明细_申请人/申请人工号/替班人/替班人工号/换班日期/还班日期, 说明
本模块只做映射与校验，不查库、不发请求。
"""
from datetime import datetime, timedelta, timezone

# 飞书毫秒时间戳按北京时间转日期；用固定 +8 而非容器本地时区，
# 避免容器为 UTC 时把「当日」算成前一天（与 api.py DELICLOUD_TZ 同基准）。
_TZ = timezone(timedelta(hours=8))

# 换班表链接是 /wiki/ 形式，其 app_token 为 wiki 节点，需先解析真实 obj_token；
# 该常量放在此处便于同步脚本与测试共用。
SWAP_WIKI_NODE = "QVUMwYsggiYGK8kdt0CcJifmnvf"
SWAP_TABLE_ID = "tbltVlpHx6NzG6WG"
REST_LEAVE_APP_TOKEN = "XLD0bPiGXaP0JTsicHCcSLA4nlQ"
REST_LEAVE_TABLE_ID = "tblYHBgNJdvVCiFM"


def ts_to_date(ms):
    """飞书毫秒时间戳 → "YYYY-MM-DD"；空/0/异常返回 None。"""
    try:
        if not ms:
            return None
        return datetime.fromtimestamp(int(ms) / 1000, tz=_TZ).strftime("%Y-%m-%d")
    except Exception:
        return None


def _num(fields, key):
    return str(fields.get(key) or "").strip()


def rest_leave_fields(fields):
    """调休记录 → 待写入字段；缺人员或缺开始日期时返回 None。"""
    num = _num(fields, "调休人员_工号")
    name = _num(fields, "调休人员_姓名")
    start = ts_to_date(fields.get("调休人员_开始时间"))
    if not (num or name) or not start:
        return None
    return {
        "employee_number": num,
        "employee_name": name,
        "start_date": start,
        "end_date": ts_to_date(fields.get("调休人员_结束时间")) or start,
        "rest_days": float(fields.get("调休人员_调休天数") or 0),
        "remarks": str(fields.get("说明") or ""),
    }


def swap_fields(fields):
    """换班记录 → 待写入字段；缺任一方人员或缺换班日期时返回 None。"""
    anum = _num(fields, "明细_申请人工号")
    aname = _num(fields, "明细_申请人")
    snum = _num(fields, "明细_替班人工号")
    sname = _num(fields, "明细_替班人")
    swap_date = ts_to_date(fields.get("明细_换班日期"))
    if not (anum or aname) or not (snum or sname) or not swap_date:
        return None
    return {
        "applicant_number": anum,
        "applicant_name": aname,
        "substitute_number": snum,
        "substitute_name": sname,
        "swap_date": swap_date,
        "repay_date": ts_to_date(fields.get("明细_还班日期")),
        "remarks": str(fields.get("说明") or ""),
    }
```

- [ ] **Step 4: 运行确认通过**

Run: 同 Step 2
Expected: PASS（11 个用例）

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/swap_mapping.py apps/hb_attendance_app/tests/test_swap_mapping.py
git commit -m "feat: 新增调休/换班飞书字段映射纯模块"
```

---

### Task 2: 两个 DocType 定义

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_rest_leave_record/__init__.py`
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_rest_leave_record/hbos_rest_leave_record.json`
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_rest_leave_record/hbos_rest_leave_record.py`
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_shift_swap_record/__init__.py`
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_shift_swap_record/hbos_shift_swap_record.json`
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_shift_swap_record/hbos_shift_swap_record.py`
- Test: `apps/hb_attendance_app/tests/test_rest_leave_swap_doctype.py`

**Interfaces:**
- Consumes: 无。
- Produces: DocType 名（同步脚本 Task 3/4 依赖）：
  - `HBOS Rest Leave Record`，字段 `employee,employee_name,employee_number,department,start_date,end_date,rest_days,approval_status,feishu_approval_id,feishu_sync_time,remarks`
  - `HBOS Shift Swap Record`，字段 `applicant,applicant_name,applicant_number,applicant_department,substitute,substitute_name,substitute_number,substitute_department,swap_date,repay_date,approval_status,feishu_approval_id,feishu_sync_time,remarks`

两个 DocType 的公共约定：`autoname=field:feishu_approval_id`、`naming_rule=By fieldname`、
`module=HBOS Attendance`、`engine=InnoDB`、`track_changes=1`、`sort_field=modified`、`sort_order=DESC`、
权限三角色读写（System Manager / HR Manager / HR User），与 `hbos_overtime_record.json` 一致。

- [ ] **Step 1: 写失败测试**

```python
import json
import unittest
from pathlib import Path

DT = (Path(__file__).parents[1]
      / "hb_attendance_app/hbos_attendance/doctype")


def load(name):
    return json.loads((DT / name / (name + ".json")).read_text())


class DocTypeExistsTest(unittest.TestCase):
    def test_rest_leave_doctype(self):
        d = load("hbos_rest_leave_record")
        self.assertEqual(d["name"], "HBOS Rest Leave Record")
        self.assertEqual(d["autoname"], "field:feishu_approval_id")
        self.assertEqual(d["module"], "HBOS Attendance")
        fields = [f["fieldname"] for f in d["fields"]]
        for f in ("employee", "start_date", "end_date", "rest_days",
                  "approval_status", "feishu_approval_id", "feishu_sync_time", "remarks"):
            self.assertIn(f, fields)

    def test_shift_swap_doctype(self):
        d = load("hbos_shift_swap_record")
        self.assertEqual(d["name"], "HBOS Shift Swap Record")
        self.assertEqual(d["autoname"], "field:feishu_approval_id")
        fields = [f["fieldname"] for f in d["fields"]]
        for f in ("applicant", "substitute", "swap_date", "repay_date",
                  "approval_status", "feishu_approval_id", "feishu_sync_time", "remarks"):
            self.assertIn(f, fields)

    def test_both_have_three_role_permissions(self):
        for name in ("hbos_rest_leave_record", "hbos_shift_swap_record"):
            roles = sorted(p["role"] for p in load(name)["permissions"])
            self.assertEqual(roles, ["HR Manager", "HR User", "System Manager"])

    def test_feishu_id_unique(self):
        for name in ("hbos_rest_leave_record", "hbos_shift_swap_record"):
            f = [x for x in load(name)["fields"] if x["fieldname"] == "feishu_approval_id"][0]
            self.assertEqual(f.get("unique"), 1)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_rest_leave_swap_doctype -v
```
Expected: FAIL（FileNotFoundError）

- [ ] **Step 3: 创建 DocType 文件**

`hbos_rest_leave_record/__init__.py` 与 `hbos_shift_swap_record/__init__.py`：空文件。

`hbos_rest_leave_record/hbos_rest_leave_record.json`：

```json
{
 "actions": [],
 "allow_rename": 1,
 "autoname": "field:feishu_approval_id",
 "creation": "2026-09-11 00:00:00.000000",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "employee",
  "employee_name",
  "employee_number",
  "department",
  "column_break_1",
  "start_date",
  "end_date",
  "rest_days",
  "section_break_6",
  "feishu_approval_id",
  "approval_status",
  "feishu_sync_time",
  "remarks"
 ],
 "fields": [
  {"fieldname": "employee", "fieldtype": "Link", "in_list_view": 1, "in_standard_filter": 1,
   "label": "调休人员", "options": "Employee", "reqd": 1},
  {"fetch_from": "employee.employee_name", "fieldname": "employee_name",
   "fieldtype": "Data", "in_list_view": 1, "label": "姓名", "read_only": 1},
  {"fetch_from": "employee.employee_number", "fieldname": "employee_number",
   "fieldtype": "Data", "in_list_view": 1, "label": "工号", "read_only": 1},
  {"fetch_from": "employee.department", "fieldname": "department", "fieldtype": "Link",
   "in_list_view": 1, "label": "部门", "options": "Department", "read_only": 1},
  {"fieldname": "column_break_1", "fieldtype": "Column Break"},
  {"fieldname": "start_date", "fieldtype": "Date", "in_list_view": 1,
   "in_standard_filter": 1, "label": "开始日期", "reqd": 1},
  {"fieldname": "end_date", "fieldtype": "Date", "in_list_view": 1, "label": "结束日期", "reqd": 1},
  {"fieldname": "rest_days", "fieldtype": "Float", "in_list_view": 1, "label": "调休天数"},
  {"fieldname": "section_break_6", "fieldtype": "Section Break", "label": "飞书同步信息"},
  {"fieldname": "feishu_approval_id", "fieldtype": "Data", "in_list_view": 1,
   "label": "飞书审批单号", "reqd": 1, "unique": 1},
  {"default": "已通过", "fieldname": "approval_status", "fieldtype": "Select",
   "in_list_view": 1, "in_standard_filter": 1, "label": "审批状态",
   "options": "已通过\n已驳回\n已撤销\n审批中"},
  {"fieldname": "feishu_sync_time", "fieldtype": "Datetime", "label": "同步时间", "read_only": 1},
  {"fieldname": "remarks", "fieldtype": "Small Text", "label": "说明"}
 ],
 "grid_page_length": 50,
 "index_web_pages_for_search": 0,
 "links": [],
 "modified": "2026-09-11 00:00:00.000000",
 "modified_by": "Administrator",
 "module": "HBOS Attendance",
 "name": "HBOS Rest Leave Record",
 "naming_rule": "By fieldname",
 "owner": "Administrator",
 "permissions": [
  {"create": 1, "delete": 1, "email": 1, "export": 1, "print": 1, "read": 1, "report": 1,
   "role": "System Manager", "share": 1, "write": 1},
  {"create": 1, "delete": 1, "email": 1, "export": 1, "print": 1, "read": 1, "report": 1,
   "role": "HR Manager", "share": 1, "write": 1},
  {"create": 1, "delete": 1, "email": 1, "export": 1, "print": 1, "read": 1, "report": 1,
   "role": "HR User", "share": 1, "write": 1}
 ],
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": [],
 "title_field": "employee_name",
 "track_changes": 1
}
```

`hbos_shift_swap_record/hbos_shift_swap_record.json`：

```json
{
 "actions": [],
 "allow_rename": 1,
 "autoname": "field:feishu_approval_id",
 "creation": "2026-09-11 00:00:00.000000",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "applicant",
  "applicant_name",
  "applicant_number",
  "applicant_department",
  "column_break_1",
  "substitute",
  "substitute_name",
  "substitute_number",
  "substitute_department",
  "section_break_2",
  "swap_date",
  "repay_date",
  "section_break_6",
  "feishu_approval_id",
  "approval_status",
  "feishu_sync_time",
  "remarks"
 ],
 "fields": [
  {"fieldname": "applicant", "fieldtype": "Link", "in_list_view": 1, "in_standard_filter": 1,
   "label": "申请人", "options": "Employee", "reqd": 1},
  {"fetch_from": "applicant.employee_name", "fieldname": "applicant_name",
   "fieldtype": "Data", "in_list_view": 1, "label": "申请人姓名", "read_only": 1},
  {"fetch_from": "applicant.employee_number", "fieldname": "applicant_number",
   "fieldtype": "Data", "in_list_view": 1, "label": "申请人工号", "read_only": 1},
  {"fetch_from": "applicant.department", "fieldname": "applicant_department",
   "fieldtype": "Link", "label": "申请人部门", "options": "Department", "read_only": 1},
  {"fieldname": "column_break_1", "fieldtype": "Column Break"},
  {"fieldname": "substitute", "fieldtype": "Link", "in_list_view": 1, "in_standard_filter": 1,
   "label": "替班人", "options": "Employee", "reqd": 1},
  {"fetch_from": "substitute.employee_name", "fieldname": "substitute_name",
   "fieldtype": "Data", "in_list_view": 1, "label": "替班人姓名", "read_only": 1},
  {"fetch_from": "substitute.employee_number", "fieldname": "substitute_number",
   "fieldtype": "Data", "in_list_view": 1, "label": "替班人工号", "read_only": 1},
  {"fetch_from": "substitute.department", "fieldname": "substitute_department",
   "fieldtype": "Link", "label": "替班人部门", "options": "Department", "read_only": 1},
  {"fieldname": "section_break_2", "fieldtype": "Section Break", "label": "换班日期"},
  {"fieldname": "swap_date", "fieldtype": "Date", "in_list_view": 1, "in_standard_filter": 1,
   "label": "换班日期", "reqd": 1},
  {"fieldname": "repay_date", "fieldtype": "Date", "in_list_view": 1, "label": "还班日期"},
  {"fieldname": "section_break_6", "fieldtype": "Section Break", "label": "飞书同步信息"},
  {"fieldname": "feishu_approval_id", "fieldtype": "Data", "in_list_view": 1,
   "label": "飞书审批单号", "reqd": 1, "unique": 1},
  {"default": "已通过", "fieldname": "approval_status", "fieldtype": "Select",
   "in_list_view": 1, "in_standard_filter": 1, "label": "审批状态",
   "options": "已通过\n已驳回\n已撤销\n审批中"},
  {"fieldname": "feishu_sync_time", "fieldtype": "Datetime", "label": "同步时间", "read_only": 1},
  {"fieldname": "remarks", "fieldtype": "Small Text", "label": "说明"}
 ],
 "grid_page_length": 50,
 "index_web_pages_for_search": 0,
 "links": [],
 "modified": "2026-09-11 00:00:00.000000",
 "modified_by": "Administrator",
 "module": "HBOS Attendance",
 "name": "HBOS Shift Swap Record",
 "naming_rule": "By fieldname",
 "owner": "Administrator",
 "permissions": [
  {"create": 1, "delete": 1, "email": 1, "export": 1, "print": 1, "read": 1, "report": 1,
   "role": "System Manager", "share": 1, "write": 1},
  {"create": 1, "delete": 1, "email": 1, "export": 1, "print": 1, "read": 1, "report": 1,
   "role": "HR Manager", "share": 1, "write": 1},
  {"create": 1, "delete": 1, "email": 1, "export": 1, "print": 1, "read": 1, "report": 1,
   "role": "HR User", "share": 1, "write": 1}
 ],
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": [],
 "title_field": "applicant_name",
 "track_changes": 1
}
```

两个 `.py` 文件（同名，内容相同）：

```python
from frappe.model.document import Document


class HBOSRestLeaveRecord(Document):
    pass
```

换班目录下的文件里类名改为 `HBOSShiftSwapRecord`（文件名与类名按 DocType 驼峰推导：`hbos_rest_leave_record` → `HBOSRestLeaveRecord`，`hbos_shift_swap_record` → `HBOSShiftSwapRecord`）。

- [ ] **Step 4: 运行确认通过**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_rest_leave_swap_doctype -v
```
Expected: PASS（4 个用例）

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_rest_leave_record/ apps/hb_attendance_app/hb_attendance_app/hbos_attendance/doctype/hbos_shift_swap_record/ apps/hb_attendance_app/tests/test_rest_leave_swap_doctype.py
git commit -m "feat: 新增调休记录与换班记录 DocType"
```

---

### Task 3: 调休同步脚本

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py`
- Test: `apps/hb_attendance_app/tests/test_sync_rest_leave.py`

**Interfaces:**
- Consumes: Task 1 的 `rest_leave_fields`、`REST_LEAVE_APP_TOKEN`、`REST_LEAVE_TABLE_ID`；
  Task 2 的 DocType `HBOS Rest Leave Record`；
  既有 `api._get_token()`、`api._fetch_all_records_custom(token, app_token, table_id)`。
- Produces: `@frappe.whitelist() sync_rest_leave_from_bitable() -> {"created","updated","skipped"}`

- [ ] **Step 1: 写失败测试（契约式，不连飞书）**

```python
import unittest
from pathlib import Path

SRC = (Path(__file__).parents[1]
       / "hb_attendance_app/hbos_attendance/sync_rest_leave.py")


class SyncRestLeaveContractTest(unittest.TestCase):
    def setUp(self):
        self.src = SRC.read_text()

    def test_uses_shared_mapping(self):
        self.assertIn("from hb_attendance_app.hbos_attendance.swap_mapping import", self.src)
        self.assertIn("rest_leave_fields", self.src)

    def test_reads_correct_table(self):
        self.assertIn("REST_LEAVE_APP_TOKEN", self.src)
        self.assertIn("REST_LEAVE_TABLE_ID", self.src)

    def test_only_approved_records(self):
        self.assertIn('"已通过"', self.src)

    def test_unique_key_prefix(self):
        self.assertIn("feishu-bitable-restleave-", self.src)

    def test_is_whitelisted_entry(self):
        self.assertIn("@frappe.whitelist()", self.src)
        self.assertIn("def sync_rest_leave_from_bitable(", self.src)

    def test_does_not_touch_attendance_generation(self):
        # 只读登记：不得触发配对/重算
        self.assertNotIn("regenerate_attendance", self.src)
        self.assertNotIn("pair_employee_checkins", self.src)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_sync_rest_leave -v
```
Expected: FAIL（FileNotFoundError）

- [ ] **Step 3: 实现同步脚本**

```python
"""飞书「调休」表 → HBOS Rest Leave Record（只读登记，不改考勤判定）。

对齐 api.sync_overtime_from_bitable 的写法：取 token → 分页拉全量 → 只取「已通过」
→ 以 feishu_approval_id 为唯一键 upsert。Owner 2026-09-11：本轮只做记录界面。
"""
import frappe

from hb_attendance_app.hbos_attendance.api import _get_token, _fetch_all_records_custom
from hb_attendance_app.hbos_attendance.swap_mapping import (
    rest_leave_fields, REST_LEAVE_APP_TOKEN, REST_LEAVE_TABLE_ID,
)

DOCTYPE = "HBOS Rest Leave Record"
ID_PREFIX = "feishu-bitable-restleave-"


def _match_employee(num, name):
    """工号优先匹配，其次姓名；都匹配不到返回 None。"""
    if num:
        emp = frappe.db.get_value("Employee", {"employee_number": num}, "name")
        if emp:
            return emp
    if name:
        return frappe.db.get_value("Employee", {"employee_name": name}, "name")
    return None


@frappe.whitelist()
def sync_rest_leave_from_bitable():
    try:
        token = _get_token()
        records = _fetch_all_records_custom(token, REST_LEAVE_APP_TOKEN, REST_LEAVE_TABLE_ID)
    except Exception as e:
        frappe.log_error(str(e), "飞书调休同步")
        frappe.throw(f"读取飞书调休表格失败: {e}")

    created = updated = skipped = 0
    for record in records:
        fields = record.get("fields", {}) or {}
        if fields.get("申请状态") != "已通过":
            skipped += 1
            continue
        rid = record.get("id", "")
        if not rid:
            skipped += 1
            continue
        mapped = rest_leave_fields(fields)
        if not mapped:
            skipped += 1
            continue
        emp = _match_employee(mapped["employee_number"], mapped["employee_name"])
        if not emp:
            skipped += 1
            continue

        aid = ID_PREFIX + rid
        exists = frappe.db.exists(DOCTYPE, {"feishu_approval_id": aid})
        doc = (frappe.get_doc(DOCTYPE, {"feishu_approval_id": aid}) if exists
               else frappe.get_doc({"doctype": DOCTYPE, "feishu_approval_id": aid}))
        doc.update({
            "employee": emp,
            "start_date": mapped["start_date"],
            "end_date": mapped["end_date"],
            "rest_days": mapped["rest_days"],
            "remarks": mapped["remarks"],
            "approval_status": "已通过",
            "feishu_sync_time": frappe.utils.now_datetime(),
        })
        try:
            doc.save(ignore_permissions=True)
            frappe.db.commit()
            if exists:
                updated += 1
            else:
                created += 1
        except Exception as e:
            frappe.log_error(str(e), "飞书调休写入失败")
            skipped += 1

    return {"created": created, "updated": updated, "skipped": skipped}
```

- [ ] **Step 4: 运行确认通过 + 全量**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && python3 -m compileall -q apps/hb_attendance_app && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_sync_rest_leave -v && python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -3
```
Expected: 6 用例 PASS；全量 OK

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_rest_leave.py apps/hb_attendance_app/tests/test_sync_rest_leave.py
git commit -m "feat: 新增飞书调休记录同步（只读登记）"
```

---

### Task 4: 换班同步脚本（含 wiki 节点解析）

**Files:**
- Create: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_shift_swap.py`
- Test: `apps/hb_attendance_app/tests/test_sync_shift_swap.py`

**Interfaces:**
- Consumes: Task 1 的 `swap_fields`、`SWAP_WIKI_NODE`、`SWAP_TABLE_ID`；
  Task 2 的 DocType `HBOS Shift Swap Record`；既有 `api._get_token`、`api._fetch_all_records_custom`。
- Produces:
  - `resolve_wiki_obj_token(token, node_token, get=requests.get) -> str`（可注入 `get` 便于离线测试；
    失败抛 `Exception`，**不静默返回空**）
  - `@frappe.whitelist() sync_shift_swap_from_bitable() -> {"created","updated","skipped"}`

- [ ] **Step 1: 写失败测试（含 wiki 解析的成功/失败两路）**

```python
import unittest

from hb_attendance_app.hbos_attendance.sync_shift_swap import resolve_wiki_obj_token


class _Resp:
    def __init__(self, payload):
        self._p = payload

    def json(self):
        return self._p


class ResolveWikiTokenTest(unittest.TestCase):
    def test_success_returns_obj_token(self):
        def fake_get(url, params=None, headers=None, timeout=None):
            self.assertIn("wiki/v2/spaces/get_node", url)
            return _Resp({"code": 0, "data": {"node": {"obj_token": "XLD0bPiGXaP0JTsicHCcSLA4nlQ",
                                                       "obj_type": "bitable"}}})
        self.assertEqual(resolve_wiki_obj_token("tok", "node123", get=fake_get),
                         "XLD0bPiGXaP0JTsicHCcSLA4nlQ")

    def test_nonzero_code_raises(self):
        def fake_get(url, params=None, headers=None, timeout=None):
            return _Resp({"code": 131006, "msg": "node not found"})
        with self.assertRaises(Exception) as ctx:
            resolve_wiki_obj_token("tok", "node123", get=fake_get)
        self.assertIn("131006", str(ctx.exception))

    def test_missing_obj_token_raises(self):
        def fake_get(url, params=None, headers=None, timeout=None):
            return _Resp({"code": 0, "data": {"node": {}}})
        with self.assertRaises(Exception):
            resolve_wiki_obj_token("tok", "node123", get=fake_get)

    def test_network_error_raises(self):
        def fake_get(url, params=None, headers=None, timeout=None):
            raise RuntimeError("conn reset")
        with self.assertRaises(Exception):
            resolve_wiki_obj_token("tok", "node123", get=fake_get)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_sync_shift_swap -v
```
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现同步脚本**

```python
"""飞书「换班」表 → HBOS Shift Swap Record（只读登记，不改考勤判定）。

注意：换班表链接是 /wiki/ 形式，其节点 ID 不是 bitable app_token，
必须先用 wiki API 解析真实 obj_token，否则读取会失败（且不能静默跳过）。
"""
import frappe
import requests

from hb_attendance_app.hbos_attendance.api import _get_token, _fetch_all_records_custom
from hb_attendance_app.hbos_attendance.swap_mapping import (
    swap_fields, SWAP_WIKI_NODE, SWAP_TABLE_ID,
)

DOCTYPE = "HBOS Shift Swap Record"
ID_PREFIX = "feishu-bitable-swap-"
WIKI_NODE_API = "https://open.feishu.cn/open-apis/wiki/v2/spaces/get_node"


def resolve_wiki_obj_token(token, node_token, get=requests.get):
    """wiki 节点 → 真实 bitable app_token；任何异常都抛出（不静默）。"""
    resp = get(WIKI_NODE_API, params={"token": node_token},
               headers={"Authorization": "Bearer " + token}, timeout=15)
    data = resp.json()
    if data.get("code") != 0:
        raise Exception("解析 wiki 节点失败: code=%s msg=%s"
                        % (data.get("code"), data.get("msg")))
    node = (data.get("data") or {}).get("node") or {}
    obj_token = node.get("obj_token")
    if not obj_token:
        raise Exception("wiki 节点未返回 obj_token: %s" % node_token)
    return obj_token


def _match_employee(num, name):
    if num:
        emp = frappe.db.get_value("Employee", {"employee_number": num}, "name")
        if emp:
            return emp
    if name:
        return frappe.db.get_value("Employee", {"employee_name": name}, "name")
    return None


@frappe.whitelist()
def sync_shift_swap_from_bitable():
    try:
        token = _get_token()
        app_token = resolve_wiki_obj_token(token, SWAP_WIKI_NODE)
        records = _fetch_all_records_custom(token, app_token, SWAP_TABLE_ID)
    except Exception as e:
        frappe.log_error(str(e), "飞书换班同步")
        frappe.throw(f"读取飞书换班表格失败: {e}")

    created = updated = skipped = 0
    for record in records:
        fields = record.get("fields", {}) or {}
        if fields.get("申请状态") != "已通过":
            skipped += 1
            continue
        rid = record.get("id", "")
        if not rid:
            skipped += 1
            continue
        mapped = swap_fields(fields)
        if not mapped:
            skipped += 1
            continue
        applicant = _match_employee(mapped["applicant_number"], mapped["applicant_name"])
        substitute = _match_employee(mapped["substitute_number"], mapped["substitute_name"])
        if not applicant or not substitute:
            skipped += 1
            continue

        aid = ID_PREFIX + rid
        exists = frappe.db.exists(DOCTYPE, {"feishu_approval_id": aid})
        doc = (frappe.get_doc(DOCTYPE, {"feishu_approval_id": aid}) if exists
               else frappe.get_doc({"doctype": DOCTYPE, "feishu_approval_id": aid}))
        doc.update({
            "applicant": applicant,
            "substitute": substitute,
            "swap_date": mapped["swap_date"],
            "repay_date": mapped["repay_date"],
            "remarks": mapped["remarks"],
            "approval_status": "已通过",
            "feishu_sync_time": frappe.utils.now_datetime(),
        })
        try:
            doc.save(ignore_permissions=True)
            frappe.db.commit()
            if exists:
                updated += 1
            else:
                created += 1
        except Exception as e:
            frappe.log_error(str(e), "飞书换班写入失败")
            skipped += 1

    return {"created": created, "updated": updated, "skipped": skipped}
```

- [ ] **Step 4: 运行确认通过 + 全量**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && python3 -m compileall -q apps/hb_attendance_app && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_sync_shift_swap -v && python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -3
```
Expected: 4 用例 PASS；全量 OK

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hbos_attendance/sync_shift_swap.py apps/hb_attendance_app/tests/test_sync_shift_swap.py
git commit -m "feat: 新增飞书换班记录同步（含 wiki 节点解析）"
```

---

### Task 5: 定时任务与界面入口

**Files:**
- Modify: `apps/hb_attendance_app/hb_attendance_app/hooks.py`（在既有 `*/30` cron 数组追加两项）
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/setup.py`（侧栏）
- Modify: `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/workspace/海滨考勤工作台/海滨考勤工作台.json`（工作台卡片）
- Test: `apps/hb_attendance_app/tests/test_rest_leave_swap_entry.py`

**Interfaces:**
- Consumes: Task 3/4 的同步函数全路径。
- Produces: 定时与入口；无新函数。

- [ ] **Step 1: 写失败测试**

```python
import json
import unittest
from pathlib import Path

APP = Path(__file__).parents[1] / "hb_attendance_app"


class EntryContractTest(unittest.TestCase):
    def test_hooks_registers_both_syncs(self):
        src = (APP / "hooks.py").read_text()
        self.assertIn("sync_rest_leave.sync_rest_leave_from_bitable", src)
        self.assertIn("sync_shift_swap.sync_shift_swap_from_bitable", src)

    def test_sidebar_has_two_new_items(self):
        src = (APP / "hbos_attendance/setup.py").read_text()
        self.assertIn("HBOS Rest Leave Record", src)
        self.assertIn("HBOS Shift Swap Record", src)

    def test_workspace_has_two_new_links(self):
        ws = json.loads((APP / "hbos_attendance/workspace/海滨考勤工作台/海滨考勤工作台.json").read_text())
        labels = [l.get("label") for l in ws["links"]]
        self.assertIn("飞书调休记录", labels)
        self.assertIn("飞书换班记录", labels)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行确认失败**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_rest_leave_swap_entry -v
```
Expected: FAIL

- [ ] **Step 3: 加定时与入口**

`hooks.py` 的 `"*/30 * * * *"` 数组追加两项（保持既有三项不变）：

```python
        "*/30 * * * *": [
            "hb_attendance_app.hbos_attendance.api.sync_from_bitable",
            "hb_attendance_app.hbos_attendance.api.sync_overtime_from_bitable",
            "hb_attendance_app.hbos_attendance.sync_rest_leave.sync_rest_leave_from_bitable",
            "hb_attendance_app.hbos_attendance.sync_shift_swap.sync_shift_swap_from_bitable",
        ],
```

`setup.py` 的 `PRIMARY_SIDEBAR_ITEMS` 在「飞书加班记录」之后插入两项：

```python
	{"label": "飞书调休记录", "link_type": "DocType", "link_to": "HBOS Rest Leave Record", "type": "Link", "icon": "leave"},
	{"label": "飞书换班记录", "link_type": "DocType", "link_to": "HBOS Shift Swap Record", "type": "Link", "icon": "refresh-cw"},
```

工作台 JSON：在「考勤导入日志」链接之后插入两条（`links` 数组，`type: "Link"`）：

```json
  {
   "hidden": 0,
   "is_query_report": 0,
   "label": "飞书调休记录",
   "link_count": 0,
   "link_to": "HBOS Rest Leave Record",
   "link_type": "DocType",
   "onboard": 0,
   "type": "Link"
  },
  {
   "hidden": 0,
   "is_query_report": 0,
   "label": "飞书换班记录",
   "link_count": 0,
   "link_to": "HBOS Shift Swap Record",
   "link_type": "DocType",
   "onboard": 0,
   "type": "Link"
  },
```

并把「导入与数据」Card Break 的 `link_count` 由 3 改为 5。

- [ ] **Step 4: 运行确认通过 + 全量**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest apps.hb_attendance_app.tests.test_rest_leave_swap_entry -v && python3 -m unittest discover -s apps/hb_attendance_app/tests 2>&1 | tail -3
```
Expected: 3 用例 PASS；全量 OK

- [ ] **Step 5: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add apps/hb_attendance_app/hb_attendance_app/hooks.py apps/hb_attendance_app/hb_attendance_app/hbos_attendance/setup.py "apps/hb_attendance_app/hb_attendance_app/hbos_attendance/workspace/海滨考勤工作台/海滨考勤工作台.json" apps/hb_attendance_app/tests/test_rest_leave_swap_entry.py
git commit -m "feat: 调休/换班记录接入定时同步与工作台入口"
```

---

### Task 6: 运行态验证与文档（需 Owner 授权）

**Files:**
- Modify: `docs/PROJECT_STATUS.md`、`docs/CURRENT_MILESTONE.md`（M1-FIX-B5 追加）

- [ ] **Step 1: 离线全量确认**

Run:
```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding" && python3 -m compileall -q apps/hb_attendance_app; export PYTHONPATH="$PWD/apps/hb_attendance_app"; python3 -m unittest discover -s apps/hb_attendance_app/tests -v 2>&1 | tail -5
```
Expected: 全部 PASS（含本批新增）

- [ ] **Step 2: 运行态注册（Owner 授权后）**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
docker compose exec -T backend bench --site frontend migrate
docker compose restart backend scheduler queue-long queue-short
```
> 注意：`migrate` 会注册两个新 DocType；改过 `.py` 后必须 **restart**（不要 `up -d`，重建会换 IP 导致 nginx 502）。

- [ ] **Step 3: 首次同步（Owner 授权后，只读飞书 → 写本地）**

```bash
docker compose exec -T backend bench --site frontend execute hb_attendance_app.hbos_attendance.sync_rest_leave.sync_rest_leave_from_bitable
docker compose exec -T backend bench --site frontend execute hb_attendance_app.hbos_attendance.sync_shift_swap.sync_shift_swap_from_bitable
```
Expected：各返回 `{"created": N, "updated": 0, "skipped": M}`；无异常日志。
核对：Desk 中两个列表的记录数 = 飞书表中「已通过」条数（换班需 wiki 解析成功）。

- [ ] **Step 4: 浏览器核对入口**

工作台「导入与数据」区应出现「飞书调休记录」「飞书换班记录」；侧栏「海滨考勤」模块同。

- [ ] **Step 5: 文档**

`docs/PROJECT_STATUS.md` / `docs/CURRENT_MILESTONE.md` 的 M1-FIX-B5 段追加：
「2026-09-11 追加：新增飞书调休记录 / 换班记录两个只读 DocType 与同步脚本（**不参与考勤判定**，
与请假表「类型=调休」各记各的），接入工作台与侧栏入口。」

- [ ] **Step 6: 提交**

```bash
cd "/Users/xinxianghaibinzongheguanlizhongxin/Vibe Coding"
git add docs/PROJECT_STATUS.md docs/CURRENT_MILESTONE.md
git commit -m "docs: 记录调休/换班记录界面落地（M1-FIX-B5 追加）"
```
