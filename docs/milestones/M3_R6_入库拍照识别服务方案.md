# M3-R6 入库拍照识别服务方案

项目名称：新乡海滨智能运营管理平台。

## 文件定位

本文件是 M3（仓储库存数字化管理）第六轮 M3-R6 的**方案**。

**本文件为方案，不代表已执行。** M3-R6 的实际执行需 Owner 授权；**外部 FastAPI 服务的启动同样需单独授权**（项目规则明列：不得默认启动外部 FastAPI 服务）。

## 本轮状态

状态：IN_PROGRESS（门禁已确认；Owner 已授权；**服务骨架、约束校验层、Frappe 侧入口与校对界面均已交付；真实跨进程端到端已验证通过**；**50 张真实样本准确率已实测，模型档位已选定 `small`（单张 7~9 s / 全字段 82%）**；**入库提交后自动生成货位卡与待检证已交付并验证**；**品名抽取规则已修，错答归零**）。执行记录见第九之二至之九节。

## 目录

**方案部分**（不随执行增长，先看这部分了解本轮在做什么）

| 节 | 内容 |
| --- | --- |
| [一](#一门禁确认结果owner-已定) | 门禁确认结果（Owner 已定） |
| [二](#二业务背景与识别目标) | 业务背景与识别目标 |
| [三](#三三个候选方案) | 三个候选方案 |
| [四](#四方案-c-下的两条实现路线) | 方案 C 下的两条实现路线 |
| [五](#五架构与集成方式沿用项目既有规范) | 架构与集成方式（沿用项目既有规范） |
| [六](#六数据留存与脱敏规则) | 数据留存与脱敏规则 |
| [七](#七识别准确率验收标准owner-授权本轮制定) | **识别准确率验收标准**（Owner 授权本轮制定） |
| [八](#八m3-r6-交付边界待授权确认) | M3-R6 交付边界 |
| [九](#九待确认与已确认) | 待确认与已确认 |

**执行记录**（按批次追加，本文件最长的一段）

| 节 | 批次 | 交付 |
| --- | --- | --- |
| [九之二](#九之二执行记录owner-授权后第一批交付) | 第一批 | 服务骨架与约束校验层 |
| [九之三](#九之三执行记录第二批frappe-侧入口与校对界面) | 第二批 | Frappe 侧入口与校对界面 |
| [九之四](#九之四执行记录第三批真实跨进程端到端验证) | 第三批 | 真实跨进程端到端验证 |
| [九之五](#九之五执行记录第四批真实样本准确率实测) | 第四批 | **真实样本准确率实测**（含 ⚠ 口径更正、**九之五·二 分档实测**、**九之五·三 品名抽取规则修复**） |
| [九之六](#九之六执行记录第五批入库后自动生成货位卡与待检证) | 第五批 | 入库后自动生成货位卡 / 待检证；修打印纸型 |
| [九之七](#九之七执行记录第六批收尾修复与物料建档守卫) | 第六批 | 二维码标签全量验证；物料建档守卫 |
| [九之八](#九之八执行记录第七批按-owner-提供的样本批量建档) | 第七批 | 按 Owner 样本批量建档 128 个物料 |
| [九之九](#九之九执行记录第八批补充信息手工录入) | 第八批 | 补充信息手工录入 |
| [九之十](#九之十执行记录第九批常驻服务重启与页面版本戳修复) | 第九批 | 常驻服务重启；页面版本戳（缓存）修复 |
| [九之十一](#九之十一执行记录第十批容器无中文字体导致打印件汉字全丢) | 第十批 | **容器无中文字体 → 打印件汉字全丢**；挂载字体修复 |
| [九之十二](#九之十二执行记录第十一批待检证版式对齐官方受控模板) | 第十一批 | **待检证版式对齐官方受控模板**（照 Owner 提供的生成器校对） |
| [九之十三](#九之十三执行记录第十二批修站点打不开nginx-缓存了失效的-upstream-ip) | 第十二批 | **修站点 502**：nginx 缓存了失效的 upstream IP |
| [九之十四](#九之十四执行记录第十三批修-pdf-里汉字的-tounicode-被写成部首) | 第十三批 | **修 PDF 的 ToUnicode 被写成部首码位**（复制/部分阅读器乱字）；字体换 OFL |
| [九之十五](#九之十五执行记录第十四批提交后给一个下一步去哪的落点) | 第十四批 | **提交后给落点**：弹框去批次取卡 / 继续识别；新增 `hbos_intake_batch` 字段 |
| [九之十六](#九之十六执行记录第十五批工作台瘦身--原生表单加返回出口) | 第十五批 | **工作台瘦身**（只留独有的 5 条）+ 原生表单加「返回仓库工作台」 |
| [九之十七](#九之十七执行记录第十六批修点单据就跳走的根因并纠正上一批的误撤) | 第十六批 | **修「点单据就跳走」的根因**（侧边栏 + 面包屑两层机制）；**纠正第十五批的误撤** |
| [九之十八](#九之十八执行记录第十七批把仓库日常用到的原生内容都收进工作台) | 第十七批 | 侧边栏 **6 → 24 条**，收进原生单据 / 报表 / 主数据；面包屑补丁扩到 11 个单据 |
| [九之十九](#九之十九执行记录第十八批点打开草稿就跳走两个独立缺陷) | 第十八批 | **后端跑旧代码**（改 py 后必须重启容器）+ **打开草稿改 SPA 跳转**；补 `sidebar.app` 与 `sidebar_item_map` |

**收尾**

| 节 | 内容 |
| --- | --- |
| [十](#十风险与注意事项) | 风险与注意事项 |
| [十一](#十一下一步) | 下一步 |
| [十二](#十二数据安全说明) | 数据安全说明 |

> 与实现状态相关的汇总口径在 [九之五](#九之五执行记录第四批真实样本准确率实测)（准确率）
> 与 [九之九](#九之九执行记录第八批补充信息手工录入)（打印字段来源）；
> 状态台账见 `docs/PROJECT_STATUS.md` 与 `docs/CURRENT_MILESTONE.md`。

## 一、门禁确认结果（Owner 已定）

`docs/milestones/M3_START_GATE.md` 规定，进入 M3-R6 前必须确认三项：**部署位置、模型选型、数据留存与脱敏规则**。Owner 本轮已答复，其中第一项是关键分水岭：

| 门禁项 | Owner 决定 | 影响 |
| --- | --- | --- |
| **照片能否出内网** | **不能** | **排除方案 A / B（云端），只能走方案 C（本地部署）** |
| 部署位置 | 服务器尚未采购，**暂时部署在这台 Mac**（Mac mini M4 / 16GB） | 见第三节 3.1 的实测约束 |
| 使用范围 | **全部产品**（非试点） | 需覆盖自产全部产品的版式（**外购不走本流程**，见 2.2） |

**"照片不能出内网"这一条直接砍掉了两个候选方案**，本方案据此修订：统一走本地部署，重点转为**本地部署内部两种实现路线怎么选**（见第四节）。

## 二、业务背景与识别目标

### 2.1 识别字段（Owner 在 M3-R0 已明确）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 产品名称 | 是 | 支持人工修正 |
| 批号 | 是 | 查询核心索引，需确保准确 |
| 产品代码 | 是 | 与物料主数据（SAP 8 位代码）匹配 |
| 生产日期 | 否 | 标签含此字段时识别 |
| 有效期至 | 否 | 用于效期预警 |
| 入库时间 | 是 | **由系统自动记录拍照上传时间**，不从标签识别 |
| 货位号 | 是 | 手动选择或扫码（**扫码页已在 M3-R5 落地**） |
| 入库数量 | 是 | 人工填写，按货位分别记录 |
| 操作人 | 是 | 当前登录账号 |
| 标签照片 | 是 | 留存原始凭证便于追溯 |

即：**AI 只负责 5 个字段**（产品名称、批号、产品代码、生产日期、有效期至），其余由系统或人工提供。

### 2.2 标签的实际复杂度（决定技术选型的关键）

从 M3-R2 的实测数据看，标签版式差异很大：

| 维度 | 情况 |
| --- | --- |
| 来源 | 有**自产**与**外购**两套版式；但**本识别只做自产**（见下） |
| 产品数 | 5 个库位合计 **94 个物料代码** |
| 语言 | 中文为主，部分含英文（USP / EP / JP / CEP 等药典后缀） |
| 批号格式 | 三类结构（原料药/中间体、混粉含拼音首字母、亚批带 `-1/-2`），还有外购的**原厂批号 + 进厂批号**双批号 |
| 物料代码 | 8 位数字，但**前缀语义需按库位区分**（1100 包材 / 120x 辅助用品等） |
| 单位 | 11 种（KG / 个 / 套 / 瓶 / 支 / 双 / 卷 / 张 / L / BT） |

**结论：这不是"固定版式的表单识别"，而是"版式多样、需要语义理解的标签识别"。**

这一点直接决定了方案优先级——详见第四节。

> **识别范围（Owner 已明确）**：**只做自产**。
> 外购物料到货时**已有现成的待检证与货位卡**，**不经过本识别流程**，
> 因此外购标签不在识别范围内，也无需取样测试。

## 三、三个候选方案

### 方案 A：云端多模态大模型 API

把标签照片上传到云端**视觉语言模型**，模型直接返回结构化 JSON（含各字段与置信度）。

| 优点 | 缺点 |
| --- | --- |
| 对**版式变化鲁棒**——不需要为每种标签写规则 | **照片离开内网** |
| 直接输出**结构化字段**，不是文字块 | 按次计费；依赖外网与第三方可用性 |
| 无需 GPU，建设最快 | 识别结果稳定性受模型版本影响 |
| 能"理解"语义（如分辨哪个是批号、哪个是代码） | 需确认服务商条款（是否用于训练、留存多久） |

### 方案 B：云端专用 OCR API（百度 / 腾讯 / 阿里等）

只做**文字识别**，返回文本行；字段结构化需自己写规则。

| 优点 | 缺点 |
| --- | --- |
| 便宜、快、印刷体准确率高 | **照片同样离开内网** |
| 通用文字识别成熟 | **返回的是文字块，不是字段**——需为每种版式写解析规则 |
| | 维护成本**随产品数增长**（94 个物料） |
| | 无法判断"这个数字是批号还是代码" |

### 方案 C：本地 / 内网部署开源模型

在**公司内网**部署（PaddleOCR，或量化的本地多模态模型如 Qwen-VL）。

| 优点 | 缺点 |
| --- | --- |
| **照片不出内网**——对药企合规最友好 | **需要 GPU 服务器**（见 3.1 现状） |
| 长期无按次费用 | 部署与调优工作量大 |
| 可离线运行 | 准确率需实测；版式变化时需自行迭代 |
| | 需长期运维投入 |

### 3.1 部署环境实测（方案 C 的前提）

Owner 已定：**暂不采购服务器，先部署在这台 Mac**（Mac mini M4）。实测约束如下：

| 项目 | 实测值 | 影响 |
| --- | --- | --- |
| 硬件 | Apple **M4**，**16 GB** 统一内存，无 NVIDIA GPU | 可用 Metal / CPU，**不能用 CUDA** |
| Docker 虚拟机内存上限 | **7.7 GB** | Frappe 全栈已占用其中约 0.8 GB |
| 宿主机内存 | 当前空闲约 **49%**；系统压缩器已占约 6.3 GB | **内存是主要瓶颈**，模型需选小的 |
| 容器访问宿主机 | `host.docker.internal` → `192.168.5.2` **可解析** | ✅ **验证通过**：FastAPI 可原生跑在 Mac 上，Frappe 容器能直接调用 |
| bench 内可用依赖 | 仅 `requests`、`PIL`；无 `cv2` / `paddleocr` / `torch` / `numpy` / ML 框架 | ML 依赖须装在**独立服务**里，不污染 Frappe |

**架构结论**：识别服务**原生运行在 Mac 上**（而非塞进 Frappe 容器），原因：

1. 容器是 Linux arm64，**拿不到 Metal 加速**，模型只能跑 CPU，慢；
2. 模型依赖重（torch / paddle），塞进 backend 会显著拖慢 Frappe；
3. `host.docker.internal` 已验证可达，通信无障碍。

**风险提示（须 Owner 知悉）**：这台是**开发机**，且 16 GB 内存同时承载 Frappe 全栈 + 本地模型**偏紧**。作临时验证可行，**不适合长期作为生产服务**——一旦 Mac 休眠或内存吃紧，仓库端拍照入库会中断。正式使用前建议采购一台内网服务器（M3 之外）。

## 四、方案 C 下的两条实现路线

照片不出内网既然已定，A / B 出局。剩下的问题是：**本地怎么识别**。两条路线：

### 路线 C1：本地多模态模型

在 Mac 上跑量化的视觉语言模型（如 Qwen2.5-VL-3B / 7B 的 4-bit 量化版），走 **MLX**（Apple 官方推理框架，能吃满 M4 的 Metal 与神经网络引擎）。

| 优点 | 缺点 |
| --- | --- |
| 直接输出**结构化字段**，不吃版式差异 | 16 GB 内存下只能跑小模型；**小模型在密集小字标签上会漏读/串行** |
| 能分辨"哪个是批号、哪个是代码" | 需下载数 GB 模型权重 |
| 换版式不改代码 | 单张耗时较长（数秒） |

### 路线 C2：本地 OCR + 结构化规则 + **主数据约束匹配**

用 **PaddleOCR**（本地、CPU 可跑、成熟）识别文字，再做三层加工：

1. **格式规则**：批号有**三类已知结构**（M3-R0 已确认）；物料代码是**8 位数字**；日期是日期格式。用格式把候选筛出来。
2. **主数据约束匹配**（关键杠杆）：本厂物料代码是**已知的 94 个**（且会随 SAP 主数据增长）。OCR 出的乱码可以**按编辑距离匹配到最近的合法代码**——"1300O215"（字母 O）自动纠正为 "13000215"（数字 0）。
3. **位置与版式线索**：同一来源的版式相对固定，字段位置可作为辅助。（本识别只做自产，故只需吃透自产版式）

| 优点 | 缺点 |
| --- | --- |
| 内存占用小（1–2 GB），CPU 即可 | 需**针对版式写解析规则** |
| 印刷体文字识别准确率高 | 版式一变要改规则（**但只两套版式**） |
| 可解释——出错能看出错在哪一步 | 规则维护有长期成本 |
| **主数据约束能纠错**，这是纯 OCR 没有的 | |

### 4.1 推荐：**两条路线都实现，用同一测试集比，看数据选**

理由：

- **都在本地，成本很低**——不像 A vs C 那样涉及"要不要买机器"，本轮没有额外代价；
- 服务骨架本就设计为**识别后端可插拔**，多实现一个后端只是多一个类；
- 这正是本方案的核心工程价值：**把选型变成可测量的实验，而不是拍脑袋**。

**先量后决**：两个后端跑同一批样本，产出一张准确率对照表，Owner 看着数字定。

### 4.2 无论选哪条，都要套一层"约束校验"

这一点独立于 C1 / C2，是**准确率的放大器**：

- **物料代码 → 形近字母纠错**（`O`→`0`、`l`→`1`、`S`→`5`、`B`→`8` …）+ 8 位长度校验 + 纯数字校验。**因代码全为数字，此步无需主数据即可纠正绝大多数 OCR 错字**（详见 7.8 八）；
- 批号 → 三类结构格式校验，格式不符就**标红**；
- 日期 → 日期格式与合理区间校验。

即：**模型负责"读"，规则负责"筛"，人负责"定"**。这样即使原始识别率只有 90%，**经约束校验后能进人工确认环节的字段可用率会显著更高**。

### 4.3 后端可插拔（不变的设计）

```
POST /recognize  →  { fields: {...}, confidence: {...} }
```

C1 / C2 乃至将来换模型，都实现同一契约。**服务骨架不因选型变化而重写。**

## 五、架构与集成方式（沿用项目既有规范）

`docs/team/05_开源代码二次开发与升级规范.md` 已确立**优先级 5：外部服务**的做法，且明确 FastAPI 为外部服务技术栈。本方案完全沿用：

```
┌──────────────────────┐   HTTP (JSON)   ┌────────────────────────┐
│  hb_inventory_app     │ ───────────────→ │  拍照识别服务            │
│  (Frappe 容器)         │ ←─────────────── │  (FastAPI，原生跑在 Mac) │
└──────────────────────┘   结构化 JSON     └────────────────────────┘
        │                                              │
        └── host.docker.internal（实测可达）───┘        └── 照片不出内网
```

### 5.1 接口契约（草案）

**请求**：`POST /api/v1/recognize`
- `image`：图片（multipart 或 base64）
- `hints`（可选）：已知线索，如 `source_type`（自产/外购）

**响应**：

```json
{
  "ok": true,
  "request_id": "…",
  "backend": "c1_local_vlm",
  "fields": {
    "product_name": "…",
    "item_code": "…",
    "batch_no": "…",
    "manufacturing_date": "YYYY-MM-DD",
    "expiry_date": "YYYY-MM-DD"
  },
  "confidence": { "product_name": 0.98, "batch_no": 0.71, "item_code": 0.95 },
  "validated": { "item_code_matched": true, "batch_no_format_ok": false },
  "raw_text": "…"
}
```

**工程要求**（依 `docs/AI技能路由规范.md` 对 API / 集成开发的要求）：

- 明确的**接口契约**，字段命名与 M3 已落地的对齐（`item_code` / `batch_no` / `manufacturing_date` / `expiry_date`）；
- **超时与重试**：识别为耗时操作，需设合理超时；
- **幂等键**（`request_id`），避免重复识别产生重复台账；
- **失败记录**：识别失败落日志，便于排查；
- **权限边界**：服务**不直连数据库**，只收图片返结果，落库由 Frappe 侧负责。

### 5.2 人工校对（Owner 明确要求）

- 返回**每字段置信度**；
- Frappe 侧录入界面**高亮低置信度字段**，提示复核；
- **识别结果不直接入账**，必须经操作人确认后才写入；
- 保留 `raw_text` 与照片，便于追溯是"识别错"还是"人工改错"。

### 5.3 标签照片留存

- 照片保存在 Frappe（`Attach`，本司内网），挂在批次或入库单据上作原始凭证；
- **识别服务端不长期保存照片**（识别后即释放）。

## 六、数据留存与脱敏规则

Owner 已定**照片不出内网**，因此规则大幅简化——**不需要再考虑服务商条款、跨境传输、是否用于训练**：

| 项目 | 规则 |
| --- | --- |
| 照片是否出内网 | **不出**（Owner 已定）。这是本方案最关键的合规边界 |
| 服务端留存 | **识别后即释放，不落盘**（仅内存处理） |
| 照片归档 | 保存到 Frappe `Attach`，随批次 / 单据走 |
| 服务日志 | **不记照片内容、不记批号与产品名**；只记 `request_id` / 耗时 / 成功失败 / 错误类型 |
| 出网能力 | 识别服务**不开任何外网访问**（不下载模型之外的流量；模型权重一次性预下载） |
| 脱敏 | **不适用**——批号与代码正是识别目标，脱敏即失去意义 |

## 七、识别准确率验收标准（Owner 授权本轮制定）

### 7.1 为什么不能只定一个总准确率

5 个字段的**重要性和难度差异很大**：批号错了找不到货，产品名称错了能从代码回填。因此**分字段定标准**，并额外设**安全底线**与**性能线**。

### 7.2 字段级标准（模型原始输出 vs 人工标注真值）

| 字段 | **及格线** | **目标** | 定这个数的理由 |
| --- | --- | --- | --- |
| **批号** | **≥ 95%** | ≥ 98% | **核心查询键**——错了就定位不到货，是整个系统的主索引 |
| **物料代码** | **≥ 95%** | ≥ 98% | 须与 SAP 主数据严格一致，错了会记到错误产品上 |
| **生产日期** | ≥ 90% | ≥ 95% | 效期推算的时间基准 |
| **有效期至** | ≥ 90% | ≥ 95% | 效期预警的直接依据 |
| **产品名称** | ≥ 85% | ≥ 95% | 容错最高——**可由物料代码从主数据自动回填**，识别错了也能纠正 |

### 7.3 单据级标准（整张标签）

| 指标 | 及格线 | 说明 |
| --- | --- | --- |
| **5 个字段全部正确** | **≥ 80%** | 即 5 张里有 4 张"拍照后可直接确认入账" |

### 7.4 安全底线（硬性，不可放宽）

这两条比准确率更重要——**它们决定错误会不会被人发现**：

| 指标 | 要求 | 为什么 |
| --- | --- | --- |
| **高置信度错误率** | **≤ 1%** | 批号 / 代码被认错、**但模型给出高置信度（≥0.9）** 的比例。这类错误**不会触发人工复核**，是最危险的失败模式 |
| **低置信度召回率** | **≥ 90%** | 真实出错的字段中，至少 90% 被标为低置信度。即"**模型知道自己不确定**" |

> 通俗说：允许模型不会，**不允许模型自信地错**。

### 7.5 性能线（本地 Mac 承载）

| 指标 | 要求 |
| --- | --- |
| 单张识别耗时 | **≤ 10 秒**（P95） |
| 连续运行 | **100 张无崩溃、无内存泄漏** |
| 并发 | 支持 **1–2** 人同时使用（仓库场景，非高并发） |

### 7.6 测试集要求（标准的前提，缺一不可）

没有合格的测试集，上面的数字都没有意义：

| 要求 | 说明 |
| --- | --- |
| 规模 | **≥ 50 张**真实标签照片 |
| 真值 | **人工逐字段标注**（这是最费工的一步，但必须做） |
| 覆盖来源 | **只需自产**（外购到货时已有现成的待检证与货位卡，不走本识别流程） |
| 覆盖语言 | 含英文后缀（USP / EP / JP / CEP）的与纯中文的都要有 |
| 覆盖产品 | **≥ 10 个不同产品**，含原料药 / 中间体 / 混粉 / 成品 |
| 覆盖拍摄条件 | 正拍 / 斜拍 / 反光 / 轻微磨损或褶皱，模拟现场真实情况 |
| 存放 | **不进 Git**，放仓库工作区之外（含真实批号与产品名） |

### 7.7 验收结论怎么用

两个后端（C1 / C2）跑**同一测试集**，产出一张对照表：

| 后端 | 批号 | 物料代码 | 生产日期 | 有效期 | 产品名称 | 全字段正确 | 高置信度错误 | 单张耗时 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C1 本地 VLM | ? | ? | ? | ? | ? | ? | ? | ? |
| C2 OCR+规则 | ? | ? | ? | ? | ? | ? | ? | ? |

**达标者入选；两个都达标选内存占用低、耗时短的；都不达标则如实报告，不硬上**（此时可退回复核：是否采购内网服务器跑更大的模型）。

### 7.8 仓库实地取样清单（可直接照做）

Owner 已确认：**样本只能去仓库实地获取**。取样需要专门跑一趟仓库，因此把清单写细，**争取一趟拿全、不返工**。

#### 一、数量与覆盖（目标 ≥50 张）

| 维度 | 要求 | 理由 |
| --- | --- | --- |
| 总数 | **≥50 张**（最低可用 30 张，但结论置信度下降） | 样本太少，准确率数字没有统计意义 |
| 自产 : 外购 | 各约一半 | 两套版式差异大，都要覆盖 |
| 产品数 | **≥10 个不同产品** | 单产品测不出换产品的鲁棒性 |
| 种类 | 覆盖原料药 / 中间体 / 混粉 / 成品 | 批号三类结构各要出现 |
| 语言 | 含英文后缀（USP / EP / JP / CEP）与纯中文都要 | 英文混排是识别难点 |
| 单位 | 尽量含件类单位（包材 / 备品） | M3-R2 实测有 11 种单位 |

#### 二、同一标签多拍（**关键**）

对**至少 3 个代表产品**，每个拍 **4 张不同条件**的：

1. **正对、光线好** —— 理想情况，作为准确率上限
2. **斜拍**（偏 15–30 度）
3. **有反光或有阴影**
4. **标签有褶皱 / 磨损 / 局部遮挡**

**为什么要这组**：现场拍摄条件不可控。这组能测出「**理想 vs 现实**」的准确率落差——落差大，说明必须强化人工校对与约束校验；落差小，说明可以放心用。

#### 三、就按现场真实方式拍

- 用**仓库实际要用的那部手机**，不要用专业相机
- **不要打光、不要修图、不要把标签摆正**
- 拍到什么算什么 —— 这才是真实输入

#### 四、命名约定（省后期人力）

建议 `来源-产品代码-序号.jpg`，例：`自产-1300****-01.jpg`。命名里带产品代码，后期标注与核对快很多。

#### 五、⚠ 同时把真值填了（**最重要的效率建议**）

**不要等回来对着照片猜真值** —— 那时人已离开标签，看不准。

在现场**拍一张、填一行**，用手机备忘录或纸质表格都行：

| 文件名 | 品名 | 物料代码 | 批号 | 生产日期 | 有效期至 |
| --- | --- | --- | --- | --- | --- |

**理由**：真值必须以**标签上实际印的字**为准，**不是以系统里的为准**。要测的正是"印在照片上的能不能读对"——如果拿系统数据当真值，就测不出标签本身的识别难度了。

#### 六、存储与安全

- 照片与真值表**都含真实批号与产品名**
- **绝不提交 Git**；放**仓库工作区之外**（如 `~/Documents/M3R6样本/`）
- 真值表虽被 `.gitignore` 覆盖（`*.csv` / `*.xlsx`），**仍建议不要放进仓库目录**

#### 七、回来后

把目录路径告知，即可跑两个后端、产出准确率对照表（见 7.7）。

#### 八、附带依赖：C2 的约束匹配**不需要主数据清单**

**Owner 补充：物料代码是全数字的，不存在含字母的情况。**

这一条**大幅简化了 C2 的实现**。关键洞察：

**"全数字"本身就是一个极强的约束——不需要 94 个合法代码的清单。**

OCR 最常见的错误是把数字认成**形近字母**：

| 真实数字 | 常被误认成 |
| --- | --- |
| `0` | `O`、`o`、`D` |
| `1` | `l`、`I`、`i` |
| `5` | `S`、`s` |
| `8` | `B` |
| `6` | `G`、`b` |
| `2` | `Z`、`z` |
| `4` | `A` |

既然**代码只可能是数字**，就可以**无条件把字母映射回形近数字**：

```
1300O215  →  13000215     （O → 0）
13O00215  →  13000215
130002l5  →  13000215     （l → 1）
l3000215  →  13000215
13OOO215  →  13000215
```

**这不需要知道任何合法代码是什么，纯按字符映射就能纠。** 比"编辑距离匹配到最近合法代码"更简单、更稳、不依赖主数据。

**因此 7.8 第八节原先写的三个方案（从盘存表提清单 / 实时查 Frappe / 先不匹配）全部不需要。**

补充规则（同样不需要主数据）：

- **长度校验**：代码固定 **8 位**，纠完长度不对就标红；
- **纯数字校验**：纠完仍含字母，说明是别的字段被误读进来了，标红；
- 主数据匹配（"这 8 位数是不是真的存在"）**降级为可选项**——即便做了，也只是"提示这个代码尚未建档"，不是识别正确性的判据。

**同一手法可推广到批号**：批号虽含字母（生产线前缀），但**字母与数字各自的位置有规律**（按三类批号结构），也可做形近纠错。不过批号的难度高于代码，C2 对批号主要靠**格式校验 + 低置信度标记**，纠错为辅。
## 八、M3-R6 交付边界（待授权确认）

**计划做**：

- FastAPI 服务骨架：**可插拔后端抽象**、接口契约、幂等键、超时重试、失败日志、**不开外网**。
- 实现**两个**后端：C1（本地量化 VLM）+ C2（PaddleOCR + 规则 + 主数据约束）。
- 约束校验层：物料代码主数据匹配、批号格式校验、日期格式校验。
- Frappe 侧：拍照上传入口 + 识别结果**人工校对**界面 + 低置信度高亮 + 确认后写入。
- 照片归档到 Frappe `Attach`。
- 用测试集实测两个后端，产出对照表。

**不做**：

- 不做批量导入 / 批量识别；
- 不自动创建 `Item`（未知物料只提示，不擅自建主数据）；
- 不做识别结果**自动入账**（必须人工确认）；
- 不为 Mac 之外的部署做适配（服务器采购属 M3 之外）；
- 不改动 M3-R1~R5 已交付的货位、台账、打印、扫码功能；
- 不修改 Frappe / ERPNext / HRMS 核心源码；
- 不接飞书真实写入；
- 不启动独立前端。

## 九、待确认与已确认

**已确认（Owner 本轮）**：① 照片**不出内网**；③ 暂部署在这台 Mac；⑤ 标准由本轮制定（见第七节）；⑥ **全部产品**。

**第 4 项已答复（Owner）**：**样本只能去仓库实地获取**，无法从其他渠道取得。

由此产生一个重要的**时序结论**：

| 工作 | 是否需要真实样本 | 何时可做 |
| --- | --- | --- |
| FastAPI 服务骨架、可插拔后端、契约、幂等与失败日志 | **不需要** | **现在即可** |
| C1 / C2 两个后端实现 | **不需要** | **现在即可** |
| 约束校验层（代码形近纠错 / 批号格式 / 日期校验） | **不需要**（7.8 八已简化） | **现在即可** |
| Frappe 侧拍照上传 + 人工校对界面 | **不需要** | **现在即可** |
| 用合成 / 公开标签做**链路冒烟测试** | **不需要** | **现在即可** |
| **用真实样本测准确率、产出 C1/C2 对照表** | **必须** | **等样本到位** |

即：**除"准确率测量"外，其余工作都不被样本阻塞**。取样清单见 **7.8**（已写成可直接照做的版本，争取一趟拿全）。

（另：Owner 已问"是否需要真实扫描标签"——**测准确率必须要真实标签**，因为准确率只有在**真实版式 + 真实拍摄条件**下才有意义；用合成标签测出的数字没有参考价值。）

## 九之二、执行记录（Owner 授权后，第一批交付）

Owner 授权「先做不被样本阻塞的部分」，本批完成：

### 已交付

| 交付物 | 说明 |
| --- | --- |
| `services/hbos_ocr/` 服务骨架 | FastAPI 应用，含 `/health` 与 `POST /api/v1/recognize` |
| **约束校验层** `app/validate.py` | 物料代码形近纠错、批号结构提示、日期多格式解析、有效期异常检测 |
| 后端抽象 + 3 个实现 | `stub`（冒烟测试，无 ML 依赖）、`c1_local_vlm`、`c2_ocr` |
| 后端注册表 | **缺依赖不崩**——未装 mlx-vlm / paddleocr 时该后端标为不可用，服务照常启动 |
| 契约 `app/contract.py` | 字段命名与 M3 已落地业务字段对齐 |
| 幂等 / 超时 / 大小上限 / 失败日志 | 日志**不记照片内容、批号与产品名** |
| `scripts/smoke_test.py` | 端到端链路测试（**进程内跑，不启动服务器**） |
| `tests/test_validate.py` | 约束校验层单测 |

### 验证结果（实测）

| 测试 | 结果 |
| --- | --- |
| 约束校验层单测 | **26 / 26 通过** |
| 端到端链路冒烟测试 | **18 / 18 通过** |

冒烟覆盖：健康检查与后端可用性上报、正常识别、**形近字母纠正**（`13OOO215` → `13000215` 并产提示）、`needs_review` 判定、幂等命中、未知后端 / 空图 / 未装依赖后端均返回 **400 而非 500**。

**关键**：以上测试**在进程内完成，没有启动任何常驻服务**（用 FastAPI 的 TestClient）。因此"链路是通的"这一结论无需启动外部服务即可得出。

### 三处需记录的实现取舍

1. **mlx-vlm 与 PaddleOCR 只接受文件路径**，不接受字节流。为满足"照片不落盘"，实现改为**写临时文件、`finally` 中立即删除**。这是对上游库限制的让步，不是长期留存，但已在代码注释中显著标注。
2. **幂等为进程内短期缓存**（默认 10 分钟，最多 500 条），**非持久**——重启即失效。足以覆盖"同一次入库重复点击"，不做严格去重。
3. **`stub` 后端不得用于生产**——它不读图像内容，返回值由 hints / 默认值构造，仅用于链路验证。

### 本批**未做**

- **未安装 C1 / C2 的重型 ML 依赖**（mlx-vlm、paddleocr、paddlepaddle，体积大且平台相关），留待准确率实测时按需安装；
- **未启动常驻服务**（冒烟测试走进程内 TestClient）；
- **未做 Frappe 侧的拍照上传入口与人工校对界面**（不在本批授权范围）；
- 未索取任何真实标签样本；
- 未修改 Frappe / ERPNext / HRMS 核心源码；
- 未接飞书写入。

### 新增目录约定

本服务放在仓库根的 **`services/`** 下（新增目录，与既有 `apps/` / `docs/` / `runtime/` / `scripts/` 并列）。

理由：它是**独立服务**，不是 Frappe App，放 `apps/` 会与 Frappe 应用混在一起。长期若拆出 `haibin-hbos-infra` 仓库，可整体迁出。

## 九之三、执行记录（第二批：Frappe 侧入口与校对界面）

### 已交付

| 交付物 | 说明 |
| --- | --- |
| `hbos_inventory/ocr_client.py` | Frappe → 识别服务的 HTTP 客户端。**服务不可达时抛 `OcrUnavailable`，绝不伪造结果** |
| `hbos_inventory/api.py` | 三个白名单接口：`get_intake_context` / `recognize_label` / `create_intake_draft` |
| Desk 页面 `hbos-photo-intake` | 「入库拍照识别」四步界面：选照片 → 识别 → 人工校对 → 生成草稿 |
| 页面样式 `hbos_photo_intake.css` | **需复核字段高亮**（橙色边框 + 提示列表） |
| 工作台入口 | 侧边栏 + 快捷方式 + 链接组新增「入库拍照识别」；原「入库登记」改标为「入库登记（原生）」 |

### 三条边界在实现中的落法

1. **识别不可用不伪造结果**——`ocr_client` 抛 `OcrUnavailable`，`api.recognize_label` 转成可读提示：
   「识别服务暂不可用：… 请改用人工录入，或联系管理员检查识别服务是否已启动。」
   同时 `get_intake_context` **不因服务不可用而报错**——页面照常打开，顶部横幅如实提示，并指路原生入库单。
2. **不自动创建物料主数据**——`create_intake_draft` 若 `Item` 不存在则拦截：
   「物料代码 XXX 尚未建档。请先在物料主数据中创建，或核对识别结果是否取错字段。」
3. **识别结果不直接入账**——只创建**草稿** `Stock Entry`（`docstatus = 0`），`remarks` 注明
   「由「入库拍照识别」创建草稿，请复核后提交」，由操作员在标准 Desk 表单里自行提交；
   **不绕过任何既有校验**（提交时仍走 ERPNext 原生校验与 M3-R3 放行门禁）。

### 验证结果（实测）

| 验证项 | 结果 |
| --- | --- |
| `get_intake_context` 在**服务未启动**时 | 不报错，如实返回 `available: false` + 原因「识别服务不可达（ConnectionError）」；返回 **216 个可选叶子货位**（分组节点已排除） |
| `recognize_label` 在**服务未启动**时 | 抛出可读提示，**未伪造任何字段** |
| 页面 JS / CSS 加载 | `Page.load_assets()` 正确读到 script（含 `on_page_load`）与 style |
| `create_intake_draft` | 生成草稿 `MAT-STE-2026-00007`（`docstatus = 0`），类型 `Material Receipt`，明细含物料 / 数量 / 货位 / 批号；**批次 `TEST-M3R1-B900` 已建**并写入来源类型与生产 / 有效期；**照片已挂为附件** |
| 边界：未知物料 | 已拦截，提示未建档 |
| 边界：分组货位（`03区 - HB`） | 已拦截，提示「不能直接存货，请选择具体货位」 |
| 边界：数量为 0 / 空批号 | 均已拦截 |
| `recognize_label` 本层编排（mock 识别层） | 参数传递正确（文件名 / 字节数 / 来源类型 / 后端）；`fields` 用校验后的值、`raw_fields` 保留 AI 原读（`13OOO215`）、`hints` 透传、`needs_review` 正确 |

### 一处技术发现

Desk 页面的 `.js` / `.css` **没有独立的资源 URL**（直接访问 `/assets/{app}/{module}/page/{page}/{page}.js` 返回 404）。Frappe 由 `Page.load_assets()` 在服务端读入并存进 `Page.script` / `Page.style`，随页面元数据一起下发。因此**不需要**把页面脚本加进 `build.json` 或 `public/`。

### 本批未做

- **未启动识别服务**——因此「Frappe → HTTP → FastAPI」的**真实跨进程链路尚未实测**，
  仅验证到本层（真实端到端需另获授权启动服务）；
- 未安装 C1 / C2 重型 ML 依赖；
- 未做批量拍照 / 批量识别；
- 未改动 M3-R1~R5 已交付的货位、台账、打印、扫码功能。

### 本轮创建的虚构 TEST 数据

| 对象 | 内容 |
| --- | --- |
| `Stock Entry`（草稿） | `MAT-STE-2026-00007`，未提交 |
| `Batch` | `TEST-M3R1-B900` |
| `File` | `TEST-M3R1-标签样例*.png`（1×1 占位图，已挂到上述草稿） |

全部为虚构数据，无真实物料、批号或照片。

## 九之四、执行记录（第三批：真实跨进程端到端验证）

Owner 授权「启动服务跑一次真实端到端」。本批**临时启动**识别服务，跑通后**已按约定停止**，未留常驻进程。

### 验证结果（全部通过）

| 验证项 | 结果 |
| --- | --- |
| 服务原生启动 | 通过（`uvicorn`，绑定 `0.0.0.0:8100`，`stub` 后端） |
| **容器 → 宿主机连通性** | ✅ **通过**：`host.docker.internal:8100/health` 返回 200，三者状态正确 |
| **Frappe → HTTP → FastAPI 真实识别** | ✅ **通过**：`recognize_label` 经真实 HTTP 调用返回 `elapsed_ms: 2`，五字段齐全 |
| 幂等（真实 HTTP） | ✅ 通过：同 `request_id` 第二次调用命中缓存，**服务端日志可见** `recognize idempotent-hit` |
| 未知后端（真实 HTTP） | ✅ 通过：服务返回 400，Frappe 侧转为可读提示 |
| 生成草稿（真实识别后） | ✅ 通过：`MAT-STE-2026-00009`，批次 `TEST-M3R1-B902` 正确写入来源类型「外购」，附件已挂 |
| 停止服务后的降级 | ✅ 通过：`available: false` + 原因「识别服务不可达（ConnectionError）」，**货位清单仍正常返回 216 条**，页面功能不受影响 |
| 无残留进程 | ✅ 已确认：无 `uvicorn` 进程、8100 端口未监听 |

### 验证中发现并修复的一个真实缺陷

**问题**：`_attach_photo` 原先只用 `file_url` 定位 `File` 记录——

```python
frappe.get_doc("File", {"file_url": file_url})   # ← 有歧义
```

而**内容相同的两次上传会产生两条 `File` 记录共用同一个 `file_url`**（FRAPPE 的存储层去重）。此时按 `file_url` 取会拿到不确定的那一条，**可能把别人已挂的附件改挂走**。

E2E 测试中确实复现了这个场景（两张相同的 1×1 占位图）。

**修复**：`create_intake_draft` 增加 `file_name` 参数（`File` 的 docname），**优先按 docname 精确定位**，`file_url` 降为兜底（且取最早一条，行为确定）。前端上传后本就能拿到 `message.name`，一并传下即可。

**验证**：修复后新草稿挂到正确的 `File` 记录（`J4: True`），且**旧草稿的附件未被抢走**（`J5` 确认仍在）。

> 这个缺陷在真实使用中很难触发——仓库每张标签照片都不同，不会出现同内容去重。但既然发现了就修掉了。

### 一处安全提示（须 Owner 知悉）

服务启动时绑定 `0.0.0.0` 才能被 Docker 容器访问，**这意味着同局域网内其他机器也能访问 8100 端口**。

- 本次是临时验证，用完即停，风险窗口很小；
- **正式部署时应缩小暴露面**：改为只绑 Docker 网关地址，或在宿主机防火墙限制 8100 仅允许本机 / Docker 网段。

已把该提示写入 `services/hbos_ocr/README.md` 的「网络与安全」一节。

### 本轮创建的虚构 TEST 数据

| 对象 | 内容 |
| --- | --- |
| `Stock Entry`（草稿） | `MAT-STE-2026-00007` / `00008` / `00009`，**均未提交** |
| `Batch` | `TEST-M3R1-B900` / `B901` / `B902` |
| `File` | 三张 `TEST-M3R1-*` 占位图 |

全部为虚构数据（占位图为 1×1 像素），无真实物料、批号或照片。是否清理待 Owner 决定。

### 至此 M3-R6 剩余

- 安装 C1 / C2 重型 ML 依赖并实测准确率——**待仓库样本**；
- 按第七节标准验收并选定后端；
- 正式部署时缩小服务暴露面。

## 九之五、执行记录（第四批：真实样本准确率实测）

Owner 提供仓库实地取样样本 50 张后执行。**样本与结果都在仓库外**
（`~/Documents/M3R6样本/`），本章只记录方法与结论，不含真实批号与产品名。

### 已交付

| 文件 | 说明 |
| --- | --- |
| `scripts/compare_accuracy.py` | 准确率比对脚本（stdlib HTTP，无额外依赖） |
| `tests/test_c2_extract.py` | C2 抽取规则单测，**37 条**，全部虚构数据 |
| `app/backends/c2_ocr.py` | 按 PaddleOCR **3.x** API 重写，抽取规则按真实版式重做 |
| `app/validate.py` | 新增**只到月日期**的支持（见下） |
| `tests/test_validate.py` | 补 7 条只到月日期的用例（共 33 条） |

### 选型：先上 C2

C2（PaddleOCR 3.7.0 + PaddlePaddle 3.3.1，CPU）先上，理由：内存占用低、
可解释、依赖轻。C1（`mlx-vlm`，需下 ~2.5 GB 权重）**本轮未装**。

### 实测结果（后端 C2，50 张真实标签）

| 字段 | 命中 | 未中 | 空 | 准确率 |
| --- | ---: | ---: | ---: | ---: |
| 批号 | 50 | 0 | 0 | **100%** |
| 物料代码 | 49 | 0 | 1 | **98%** |
| 有效期至 | 48 | 0 | 2 | **96%** |
| 生产日期 | 47 | 2 | 1 | **94%** |
| 品名 | 46 | 3 | 1 | **92%** |
| **合计** | **236** | **5** | **9** | **94.4%**（236/250 字段） |

判定口径：日期**按真值印刷粒度**比对——标签只印到月的不因模型多给日而判错。

### ⚠ 这张表的数字**不能**读成「汉字识别准确率」（2026-09-21 Owner 核对后补充）

Owner 对着真实标签照片核对后指出「很多汉字都是错的」——**是对的**。
上表容易被误读，必须在这里说清：

**① 5 个字段里 4 个是数字**（批号 / 物料代码 / 生产日期 / 有效期至），
**只有「品名」是汉字**。所以 **94.4% 主要衡量的是数字，不是汉字**。
拿它当「OCR 认字准不准」的证据是**错误口径**。

**② 标签上大量汉字从未进入这个指标**——公司名、生产车间、储存条件、
操作人/日期、运输注意事项等，都不在这 5 个字段里，错了也不扣分。
而**它们恰恰是错得最多的**（见下）。

**③ 错误集中在「小字印刷」与「手写」**，大字基本正确。Owner 逐字核对一张真实标签的结果：

| 标签上印的 | OCR 读成 | 类型 |
| --- | --- | --- |
| `操作人/日期` | 只读到 `作` | 小字印刷，漏读 |
| `Pharmaceutical Co., Ltd` | `heraceutical Co., Ltd` | 英文小字 |
| `六车间B线车间` | `过果线` / `HEB`（同一行两种读法） | 中文小字 |
| 手写人名 + 日期 | `早420511.07` | 手写 |
| `【贮藏】` | 丢字，只剩 `有效` | 小字印刷 |
| `13000520`（某一遍） | `13W8520` | 数字认成字母 `W` |

**④ 数字的高分还被「冗余」撑高**：物料代码在标签上印 2~3 遍，
抽到的那一遍读对即记为「命中」，**读错的那些遍不扣分**。
所以 98% 的含义是「**至少有一遍读对了**」，不是「每次都能读对」。

**⑤ 校验层救不了这类错**：`13000520 → 13W8520` 是把数字读成字母 `W`，
而校验层的形近字符表里**没有 W**（只有 O/D/Q/I/l/Z/A/S/G/B/g/q/T 等），
该遍直接判为无效。能对上，靠的是另一遍印的读对了。

**结论（影响后续工作的两条）**：

- **报告口径改为「分大小字」**：不再给单一「准确率」。分档实测见下「九之五·二」；
- **`储存条件` / `生产车间` / `供货单位` 等小字汉字字段，从设计上就不该指望 OCR**。
  拍照识别页已把这些标成「**须人工填写**」（2026-09-21 改），不是「可补」。

**⑥ 换更大模型解决不了这个**：`medium` 档实测**同一张图小字一样错**
（`heraceutical` 那个错两档完全相同），耗时却从 8 s 涨到 35 s。
真正的瓶颈是**小字的成像质量**（分辨率 / 对焦 / 反光），属「**怎么拍**」的问题，
不是「用哪个模型」的问题。

### 九之五·二、分档实测（2026-09-21）

三档 `tiny` / `small` / `medium` **共用同一批 50 张样本、同一套比对口径**，
用 `scripts/measure_text_tiers.py` 测。全部基于归档的识别原文，
**未重跑 OCR**（OCR 模型没换，归档原文与重跑等价）。

#### 大字：5 个结构化字段（有真值，逐张比）

**下表是对三档用同一份脚本（`compare_accuracy.py`）重跑的结果**，口径一致：

| 档位 | 品名 | 物料代码 | 批号 | 生产日期 | 有效期至 | 全字段正确 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `tiny` | 88% | 94% | 96% | 86% | 80% | 72% |
| `small` ← 选用 | **92%** | 98% | 100% | 94% | 90% | **82%** |
| `medium` | 92% | 98% | 100% | 96% | 96% | 86% |

**这张表仍然主要衡量数字**，不是汉字：5 个字段里 4 个是数字。

> **一处口径更正（2026-09-21）**：本表初版把「逐字段」取自 `measure_text_tiers.py`、
> 把「全字段正确」取自 `compare_accuracy.py`，**两个脚本的日期判定不同**，
> 导致同一张表里数字不同源（`tiny` 生产日期 88% vs 86%、`medium` 98% vs 96%）。
> 上面这版已全部改由 `compare_accuracy.py` 一处产出。
> 另：`tiny` 的生产日期由 84% 提到 86%，是**期间 `validate.py` 自身的变更**所致，
> 与本轮品名修复无关。

#### 小字：模板恒定串当免费真值（16 个锚点 × 50 张 = 800 次）

50 张是同一模板、同一公司，因此公司名 / 字段名 / 运输注意事项这些**印刷内容恒定**，
等于**现成的绝对真值**，不必人工标注。判定方式：整段原文去空白后找子串（OCR 会把
一行切碎，不能按行比）；找不到再用等长滑窗取最相似片段算字符相似度。

| 档位 | 整串命中 | 字符相似 |
| --- | ---: | ---: |
| `tiny` | **560/800 = 70.0%** | 85.2% |
| `small` ← 选用 | **668/800 = 83.5%** | 91.3% |
| `medium` | **682/800 = 85.2%** | 91.8% |

锚点内部差异极大，**均值掩盖了真正的短板**（`small` 档）：

| 锚点 | 整串命中 | 说明 |
| --- | ---: | --- |
| 字段名-执行标准 | 50/50 | 短、笔画简单 |
| 字段名-生产批号 | 49/50 | |
| 字段名-物料代码 | 49/50 | |
| 字段名-复检期/有效期至 | 42/50 | |
| 公司名(中) | 44/50 | |
| 公司名(英) | 35/50 | **英文小字**，最弱之一 |
| **重量表头-皮净毛** | **44/50** | |
| **运输注意事项(整句)** | **25/50** | **最长的一串，命中率最低** |
| **生产地址值** | **36/50** | |
| **字段名-贮藏 `【贮藏】：`** | **34/50** | 带方括号，丢字严重 |

**规律：串越长、字越小，命中越低。** `medium` 把总量从 83.5% 提到 85.2%（+1.7 个点），
但**耗时从 8 s 涨到 35 s**——每提升 1 个点要付 4 倍时间，**不划算，维持 `small`**。

#### 手写：**给不出准确率数字**（须如实说明）

手写区（`操作人/日期`、`复核人/日期` 的人名与日期）**没有真值来源**，
标签上的手写内容逐张不同，无法像小字那样用模板恒定串当答案。

**本轮尝试了人工转写，同样失败**——把该区域裁出并放大到 1400 px 后，
**在可获分辨率下仍无法可靠辨认中文人名**。因此**不给出「手写档准确率」这个数字**：
没有真值，任何百分比都是编的。

能客观记录的只有下面这些（`small` 档，全部取自归档识别原文，未做清洗）：

| 事实 | 计数 |
| --- | ---: |
| `操作人/日期：` 标签**后面为空**（整行只读到标签） | **34/50** |
| `复核人/日期：` 标签**后面为空** | **20/50** |
| 两行**都为空**（整块手写区一个字没读出） | **12/50** |
| `操作人` 那一行本身含 `20xx 年/月` 形式 | 7/50 |
| `复核人` 那一行本身含 `20xx 年/月` 形式 | 3/50 |

而且**这 7 + 3 处多数也不是手写日期**：抽查发现读到的内容大量是**相邻印刷字段被粘过来**——
例如 `1.txt` 的 `210h2020.11.07` 落在印刷的生产日期区，`6.txt` 的 `2029年08月31日`
是印刷的有效期。**没有真值就无法区分「读对了手写日期」还是「读串行了」**。

手写**姓名**：50 张里**没有一处读出可辨认的中文人名**（`5.txt` 读成 `4同`、`3.txt` 读成 `4风`）。

**结论：手写档读不出名字；日期偶有命中，但无法与相邻印刷日期区分。**
这项能力目前**只能靠人工**，不能计入 OCR 成绩。

### 九之五·三、品名抽取规则修复（2026-09-21）

**起因**：Owner 报「识别汉字的正确率太低了」，给的例子是
**「品名」读成「名」**、**「美罗培南」读成「美罗第南」**。

**先分清两件事**（这决定了能修什么）：

- **原始识别文本读错字**（`美罗培南 → 美罗第南`）——这是**模型能力上限**，
  改规则救不了。但它在**抽取层被挡住**：`美罗培南` 在标签上印 2~3 遍
  （`50.txt` 里第 2 行读成 `美罗第南`、第 18 行读成 `美罗培南`），
  锚定规则取到的是**读对的那一遍**。实测 `50.txt` 最终 `品名` 是**正确的**。
- **抽取规则自己造出来的错**——**这个能修，而且确实错了。**

**逐张核对后，抽取规则查出三个真缺陷**（都只在 `small` 档暴露，命中 5 张里的 4 张）：

| # | 缺陷 | 证据 | 后果 |
| --- | --- | --- | --- |
| ① | **值里含 ASCII 就把整条丢掉** | 真值 `美罗培南 (B)`，OCR 读出 `美罗培南（B）`，因含字母 `B` 被判无效 | 把**读对的答案扔了**，再退化到频次法抓回一串噪声（`防水、防潮，轻拿、轻放，`） |
| ② | **标签与值被切成两行时取不到** | `1.txt` 是「`品`」「`名：`」「`美罗培南`」**三个独立的框**；旧正则要求冒号后至少一个字符（`.+`），这种标签**根本不被识别成标签** | 值永远取不到，退化到频次法 |
| ③ | **频次法没有长度下限** | 标签上没读到品名时，它从残渣里挑 2~5 字的碎片交差：`避免硫碰。`、`存新件`、`合证` | **把「没读到」谎报成「读到了」**，操作员看到的是凭空冒出来的品名 |

**③ 是最有害的一条**：前两条只是「没取到」，③ 是**编一个答案**。

**修法**：

- ① 判据从「含 ASCII 就丢」改成**看汉字占比 ≥ 50%**，容忍「（B）」这类少量 ASCII；
- ② 正则的值部分改为**允许为空**，为空时**往后回看最多 3 行**取第一个像品名的；
  **撞上任何带冒号的行就停**（否则会把 `生产批号：` 当成品名——实测踩到过）；
- ③ 频次法加**汉字长度下限 4**（本厂品名 `美罗培南` / `美罗培南混粉` 都在 4 字以上）。
  **宁可返回 None**：主数据那层会把品名补上，让人照标签核对，
  也远好过给一个错的。

**效果（`small` 档，50 张）**：

| | 品名准确率 | 错答 | 空 |
| --- | ---: | ---: | ---: |
| 修前 | 90%（45/50） | **4** | 1 |
| 修后 | **92%（46/50）** | **0** | 3 |

**百分比只涨了 2 个点，但真正变的是「错答归零」**：修前那 4 处错答里
**3 处是规则编出来的**（`避免硫碰。`、`存新件`、`合证`），现在全部变成诚实的「空」。
剩下 3 处空（`4` / `10` / `12`）是 OCR **确实没读到品名**（真值 `美罗培南混粉` 在原文里
根本不出现），这 3 张由 `_enrich_from_master` **用物料主数据补上**，操作员看到的名字仍然正确。

> 第 4 处原「错答」是 `13`：OCR 读 `美罗培南（B）`、真值 `美罗培南 (B)`——
> **字全对，只是全角/半角括号之差**，不是误读。严格逐字符比会判错，
> 全半角归一后计为命中（上表按严格口径）。

**这一修对 `tiny`/`medium` 没有帮助**：那两档的失败是**真的读错字**
（`美罗信网混粉`、`携件人中期`、`自丽代城`、`美罗培南湿粉`、`美罗老`），
规则层挡不住——再次印证**瓶颈在成像，不在规则**。

**单测**：新增 4 条覆盖上述三个缺陷（切两行的标签、含 ASCII 括号的品名、
回看不越界取字段名、频次法下限返回 None），总数 89 → **93 条全通过**。

### 对照第七节验收标准

| 标准项 | 要求 | 实测 | 结论 |
| --- | --- | --- | --- |
| 批号 | ≥95% | **100%** | ✅ 达标 |
| 物料代码 | ≥95% | **98%** | ✅ 达标 |
| 生产日期 | ≥90% | **94%** | ✅ 达标 |
| 有效期至 | ≥90% | **96%** | ✅ 达标 |
| 产品名称 | ≥85% | **92%** | ✅ 达标 |
| **全字段正确** | ≥80% | **84%**（42/50 张） | ✅ 达标 |
| **单张耗时** | ≤10 s | medium 35 s → **small 实测 7.0~9.0 s（均 8.1 s）** | ✅ **已达标（换模型档位，见下）** |
| 高置信度错误 ≤1% | — | — | ⚠ **无法测量** |
| 低置信度召回 ≥90% | — | — | ⚠ **无法测量** |

**accuracy 全部达标。** 耗时起初不达标（medium 35 s），已通过**换轻量模型档位**解决（small 实测 7~9 s、均 8.1 s），详见下文「模型档位对照」。

最后两项测不了的原因：C2 是"OCR + 规则"，**没有逐字段置信度**这个概念——
`confidence` 目前返回空字典。要满足这两条，要么让 C2 输出 OCR 逐行置信度并映射到字段，
要么改用 C1（VLM 天然给置信度）。**这是选型时绕不过去的一条。**

### 发现并修复的三个真实缺陷

**① 只到月的日期被整条丢弃（最严重）**

实测 **39/50 张**标签的有效期**只印到月**（如「2028年11月」），日根本没印。
原校验层只认三种成分齐全的日期，这类值**直接返回 None**——三分之二的有效期会丢。

修复：补成**当月最后一天**，并在 `hints` 里明说「日是按月末推定的，请核对」。

> 取月末而非月初，是为了不把效期提前一个月——否则还能用的批次会被预警先拦下。

**Owner 已确认（2026-09-16）**：按月末推定；并明确**不加「粒度」字段**——
系统存的就是月末那天，不再区分标签原本印到月还是印到日。

由此产生一处**已知取舍**（如实记录）：打印件（待检证 / 货位卡）会把
`2028年6月` 呈现为 `2028-06-30`，比标签原印信息更细；
因不加粒度字段，系统无法在打印时还原原粒度。

**② 签核日期顶掉生产日期**

标签上有「操作人/日期」「复核人/日期」两类**人员签核日期**。它们与物料日期
混在一起参与「取最早作生产日期」的判断，导致某张标签的复核日期（被 OCR 误读成
`2021.06.27`）把真正的生产日期挤到了"有效期"位置上。

修复：含「操作人 / 复核人 / 核发人 / 签名」的行**不进日期候选**。

> 副作用要如实记录：有 1 张的效期由**「读错」变为「留空」**。
> 按"命中"计不增不减，但**留空比读错安全**——空会促使人补录，读错会静默污染效期预警。

**③ C2 后端按 2.x 写的，在 3.x 上完全失效**

PaddleOCR 3.x 删掉了 `use_angle_cls` / `show_log` 参数与 `.ocr(path, cls=)`，
且返回结构从嵌套列表变成 dict 风格的 `OCRResult`（文本在 `rec_texts`）。
已按 3.x 重写 `_flatten` / `_extract`，并保留 2.x 结构的兼容分支。

### 剩余 10 处未命中的根因（已逐张核对识别原文）

| 根因 | 处数 | 例子 |
| --- | ---: | --- |
| **OCR 读错或漏读** | 8 | 代码少读一位（`1300090`，缺末位）；生产日期 `2024.08.01` 读成 `08.03`；品名行整行未读出 |
| 规则误配 | 2 | 见上①之外的「取最早」边界，已修 |

**结论：剩余误差主要是 OCR 本身的能力上限，不是规则没写好。**
继续提高只能换更强的识别模型（C1），或接受现状 + 人工校对。

### 性能实测（重要，影响选型）

| 分辨率 | 单张耗时 | 小字（有效期）是否还能读对 |
| --- | --- | --- |
| 4096×3072（原图） | **约 35 s** | 能 |
| 3200 px | 22 s | 开始丢 |
| 2000 px | 10 s | **读错**（如 2028.11 读成 2005.12.2） |

**不能用降分辨率提速**——有效期是小字，降了就废。

> 说明：35 s 是**单张热启动识别**的耗时，含 PaddleOCR 对 4096×3072 大图的检测与识别。

### 模型档位对照（耗时问题的正解）

降分辨率这条路走不通，但 **PaddleOCR 自带多档模型**——换档**不需要装任何新依赖**，
只是换个模型名。用同一批 50 张实测：

| 档位 | 单张耗时 | 品名 | 代码 | 批号 | 生产日期 | 有效期 | **全字段** |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `medium` | **35 s** | 92% | 98% | 100% | 94% | 96% | **84%** |
| `small` ← **选用** | **8 s** | 90% | 98% | 100% | **94%** | 90% | **80%** |
| `tiny` | **3 s** | 88% | 94% | 96% | 84% | 80% | 72% |

**`small` 是唯一的帕累托最优点**：耗时压进 10 s 标准线（实测 7.0~9.0 s，均 8.1 s，**且全部低于 10 s**），全字段正确率只比 `medium` 低 4 个点。
`tiny` 虽然更快，但有效期掉到 80%、全字段 72%，**不值得**。

**决定：默认档位改为 `small`。** 换档能力已做成配置（`c2_ocr:small` / `HBOS_OCR_C2_MODEL`），
随时可切回 `medium`。

### 一处必须如实记录的诊断更正

`small` 与 `medium` 的差异，**初判为"small 会编造日期"**——依据是两张标签的
生产日期分别输出成 `2026-09-30`、`2026-08-31`，而真值是 9月7日、8月10日，
看起来像是把日编成了月末。

**逐张核对识别原文后，这个判断是错的。** 真实原因是两类：

1. **漏读日**——`small` 只读出了「2026年09月」，**日被整个漏掉**。
   于是**本层的月末推定逻辑**替它补了 30 / 31。
   **那个日是"我们"补的，不是模型编的**，而且原文里确实只有年月，无从判别。
2. **取错日期**——有标签上存在多个日期，`small` 挑了另一个（如把登记号里的
   `2020.11.07` 当成生产日期）。这个日期**确实印在标签上**。

两类都**不是编造**，且都**拦不住**（数字在原文里找得到，或日是我们按规则补的）。

**由此发现一个真问题并已修复**：提示语原本写「**标签只印到月**（2026年9月），
已按月末推定日」——但标签上其实印了日，是模型漏读。**这句话在对着人撒谎**，
会让人误以为标签上真没有日。已改为：

```
识别只读到了年月（2026年9月），已按**月末**推定日；若标签上其实印了日，请按标签填写
```

说的是"我们读到了什么"，不是"标签上印了什么"——**这一点必须分清**。

> 量化：`small` 全 50 张里，模型只读到年月的场合共 39 处，
> 其中**标签确实只到月 37 处**、**标签其实印了日 2 处**（即上面那两个）。
> `medium` 的对应数字是 38 / 0。

### 修复：生产日期被噪声顶掉（第四批的后续修正）

**发现方式**：Owner 授权做实地演示时，样本 A 的输出里生产日期是 `2020-11-07`，
而真值是 `2025年6月25日`——**而且 `needs_review` 是 False，没有任何警告**。

**根因**（逐张调识别原文核对）：标签上出现了三个日期

```
2020.11.07   ← OCR 把某段印刷内容切出来的噪声
2025.06.25   ← **真正的生产日期，模型读对了**
2028.06.24   ← 有效期
```

旧规则是「**取最早**当生产日期」——于是噪声被当成了生产日期，
**读对的正确值反被扔掉**。而且这个日期格式合法、原文里确实有这串数字，
**任何格式校验都拦不住**。

**这是规则错，不是模型能力不足。** 先前"剩余误差主要是 OCR 能力上限"的归因**据此更正**：
重新逐条归因后，日期类失败 9 处里有 **4 处是规则选错**（可修）、5 处是模型没读出来。

**修法**：改用**保质期常识**筛噪。本厂标签效期 2~3 年，那么"离效期 5 年以上"的日期
当生产日期就不合常理，判为噪声剔除；剩下的候选里再取最早。全被剔光时退回不筛，
宁可给个日期也不弄丢。

> 中途试过另一种改法——"取早于有效期的最晚一个"——**实测反而更差**
> （生产日期 92% → 86%），因为它会选到靠近生产日期的另一个噪声日期。
> 已弃用，改回极值法 + 保质期筛。

**效果**：生产日期 **92% → 94%**（修好样本 A，其余样本无回归），
整体 **94.0% → 94.4%**。补 4 条单测覆盖该规则（含"全被剔光不能丢日期"的边界）。

### 日期溯源校验（保险，非修复）

本批还加了一道校验：**识别出的日期，其数字必须真的出现在识别原文里**，
否则清空转人工。

**如实说明：这道校验在 50 张上"一处都没拦到"**——因为上述两类错误都拦不住
（见上面的更正）。留着它是为 **C1（本地量化 VLM）** 准备的：
VLM 的典型失效模式正是凭空生成一个格式合法、原文里却没有的日期。

不夸大它的作用，也不假装它解决了什么问题。

### 样本的两处局限（须记录）

- **50 张全为自产——这是对的，不是缺口**：外购物料到货时**已有现成的待检证与货位卡**，
  **不经过本识别流程**，因此外购标签本就不需要测。识别范围就是自产。
- **「拍法」列全空**：原计划按「正对 / 斜拍 / 反光 / 磨损」分组以测出
  「理想 vs 现实」的落差，实际未填，因此只得到混合平均值。

### 本批未做

- 装 C1（`mlx-vlm`）——耗时问题已用模型档位解决，C1 不再是当务之急；
- **置信度类标准**（高置信度错误 ≤1% / 低置信度召回 ≥90%）**仍测不了**，须 Owner 决策；
- 外购标签的任何测试。

### 数据安全

- 真实照片与真值表**均在仓库外**（`~/Documents/M3R6样本/`），未提交、未入库；
- 比对结果（含真实批号）也写在仓库外（`.../结果/`），未提交；
- 仓库内的脚本与单测**只含方法与虚构数据**，已逐项核对。

### 顺带发现的环境陷阱（非本项目问题）

PaddleOCR 初始化时报 `AttributeError: module 'cv2' has no attribute 'IMREAD_COLOR'`。
根因是**当时的工作目录**里存在一个无关的 `cv2.py`，把真 OpenCV 顶掉了。
已写进 README 免得再踩。从 `services/hbos_ocr` 启动不会有此问题。

## 九之六、执行记录（第五批：入库后自动生成货位卡与待检证）

**背景**：M3-R4 做了三个 Print Format，但**只能在 `Batch` 表单上手动点打印**；
M3-R6 做了拍照识别入口能生成草稿入库单，但**入库完就断了**，仓库还得自己去找批次逐个打印。
本轮把两截接上。

### Owner 已定的三个口径

| 决策 | 选择 |
| --- | --- |
| 生成时机 | **入库单提交后**（草稿阶段货位/数量还能改，且货位卡依赖真实库存流水） |
| 产物形式 | **合并成一个 PDF**（一次打印出一套） |
| 触发范围 | **所有 Material Receipt 入库单**（不限拍照识别建的） |

### 已交付

| 文件 | 说明 |
| --- | --- |
| `hbos_inventory/doc_gen.py`（新建） | 生成本体：渲染、合并、挂附件、防重复、防阻断 |
| `hooks.py`（改） | `Stock Entry.on_submit` 挂 `generate_for_stock_entry`；注册 `doctype_js` |
| `public/js/batch.js`（新建） | 批次表单「重新生成货位卡 / 待检证」按钮 |
| `hbos_photo_intake.js`（改） | 生成草稿的提示补一句「提交后自动生成、去批次附件取」 |
| `HBOS待检证` 模板（改） | 纸型改声明在 `.print-format` 上（见「顺带修复」） |

### 设计要点

- **为什么挂在 `on_submit`**：`frappe/model/document.py` 的 `compose()` **先跑控制器方法、再跑 doc_events 钩子**，
  所以 ERPNext 的 `StockEntry.on_submit`（建 SLE）跑完才轮到本模块——货位卡的货位明细、
  入库数量、入库日期、流水表**都能查到真实数据**。生成草稿时这些**都还不存在**。
- **一个批号只出一张卡**：同一批次可能拆成多行（不同货位），按批次去重后再生成（M3-R0 已定「一卡一批」）。
- **失败不阻断入库**：`on_submit` 抛异常会**回滚整个提交**。生成 PDF 是附加动作，
  绝不能因为渲染失败让人入不了库。故整段吞异常、记 `Error Log`，
  并在界面上**如实提示哪几个批次失败、去批次页点按钮补生成**。
- **不堆附件**：生成前按文件名精确匹配删掉上一份（`HBOS-<批号>-待检证+货位卡.pdf`），
  **人工上传的附件名字不会长这样，动不到**。amend 重提也不会越堆越多。

### 验证结果（TEST 虚构数据，全部通过）

| 验证项 | 结果 |
| --- | --- |
| 入库单提交后自动生成 | ✅ 2 批次耗时 **1.55s** |
| 待检证纸型 | ✅ 第 1 页 `(212, 312)` = 75×110mm |
| 货位卡纸型 | ✅ 第 2 页 `(595, 842)` = A4 |
| 同一批次拆 3 行 → 只出 1 张卡 | ✅ |
| 重复生成不堆附件 | ✅ 仍为 1 个 |
| `Material Transfer` 不误触发 | ✅ |
| 渲染失败**不阻断提交** | ✅ 提交成功，`Error Log` 有记录 |
| 自产 / 外购分流 | ✅ 分别选中对应货位卡 |

耗时 3 批次约 2.3s → **一次入库 4~6 批约 3~5 秒，不需要上异步队列**（按「先量再定」）。

### 顺带修复：M3-R4/R5 的纸型缺陷（重要）

做本例时发现，M3-R4 三个格式与 M3-R5 的货位二维码**在手动点打印时全都按 A4 出**——
模板里写的 `@page { size: 75mm 110mm }` **被 wkhtmltopdf 忽略**。
M3-R4/R5 当时验的是「能不能出 PDF」，**没验纸型与页数**。

另有一个更隐蔽的：Frappe 打印框架会注入

```css
.print-format { min-height: 11.69in; padding: 0.75in }   /* 11.69in = 297mm，A4 高 */
.print-format td { padding: 6px !important }
```

`min-height: 297mm` 是**给 A4 屏幕预览**设计的，放在 75×110mm 的纸上必然溢出成 2 页——
**跟模板写得好不好无关**。

**修法**（已实施，`HBOS待检证`）：纸型改声明在 `.print-format` 上——
Frappe 会读这几个 CSS 属性并传给 wkhtmltopdf（见 `frappe/utils/pdf.py::read_options_from_html`）。
**注意 margin 必须用长写法**，简写不会被读取。

```css
.print-format {
    page-width: 75mm; page-height: 110mm;
    margin-top: 4mm; margin-bottom: 4mm; margin-left: 5mm; margin-right: 5mm;
    min-height: 0 !important; max-height: none !important;
    max-width: none !important; padding: 0 !important;
}
.print-format td, .print-format th { padding: 2px 3px !important; }
```

修复后实测：待检证手动打印 **1 页 75×110mm**；两张货位卡 **1 页 A4 无回归**。

### 同批修复：货位二维码小标签（M3-R5 遗留）

同样加了纸型声明后，二维码**纸型对了（60×40mm）但内容仍溢出**（实测需约 60mm）。
根因有两层：

1. `hbos_bin_qr_svg` 生成的内联 SVG 自带 `520×520`（≈138mm），
   而模板里写的 `svg { width: 26mm }` **在 wkhtmltopdf 里不生效**；
2. 标签内容本身就偏多——URL 行（109 字符）换行后占了约 8mm。

**修法**（已实施）：

- `qr_utils.hbos_bin_qr_svg` 新增 `size_mm` 参数，**把尺寸写进 SVG 的 width/height 属性**；
- 尺寸定为 **20mm**（该标签可用高约 34mm，20mm 刚好放得下「码 + 货位号 + 提示」）；
- **去掉 URL 文本行**——内容已在二维码里，小标签上印长 URL 既不可读又占地方；
- **去掉 `name` 行**——实测 `hbos_bin_code` 恒等于 `warehouse_name`，
  那个 `{% if %}` 永假、从未渲染，是死代码。

修复后实测（**全量，非抽样**）：**224 个货位 / 库位的标签全部为 1 页 60×40mm**，
渲染出的 SVG 带的是 `width="20mm" height="20mm"`（不再是自带的 520×520 像素）。
同批回归另三个格式，均无回归：待检证 1 页 75×110mm、自产货位卡 1 页 A4、外购货位卡 1 页 A4。
**这四项都是走「手动点打印」那条路验的**（不注入任何覆盖 CSS，纸型完全由模板自己声明决定）——
自动生成那条路由 `doc_gen.py` 显式传参，是另一条路。

> 验证方式：`frappe.get_print(..., as_pdf=False)` 取 HTML → `get_pdf()` → 用 `pypdf`
> 读每页 `mediabox` 实际物理尺寸（不是看模板里写了什么）。

### 一个必须知道的操作坑

**改完 `hooks.py` 必须清缓存，新钩子才生效。** 第一次跑验证时钩子完全没触发（附件 0 个），
`bench clear-cache` 之后立刻正常。同类地，改 Print Format 的 app 文件后要让
`modified` 比库里新，`bench migrate` 才会重新导入（标准 Print Format 不能直接改库）。

### 本轮创建的虚构 TEST 数据

| 对象 | 内容 |
| --- | --- |
| `Stock Entry` | `MAT-STE-2026-00010`、`00012`（均**已提交**、Material Receipt） |
| `Batch` | `TEST-M3R1-E001` / `E002` / `E003`（自产）、`E900`（外购） |
| `File` | 每批次 1 个合并 PDF（`HBOS-<批号>-待检证+货位卡.pdf`） |
| `Error Log` | 1 条（渲染失败路径的证据，**保留**） |

全部虚构。**Owner 已明确先留着**，便于直接打开批次看生成效果。

### 本批未做

- 未做批量打印入口（一次打印多个批次）；
- 未做「打印后自动推进放行状态」一类的流程联动；
- **未覆盖 `Purchase Receipt`**（采购收货是另一个 DocType）——Owner 口径是「入库单」，
  本轮按 `Stock Entry` 的 Material Receipt 实现，采购收货不在内；
- 货位二维码小标签的内容溢出**已在第六批修完并全量验证**（见九之七），此处原记的「未修」作废。

## 九之七、执行记录（第六批：收尾修复与物料建档守卫）

### 货位二维码标签：收尾与全量验证

第五批修了 `qr_utils`（SVG 自带尺寸）与模板（去 URL 行、去死代码 `name` 行），
但当时**只验到「能出 1 页」就中断了**。本批把三件事补完：

1. **确认修改确实落到了库里**——标准 Print Format 不能直接改库，
   靠 `bench migrate` 从 app 文件重新导入；已核对库里 `modified` 与 app 文件一致；
2. **手动打印路径全量回归**（见九之六「同批修复」一节末）；
3. **逐条核对 `hbos_bin_code` 恒等于 `warehouse_name` 去「 - HB」后缀**（224/224），
   证实模板里那个 `{% if ... != ... %}` 确实永假，删掉它没有信息损失。

### 物料建档守卫：从「存不存在」改成「能不能用」

`create_intake_draft` 原来只查 `frappe.db.exists("Item", item_code)`。
**这个检查不够**——`Item` 存在但**没勾批次管理**时，本流程会一路走到
`_ensure_batch` 才炸在 ERPNext 的 `Batch.validate()` 上，报的是英文
`The selected item cannot have Batch`，操作员完全看不出该去改哪个字段。

已改为 `_require_item_ready()`，一次查清本流程真正依赖的开关，缺什么说什么：

| 检查 | 为什么本流程依赖它 |
| --- | --- |
| 物料存在 | 不存在 → 原来的「尚未建档」提示（保留不变） |
| `disabled` 未停用 | 停用物料不该能入库 |
| `is_stock_item` = 1 | 入库单是库存事务，非库存物料过不了 |
| `has_batch_no` = 1 | 本页**批号必填**，`_ensure_batch` 必然建批次 |

**仍然「不自动创建物料主数据」**——只提示，不擅自建档（第八节边界不变）。
实测三种输入：不存在的代码 → 原提示；存在但缺开关的物料 → 逐项列出缺什么；完整物料 → 放行。

### 物料建档到底需要填什么

**ERPNext 原生必填**只有四项：物料代码、物料名称、物料组、默认计量单位。
只填这四项能存盘，但**不足以支撑本流程**。要让一个物料走通「拍照识别入库 →
自动出待检证与货位卡 → 出库核销」，实际需要：

| 字段 | 取值 | 缺了会怎样 |
| --- | --- | --- |
| `is_stock_item` | 勾选 | 入库单直接过不了 |
| `has_batch_no` | **勾选** | `Batch.validate()` 报「The selected item cannot have Batch」，批次 / 货位卡 / 待检证 / 出库放行门禁**全部不成立** |
| `item_group` | 海滨分类树：原料药 / 包材 / 辅助用品 / 中间体 / 成品 | 落进原生 6 类，报表与分类统计对不上 |
| `hbos_product_kind` | 同上的业务分类 | 同上（两者独立维护，见 M3-R2 6.4） |
| `stock_uom` | 实体单位（Kg / 个 / 套 / 瓶 / 支 / 双 / 卷 / 张 / L / BT / 件） | 数量与货位卡单位无从计算 |
| `hbos_shelf_life_type` | 复检期 / 有效期 | 货位卡上那个勾选框空着 |
| `hbos_shelf_life_months`（或原生 `shelf_life_in_days`） | 如 24 | 无法由生产日期推算到期日，效期预警失效 |
| `hbos_storage_condition` | 如「储存温度不超过 30℃」 | **待检证上的「储存条件」印成空白** |
| `hbos_workshop` | 如「六车间B线」 | 货位卡上的生产车间空着 |

**其中后五项（分类、效期类型与期限、储存条件、生产车间）需要仓库 / 质量那边提供口径，
前四项是建档时的技术开关。**

> 口径来源：M3-R2 第二节与 6.3 / 6.4；字段本身见 `hbos_inventory/setup.py::sync_custom_fields`。
> **真实物料的代码与品名不入库**（见 `.gitignore` 第 32-37 行），本节不举例。

### 本批未做

- 未替 Owner 修改任何真实物料的主数据（勾 `has_batch_no` 等属业务数据变更，须 Owner 授权）；
- 未做「建档引导页」一类的辅助入口。

## 九之八、执行记录（第七批：按 Owner 提供的样本批量建档）

**背景**：Owner 授权，把 `待检证/`、`自产/`、`新版货位卡外购/` 三个目录里出现的物料
建成 `Item` 主数据——否则拍照识别入库走不到最后一步（`create_intake_draft` 会拦下未建档的代码）。

### 数据来源与规模

| | |
| --- | --- |
| 扫描文件 | 312 个（`.docx` 走 `zipfile` 解 XML，`.doc` 走 macOS 自带 `textutil`） |
| 跳过 | 34 个（`~$` 开头的 Word 锁文件、`.lnk.重命名` 快捷方式、`desktop.ini`） |
| 抽出代码 | 275 个文件 → **128 个不重复物料** |
| 已建档 | **128 个全部建成**（另有 1 条已存在，跳过） |
| 无代码 | **37 个文件**，其「物料代码」栏印的是 `/` — 见下 |

### 各字段从哪来（每个值都可追溯）

| 字段 | 取法 | 覆盖率 |
| --- | --- | --- |
| `item_code` / `item_name` | 货位卡或待检证的「物料代码」「品名」单元格 | 128/128 |
| `item_group` / `hbos_product_kind` | **代码前四位**（M3-R2 6.1 的 Owner 口径） | 128/128 |
| `stock_uom` | 货位卡「单位」单元格 → 「数量/件数」单元格 → 待检证正文 | 98 个来自文件，30 个按前缀兜底 |
| `hbos_workshop` | 自产货位卡的「生产车间」 | 31/128 |
| `hbos_storage_condition` | 待检证的「储存条件」 | 28/128 |
| `is_stock_item` / `has_batch_no` | 固定 = 1（本流程必填，否则建不了批次） | 128/128 |

**单位为什么不能只看「包装规格」**：`30%过氧化氢` 规格是 `500ml/瓶`，但「数量/件数」是
`300瓶/300件`——**存货单位是「瓶」不是 ml**。同理橡胶塞名字里带 `(50ml)`，实际按「个」存。
所以单位一律取「数量/件数」里个数后面那个字。

### 抽不准的两类，如实记录

1. **待检证正文没有表格边界**（老式 `.doc` 尤其）。这类文件里品名是靠「代码前面那段中文」
   切的，单元格被拆碎时会切出 `CN1）`、`90）` 这种残片。**共 6 个品名做了人工订正**
   （`10001200` / `12002714` / `12028724` / `12028725` / `13000934` / `13001008`），
   其余 122 个都是单元格原文，一字未改。
2. **8 位数字不等于物料代码**。待检证上印着 `20251201` 这类**日期**，第一版把它们当代码
   抽出来 12 个假物料。已按 SAP 口径加 `^1\d{7}$` 过滤（代码一律以 1 开头），
   被挡下的日期逐条记在抽取产物里，**没有静默丢弃**。

### 顺带修掉 M3-R2 的一处缺陷：分类节点建成了「分组节点」

M3-R2 的 `setup.py` 把海滨分类树 5 个节点全建成了 `is_group = 1`。
**分组节点不放物料**——也就是说 M3-R2 建完分类树之后，**任何物料都进不去**，
树形同虚设（而已有的那条物料用的是原生 `Products`，绕开了这个问题，所以一直没暴露）。

修法：`ITEM_GROUPS` 改为 `is_group = 0`，并给 `sync_item_groups` 补**纠偏逻辑**——
因为 `sync_*` 一律「存在即跳过」，光改常量对库里已有的节点无效，
`bench migrate` 跑一百遍也改不回来。纠偏只对**没有子节点**的节点生效，
避免把真正的分组节点改坏。

> 这是 Owner 2026-09-18 明确选定「改成叶子节点再用」后的实施结果，
> 不是擅自改动 M3-R2 的设计。

### 建档后的分布

| 物料组 | 个数 | | 出厂单位 | 个数 |
| --- | --- | --- | --- | --- |
| 原料药 | 30 | | Kg | 60 |
| 包材 | 24 | | 个 | 41 |
| 辅助用品 | 42 | | 支 | 9 |
| 中间体 | 31 | | 瓶 | 7 |
| 小计 | **127 新建** | | 双/套/张/卷/件/L 等 | 剩余 |

### 仍未建档：37 个文件上的 19 个物料

这些文件（主要是**培养基**，以及部分**洁净鞋 / 滤芯 / 无菌粉 / 辐照用品**）的
「物料代码」栏印的是 `/`，即**尚未在 SAP 中分配代码**，因此无法建档。

**为什么不在本文档列名单**：品名属真实业务数据，本仓库是 **PUBLIC**，
`.gitignore` 已明令此类内容不入库（第 32-37 行）。名单改由**仓库外**交付：
`~/Documents/M3R6样本/物料建档/M3R6_待SAP分配代码清单.md`（与既有样本放一起；该目录不在仓库内）。
按大类分布如下：

| 大类 | 数量 |
| --- | --- |
| 培养基 / 琼脂（辅助用品） | 12 |
| 滤芯（辅助用品） | 2 |
| 洁净鞋（辅助用品） | 2 |
| 无菌粉（中间体） | 1 |
| 喷壶（辅助用品） | 1 |
| 辐照用品（辅助用品） | 1 |

**要建档须先让 SAP 管理员分配 8 位代码**——这是 SAP 侧的动作，本平台做不了。

### 产物与脚本归档在仓库外

含真实代码 / 品名的产物，以及一次性抽取脚本，都放在
`~/Documents/M3R6样本/物料建档/`（**不在仓库内**，故不会随提交泄漏，也不会被 `migrate` 影响）：

| 文件 | 说明 |
| --- | --- |
| `已建档物料清单.json` | 128 条的代码 / 品名 / 分类 / 单位 / 车间 / 储存条件 |
| `M3R6_待SAP分配代码清单.md` | 上文 19 个无代码物料的名单与来源文件 |
| `extract_items3.py` | 从 docx / doc 抽字段（含本节的三个坑与修法） |
| `build_uom.py` | 品名归一 + 单位推导 |
| `create_items.py` | 幂等建档 |

**为什么归档而不入库**：本仓库 PUBLIC，`.gitignore` 第 32-37 行已明令
待检证 / 货位卡类内容不入库；抽取产物是真实业务数据的派生，同理。
脚本本身不含数据，但属一次性工具、不在交付物范围内，故一并放仓库外。

### 仍然空的字段（不在样本里）

| 字段 | 为什么空 |
| --- | --- |
| `hbos_shelf_life_type`（复检期 / 有效期） | 样本上那两个勾选框**全部是 `□`**（140 处，无一处勾选）——它们是空白打印模板 |
| `hbos_shelf_life_months`（效期期限） | 同上，样本里没有这个信息 |

这两个字段的用处是货位卡上那两个勾选框和效期预警。**要填须由质量 / 仓库提供口径。**

### 数据安全

- 真实物料代码与品名**只存在于数据库**，抽取产物全部写在 `/tmp`，**未进仓库**；
- 抽取脚本本身不含任何真实数据，但**也未进仓库**（一次性工具，不属于交付物）；
- 已按要求核对：`grep` 全仓库无真实代码 / 品名泄漏。

### 本批未做

- 未替 Owner 勾现有那条物料的「启用批号管理」（它仍是 `has_batch_no = 0`，
  **拍照识别入库到它时仍会被拦下**）。属业务数据变更，须 Owner 授权；
- 未把该条物料从原生 `Products` 挪到「中间体」（同上）；
- 未替 SAP 侧分配代码（37 个文件涉及的 19 个物料）；
- 未填 `hbos_shelf_life_type` / `hbos_shelf_life_months`（样本里没有）；
- 未做物料主数据的管理界面 / 导入模板。

## 九之九、执行记录（第八批：补充信息手工录入）

**背景**：Owner 要求「拍照后获取信息生成文件，**没有获取的信息可以手动加入**」。

OCR 只能读 5 个字段（品名 / 物料代码 / 批号 / 生产日期 / 有效期至），而待检证与货位卡上
要印的信息**远不止这些**。缺的那些（储存条件、生产车间、复检期还是有效期、包装规格、
供货单位、生产单位、原厂批号）此前**在页面上无从录入**，只能事后去 `Batch` / `Item`
表单逐个补，或者干脆印成空白。

### ⚠ 措辞更正：这些不是「可补」，是「**须人工填**」（2026-09-21）

本批初版把这几项写成「识别不到的在此手工填」，**措辞是错的**——它读起来像
「等识别读不到时才轮到你填」，是个**备选方案**。

对着真实标签核实后（见第九之五节的 ⚠ 小节）：**这几项从来就不可能靠 OCR 拿到**。

| 字段 | 标签上是什么 | 实测 |
| --- | --- | --- |
| 储存条件 | 小字汉字（`储存温度不超过30℃`） | 读不出来 |
| 生产车间 | 小字汉字（`六车间B线车间`） | 读成「过果线」 |
| 供货单位 | 小字汉字（公司全名） | 读不出来 |
| 复检期 / 有效期 | 勾选框 | 不是文字，读不了 |

所以它们是**必需的人工动作，不是备选**。已改的措辞：

- 分组标题加 **「须人工填写」** 徽标（新增 `.hbos-pi-must` 样式，偏警示色）；
- 空字段的状态标从「**为空，可补**」改成「**须人工填**」；
- 分组说明从「识别读不到的可以手工补」改成「**标签上是小字，OCR 读不准，须照标签人工填写**」；
- 页面初始占位文案同步改。

> 这条**只改文案与样式**，不改任何逻辑——字段、接口、落库规则都没动。

### 打印字段的数据来源（改动前核实）

| 打印字段 | 来源 | 层级 |
| --- | --- | --- |
| 品名 | `Item.item_name`（`Batch.item_name` 是 `fetch_from` 快照） | 物料 |
| 储存条件 | `Item.hbos_storage_condition` | 物料 |
| 生产车间（自产的「生产单位」也取它） | `Item.hbos_workshop` | 物料 |
| 复检期 / 有效期勾选 | `Item.hbos_shelf_life_type` | 物料 |
| 包装规格 / 件数 | `Batch.hbos_packaging` 子表 | 批次 |
| 供货单位 | 标准 `Batch.supplier`（**只读的 Supplier 链接**） | 批次 |
| 生产单位 / 原厂批号 | `Batch.hbos_manufacturer` / `hbos_supplier_batch_no` | 批次 |
| 数量 / 货位 / 入库日期 | 库存流水推算，无需录入 | — |

### Owner 已定的四个决策

| 决策 | 选择 |
| --- | --- |
| 物料级字段落哪 | **写回物料主数据，仅补空**——已有值绝不覆盖 |
| 批次级字段落哪 | 写进本批次 |
| 供货单位怎么存 | **新建 `Batch.hbos_supplier_name` 自由文本字段** |
| 谁能补 | 页面已有的三个角色（System Manager / Stock Manager / Stock User） |

**供货单位为什么另起字段**：标准 `Batch.supplier` 是 `read_only` 的 **Link → Supplier**，
而库里供应商档案只有 1 条（还是 TEST 虚构的），样本上却有十几家。往里塞自由文本，
`hbos_supplier_name` 打印时能读出来，但**该批次下一次 `save()` 会报「找不到 Supplier」**，
批次从此保存不了。更糟的是 `hbos_batch_source` 会把有 `supplier` 的批次判成「外购」，
进而把自产批次的「生产单位」从生产车间改成 `hbos_manufacturer`——静默串味。

**权限为什么绕开 ERPNext 矩阵**：实测 `Item` 只有 `Item Manager` 有写权限，
`Stock Manager` / `Stock User` / 连 `System Manager` **都只有读**（`tabDocPerm` 里
根本没有 System Manager 行）。若改用 `frappe.has_permission("Item","write",throw=True)`，
页面允许的三个角色**会被自己的权限门全部挡死**，功能对任何操作员都不可用。
故沿用页面已有的 `_require_permission()`，写库时 `ignore_permissions=True`，
风险由「仅补空」+「Comment 留痕」两道保险承担。这是**有意的取舍**，不是疏忽。

### 已交付

| 文件 | 改动 |
| --- | --- |
| `setup.py` | 新增 `Batch.hbos_supplier_name`（Data「HBOS 供货单位」）与 `Batch.hbos_label_text`（Long Text「标签原文」） |
| `print_utils.py` | `hbos_supplier_name()` 改为**优先取新字段**，为空回落标准 `supplier` |
| `api.py` | `create_intake_draft` 增 7 个可选参数；新增 `_fill_item_master_gaps()`、`_parse_packaging()`、`_replace_packaging()`；扩展 `_ensure_batch()` |
| `hbos_photo_intake.js` | 校对区改为**一处列出全部字段**（识别到的 + 手工补的 + 包装构成），末尾把**标签原文按行拆成可编辑行**；品名改只读对照；物料级回显；包装构成行编辑 |
| `hbos_photo_intake.css` | 配套样式（沿用既有 `.hbos-pi-*` 约定） |

> **界面布局在 Owner 反馈后调整过两次**：
>
> 1. 初版把「补充信息」做成**另一张卡片**，实操时发现操作员得在两张卡片之间来回看。
>    已合并进校对区——**标签上要印的信息全部列在一处**，识别到的预填、没读到的留空，就地补。
> 2. 识别原文最初是**只读的折叠块**。Owner 要「直接填进人工校对里」——
>    已改为**按行拆成可编辑的输入框**（见下），并加了「恢复识别原文」按钮。

### 标签原文：为什么要可编辑、并且要存下来

OCR 会读错字（实测把「贮藏：避免硫碰」读成过品名）。**只读展示的话，操作员看得到错但改不了**，
校对就成了走过场。所以逐行拆成输入框。

校对后的文本存到 `Batch.hbos_label_text`（新增 Long Text 字段，空行与行首尾空白自动去掉）。
理由是**照片与文字各有各的用处**：

| | 照片（已有） | 校对后的原文（本批新增） |
| --- | --- | --- |
| 地位 | **原始凭证**，挂在入库单上 | **转录件** |
| 能不能搜 | **不能**——要人眼一张张翻 | **能**——文字可以检索、可以比对 |

**它不参与打印**（已实测：待检证与两张货位卡上都不出现）。只是给日后查证留一条可检索的线索。

> 这也顺带修掉了本条批第 6 条批评过的「假控件」问题：**改了会生效，才配做成输入框**。

### 六个必须知道的实现要点

做这一批时踩到/避开的坑，都写进代码注释了，此处汇总：

1. **子表不能用 `frappe.db.set_value` 写**。`db.set_value` 对 Table 字段会发出
   `UPDATE tabBatch SET hbos_packaging = ...`，MariaDB 直接报「未知列」。
   必须走 `frappe.get_doc()` → `append()` → `save()`。顺带 `save()` 也跑一遍
   `Batch.validate()`，是好事。
2. **包装构成必须「替换式」而非「追加式」**。`hbos_packaging_count` /
   `hbos_packaging_spec`（print_utils.py）都是**汇总求和**的——同一批次重复入库若只
   `append`，件数会翻倍、包装规格会重复打印。故先 `set("hbos_packaging", [])` 再写。
3. **「仅补空」不能用「先读再写」**。那有 TOCTOU 竞态：两个操作员同时给同一物料入库、
   都读到空，后提交的就会覆盖前者，与承诺矛盾。改成把「为空」写进 `WHERE` 条件、
   用 `ROW_COUNT()` 确认是否真写了。实测：先到者留下，后到者不覆盖。
4. **`SELECT` 值要先校验**。`hbos_shelf_life_type` 是 Select，裸 SQL / `set_value` 都
   不走校验，非法值会直接进主数据。已用 `frappe.get_meta` 读 options 比对。
   容器类型、件数 > 0 同理（在 `_parse_packaging` 里）。
5. **裸 SQL 不走 Frappe 的文档缓存失效**，需显式 `frappe.clear_document_cache()`；
   且**不产生 Version 记录**，主数据会改得无声无息——故补写一条 `Comment` 留痕。
6. **品名这个输入框原本是假的**。页面渲染了可编辑的「产品名称」，但**从未把它传给
   后端**；而打印取的是 `Item.item_name`，`Batch.item_name` 又是 `fetch_from` 快照、
   已有批次不会重取。也就是说操作员改了它**什么也不会发生**。已改为**只读对照展示**
   （「标签上读作 X」vs「主数据中 Y」），把认错的风险摆出来，而不是假装可改。
   > 顺带发现：`Item.item_name` 并非必填，为空时**两个货位卡的品名会印成空白**。
7. **改了页面 `.js`，浏览器不一定看得到（这个坑真踩了）。**
   第一次改完页面，操作员反馈「人工校对还是老样子」。排查后确认**服务端下发的一直是新
   `.js`**（`Page.load_assets()` 每次请求都**从磁盘重读**，实测 22063 字符、内容正确），
   卡在**浏览器端**：

   Frappe 会把整个 Page 定义（含 script / style）**缓存进 `localStorage`**，键为
   `_page:<name>`。除非 `frappe.boot.developer_mode` 为真，**它一律读缓存、不发请求**：

   ```js
   // frappe/views/pageview.js:23
   } else if (localStorage["_page:" + name] && frappe.boot.developer_mode != 1) {
       frappe.model.sync(JSON.parse(localStorage["_page:" + name]));   // ← 读缓存，改文件无效
   ```

   而缓存的失效**只认 `Page` 记录的 `modified`**，不认 `.js` 文件的改动
   （`frappe/desk.js:345` 的 `sync_pages()`：`page_info[name].modified != p.modified` → 才 `delete`）。

   **所以：改了页面 `.js` / `.css` 之后，必须把 `.json` 里的 `modified` 也推新、再 `bench migrate`。**
   本批一开始漏了这一步，`.json` 的 `modified` 还停在 2026-09-14，于是无论改多少遍 `.js`，
   浏览器拿到的都是缓存里的旧版本。已把该文件的 `modified` 推新并迁移验证。

   > 与本批第 6 条（Print Format 要推 `modified` 才会被 `migrate` 重导）是**同一类陷阱**：
   > Frappe 这套文件，**内容变了不等于元数据变了**——凡是有缓存的环节，都得把版本戳一起推。

### 验证结果（全部实测通过）

| 验证项 | 结果 |
| --- | --- |
| 物料级「仅补空」：字段已有值 | ✅ 原值未变，`filled` 返回空（界面不会谎称已补录） |
| 物料级「仅补空」：字段为空 | ✅ 写入成功 + **Comment 留痕 2 条** |
| 并发两次不同值 | ✅ **保留先到者**，后到者未覆盖 |
| 包装构成替换式 | ✅ 连写两次：行数 2→1，**件数 33→10 未翻倍** |
| 批次级标量仅补空 | ✅ 第二次传不同值，仍是第一次的值 |
| 端到端（待检证） | ✅ 储存条件、供货单位、批号都印上 |
| 端到端（自产货位卡） | ✅ 生产车间、包装规格 `5.00kg*33件`、件数 `33件` 都印上 |
| 端到端（外购货位卡） | ✅ 生产单位、原厂批号、有效期勾选 `☑` 都印上 |
| **回归**（不传任何新参数） | ✅ 行为与改动前完全一致，批次级新字段全空、包装子表为空 |
| 权限：以 **Stock User** 身份 | ✅ 通过页面权限门并成功补录（`has_permission('Item','write')` 此时为 **False**，正是要绕开的那个门） |
| 页面 JS / CSS 加载 | ✅ `Page.load_assets()` 正确读到（JS 22.1 KB / CSS 4.4 KB）——**但这条并不能证明浏览器拿到的是新版本**，见下面「一处我验错了的地方」 |
| **识别原文传到前端** | ✅ `raw_text` 371 字（实测走真实 c2_ocr 后端），逐行拆成 33 行输入框 |
| **原文逐行校对后落库** | ✅ 空行与行首尾空白已去；`Batch.hbos_label_text` 写入正确 |
| **原文「仅补空」** | ✅ 批次已有原文时，再次传入不覆盖 |
| **原文不参与打印** | ✅ 待检证与两张货位卡上均不出现（实测） |
| `services/hbos_ocr` 单测 | ✅ 83 条全通过 |

### 一处顺带修复

`_ensure_batch` 里已存在批次的分支，原本在 `patch` 为空时**也会**执行
`frappe.db.set_value`（写一个空 dict）。改为真正有内容才走，并顺带把
`Batch.hbos_packaging` 的写入从 `db.set_value` 换成文档对象。

### 一处我验错了的地方（须记录）

我上一批写「页面 JS / CSS 加载 ✅」用的验证是 `Page.load_assets()` 能读到新内容。
**这条证明不了任何事**——`load_assets()` 每次请求都从磁盘重读，它**永远**是新的。
真正的问题在浏览器端的 `localStorage` 缓存（见「实现要点」第 7 条）。

**教训**：验证要**打在真正会出问题的那一环上**。我验的是「服务端能不能读到这个文件」，
而实际风险是「浏览器会不会用这个文件」——两者之间隔着一层缓存，我漏掉了它。
以后凡涉及前端资源，验证必须包含**缓存这一层**（`frappe.boot.page_info` 的 `modified`
是否与文件版本一致），否则就是假通过。

### 已知取舍

- **包装构成与供货单位在「生成草稿」时就写进 Batch 了**；若操作员最终废弃这张草稿，
  这些值**不会回滚**。与现有草稿语义一致（物料级的补录同理），本批不额外处理。
- **界面视觉未能在浏览器里实测**：本 install 的 Browser 预览只支持 attach 到 URL 的形式，
  而该模式当前不可用。因此**界面是通过后端验证的**（资源能加载、渲染分支正确、
  传参正确、端到端数据流正确），**未见真实浏览器渲染**。已在下面的「本批未做」里列明。

### 本批未做

- **未在真实浏览器里点过这个界面**（原因见上）；建议 Owner 打开
  `/app/hbos-photo-intake` 走一遍，重点看「④ 补充信息」的布局与包装构成行编辑是否顺手；
- 不为「供货单位」自动创建 Supplier 主数据（沿用自由文本字段）；
- 不改 OCR 服务本身（品名读错属识别能力问题，靠人工校对兜底）；
- 未把 `Item.item_name` 也纳入缺口补录（本批只读展示）；
- 未处理「操作员在 Desk 表单里改了批号、补充信息留在原批次上」这种情况。

## 九之十、执行记录（第九批：常驻服务重启与页面版本戳修复）

Owner 指示「重启，并把该修的都修了」。查出**两个缺陷**，都属同一类：
**内容变了，元数据没变。**

### 一、常驻服务跑的是旧代码（已重启）

服务进程是 **2026-09-21 08:54** 启的，而 `c2_ocr.py` / `validate.py` 在那之后
又改过（含当天的品名抽取规则修复）。启动命令**没带 `--reload`**，uvicorn 只在
启动那一刻读一次代码，于是：

| 现象 | 重启前 | 重启后 |
| --- | --- | --- |
| `backend=c2_ocr:small` | `400 未知后端：c2_ocr:small`（**文档却写着可用**） | `200`，正常出结果 |
| `1.jpg` 品名 | `避免硫碰。`（规则已修掉的垃圾串） | **`美罗培南`** |
| `10.jpg` / `12.jpg` 品名 | `存新件` / `合证` | **空**（诚实的空，由主数据补） |

**已重启**并复测通过；容器侧 `docker exec … curl host.docker.internal:8100/health`
返回 200，Frappe 页面可用。

### 二、页面版本戳没推，浏览器永远读旧页（已修）

`frappe/views/pageview.js` 的规则是：只要 `localStorage["_page:<name>"]` 存在，
**一律用缓存、根本不发请求**；缓存的失效条件**只看 `Page.modified`**
（`desk.js` 的 `sync_pages()` 比对 `bootinfo.page_info[name].modified`）。

改名那一批改了拍照识别页的 `.js`（返回按钮 → `Workspaces/仓库工作台`），
**但 `.json` 的 `modified` 停在 09-21 17:30**，于是服务端虽已下发新内容
（`Page.load_assets()` 每次从磁盘重读），**浏览器侧永远不会去取**。

已把 `hbos_photo_intake.json` 的 `modified` 推到 `2026-09-23 10:20` 并 `bench migrate`：
DB `Page.modified`、`bootinfo.page_info` 均已随之变化，浏览器下次进入即丢弃旧缓存。
复验服务端下发内容：**含 `Workspaces/仓库工作台`、不含 `仓储库存工作台`**。

### 教训

**内容变了 ≠ 元数据变了。** 凡是有缓存的环节，都要单独推版本戳：

| 改了什么 | 必须一起做 |
| --- | --- |
| `page/**/*.js|.css` | 推同名 `.json` 的 `modified` → `bench migrate` |
| `hooks.py` | `bench clear-cache` |
| `services/hbos_ocr/app/**` | **重启常驻服务**（启动命令无 `--reload`） |

「服务端能读到新文件」**证明不了用户会用上它**——两处缺陷当时都被这类假通过盖住了。

### 本批复验

| 项 | 结果 |
| --- | --- |
| 服务健康（本机 + 容器内） | ✓ 200，`c2_ocr` 可用 |
| 档位写法 `c2_ocr:small` | ✓ 200 |
| 桌面图标（真实 boot 流程） | ✓ 47 个，含「仓库工作台」`boxes`，三者同名、旧名无残留 |
| 页面版本戳 | ✓ 拍照识别页的 DB `modified` 已更新，`bootinfo.page_info` 随之变化 |
| 单测 | ✓ OCR 87（约束 42 + 抽取 45）、仓储契约 9，全通过 |

**仍未在真实浏览器里验证**（进 Desk 需登录凭据）。请 Owner 强制刷新
（`Cmd+Shift+R`）后打开 `/app/仓库工作台` 与其中的「入库拍照识别」，
确认桌面入口与页面返回按钮都对。

## 九之十一、执行记录（第十批：容器无中文字体导致打印件汉字全丢）

Owner 报「打开的待检证+货位卡 PDF **全是乱码**」。

### 根因：后端容器里一个中文字体都没有

`wkhtmltopdf` 渲染 PDF 时靠**系统字体**，而 `frappe/erpnext` 镜像里只有 DejaVu 一族：

```
$ docker exec … fc-list | sed 's/.*: //' | sort -u
DejaVu Sans Mono / DejaVu Sans / DejaVu Serif
$ docker exec … fc-list :lang=zh | wc -l
0
```

**汉字被静默丢弃，ASCII 与数字照常出**——不报错、不打日志。
最小复现（直接喂 wkhtmltopdf）：

```
输入： 自产物料/产品货位卡 待检证 BMT2307002 混粉
输出： /            ← 汉字全没了
      BMT2307002   ← ASCII 留着
```

那一批 PDF 里因此只剩 `Quarantine`、`Inventory Card of Self-Produced Materials/Products`、
批号、代码这些**英文与数字**，中文全空 —— 看起来就是「乱码」。

### 影响面：四个打印格式 × 四个容器

| 打印格式 | 是否含中文 | 结果 |
| --- | --- | --- |
| `HBOS 待检证` | 81 种汉字 | 中文全丢 |
| `HBOS 自产货位卡` | 52 种 | 中文全丢 |
| `HBOS 外购货位卡` | 58 种 | 中文全丢 |
| `HBOS 货位二维码` | 68 种 | 中文全丢 |

渲染发生在 `backend`（表单点打印）或 `queue-*` / `scheduler`（后台任务），
这四个容器的字体情况**完全一样**（都是 0）。

### 为什么之前没发现

M3-R4 当时记录「三个模板渲染与 PDF 生成均验证通过」、M3-R6 第六批记录「全量 224 个
标签验证」—— **验的都是页数与物理尺寸**（`pdfinfo` 的 Pages / Page size），
**从没验过字形**。于是「1 页 75×110mm」全部通过，**纸上的字却一个都没有**。
这与第九批的版本戳问题同源：**验证停留在「文件生成成功」，没到达「内容对不对」**。

### 修复：把中文字体挂进容器（Owner 本轮授权改 compose）

1. 宿主建 `runtime/fonts/`（`runtime/` 已在 `.gitignore`，字体**不入仓库**），
   放入 `Songti.ttc`（宋体，与模板 `font-family: SimSun, "宋体", serif` 的意图一致）；
2. `docker-compose.yml` 给 **backend / queue-long / queue-short / scheduler**
   四处加只读挂载 `./runtime/fonts:/usr/share/fonts/truetype/hbos:ro`；
3. `docker compose up -d` 重建这四个容器（**命名 volume 全保留，未动 db-data**）。

### 验证（同一批数据，修复前后对照）

| 项 | 修复前 | 修复后 |
| --- | --- | --- |
| 容器 CJK 字体数 | 0 | **8**（Songti SC / STSong） |
| 最小复现文本 | `/` + `BMT2307002` | **`自产物料/产品货位卡 待检证 BMT2307002 混粉`** |
| 待检证 | 只有 `Quarantine` | **`品名：美罗培南混粉` / `物料代码：13000912` / `批号：BMT2307002`** |
| 自产货位卡 | 只有英文副标题 | **`自产物料/产品货位卡` / `生产车间` / `货位号 16-03-207`** |
| 货位二维码 | （同因全丢） | **`3915 六车间备品备库` / `扫码查看该货位在库明细`** |

四个格式全部恢复中文，页型无回归。

### 已知两点

- **复制粘贴的字符可能被读成「康熙部首」码位**（如 `生` → U+2F63）。
  这是 `Songti.ttc` 的 ToUnicode 映射写法所致，**打印出来的字形是对的**，
  只有从 PDF 里 copy/搜索会拿到部首码位。**不影响打印**，如后续要文本可搜，
  换 Noto 系字体可解。
- **宿主字体是 macOS 自带**（Apple 授权，仅本机使用、不入仓库）。
  **搬到真正的服务器时必须换成 OFL 字体**，如 `apt-get install fonts-noto-cjk`，
  否则那台机器同样会丢汉字。这一条已写进第十一节的下一步。

### 已生成 PDF 已全部重出（2026-09-23）

字体只影响**新渲染**，此前生成的 6 个附件（3 个真实批次
`BMT2307002` / `B2412583` / `B2608506` + 3 个 `TEST-M3R1-`）
**当时仍是缺汉字的那一版**。已按 Owner 指示**全部重出**：

| 批次 | 货位卡类型 | 重出后大小 |
| --- | --- | --- |
| `TEST-M3R1-E900` | 外购货位卡 | 68.9 KB |
| `TEST-M3R1-E002` | 自产货位卡 | 74.4 KB |
| `TEST-M3R1-E001` | 自产货位卡 | 74.6 KB |
| `B2608506` | 自产货位卡 | 72.9 KB |
| `B2412583` | 自产货位卡 | 73.0 KB |
| `BMT2307002` | 自产货位卡 | 72.7 KB |

复验：**附件仍是 6 个**（`_purge_previous` 先删旧的，没有堆积，也没动手工附件），
体积由约 34 KB 涨到约 73 KB（内嵌中文字体的正常增量），
`HBOS-BMT2307002-待检证+货位卡.pdf` 文本已恢复
`待检证（Quarantine）` / `品名：美罗培南混粉` / `批号：BMT2307002` / `自产物料/产品货位卡`。

## 九之十二、执行记录（第十一批：待检证版式对齐官方受控模板）

Owner 提供了一份**生成器参考工具**（`待检证生成器_单文件版/`，放在仓库根目录、
**未入库**：里面是 Windows `exe` 与含真实品名批号的产物，不适合进 PUBLIC 仓库），
让我照着对齐。工具内含**官方待检证模板**（`data/templates/待检证模板.docx`）。

### 一、先做校对：结构本来就是对的

把工具里的模板与仓库 `待检证/` 下 Owner 早先给的样板逐件比对 —— **`document.xml`
除数据外完全一致**，说明 M3-R4 当初就是照这份受控表做的。再有：

| 项 | 官方模板 | 我们 |
| --- | --- | --- |
| 页型 | `sectPr` = 4253×6237 twips = **75.0×110.0mm** | 75×110mm ✅ 一致 |
| 表宽 / 列 | 3652 twips（64.3mm），四列 948/560/280/1864 | 同构 ✅ |
| 10 行结构、标签与顺序 | 标题 / 品名 / 物料代码 / 供货单位 / 生产单位 / 批号 / 数量 / 储存条件 / 空行 / 操作人日期 | 完全一致 ✅ |
| 编码串 | `APP-SOP-QA -1-A-003-13/4.0` | 一致 ✅ |
| 页眉页脚图片 | 无 | 无 ✅ |
| 自产口径 | 供货单位 `/`、生产单位为车间名 | 一致 ✅ |

**要改的是版式，不是结构。**

### 二、差在哪（实测，不是目测）

把官方模板渲染成 PDF，逐像素量：

| 项 | 官方 | 我们（改前） |
| --- | --- | --- |
| 正文字号 | **12pt**（模板 `sz=24`） | 9pt |
| 内容占标签高度 | **90%** | **63%**——下面三分之一是空的 |
| 边框 | **无竖线、无外框**，只在各个「值」单元格下有一条横线 | **全格子**（每格四边都画） |
| 储存条件行 | 值用 **10pt**（`sz=20`），左对齐 | 同正文 |

### 三、改法

重写 `HBOS 待检证` 的模板 HTML（`print_format/HBOS待检证/*.json` 的 `html`）：

1. **字号** 正视 9pt → **12pt**，储存条件值单独 **10pt**；字体栈加上 Times New Roman
   （官方 latin 走 Times），中文回落宋体；
2. **边框** 去掉全格子，只给「值」单元格加 `border-bottom`，**空行整行**加，
   与官方那条 9 条横线的分布逐一对齐；
3. **列宽** 按官方 `tblGrid` 折成 mm（16.8 / 9.9 / 4.8 / 33.5）；
4. **行高** `tr { height: 8.2mm }`，标题行独立加高（官方是两行的标题）；
5. 两处换行**显式指定**，不靠引擎自动断：标题在 `APP-SOP-QA` 后断行、
   操作人日期之间留可断点（避免把 `2026.09.23` 从中间拆开）。

**踩到的两个坑（都已修，记在这里）**：

- **`word-break: break-all` 会把 `13/4.0` 断成 `13/4` + `.0`**，标题被拆成三行。
  改用 `overflow-wrap: break-word`：只在单个词确实放不下时才断。
- **`border-bottom: 0.5pt` 在 wkhtmltopdf 里会被渲染成极浅的灰**（实测最深只有 195），
  和官方那条实线一眼可分。改成 `1px`（≈0.75pt）才是实的。

### 四、复验（改后与官方逐条对照）

9 条横线的位置与横向范围，**逐条吻合**：

| # | 官方 y / x 范围 | 改后 y / x 范围 |
| --- | --- | --- |
| 1 | 33.5mm, 21.7~69.4 | 27.4mm, 21.9~70.0 |
| 2 | 41.6mm, 31.7~69.4 | 35.6mm, 31.8~70.0 |
| 3 | 49.8mm, 31.7~69.4 | 43.9mm, 31.8~70.0 |
| 4 | 58.1mm, 31.7~69.4 | 52.1mm, 31.8~70.0 |
| 5 | 66.2mm, 21.7~69.4 | 60.3mm, 21.9~70.0 |
| 6 | 74.3mm, 21.7~69.4 | 68.4mm, 21.9~70.0 |
| 7 | 82.5mm, 31.7~69.4 | 76.6mm, 31.8~70.0 |
| 8 | 90.8mm, **5.1~69.4**（整行） | 84.8mm, **5.0~70.0** |
| 9 | 98.9mm, 36.4~69.4 | 95.2mm, 36.5~70.0 |

横向范围**完全一致**（说明列宽与合并方式对上了）。纵向整体比官方高约 5mm ——
官方页上边距是 0、标题行更高；我们保留 4mm 上边距更利于打印机咬纸，
内容占比由 63% 提到 **85%**（官方 90%）。

**四个打印格式回归**：待检证 1 页 75×110mm、自产/外购货位卡各 1 页 A4、
货位二维码 1 页 60×40mm，纸型无回归。单测 OCR 87 + 仓储契约 9 全通过。
**已生成的 6 份 PDF 已再次重出**（附件仍 6 个）。

### 五、这份参考工具还告诉了别的

- 它是**模板填充器**，不是格式化器：`数量` 等单元格塞的是操作员手打的自由文本
  （样本里有 `143.49/29件`、也有光秃秃的 `1200`），所以**不存在一个「数量格式」**；
- 使用说明写明「货位卡中的**日期、复检期/有效期勾选、放行结论、下方流水台账和备注
  留给人工填写**」—— 与 M3-R4 的定案一致（我们额外把流水表按 SLE 预填了，是增强）。
- 工具里的**货位卡模板未随附**（`data/templates/` 只有待检证模板），
  所以货位卡这次没得比对。

## 九之十三、执行记录（第十二批：修「站点打不开」——nginx 缓存了失效的 upstream IP）

第十批为挂载字体重建了 backend / queue-long / queue-short / scheduler 四个容器，
**但没有重建 frontend**。结果是站点整个 502。

### 根因：nginx 只在启动时解析一次 upstream 名字

frontend 容器里的 nginx 配置写的是 `proxy_pass http://backend;`，
**在启动那一刻解析成 IP 并一直用**（配置里没有 `resolver` 指令，不做动态重解析）。
backend 被重建后拿到新 IP `172.18.0.6`，而 nginx 还指着旧的 `172.18.0.7` ——
那个 IP 这时已经**被 scheduler 容器复用**，而 scheduler 不监听 8000：

```
$ docker logs frontend
connect() failed (111: Connection refused) while connecting to upstream,
client: 172.18.0.1, request: "GET / HTTP/1.1", upstream: "http://172.18.0.7:8000/"
```

**表现是 502（不是超时）**，后端本身完全正常（容器内 `curl 127.0.0.1:8000` 有响应）。

### 修复

```bash
docker compose up -d --force-recreate frontend
```

**注意 `--force-recreate` 是必需的**：frontend 的 compose 定义没变，
普通的 `up -d frontend` 只会输出 "Running" 什么也不做（第一次就是这么试的，没用）。
重建后 nginx 重新解析：`getent hosts backend → 172.18.0.6`。

### 复验

| 检查 | 结果 |
| --- | --- |
| `/`、`/login` | 200，登录页正常渲染 |
| `/app` | 301（正常的登录跳转） |
| `/assets/...` 静态资源 | 200 |
| nginx 解析 backend / websocket | `172.18.0.6` / `172.18.0.8`，**均与实际一致** |
| 数据库 | 后端查 `Batch` 正常返回 |
| OCR 服务 | `health` 200 |
| 已生成的 6 份货位卡 PDF | 仍在 |

### 教训

**重建任何一个被 nginx 反代的服务后，frontend 必须一起重建。**
这条与第九批、第十批同属一类：**改了 A，忘了 B 也缓着旧东西。**
具体到本项目，涉及 `backend` / `websocket` 两个被反代的容器
（`proxy_pass http://backend` 与 `proxy_pass http://socketio-server`）。

> 根治办法是给 nginx 加 `resolver 127.0.0.11 valid=10s;` 并把 upstream 改成变量，
> 让它在运行时解析。**但那要改 frontend 的镜像/配置，本批没做**——
> 当前只是把「重建顺序」记成规矩，先保证可用。

## 九之十四、执行记录（第十三批：修 PDF 里汉字的 ToUnicode 被写成「部首」）

Owner 反馈「PDF 打开还是乱码」。逐项查证后的结论与修复如下。

### 一、先排除：文件内容本身是对的

对**全部 7 份**已生成的 PDF 逐个核：

| 检查 | 结果 |
| --- | --- |
| 中文字符数（提取） | 46～54 个/份，**都有** |
| 中文嵌入字体 | 每份 4 个（正文/粗体） |
| 渲染（poppler 150dpi 出图） | 汉字**全部正确**，无缺字、无方块 |

**所以不是「汉字没渲染出来」**——第十批那个问题确实已经修好了。

### 二、真问题：ToUnicode 把一批汉字写成了「部首」码位

提取文本时发现 **8 个常用字**拿到的是**部首码位**而不是汉字：

| 提取到的 | 应该是 | 用在哪 |
| --- | --- | --- |
| `⼈` U+2F08 | **人** U+4EBA | 操作人 |
| `⼊` U+2F0A | **入** U+5165 | 入库 / 入库数 |
| `⼿` U+2F3F | **手** U+624B | 经手人 |
| `⽇` U+2F47 | **日** U+65E5 | 操作人/日期 |
| `⽉` U+2F49 | **月** U+6708 | 年 月 日 |
| `⽣` U+2F63 | **生** U+751F | 生产单位 |
| `⾃` U+2F83 | **自** U+81EA | 自产物料 |
| `⾏` U+2F8F | **行** U+884C | 放行 |
| `⻋` U+2ECB | **车** U+8F66 | 生产车间 |
| `⻚` U+2EDA | **页** U+9875 | 第（ ）页 |

**根因**：Qt（wkhtmltopdf 的渲染内核）生成 PDF 的 ToUnicode CMap 时，对这些字形
**挑了部首块的码位**——因为它们在字体里同时挂在「部首」和「汉字」两个码位上。

**为什么后果分两种、且不容易被发现**：

- **页面上看着是对的**——渲染按**字形**走，字形本身没错，所以肉眼看、截图看都正常；
- **复制 / 搜索拿到错字**；更要紧的是，**按 Unicode 回查字体的阅读器**
  （部分手机端阅读器、部分网页预览、PDF 无障碍朗读）会去取「部首字形」——
  那是为部首区**单独设计的小号/错位字形**，**看着就是乱字**。

这条路径解释了「我这边看是好的、Owner 那边说是乱码」——**取决于用哪个阅读器**，
不是谁看错了。**换字体救不了**：实测 Noto Serif SC 与 Songti 一样中招
（`fc-list` + 渲染对照，两者都出现同样的部首码位）。

### 三、修法：写完 PDF 之后把 CMap 改回来

改在 `doc_gen.py`：`writer.write()` **之前**遍历所有字体的 `ToUnicode` 流，
把落在 `U+2E80–U+2FDF` 的**目标**码位换回汉字（`_fix_pdf_tounicode`）。

两个坑，都是实测踩出来的：

1. **不能连源码位一起改**。源是 CID，也可能正好长得像 `2Fxx`（实测有 `<2F00>`），
   一起改就整个错位。
2. **Qt 用的是数组形式的 `bfrange`**，这是漏掉一次的原因：

   ```
   beginbfrange
   <0000> <0000> <0000>                    ← 单值：改第 3 个
   <0001> <0020> [<5F85> <2F47> ...]       ← 数组：改方括号里**每一个**
   endbfrange
   ```

   第一版只处理了单值形式，结果「生产单位 / 操作人 / 日期」好了、
   「入库 / 经手人 / 自产 / 放行」还是坏的——**同一页里两种形式混着用**。

映射表：康熙部首（U+2F00–U+2FD5）用 **NFKC** 自动换（Unicode 有兼容分解）；
「CJK 部首补充」里那批**简体部首**（U+2E80–U+2EF3）**没有分解**，只能列出 16 个
（`_RADICAL_SUPPLEMENT`），其中我们的模板会碰到 `⻋`、`⻚`。

### 四、复验

7 份 PDF 全部重出，逐份核：

| 检查 | 结果 |
| --- | --- |
| 部首码位 | **0 个**（改前每份 8～10 个） |
| 关键词提取 | 生产车间 / 第（ ）页 / 操作人 / 入库 / 经手人 / 自产 / 放行 / 生产单位 **全部命中** |
| 页数 / 纸型 | 无变化（待检证 1 页 75×110mm + 货位卡 1 页 A4） |
| 字体 | 嵌入正常 |
| 单测 | OCR 87 + 仓储契约 9 全通过 |

### 五、顺带：字体换成 OFL 的 Noto Serif SC

原来挂的是 macOS 自带 `Songti.ttc`（Apple 授权、不可再分发，第十批已标注这个隐患）。
本批加装 **`NotoSerifSC-Regular/Bold.otf`（SIL OFL，可自由分发）** 进 `runtime/fonts/`。
两者都留着，fontconfig 会挑 Noto。**要搬到服务器时用 Noto 即可，不再有授权问题。**

### 六、仍未解释的部分（如实说明）

**我无法复现 Owner 看到的「乱码」**：在我这里（poppler）渲染与提取都正确，
无法看到 Owner 屏幕上的画面。上面这条 ToUnicode 缺陷是**我查到的唯一能造成
「某些阅读器显示乱字」的真实原因**，且已修。

若 Owner 那边**刷新后仍是乱码**，需要补一份具体信息才能继续定位：
**是哪一份文件、用什么打开的（手机/电脑、哪个 App）、以及截图**。

## 九之十五、执行记录（第十四批：提交后给一个「下一步去哪」的落点）

Owner 报：「打开草稿、点提交，就自动进入系统原生库存模块下的物料移动，
等于跳出了仓库工作台，很不方便，是不是跳回批次更合理」。

### 一、先澄清一个容易误会的点

**「物料移动」不是另一个模块，它就是 `Stock Entry` 的中文名**——
ERPNext 的 zh 翻译里 `Stock Entry` → `物料移动`（`Material Transfer` → `直接调拨`）。
所以 Owner 看到的是「原生 Stock 下的 Stock Entry」表单。

两条路当时都**没有落点**：

- 「打开草稿」的链接是 `/app/stock-entry/<name>`，打开的就是原生表单，
  面包屑在 Stock 下——**这一刻就已经出了仓库工作台**；
- **提交后 Frappe 与 ERPNext 都不做任何跳转**（翻过 `save.js`、
  `stock_entry.js`，提交成功分支里没有 `set_route`），所以人停在那儿。

### 二、落点为什么选「批次」、且必须放在提交之后

操作员提交完的**下一步动作是取那张待检证 / 货位卡去贴货位**，
而那个 PDF 挂在**批次**的附件上（第五批交付的 `doc_gen.py`）。

**关键时序**：批次在**生成草稿**时就建好了（`_ensure_batch`），
但 PDF 要**提交后**才生成。所以落点若放在草稿阶段，打开批次会**看不到 PDF**，
反而更困惑。**落点必须在提交之后**——这是本批设计的前提。

### 三、两个决定（Owner 已定）

**① 弹框给选项，不强制跳。** 仓库是连批作业，一次可能录几十张；
每张提交都强制跳走会打断节奏。弹框给两个按钮：

- **主按钮**「打开该批次（取货位卡 / 待检证）」
- **次按钮**「继续识别下一张」→ 回拍照识别页

**② 加专用字段，不靠 remarks 文案匹配。** 新增
`Stock Entry.hbos_intake_batch`（`Link → Batch`、`read_only`、`insert_after = remarks`），
`create_intake_draft` 写入批次名。**非空即代表来源是拍照识别**——
判据是结构化字段，不是那句会被人改掉的中文 remarks。

### 四、实现

| 文件 | 改动 |
| --- | --- |
| `hbos_inventory/setup.py` | 新增 `Stock Entry` 自定义字段 |
| `hbos_inventory/api.py` | `create_intake_draft` 写入 `hbos_intake_batch` |
| `hbos_inventory/hooks.py` | `doctype_js` 增加 `"Stock Entry": "public/js/stock_entry.js"` |
| `public/js/stock_entry.js` | 新增：`on_submit` 弹框 |

**挂在 `on_submit` 而不是 `refresh`**：`refresh` 会让每次打开已提交的单子都弹一次。
`on_submit` 是 Frappe 提交成功后才触发的表单事件（`form.js` 的 `savesubmit`
里 `script_manager.trigger("on_submit")`）。

**不是拍照识别建的单子，`hbos_intake_batch` 为空 → 直接返回、不打扰**
（普通入库、移库、领料等都不弹）。

### 五、验证

| 项 | 方式 | 结果 |
| --- | --- | --- |
| 字段已建 | `frappe.get_meta("Stock Entry")` | ✓ Link→Batch、read_only、after=remarks |
| 端到端写入 | 真调一次 `create_intake_draft`（用现成 TEST 批次 + 现成物料） | ✓ 生成的草稿 `hbos_intake_batch = TEST-M3R1-B901` |
| 未误伤批次 | 调用前后读该批次字段 | ✓ 完全一致（可选参数全为 None 时不动） |
| 收尾 | 删掉这次验证产生的草稿 | ✓ 草稿数回到原来 4 张 |
| JS 可下发 | `/assets/hb_inventory_app/js/stock_entry.js` | ✓ 200 |
| JS 会加载 | `frappe.desk.form.meta.get_meta("Stock Entry")["__js"]` | ✓ 含我们的处理函数 |
| JS 语法 | `node --check` | ✓ |
| 目标路由存在 | `/app/hbos-photo-intake`、`/app/batch/<批次>` | ✓ |
| 单测 | OCR 87 + 仓储契约 9 | ✓ 全过 |

> ⚠ **未在真实浏览器里验证弹框本身**——本 install 的 Browser 预览不可用
> （只能 attach 到 URL，而该模式未启用），进 Desk 又需要登录凭据。
> **上面验的是「字段会写对、JS 能下发到浏览器、事件挂在正确的钩子上」**，
> 弹框长什么样、点下去跳哪，**需要 Owner 亲自走一遍**。

### 六、这次没做

- **没改「打开草稿」那个链接**：草稿阶段只能走原生表单复核（这是提交前唯一的
  检查入口），改成别处反而少了复核环节。
- **没给「提交后」加更多选项**（如直接打印 PDF）——先把落点补上，
  实际用起来再谈。
- **没做 nginx 式的根治**：如果以后觉得弹框烦，可以改成「首次弹、之后静默跳批次」。

## 九之十六、执行记录（第十五批：工作台瘦身 + 原生表单加返回出口）

Owner 接着提了两个问题：「打开草稿就已经进到库存-物料移动了，那一刻就跳出了仓库工作台」
和「**为什么要单独做仓库工作台，不能集成在原生的库存里吗**」。第一个在第九之十五批
已处理（提交后给落点），**这一批处理第二个**。

### 一、先答「为什么要单独做」——三条真实理由

| # | 理由 |
| --- | --- |
| ① | **原生的「库存」里根本没有我们的东西**。四个报表是本 App 的 Script Report（`is_standard=Yes` 且 `module=HBOS Inventory`），ERPNext 原生工作台不会知道它们存在。 |
| ② | **原生模块没有本 App 的侧边栏**。进去之后侧边栏是 ERPNext 的，而桌面图标是挂在我们自己的侧边栏上的——这正是 Owner 反复撞到的「回不来」的结构性原因。 |
| ③ | 桌面图标必须挂在**一个** Workspace Sidebar 上（`Desktop Icon.sidebar` 必填且要能校验通过）。原生「库存」用的是原生侧边栏，我们挂不上去。 |

**但 Owner 的观察也指出一个真问题**：那时的侧边栏有 **13 条**，其中 9 条与原生库存重复——

- `入库登记（原生）` / `出库核销` / `货位变更` → **三条都指向同一个地方**（原生 `Stock Entry` 列表），只是标签不同，点下去**并不会按用途过滤**；
- `批次` / `物料` / `货位` / `货位二维码` → 原生「库存」工作台里**本来就有**（实测原生 `Stock` 工作台 72 条链接，含 `Batch`→Batch、`Stock Entry`→Stock Entry、`Item`→Item、`Warehouse`→Warehouse）。

所以本工作台当时既没多出信息，又让「这里和原生到底哪个才算数」变得含糊。

### 二、方案：保留 + 瘦身 + 加返回（Owner 已定）

**① 保留工作台** —— 它是那 5 条独有内容的唯一去处（③ 也要求它存在）。

**② 瘦身到只放独有的**：

| 撤掉 | 留下的 |
| --- | --- |
| `入库登记（原生）`、`出库核销`、`货位变更`、`批次`、`物料`、`货位`、`货位二维码（在货位上打印）` | `入库拍照识别`（页面）、`按批号查货位`、`货位明细表`、`效期预警`、`库级盘点三对账` |

快捷方式从 5 个减到 1 个（只留拍照识别）；工作台正文也改写成一句说明：
**「这里只放海滨特有的东西，库存单据请走左侧库存模块」**。

**③ 原生表单加「返回仓库工作台」出口** —— 重写原生表单等于改核心源码（项目红线），
所以只补一条回路。加在**会用到的两个表单**上：

- `Stock Entry`：草稿复核与提交都在这里；
- `Batch`：提交后被引导到这里取货位卡与待检证——**不加这一条，人到了批次就出不去**。

### 三、契约测试跟着改（三条新增，且改成按常量断言）

原来有一条按**源码文本**断言侧边栏含 12 个标签的测试。这次要撤掉 7 个，
**而注释里必然会提到这些名字**（说明「为什么撤掉」）——按文本查会误报成失败。
所以改成：

1. **按解析出的常量断言**，不按源码文本。为此给测试加了个小的 AST 求值器
   （原来只解顶层别名，`SIDEBAR_ITEMS` 里嵌着 `WORKSPACE_TITLE` 这种 Name 节点
   解不动，会直接 `KeyError`）。
2. 新增 **`test_sidebar_has_no_doctype_links_at_all`** —— 比逐条列举更耐用：
   以后想加「规格」「BOM」之类会先撞到这里，逼着人重新想一遍
   「这到底是不是海滨特有的」。
3. 新增 **`test_native_forms_have_a_way_back`** —— 锁住两个原生表单的返回出口。

### 四、⚠ 顺手发现的一个隐患：工作台没有 `.json` 标准文件

查「原生工作台为什么不能改」时发现的：本工作台由 `after_migrate` 钩子
**每次构建**（`Workspace` + `Workspace Sidebar` + `Desktop Icon`），
但**仓库里没有对应的 `.json` 文件**。

而 `bench migrate` 里有 `remove_orphan_entities()`：它会删掉
**「`public=1`、`module` 有值、`app` 有值，但在任何 app 目录里找不到 `.json`」**
的 Workspace（以及 `standard=True` 的 Sidebar / Desktop Icon）。

我们的工作台**三条全中**（`app='hb_inventory_app'`、`module='HBOS Inventory'`、`public=1`）——
**它之所以没被删掉，只是因为 `after_migrate` 恰好跑在删除之后、又把它重建了一遍**。

**当前行为是对的**（Hook 是单一事实来源，迁移顺序也站在我们这边），
但**理由是隐式的**：一旦哪天有一步跑到 `after_migrate` 之后去读工作台，
看到的就是「刚才那一瞬它是被删过的」。**本批未改**（改法要另议：补 `.json`、
或让 Hook 在迁移前先建好），**先记在这里**。

### 五、验证

| 项 | 结果 |
| --- | --- |
| 侧边栏 | ✓ `仓库工作台 / 入库拍照识别 / 按批号查货位 / 货位明细表 / 效期预警 / 库级盘点三对账`（13 → 6 条） |
| 快捷方式 | ✓ 只剩 `入库拍照识别`（5 → 1） |
| 工作台链接卡片 | ✓ 只剩四个报表（分「查询与台账」「效期与盘点」两组） |
| 桌面图标（真实 boot 流程） | ✓ 47 个图标，含「仓库工作台」`boxes`，`parent_icon=''` |
| 历史标题残留 | ✓ 无 |
| 两个原生表单的 JS | ✓ 已进 `get_hooks('doctype_js')`、`/assets/...` 200、`node --check` 通过 |
| 契约测试 | ✓ 12 条（原 9 + 新 3）全过 |
| 单测 | ✓ OCR 87 + 考勤契约 7 全过 |

> ⚠ **未在真实浏览器验证**（预览不可用 + Desk 需登录）：工作台瘦身后的版式、
> 两个表单上的「返回仓库工作台」按钮、以及第九之十五批那个提交后弹框，
> **都需要 Owner 亲自走一遍**。

## 九之十七、执行记录（第十六批：修「点单据就跳走」的根因，并纠正上一批的误撤）

Owner 给了两个方案让先分析：「① 把原生库存里相关的复制到仓库工作台下，操作都在
这里完成」与「② 把仓库工作台合并进原生库存」。分析过程中 Owner 提供了浏览器截图，
**截图直接推翻了我上一批的一个判断**。

### 一、截图暴露的结构：不只是面包屑，侧边栏整条都换了

Owner 截的是批次表单（`/desk/batch/B2507343`）。图上有三件事：

| 看到什么 | 说明什么 |
| --- | --- |
| 面包屑 `首页 / 库存 / 批号 / B2507343` | 第一项是**库存**，不是仓库工作台 |
| **左侧边栏整条变成原生「库存」（副标题 ERPNext）** | 侧边栏也切走了——**这个我上一批没问到，是截图才暴露的** |
| 工具条上有「返回仓库工作台」且点了能回 | 我加的那个按钮**是有效的**（Owner 确认） |

所以「返回按钮能用」≠「不跳」。**按钮是补丁，跳转本身还在。**

### 二、根因：框架的两层独立机制，我们两层都没喂

查 `sidebar.js` 与 `form/meta.py` 后确认，这是**两套互不相干的机制**：

| 层 | 由谁决定 | 规则 |
| --- | --- | --- |
| **侧边栏** | `Workspace Sidebar` 的条目 | `resolve_sidebar()` 规则 1：**当前侧边栏若已链接到该单据（item 的 `link_to` 命中），就保持不变**。`get_workspace_sidebars()` 拿 **bootinfo 里所有侧边栏**去匹配 |
| **面包屑** | `Workspace Shortcut` / `Workspace Link`（`link_type=DocType`） | `FormMeta.load_workspaces()` **先查 Shortcut**，查到就用、查不到才回落 Link |

实测（站点数据）：

| 单据 | 谁链接了它（侧边栏） | `__workspaces`（面包屑） |
| --- | --- | --- |
| `Batch` | **只有原生 `Stock`** → 唯一候选，必然切走 | `Stock` |
| `Stock Entry` | `Subcontracting` / `Stock` / `Manufacturing`（3 个） | `Manufacturing`（**完全无关的工作台**） |
| `Item` | 8 个侧边栏 | `Home` |
| `Warehouse` | `Stock` / `Manufacturing` | `Home` |

**这就是「跳走」的全部原因，也是上一批我判断错的地方。**

### 三、⚠ 纠正上一批：那几条「重复入口」是承重的，我撤错了

第九之十六批我把侧边栏里指向原生单据的条目（Stock Entry / Batch / Item /
Warehouse）当成「与原生库存重复的导航」全部撤掉。**后果就是 Owner 这次报的问题**——
我们的侧边栏一旦不含该单据，就不再是 `resolve_sidebar` 的候选，于是掉回原生。

**正确的心智模型**：那几个条目**不只是入口**，它们同时是「侧边栏不换、上下文不断」
的开关。撤掉它们省下的只是几个导航项，付出的却是每次进单据都换一套侧边栏。

**但也不是原样加回来**：原来那三条 Stock Entry（入库登记 / 出库核销 / 货位变更）
指向的是**同一个原生列表**，只是标签不同、点下去不会按用途过滤——那是**假分类**，
比只有一条更误导。现在合并成**一条**：`库存单据（入库/出库/移库）`。

### 四、改法与实测

| 层 | 改了什么 |
| --- | --- |
| **侧边栏** | 加回 4 条指向原生单据的条目（Stock Entry 一条、Batch、Item、Warehouse），并在源码与测试里**写明它们是承重的、不是重复导航** |
| **面包屑** | 工作台加 3 条 `Workspace Shortcut`（`Stock Entry` / `Batch` / `Warehouse`） |
| **`Item` 有意跳过** | ⚠ 原生 `Home` 工作台**已经有一条 Item 快捷方式**，而 `load_workspaces()` 取 `shortcut[0][0]`、**查询里没有 `order_by`** —— 再加一条会让结果**不确定**（有时 Home、有时我们）。比「稳定地错」更糟，所以不加。Item 的面包屑仍指 `Home`，**这是已知残留** |

**验证（不靠浏览器，靠真实数据 + 跑真逻辑）**：

| 项 | 结果 |
| --- | --- |
| 面包屑归属 | ✓ `Stock Entry` / `Batch` / `Warehouse` → **`仓库工作台`**；`Item` → `Home`（有意） |
| 侧边栏解析 | ✓ 把 bootinfo 的真实数据导出来，**用 `sidebar.js` 原样的 `resolve_sidebar` 逻辑在 Node 里跑**：四个单据全部命中「规则 1 → 保持不变」，侧边栏都是 `仓库工作台` |

### 五之二、⚠ 上面那行「面包屑归属 ✓」是**假通过**（Owner 截图当场推翻）

Owner 走了一遍，截图显示：**侧边栏 ✓ 已经是「仓库工作台」（这一层真修好了）**，
但面包屑是 `🏠 / 物料移动` —— **没有「仓库工作台」**。

**我的验证错在哪**：我只验了 `__workspaces == ['仓库工作台']` 这个**数据**，
**没验它会渲染出来**。这正是我在这条线上反复踩的同一个坑（第三次）：
「服务端/数据层对了」不等于「用户看得见」。

**真正的机制**（`breadcrumbs.js`）：`set_workspace_breadcrumb()` 里

```js
if (!breadcrumbs.workspace) this.set_workspace(breadcrumbs);
if (!breadcrumbs.workspace) return;          // ← 没拿到就直接不画
```

而 `set_workspace()` 分两条互斥的支路：

| 来路 | 走哪条 | 结果 |
| --- | --- | --- |
| **从工作台点进来**（`last_route[0] === "Workspaces"`） | 要求 `breadcrumbs.module` 有值，**且** `module_wise_workspaces[module]` 含该工作台 | `Stock Entry` 的模块是 `Stock`，而 `仓库工作台` 属于 `HBOS Inventory` → **永远匹配不上 → 不画** |
| 直接开 URL / 从列表点进表单 | 用 `__workspaces[0]` | **会画** |

`module_wise_workspaces` 实测：`Stock → ['Stock']`、`HBOS Inventory → ['仓库工作台']`、
`Manufacturing → ['Manufacturing']`。而 `breadcrumbs.module` 对 `Stock Entry` 是
**undefined**（只可能由 `localStorage["preferred_breadcrumbs:..."]` 提供，
那要经 `/modules/` 路由才写，v16 已无该入口）。

**所以**：我加的 3 条快捷方式在**「从工作台点进去」这条主路径上不生效**——
恰好是 Owner 走的那条。副作用是行为**不一致**（刷页面能看到，点进去看不到）。

### 五之三、修法：用框架自己留的扩展点（不是 hack）

Owner 决定修。查证后**推翻了我上一条里「属 hacks」的说法**——
`breadcrumbs.preferred` **是框架的正式扩展点，而且 ERPNext 自己就在用**：

```js
// erpnext/public/js/conf.js —— 官方文件，原样
$.extend(frappe.breadcrumbs.preferred, {
    "Item Group": "Stock", Brand: "Stock", "Sales Person": "Selling", ...
});
```

语义正是「**按单据指定它该挂到哪个模块**」。所以我们照抄同一写法：

| 改动 | 内容 |
| --- | --- |
| 新增 `public/js/desk_context.js` | `$.extend(frappe.breadcrumbs.preferred, { "Stock Entry"/Batch/Item/Warehouse → "HBOS Inventory" })` |
| `hooks.py` | `app_include_js = "/assets/hb_inventory_app/js/desk_context.js"` |

三个实现细节，都是踩出来的：

1. **必须写在全局脚本里，不能用 `doctype_js`** —— 表单 js 要等表单打开才加载，
   而 `preferred` 是**面包屑第一次绘制时**就读的，晚了。`app_include_js` 按 app 顺序
   加载，本 app 排在 frappe / erpnext 之后，`frappe.breadcrumbs` 已就绪。
2. **路径必须写成 `/assets/<app>/...` 绝对形式**。非 bundle 文件在 `bundled_asset()`
   里查不到映射，会原样交给 `abs_url()`，而它只会在前面补一个 `/` ——
   写 `public/js/x.js` 会变成 `/public/js/x.js`，**404**。（第一次就是这么写错的。）
3. **改了 `hooks.py` 必须 `bench clear-cache`** —— Frappe 把 hooks 缓存在 Redis 里，
   不清理的话 `get_hooks('app_include_js')` 里根本没有我们这条。

**验证（用真实数据跑框架原逻辑）**：

| 场景 | 结果 |
| --- | --- |
| 从仓库工作台进 `Stock Entry` / `Batch` / `Warehouse` / `Item` | ✓ 面包屑工作台 = **仓库工作台** |
| 直接开表单 URL | ✓ = 仓库工作台 |
| **从原生 Stock 工作台进 `Batch`** | ✗ 仍为 undefined（**已知边界**，见下） |
| 改动前对照 | `Stock Entry` / `Batch` 从工作台进 → undefined（复现了 Owner 看到的空白） |

做法是把 `get_module_wise_workspaces()` 的真实输出导出来，用 `breadcrumbs.js` 里
`set_workspace()` 的原样逻辑在 Node 里跑五种来路。

**仍有的边界**：从**原生库存模块或搜索框**主动进去，`last_workspace` 不是仓库工作台，
这条支路仍不画。那是「主动绕路」，不在本 App 职责内（单据自己的 `module` 是
ERPNext 的数据，改不动）。

| 侧边栏条目 | ✓ 10 条（含 4 条原生入口） |
| 快捷方式 | ✓ 4 条，且工作台正文引用的 3 个 `shortcut_name` 全部命中实际快捷方式 |
| 桌面图标 | ✓ 47 个，含「仓库工作台」`boxes`，`parent_icon=''` |
| 历史标题残留 | ✓ 无 |
| 契约测试 | ✓ **13 条**，其中 `test_sidebar_keeps_links_to_native_doctypes` 专门**防止这个错误再犯** |
| 其他 | ✓ OCR 87、考勤契约 7、JS `node --check`、静态资源 200 |

### 五、关于 Owner 提的两个方案

- **方案 ①（复制到我们这边）** = 本批做法。**可操作、便宜、可逆**，且命中真病因。
- **方案 ②（合并进原生库存）** = **不建议**。原生 `Stock` 工作台是 erpnext 的标准文档
  （带 `.json`），`bench migrate` 时「文件更新的时间戳更新就覆盖 DB」——**每次 ERPNext
  升级都会静默冲掉我们加进去的东西**；要救就得在 `after_migrate` 里每次重加，长期跟上游
  打架。项目自己的 `05_开源代码二次开发与升级规范.md` 把「Workspace 自定义」列为
  **优先级 2**，但示例是**创建自己的**工作台（如「海滨考勤工作台」），不是改别人的。

**两个方案的天花板一样高**：单据自己的 `module` 字段是 `Stock`（ERPNext 的数据），
**改不掉**。所以「从原生库存模块或搜索框主动进去」这条路，两个方案都堵不住。
方案 ① 用低一个数量级的代价拿到同样的效果。

### 六、仍未做 / 仍需 Owner 验证

- **⚠ 面包屑：已修（见「五之三」）**，并已用真实数据跑框架原逻辑验证。
  **侧边栏那一层也已实测通过** —— Owner 截图显示左侧仍是「仓库工作台」。
- **仍未在真实浏览器复核一次**（预览不可用 + Desk 需登录）：改完后 Owner 应再点一次
  「库存单据」，看面包屑第一项是不是「仓库工作台」。**这次看的是左上角那行 `🏠 / …`。**
- **`Item` 的面包屑仍是 `Home`**（上面说的那个 `order_by` 坑）。要治得动原生 `Home` 工作台，
  属于本批刻意避开的那条路。
- 第九之十五批的提交后弹框，仍待 Owner 用**新建草稿**验证（老草稿不带来源标记）。

## 九之十八、执行记录（第十七批：把仓库日常用到的原生内容都收进工作台）

Owner 确认「面包屑第一项是仓库工作台」后，按上一批提的清单把原生内容收进来。

### 一、范围（Owner 定的口径：「仓库日常用得到的都放进来」）

侧边栏由 **6 条扩到 24 条**，分五组，**全部指向原生对象或本 App 报表，没有自造入口**：

| 组 | 条目 |
| --- | --- |
| 入口 | 仓库工作台、入库拍照识别 |
| 单据（5） | 库存单据（入库/出库/移库）· 采购入库 · 销售出库 · 物料需求 · 拣货单 |
| 盘点与质检（2） | 库存对账 · 质检单 |
| 库存报表（7，原生） | 库存流水 · 库存余额 · 库龄 · 批量余额历史 · 批次到期状态 · 按仓库库存余额 · 预计库存量 |
| 货位与批次（4，海滨专有） | 按批号查货位 · 货位明细表 · 效期预警 · 库级盘点三对账 |
| 基础数据（4） | 物料 · 批次 · 货位 · 计量单位 |

工作台页面也同步分五组（同一批链接），并改写导语：
**「仓库日常都在这里完成……进去之后不会跳去原生库存模块」**。

**有意没收**（原生 `Stock` 工作台 72 条里与仓库日常无关的）：
序列号（本厂不用批次以外的追踪）、分包、定价与关税、安装单与保修等。
**照单全收只会把侧边栏淹掉** —— 这正是上一批刚纠正过的那个教训。

### 二、面包屑的范围跟着扩（改的是同一个脚本）

`desk_context.js` 里 `preferred` 的 doctype 由 4 个扩到 **11 个**
（上面 5 个单据 + 2 个盘点质检 + 4 个基础数据）。

**报表有意不列**：`query-report` 路由的面包屑走另一条分支，只显示报表标题，
本来就不带工作台前缀（`breadcrumbs.js` 的 `view == "query-report"` 分支）。

### 三、验证：这次**连渲染那一层一起跑了**

上一批我在面包屑上犯过「只验数据、不验渲染」的假通过，所以这次把
`breadcrumbs.js` 的 `set_workspace()` + `set_workspace_breadcrumb()`（含
`visible_modules` 那道会静默 `return` 的闸门）**原样搬到 Node 里**，
喂**真实的 bootinfo 数据**（`workspaces.pages` 的 29 条、`module_wise_workspaces`、
侧边栏键），跑 11 个单据 × 2 种来路：

| | 打补丁后 | 没打补丁（对照） |
| --- | --- | --- |
| 从工作台进 | **11/11 → 仓库工作台** | 11/11 **不显示** |
| 直接开表单 URL | **11/11 → 仓库工作台** | 只有 3 个对（那 3 个有 Shortcut），其余指 `Stock` |

对照那一列**复现了 Owner 看到的现象**，说明这个模拟是真的、不是自说自话。

**其余核对**：

| 项 | 结果 |
| --- | --- |
| 侧边栏 | ✓ 24 条，`bootinfo.workspace_sidebar_item` 里实到 24 条 |
| 链接分组 | ✓ 五组 `link_count` 与实际条数**逐组相等**（5/2/7/4/4） |
| 桌面图标 | ✓ 47 个，含「仓库工作台」`boxes` |
| 契约测试 | ✓ **15 条**，新增「`link_count` 必须等于该组实际链接数」——对不上时 Frappe 不报错、只是少显示，又是个静默坑 |
| 其他 | ✓ OCR 87、考勤契约 7、JS 语法、静态资源 200 |

### 四、仍未做

- **未在真实浏览器复核**（预览不可用 + Desk 需登录）：24 条侧边栏的实际观感、
  工作台五组的版式，需 Owner 看一眼。**侧边栏变长了**，如果觉得太长再说。
- **`Item` / `UOM` / `Stock Reconciliation` 的 `__workspaces` 仍是原生**
  （`Stock` / `Home`）。这**不影响面包屑**——`preferred` 里 module 的优先级高于
  `__workspaces`（模拟已验证）。但那个字段本身仍是 `Workspace Link` 兜底查出来的
  首个结果、**查询无 `order_by`**，所以**不要给它加快捷方式**（会变成不确定，见上一批）。

## 九之十九、执行记录（第十八批：「点打开草稿就跳走」——两个独立缺陷）

Owner 报「点『打开草稿』就又跳到库存页面下了」。查下来是**两个独立缺陷叠加**，
其中一个还解释了为什么上一批的修复"没生效"。

### 一、后端容器跑的是旧代码（**这也是上一批落点弹框没出现的原因**）

时间线（都是硬的）：

| 时刻（本地） | 事件 |
| --- | --- |
| **10:01:59** | backend / queue-long / queue-short / scheduler 四个容器**启动** |
| 10:55:42 | `hbos_intake_batch` 字段才落库（我在那之后又改了 `api.py` 写这个字段） |
| **15:24 / 15:25** | **Owner 建了两张草稿**（`MAT-STE-2026-00023` / `00024`，备注是拍照识别写的）→ `hbos_intake_batch` **为空** |

**原因**：gunicorn **在容器启动时导入一次 `api.py` 就缓存在 `sys.modules` 里**，
改文件不会自动生效。所以：

- `bench console`（另起进程、每次重读文件）里测**通过**；
- **Web 请求走的是 10:01 那份旧代码**，根本不写 `hbos_intake_batch`。

字段为空 → 我上一批的 `on_submit` 弹框按设计「不是拍照识别建的就不打扰」**直接返回**，
所以 Owner 既没看到弹框、也仍然被跳走。

**这与第九批（OCR 常驻服务改代码要重启）是同一类错**：改的是文件，跑的是旧进程。

**修法**：`docker compose restart backend queue-long queue-short scheduler`（都跑 App 代码）。

**验证（这次走真实 HTTP，不走 console）**：`POST /api/method/login` 拿 session →
`POST /api/method/…create_intake_draft` →

- 返回键是 `['batch','docstatus','filled','name']`（**没有 `route`**，说明跑的是新代码）；
- 建出的草稿 `hbos_intake_batch = TEST-M3R1-B901` ✓

**遗留**：Owner 那两张草稿（`00023` / `00024`）是旧代码建的、字段为空，**作废即可**——
我**没有删**，那属于业务数据。

### 二、「打开草稿」是个 `<a href>`，会整页刷新 → 掉回原生侧边栏

| 跳转方式 | 发生了什么 |
| --- | --- |
| **`<a href="/app/stock-entry/…">`（改前）** | **整页刷新**：Desk 重新加载，「当前侧边栏」为空 → `resolve_sidebar()` 规则 1 失效 → 落到规则 4 按 module 过滤 → 掉回原生 |
| `frappe.set_route(...)`（改后） | SPA 内跳转，当前侧边栏还在 → 规则 1 直接命中 |

已改成按钮 + 事件委托（那段 HTML 是生成后才插进 DOM 的，直接绑绑不到）：

```js
$(page.body).on("click", "[data-open-draft]", function () {
    frappe.set_route("Form", "Stock Entry", $(this).attr("data-open-draft"));
});
```

顺带删掉了 `create_intake_draft` 返回里已无人使用的 `route` 字段。

### 三、顺带补齐两处「硬加载」的漏洞

Owner 的用法里**不可能只有 SPA 跳转**（浏览器地址栏、F5 都会整页刷新），
所以另外补了两个地方：

**① `Workspace Sidebar.app` 原本留空** —— `resolve_sidebar()` 过滤候选时用的是
`module_app[module]` 与该字段**严格相等**比较，留空 = 谁都匹配不上 → 我们这条被滤掉。
已改为 `APP_NAME = "hb_inventory_app"`。（SPA 内跳转看不出这个问题，只有硬加载才暴露。）

**② 在前端种 `localStorage["sidebar_item_map"]`** —— 那是 `resolve_sidebar()` 规则 2
读的东西（「这个单据上次用的哪个侧边栏」）。种上之后，硬加载直接命中规则 2，
不再走会掉回原生的规则 4。

> 键空间对得上：规则 2 查的是**单据名**（`entity_from_route()` 取 `route[1]`），
> 而我们种的就是单据名；框架自己的写入方 `store_last_show_sidebar_for_item()`
> 写的是**条目 label**（`sidebar_item.html` 的 `data-id`）—— 我们那些 label 都是中文，
> 两个键空间不会撞。

### 四、验证：四种来路 × 11 个单据

把 `resolve_sidebar()` 的四级规则原样搬到 Node，喂**重启后的真实 bootinfo**：

| 来路 | 结果 |
| --- | --- |
| ① SPA 从工作台点进 | **11/11 → 仓库工作台**（规则 1） |
| ② 硬加载（已种 map） | **11/11 → 仓库工作台**（规则 2） |
| ③ 硬加载（不种，对照） | 11/11 掉回 `Stock` / `Buying` —— **复现了 Owner 看到的现象** |

**其余**：侧边栏 `app` 已落库并出现在 bootinfo（24 条）；契约测试 **15 → 18 条**
（新增：`打开草稿` 不得用 `href`、必须种 `sidebar_item_map`、`sidebar.app` 不得留空）；
OCR 87 + 考勤契约 7 + JS 语法全过；站点 200、重启后四个容器正常。

### 五、教训再记一次

**改 Python 代码后，`bench migrate` 和 `clear-cache` 都不够——跑着的 gunicorn /
worker 进程必须重启。** 这与 OCR 服务、与「页面 `.json` 的 `modified`」
是同一族问题：**改了 A，忘了 B 也缓着旧东西**。本轮是这一族的第四次。

## 十、风险与注意事项

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| **Mac 作临时服务器不稳** | 开发机可能休眠、内存吃紧，服务会中断 | 如实告知 Owner；正式使用前建议采购内网服务器 |
| **16 GB 内存吃紧** | Frappe 全栈 + 本地模型同时跑 | 选小模型；模型按需加载、用后释放；C2 优先（内存占用低） |
| 识别准确率不达预期 | 标签版式复杂、可能反光/倾斜/磨损 | 强制人工校对；**先量后决**，不达标如实报告 |
| 小模型漏读小字 | 4-bit 量化后能力下降 | 与 C2 对照；用约束校验兜底 |
| 混淆批号与代码 | 两者都是数字串，外购还有双批号 | 提示 `source_type`；格式校验；高亮低置信度；**代码全数字这一约束可用于区分**（含字母的多半是批号） |
| 依赖 Mac 上的服务 | 服务挂了拍照入库不可用 | 保留**人工录入**通道作降级路径（M3-R2 已具备） |
| 服务运维 | 新增一个服务需长期维护 | 服务**无状态**，便于重启与迁移 |
| **打印件缺中文字体** | 四个容器原本 0 个中文字体，汉字被 wkhtmltopdf 静默丢弃 | 已挂载 `runtime/fonts`（第九之十一节）。**换服务器时必须重做这一步**：基础镜像里没有中文字体，`apt-get install fonts-noto-cjk` 或挂载字体目录，否则新机器上打印件同样全是空的 |

## 十一、下一步

1. Owner 确认第九节仅剩的第 4 项（**测试样本从哪来**）。
2. Owner 授权启动 M3-R6 后，按第八节边界实施。
3. **注意**：启动外部 FastAPI 服务本身也需 Owner 单独授权（项目规则明列），届时会再确认一次。
4. M3-R6 完成后进入 **M3-R7**（总审查与收口）。
5. **已生成的历史 PDF 已全部重出**（第九之十一节），后续新入库自动用新字体。
6. **换服务器/重建环境时**：`frappe/erpnext` 基础镜像**不含中文字体**，必须补
   （挂载字体目录，或 `apt-get install fonts-noto-cjk` 并重建镜像），
   否则打印件会退回到「只有英文和数字」。当前挂的是 macOS 自带 Songti，
   上生产建议换成 OFL 字体（Noto / 思源）。

## 十二、数据安全说明

- 本轮为**方案轮**：未启动任何外部服务、未调用任何视觉模型、未创建任何代码与服务、未索取任何标签样本。
- 方案中提到的测试集（第九节第 4 项）**尚未索取**；后续若提供，含**真实批号与产品名**，**不得提交 Git**，须放仓库工作区之外。
- 本方案文档只记录**方案与规则**，不含任何真实产品名、批号或供应商名。
- **Owner 已定"照片不出内网"**，识别服务不开任何外网访问——这是本方案最重要的合规边界。
