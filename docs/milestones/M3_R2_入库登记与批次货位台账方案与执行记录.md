# M3-R2 入库登记与批次货位台账方案与执行记录

项目名称：新乡海滨智能运营管理平台。

## 文件定位

本文件是 M3-R2 的**方案**，用于固化设计决策与实现路径。

本文件既是 M3-R2 的**方案**，也是执行完成后的**执行记录**（见第九节）。

## 本轮状态

状态：REVIEWING（方案已交付并**已执行**，等待 Owner 和 Claude 审查）。

本文件原为方案；Owner 授权后已在同一 `frontend` site 执行完毕，执行记录见第九节。执行过程中的两处偏差（DocType 命名、盘点报表延后）见 9.5。

## 一、Owner 已确认的四项设计决策

| 议题 | 决策 |
| --- | --- |
| 包装构成（件 / 听 / 瓶） | **自定义子表 + 「件」仅作计数单位**。子表存「容器类型 + 单件重量 + 件数」，可完整表达 `34件3听1瓶`；`kg` 为权威数量 |
| 「待检 → 放行 / 不放行」门禁 | **`Batch` 自定义字段**：放行状态 + 放行日期 + 合格证编号 + 合格证附件；出库时校验状态 |
| 自定义字段 / UOM / 模板归属 | **授权新建 `hb_inventory_app`**，照搬 `hb_attendance_app` 的 `after_migrate` 代码化模式 |
| 产品分类 | **新建海滨分类树**（挂在 `Item Group` 下），分类为：原料药 / 包材 / 辅助用品 / 中间体 / 成品 |
| 业务分类与代码前缀 | **两者独立**：代码前缀（SAP 口径）不等于业务分类——对外发货的最终产品业务上是**成品**，但代码为 `1300`；SAP `1400` 前缀实际无用例户 | Owner 澄清（见 6.4） |
| 外购批号 | `Batch.batch_id` 用**进厂批号**；原厂批号存自定义字段 `hbos_supplier_batch_no`；扫码需支持两种批号输入 | Owner 确认（见 6.8） |
| 包装构成的适用范围 | 包装构成子表**主要适用于原料药 / 中间体**（KG 计量）；包材与备品备件按件类单位计量，包装构成意义有限 | Owner 提供数据后确认（见 6.3） |
| 盘点口径 | **库级盘点 + 三对账**（ERP数量 / 货位卡数量 / 实物数量），不要求货位级盘点 | Owner 确认（见 6.6） |
| 计量单位 | 按实际建全部 11 种（KG / 个 / 套 / 瓶 / 支 / 双 / 卷 / 张 / L / BT），另加「件」用于原料药双单位 | Owner 确认（见 6.3） |

## 二、本轮技术调研发现

调研均为只读查询，未修改任何配置或数据。

### 2.1 效期：「期限」原生可承载，但「类型」不行

`Item` 原生已有：

| 字段 | 类型 | 对海滨需求的意义 |
| --- | --- | --- |
| `has_expiry_date` | Check | 是否有效期 |
| `shelf_life_in_days` | Int | **效期天数**——2 年 = 730、3 年 = 1095，原生即可表达"期限" |
| `end_of_life` | Date | 停产日期（非效期） |

**结论**：Owner 确认的"按产品质量标准设定期限"，可用 `shelf_life_in_days` 承载，由生产日期推算到期日。但 **"复检期 / 有效期"的类型区分原生没有**，需自定义字段。货位卡上那两个勾选框对应它。

### 2.2 「件」：原生换算是固定的，装不下尾桶

`Item` 有 `uoms` 子表 = **`UOM Conversion Detail`**（原生备选单位机制，官方说明"1 Box = 12 Nos, conversion factor 12"）。

**问题**：原生备选单位是**单一固定换算系数**。而海滨的「件」不是固定值——整桶 5.00 kg、尾桶 4.33 kg，同为"件"但重量不同。

**结论**：印证 Owner 的选择——「件」只作计数单位，不参与换算；包装构成走自定义子表。

### 2.3 待检 / 放行：原生 `Quality Inspection` 不匹配"凭证"语义

`Quality Inspection` 原生存在：

| 字段 | 取值 |
| --- | --- |
| `status` | Accepted / Rejected / Cancelled（必填） |
| `inspection_type` | Incoming / Outgoing / In Process（必填） |
| `reference_type` | Purchase Receipt / Delivery Note / Stock Entry 等（必填，须挂具体单据） |
| `batch_no` | Link → Batch |
| `inspected_by` | Link → User（必填） |
| `verified_by` | Data |

**结论**：QI 是"检验记录"，要求挂单据、必填检验人，比较重；而 Owner 说的门禁依据是 **"QA 放行手续 + 合格证"**，属凭证类语义。故按 Owner 决策走 `Batch` 自定义字段；原生 QI 保留为**未来可选**（若后续要正式化检验流程再接入，不冲突）。

### 2.4 产品分类：原生只有标准 6 类

`Item Group` 现有：`All Item Groups`（根）、`Products`、`Raw Material`、`Sub Assemblies`、`Consumable`、`Services`。

**结论**：没有"原料药 / 包材 / 辅助用品 / 中间体 / 成品"这类海滨分类，需新建分类树（Owner 已确认）。分类树按 **6.1 的物料代码前缀**对应建立，与 SAP 编码口径一致。

### 2.5 更正一处我此前的误判：自定义字段**是有版本化的**

我此前看到 `hb_attendance_app/patches.txt` 为空，怀疑 M1 的 59 个自定义字段只存在数据库、未进 git。

**查证后推翻该怀疑**：实际模式在 `hb_attendance_app/hb_attendance_app/hbos_attendance/setup.py`——通过 `hooks.py` 注册 `after_migrate = "hb_attendance_app.hbos_attendance.setup.after_migrate"`，在钩子里调用 `create_custom_fields(...)` 声明式创建字段（`Employee Checkin` 3 个、`Attendance` 6 个等）。

这是**代码化、幂等、可版本化、可复现**的做法。**M3 沿用同一模式**，这也是授权新建 `hb_inventory_app` 的意义所在。

## 三、`hb_inventory_app` 设计

### 3.1 定位与边界

- 模块名：`HBOS Inventory`
- 只用于**海滨仓储特有规则**，不重写 ERPNext 已有功能。
- 不修改 Frappe / ERPNext / HRMS 核心源码。
- 与 `hb_attendance_app` 并列，各自独立，不互相依赖。

### 3.2 内容清单

| 类别 | 内容 | 承载方式 |
| --- | --- | --- |
| UOM | 新建计量单位：**KG 之外的 11 种**（个 / 套 / 瓶 / 支 / 双 / 卷 / 张 / L / BT，另加「件」） | `after_migrate`（幂等） |
| Warehouse | **六车间 8 个库位**（3902 / 3903 / 3904 / 3906 / 3907 / 3908 / 3914 / 3915） | `after_migrate`（幂等） |
| Item Group | 海滨产品分类树（原料药 / 包材 / 辅助用品 / 中间体 / 成品） | `after_migrate`（幂等） |
| 自定义字段 | 见 3.3 | `after_migrate` + `create_custom_fields` |
| 子表 DocType | `HBOS 包装明细` | App 内 DocType JSON（版本化） |
| 报表 | 按批号查货位、货位明细表、**库级盘点三对账** | App 内 Report JSON（版本化） |
| Print Format | 待检证、货位卡 | App 内（M3-R4） |
| Web 页 | 货位扫码页 | App 内（M3-R5） |

### 3.3 自定义字段清单（候选）

**`Item` 上新增**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `hbos_product_kind` | Select | 产品种类：原料药 / 中间体 / 混粉 / 成品（与批号规则、效期口径对齐） |
| `hbos_workshop` | Data | 生产车间（如"六车间B线"） |
| `hbos_shelf_life_type` | Select | 效期类型：复检期 / 有效期 |
| `hbos_shelf_life_months` | Int | 效期期限（月）；与原生 `shelf_life_in_days` 二选一，需定 |
| `hbos_storage_condition` | Small Text | 储存条件（待检证用） |

**`Batch` 上新增**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `hbos_release_status` | Select | 待检 / 已放行 / 不放行（**出库门禁依据**） |
| `hbos_release_date` | Date | 放行日期 |
| `hbos_certificate_no` | Data | 合格证编号 |
| `hbos_certificate_file` | Attach | 合格证附件（扫描件） |
| `hbos_packaging` | Table → `HBOS 包装明细` | 包装构成 |

**`HBOS 包装明细`（子表）字段**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `container_type` | Select | 容器类型：件 / 听 / 瓶（可扩展） |
| `unit_weight` | Float | 单件重量（kg）；整桶 5.00、尾桶为实际余量 |
| `count` | Int | 件数 |
| `is_tail` | Check | 是否尾桶（可选，便于识别与统计） |
| `remark` | Data | 备注 |

设计要点：`container_type + unit_weight + count` 三要素可完整表达 `34件3听1瓶`（含尾桶），且 `unit_weight × count` 汇总 = 包装总量，可与 `kg` 数量交叉校验。

### 3.4 包装置放位置

**放在 `Batch` 上（一批一条，多条明细）**，理由：

- Owner 已确认"一个批号只对应一张货位卡"，包装规格是**批次属性**，卡上要打印 `34件3听1瓶`；
- 一批若分多次入库，包装明细在 `Batch` 上可**累计汇总**，恰好是"这一批总共怎么装的"；
- 若放在 `Stock Entry` 明细行上，则是"每次入库怎么装的"，与货位卡按批出卡的口径不一致。

（注：本项为方案建议，未单列询问；若 Owner 认为需按每次入库分别记录，可在授权时调整。）

## 四、关键实现路径

### 4.1 入库登记

- 用原生 **`Stock Entry`（Material Receipt）**，明细行填 `batch_no` + `qty` + `t_warehouse`（货位）。
- 需配合已开启的 `Stock Settings.enable_serial_and_batch_no_for_item`（M3-R1 已开）。
- `Batch` 在首次入库时创建（或由车间信息预建），同时录入 `hbos_packaging` 与效期字段。

### 4.2 「按批号查货位」与「货位明细表」

**基于 `Serial and Batch Entry`**（M3-R1 实测确认；`SLE.batch_no` 在 v16 为空列不可用）：

```sql
-- 批号 → 货位 → 数量
SELECT sbe.batch_no, sbe.warehouse, SUM(sbe.qty) AS qty
FROM `tabSerial and Batch Entry` sbe
JOIN `tabSerial and Batch Bundle` sbb ON sbb.name = sbe.parent
WHERE sbb.docstatus = 1 AND sbb.is_cancelled = 0
GROUP BY sbe.batch_no, sbe.warehouse;

-- 货位 → 全部批号
SELECT sbe.warehouse, GROUP_CONCAT(DISTINCT sbe.batch_no), SUM(sbe.qty)
FROM ... GROUP BY sbe.warehouse;
```

`Bin` 仅用于"物料 × 货位"结存，不含批次。

### 4.3 出库门禁（M3-R3 使用，本方案预埋字段）

出库校验 `Batch.hbos_release_status = 已放行` 且 `hbos_certificate_no` 非空，否则拦截。字段在本轮建好，校验逻辑留 M3-R3。

## 五、M3-R2 交付边界

**计划做**：

- 创建 `hb_inventory_app`（模块 `HBOS Inventory`）。
- `after_migrate`：建 11 种 UOM、六车间 8 个库位、产品分类树、Item / Batch 自定义字段（幂等）。
- 新增子表 DocType `HBOS 包装明细`。
- 入库登记入口（录入 + 批次与包装录入）。
- 「按批号查货位」「货位明细表」两个报表。
- 用虚构 TEST 数据验证全链路。

**不做**：

- 不做出库核销、效期预警、盘点导出（M3-R3）。
- 不做待检证、货位卡 Print Format（M3-R4）。
- 不做二维码与扫码页（M3-R5）。
- 不做拍照识别（M3-R6）。
- 不创建真实产品主数据（除非 Owner 提供清单并授权）。
- 不修改 Frappe / ERPNext / HRMS 核心源码。
- 不接飞书真实写入、不启动独立前端。

## 六、产品主数据口径（Owner 已提供）

### 6.1 物料代码编码规则（Owner 确认）

物料代码为 **8 位数字**，在 **SAP 中创建**，由 **SAP 管理员维护**。按**代码前四位**区分物料大类：

| 前缀 | 大类 | 数据中出现的物料数 |
| --- | --- | --- |
| `1000` | 原料药 | 17 |
| `1100` | 包材 ⚠️ **Owner 口述规则中未提及，由 data 与《库位区分》补出** | 21 |
| `1200` | 非生产用物料 / 辅助用品 | 8 |
| `1201` | 同属辅助用品（子类） | 3 |
| `1202` | 同属辅助用品（子类） | 17 |
| `1203` | 同属辅助用品（子类） | 5 |
| `1300` | 中间体 | 23 |
| `1400` | 成品 | **0**（见 6.4） |

说明：

- Owner 口述规则为「1000 原料药、1200 非生产用物料/辅助用品、1300 中间体、1400 成品」。实际数据在 `1200` 下还有 **`1201` / `1202` / `1203`** 三个子前缀，均落在辅助用品范畴。
- **`1100`（包材）口述规则未涵盖**，《库位区分》文档注明 `3908 六车间包材库` 对应 `110*****`，数据中 1100 开头的 21 个物料**全部在 3908**，可确认为包材。**建议在分类树中单列包材**。

### 6.2 六车间库位清单（来自《库位区分》）

六车间共 **8 个库位**（4 位编号），Owner 已确认一并建入 `Warehouse`：

| 库位 | 库位名称 | 相关物料 |
| --- | --- | --- |
| 3914 | 六车间物料库 | 固体原材料相关（1000 开头） |
| 3902 | 六车间退货库 | 外发产品的退回库位 |
| 3903 | 六车间不合格品库 | 不合格物料及产品的库位 |
| 3904 | 六车间中间库 | 中间体相关（1300 开头） |
| 3915 | 六车间备品备库 | 非生产用物料、滤芯（120 开头） |
| 3906 | 六车间液体库 | 液体原材料相关（1000 开头） |
| 3907 | 六车间冷藏库 | 需冷藏物料 |
| 3908 | 六车间包材库 | 包材（无菌袋、纸板桶等，110 开头） |

其中 **5 个库位**提供了 08 月盘存表并有库存数据：`3904`、`3907`、`3908`、`3914`、`3915`。`3902`（退货库）、`3903`（不合格品库）、`3906`（液体库）暂无盘存表。

**前缀与库位的实际对应（实测）**：

| 前缀 | 出现于库位 |
| --- | --- |
| `1000` 原料药 | 3907、3914、3915 |
| `1100` 包材 | 仅 3908 |
| `1200`/`1201`/`1202`/`1203` 辅助用品 | 3914、3915 |
| `1300` 中间体 | 3904、3907、3914 |

### 6.3 物料规模（实测，仅记结构与数量，不记录物料明细）

| 库位 | 数据行 | 去重物料代码 | 单位 |
| --- | --- | --- | --- |
| 3904 | 181 | 15 | 全 KG |
| 3907 | 74 | 7 | 全 KG |
| 3908 | 30 | 21 | 个 / 套 / KG |
| 3914 | 103 | 16 | 全 KG |
| 3915 | 45 | 35 | 瓶 / 个 / 双 / 支 / 卷 / 张 / KG / L / BT |
| **合计** | **433** | **94** | （见下） |

**实测出现的计量单位共 11 种**：`KG`、`个`、`套`、`瓶`、`支`、`双`、`卷`、`张`、`L`、`BT`。

**重要修正**：M3-R0 / M3-R2 早期设想的「双单位 KG / 件」**只适用于原料药与中间体**（3904、3907、3914 全为 KG）。包材与备品备件使用**件类单位**（个 / 套 / 瓶 / 支 / 双 / 卷 / 张 / BT）与 `L`。故「件」不是全局第二单位，而是一类物料各自的计件单位。

Owner 已确认：**按实际建全部 11 种 UOM**（另加「件」用于原料药双单位）。

### 6.4 成品（1400）与"成品库"的口径（Owner 澄清）

5 个库位中 **`1400` 开头物料为 0 行**。Owner 澄清（原话要点，产品名称已脱敏）：

> 我们这里的**最终产品直接发客户，这算是成品，1300 开头的，都放在 3904 库，所以叫他成品库**。还有另一个产品系列**放在冷藏库**。

由此得到两个重要结论：

**结论一：`3904` 即业务上的"成品库"。** 它存放对外发货的最终产品，编号 1300 开头。文档《库位区分》把 3904 命名为"六车间中间库"是**结构口径**，业务上称"成品库"。

**结论二（重要）：SAP 代码前缀 ≠ 业务分类。** 最终产品在业务上是**成品**，但 SAP 代码是 **1300（中间体）**；SAP 的 `1400`（成品）前缀**实际没有物料**。因此：

- **分类不能由代码前缀直接推导**，必须另有"业务分类"字段；
- `hbos_product_kind`（业务分类）与代码前缀**是两个独立维度**，M3-R2 需分别记录；
- 分类树的真实分类应为：原料药（1000）、包材（1100）、辅助用品（120x）、中间体（1300）、**成品（1300 中的一部分）**。

**另一个产品系列** 存放于 **3907 冷藏库**（与《库位区分》"3907 需冷藏物料"一致）。

### 6.5 `16号楼产品库 = 3904`（Owner 已确认）

Owner 已确认：**M3-R1 依《产品库货位号统计.xlsx》建立的 `16号楼产品库` 就是 `3904`**。即：

- 已建的 `16号楼产品库 → 03区 → 五个层 → 203 个货位` 结构**属于 3904 库**；
- M3-R2 执行时把这棵树**改挂到 `3904` 之下**，与六车间其余 7 个库位并列，不新建重复库位；
- 库位名建议统一为 `3904 六车间中间库`（业务口径亦称"成品库"）。

### 6.6 盘存表结构（实物盘点口径）
5 份盘存表标题均为「**物料及产品盘存记录 / Material / Product Inventory Checking Record**」，含**盘点人 / 监盘人 / 部门负责人**三个签字栏，并注明「此记录为电子表格」。

**列结构（10 列）**：

| 列 | 说明 |
| --- | --- |
| 物料代码 | 8 位数字 |
| 物料名称 | — |
| 批号 | — |
| 单位 | — |
| **ERP数量** | 系统账面数 |
| 入库日期 | — |
| 复检期 / 有效期至 | **合并为一列日期**（印证 M3-R0 对两类共用到期日字段的判断）；实测有相当比例为空（记为 `/`） |
| **货位卡数量** | 货位卡数 |
| **实物数量** | 实盘数 |
| 备注 | 实测全为空 |

**关键发现：盘存表没有"货位号"列。** 即**盘点按"库"级进行**，不细到货位。盘点实质是 **ERP数量 / 货位卡数量 / 实物数量 三对账**。

Owner 已确认：**盘点按库级做三对账**，不要求货位级盘点。

**其他实测**：本批数据中**未出现亚批**（批号无 `-1` / `-2` 后缀）。


### 6.7 货位卡有两种版式：自产 / 外购（Owner 提供模板目录）

Owner 提供了两套货位卡模板目录，经解析确认**两种版式结构不同**，M3-R4 生成时须分别处理：

| 项目 | 自产货位卡 | 外购货位卡 |
| --- | --- | --- |
| 标题 | 自产物料/产品货位卡 | （新版）外购货位卡 |
| 主体字段 | 品名 / 物料代码 / **生产车间** / **生产批号** / 包装规格 / 件数 / 复检期□有效期□ / 入库数量 + 单位 / 货位号 / 待检日期 / 放行□不放行□ / 经手人 / 复核人 | 品名 / 物料代码 / **供货单位** / **生产单位** / **原厂批号** / **进厂批号** / 包装规格 / 数量（含件数） / 复检期□有效期□ / 货位号 / 状态 / 待检日期 / 放行□不放行□ / 经手人 / 复核人 |
| 批号 | **单一**（生产批号） | **两个**（原厂批号 + 进厂批号） |
| 流水列 | 日期 / **去向** / 发出量 / 结存量 / 经手人 / 复核人 | 日期 / **领料单位** / **用途** / 发出量 / 结存量 / 经手人 / 复核人 |
| 数量表达 | 入库数量 + 单位（分列） | 数量/件数（合并，如 `125.79kg/6件`） |
| 是否分页 | 未标页码 | 标「第（ ）页」 |

**两版共同的稳定结构**（M3-R4 可共用）：

- 状态序列 **入库 → 待检 → 放行□/不放行□**，每步带日期，与 M3-R0 的判断一致；
- 复检期□/有效期□ 二选一勾选；
- 出入库流水表按「件数 / 数量」双列记录；
- 经手人 / 复核人双签。

### 6.8 外购的「双批号」——已定：`Batch.batch_id` 用进厂批号

外购货位卡同时有 **原厂批号**（供应商批次）与 **进厂批号**（本厂收货批次），而 ERPNext `Batch.batch_id` 是**单一全局唯一**字段（M3-R1 已核实）。

**Owner 已确认：用「进厂批号」作为 `Batch.batch_id`。**

方案：

| 字段 | 承载 | 说明 |
| --- | --- | --- |
| `Batch.batch_id` | **进厂批号** | 本厂收货批，天然唯一 |
| `hbos_supplier_batch_no` | **原厂批号** | 新增自定义字段（`Batch`） |
| `hbos_supplier` | 供货单位 | `Batch.supplier` 原生已有，需核对是否足够 |
| `hbos_manufacturer` | 生产单位 | 新增自定义字段（`Batch`）；外购卡上供货单位与生产单位是**两个不同实体** |

配套要求：**扫码查询需同时支持进厂批号与原厂批号两种输入**（现场扫供应商标签得到的是原厂批号，需能反查到批次）。

自产与差异对照：

- 自产货位卡只有"生产车间"（如六车间B线），无供货/生产单位之分，无原厂批号；
- 外购货位卡有"供货单位 + 生产单位"两个实体，且实例中两者可能是同一家公司（如关联企业）。

### 6.9 模板数量级（说明模板是按物料逐份制作的）

| 目录 | docx | doc | 小计 |
| --- | --- | --- | --- |
| 待检证 | 120 | 31 | 151 |
| 新版货位卡外购 | 124 | — | 124 |
| 自产 | 39 | — | 39 |

说明待检证与货位卡**不是几个通用模板，而是按物料逐份制作**（品名 / 物料代码 / 生产车间等为静态预填，批号 / 数量 / 储存条件等在生成时填入）。这印证 M3-R4 应做成**由系统按物料+批次数据生成**，而不是让用户手工选模板。

待检证字段结构经抽样核对，与此前从飞书取得的模板一致（8 字段 + 固定文档编码 `APP-SOP-QA -1-A-003-13/4.0`）。

### 6.10 数据安全（重要）

上述三个模板目录共 **311 个真实业务文件**位于本仓库工作区，含**真实物料名称、物料代码、批号、供货单位**。本仓库为**公开仓库**（`PUBLIC_REPOSITORY_SCOPE.md`）。

处置：

- 本轮**未提交**这些文件，并**已在 `.gitignore` 中新增忽略规则**（`待检证/`、`新版货位卡外购/`、`自产/` 及 `*.doc`、`~$*`），防止误提交。
- 核对确认忽略生效后，未跟踪的 docx/doc 数为 **0**。
- 建议 Owner 评估是否将这批文件移出仓库工作区（如放到仓库外的素材目录）。

## 七、下一步

1. **口径已齐备**：`16号楼产品库 = 3904` 已由 Owner 确认；外购批号已定（用进厂批号）。
2. Owner 授权启动 M3-R2 执行。
3. M3-R2 执行内容按第五节的边界；`Warehouse` 部分新增六车间其余 7 个库位，并把已有货位树改挂到 `3904` 之下（**注：新增 App 需同步调整 `docker-compose.yml` 挂载，属"写 Docker Compose"，须另行授权，见下**）。
4. M3-R2 执行后，按 `docs/milestones/M3_START_GATE.md` 的门禁推进 M3-R3。

### 7.1 执行前置：需要新增 docker-compose 挂载（须授权）

当前 `docker-compose.yml` 以**逐个 App 挂载**的方式引入自定义 App：

```yaml
volumes:
  - ./apps/hb_attendance_app:/home/frappe/frappe-bench/apps/hb_attendance_app
environment:
  PYTHONPATH: /home/frappe/frappe-bench/apps/hrms:/home/frappe/frappe-bench/apps/hb_attendance_app
```

`hb_inventory_app` 要生效，需在 **8 个服务**（backend / configurator / create-site / frontend / queue-long / queue-short / scheduler / websocket）分别补：

1. volume 挂载 `./apps/hb_inventory_app`
2. `PYTHONPATH` 追加该路径
3. `frontend` 服务补 `ln -sfn .../hb_inventory_app/public /home/frappe/frappe-bench/assets/hb_inventory_app`

**这属于项目规则明列的"不写 Docker Compose"，须 Owner 明确授权后方可执行**；且改动生效需**重建容器**（不是仅 restart）。

备选（不改 compose）：`bench new-app` 在容器内创建 App，但容器可写层不持久，`docker compose` 重建后 App 丢失，**不可复现，不推荐**。

## 八、数据安全说明

Owner 提供的 5 份盘存表与《库位区分》含**真实物料名称、真实批号、真实数量**。本轮处理：

- 仅在本地读取解析，**未复制进仓库、未提交 Git**。
- 本方案文档只记录**编码规则、库位清单、单位清单、结构与数量级**，**不记录任何真实物料名称、批号或数量**。
- 后续 M3 各轮同样不得把真实物料清单、真实批次数据、盘存表提交 Git。
- Owner 另提供的 `待检证/`、`新版货位卡外购/`、`自产/` 三个模板目录（311 个真实业务文件）已在 `.gitignore` 中新增忽略规则，详见 6.10。

## 九、执行记录（Owner 授权后已执行）

### 9.1 授权与前置

- Owner 已确认 `16号楼产品库 = 3904`；
- Owner 已授权**修改 `docker-compose.yml`**（方案 7.1 所述），以挂载新建的 App。

### 9.2 `docker-compose.yml` 变更

按方案 7.1 在 8 个服务补齐挂载：

| 变更 | 处数 |
| --- | --- |
| volume `./apps/hb_inventory_app` | 8 处（backend / configurator / create-site / frontend / queue-long / queue-short / scheduler / websocket） |
| `PYTHONPATH` 追加路径 | 6 处（有 PYTHONPATH 的服务） |
| `frontend` 的 assets 符号链接 | 1 处 |

`docker compose config` 校验通过；随后 `docker compose up -d` **重建容器**（`db-data` / `sites` / `logs` 三个 volume 全部保留，未删除任何 volume，未重建 site）。容器重建后 `sites/apps.txt` 由 configurator 自动重新生成，已含 `hb_inventory_app`。

### 9.3 新建 App

- 位置：`apps/hb_inventory_app/`，模块名 `HBOS Inventory`；
- 结构照搬 `hb_attendance_app`（`pyproject.toml` / `__init__.py` / `modules.txt` / `hooks.py` / `patches.txt` / 模块目录）；
- `hooks.py` 注册 `after_migrate = "hb_inventory_app.hbos_inventory.setup.after_migrate"`；
- 已执行 `bench --site frontend install-app hb_inventory_app`，`list-apps` 显示 `hb_inventory_app 0.0.1`。

### 9.4 主数据落地结果（均幂等，二次执行「新建 0」）

| 项目 | 结果 |
| --- | --- |
| 计量单位 | 新建 10 个：个 / 套 / 瓶 / 支 / 双 / 卷 / 张 / L / BT / 件（`UOM` 总数 239 → 249） |
| 六车间库位 | 新建 7 个，加上由货位树改名而来的 3904，共 8 个 |
| 产品分类树 | 新建 5 个：原料药 / 包材 / 辅助用品 / 中间体 / 成品 |
| `Item` 自定义字段 | 5 个：业务分类 / 生产车间 / 效期类型 / 效期期限(月) / 储存条件 |
| `Batch` 自定义字段 | 7 个：放行状态 / 放行日期 / 合格证编号 / 合格证附件 / 原厂批号 / 生产单位 / 包装构成 |
| 子表 DocType | `HBOS Packaging Detail`（容器类型 / 单件重量 / 件数 / 尾桶 / 备注） |
| 报表 | `按批号查货位`、`货位明细表` |

### 9.5 两处偏差（如实记录）

**偏差一：DocType 用英文名 `HBOS Packaging Detail`，非方案里的 `HBOS 包装明细`。**

理由：与 `hb_attendance_app` 的既有约定一致（该 App 的 DocType 均为英文，如 `HBOS Attendance Import Log`、`HBOS Leave Record`），避免中文名进入 DocType 命名与文件路径。字段标签仍为中文。

**偏差二：库级盘点三对账报表**本轮未建。

理由：方案第五节的「M3-R2 交付边界」只列了两个报表，且「不做」明确包含「盘点导出（M3-R3）」。3.2 内容清单里的该项为 App 的整体规划，不代表本轮交付。**留待 M3-R3**。

### 9.6 货位树改挂到 3904

用 `frappe.rename_doc` 把 `16号楼产品库 - HB` 改名为 `3904 六车间中间库 - HB`，并同步 `warehouse_name`。

- 子节点 `03区 - HB` 的 `parent_warehouse` 被自动更新为新名；
- NestedSet（`lft`/`rgt`）完整性复验通过（无缺口、无重复、无非法区间）；
- **203 个货位全部保留**，层分布仍为 **39 / 39 / 39 / 43 / 43**；
- 3904 现与其余 7 个六车间库位并列于 `All Warehouses - HB` 下。

### 9.7 全链路验证（`TEST-M3R1-` 虚构数据）

| 验证项 | 结果 |
| --- | --- |
| 批号 → 货位 → 数量 | `TEST-M3R1-B001` 同时在 `16-03-221`（60）与 `16-03-223`（40），正确 |
| 货位 → 全部批号 | `03区`（含下级）返回 3 行，正确 |
| 包装构成 | 设为 `12件 + 1件(尾桶) + 3听 + 1瓶`，读回 4 行，汇总重量 64.421，可表达「13件3听1瓶」 |
| 放行字段 | 状态置「已放行」+ 日期 + 合格证编号 + 原厂批号，读回正确，且报表已带出该状态 |
| Item 自定义字段 | 业务分类 / 生产车间 / 效期类型 / 效期期限 / 储存条件 均可写入读回 |

验证后未清理这些虚构数据（沿用 M3-R1 的 `TEST-M3R1-` 语料），便于 M3-R3 继续使用。

### 9.8 执行中发现并修复的既有缺陷（**不在 M3 范围**）

执行 `bench --site frontend migrate` 时**全站报错**：

```
KeyError: 'doctype'
  frappe/modules/import_file.py, line 123, in import_file_by_path
```

定位为 **`hb_attendance_app` 的既有缺陷**（M1-FIX 期间产生，与 M3 无关）：

- 文件 `apps/hb_attendance_app/hb_attendance_app/hbos_attendance/page/hbos_monthly_upload/hbos_monthly_upload.json`
- 该 Page JSON **缺少 `doctype` / `name` / `module` / `owner` / `page_name` 等必需字段**，只有 `title` / `icon` / `route` / `standard` / `roles`；
- 后果：`sync_for` 读取该文件时取不到 `doc["doctype"]`，**导致整个 `bench migrate` 失败**（不止影响本 App）。

**修复**：按同目录 `hbos_attendance_import.json` 的标准结构补齐字段，`name` / `page_name` 取数据库既有值 `hbos-monthly-upload`（该 Page 此前已在库中）。

修复后 `bench migrate` **exit 0、无 Traceback**，`after_migrate` 幂等执行。

**提示**：这说明在本轮之前 `bench migrate` 一直处于失败状态；任何依赖 migrate 的操作（含 `after_migrate` 钩子）都不会生效。建议 Owner 知悉此项。

### 9.9 本轮未做

- 未创建真实产品主数据（`Item`），仅有 1 个 `TEST-M3R1-` 虚构物料；
- 未做库级盘点三对账报表（留 M3-R3）；
- 未做出库核销、效期预警、盘点导出（M3-R3）；
- 未做待检证 / 货位卡 Print Format（M3-R4）；
- 未做二维码与扫码页（M3-R5）；
- 未做拍照识别（M3-R6）；
- 未修改 Frappe / ERPNext / HRMS 核心源码；
- 未执行任何飞书写入；
- 未执行 `docker compose down -v`，未删除任何 volume，未重建 site；
- 未提交真实物料、批号、供应商、Excel / CSV。
