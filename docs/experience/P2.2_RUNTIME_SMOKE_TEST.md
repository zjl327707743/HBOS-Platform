# HBOS Portal P2.2 — Runtime Smoke Test

**状态：PASS / OWNER LOCAL RUNTIME VERIFIED**  
**执行日期：** 2026-09-25  
**正式本地目录：** `/Users/hb/Vibe Coding/HBOS-Platform`  
**Portal Worktree：** `/Users/hb/Vibe Coding/HBOS-Portal-P2.2`  
**Runtime Source HEAD：** `af8ca537bd77cffb714c4fbe2517c7108241d80b`

## 1. 目标

P2.2 首次把 `apps/hbos_portal` 临时安装进真实 Frappe `frontend` site，验证：

1. Compose 可以安全挂载 `hbos_portal`；
2. Frappe 可以安装 / 卸载该 App；
3. Site 可以 import Portal package；
4. Provider Registry 在尚无业务 Provider 时为空且正常；
5. Guest 不能读取 Portal Bootstrap；
6. 已认证 Frappe Session 可以读取 Bootstrap；
7. Bootstrap 返回真实 Frappe User identity；
8. Avatar 为空时可以正常回退；
9. Branding contract 正常；
10. Smoke 完成后正式 Attendance 工作区、site、app list 和 Docker volumes 全部恢复。

## 2. Worktree-safe 执行策略

正式 Attendance 工作区保持：

```text
/Users/hb/Vibe Coding/HBOS-Platform
branch = main
HEAD = 2675411893b0ea7a82830ba726dc819b9c17ff72
```

该工作区当时存在未提交 Attendance 修改，因此没有：

- switch Portal branch；
- stash；
- reset；
- clean；
- 覆盖本地 Attendance 文件。

Portal 使用独立 worktree：

```text
/Users/hb/Vibe Coding/HBOS-Portal-P2.2
```

Docker 仍复用正式 HBOS 的：

- `frontend` site；
- MariaDB；
- Redis；
- existing named volumes；
- 正式 Attendance bind mount。

Portal 仅通过临时 Compose override 挂入测试 worktree。

## 3. Runtime Result

| Check | Result |
|---|---|
| Formal workspace branch | `main` preserved |
| Formal workspace dirty Attendance changes | **PRESERVED** |
| Attendance bind mount | **PRESERVED** |
| Docker volumes | **UNCHANGED** |
| Volume name set | 28 / 28 与 baseline 一致 |
| `frontend` site | **PRESERVED** |
| Final installed app list | 与 baseline 完全一致 |
| `hbos_portal` temporary install | **PASS** |
| `hbos_portal` uninstall / cleanup | **PASS** |
| Python import | **PASS** |
| Package version | `0.1.0` |
| Registry entries | `0` |
| Registry failures | `0` |
| Guest Bootstrap | **PASS — HTTP 403** |
| Authenticated Bootstrap | **PASS — HTTP 200 / ok=true** |
| Contract version | `1` |
| User id | present |
| User display name | present |
| Identity provider | `frappe` |
| Avatar | `null` — expected |
| Branding product | `HBOS` |
| Branding workspace | `海滨智能运营工作台` |
| Apps | `[]` — expected |
| Business data | **UNCHANGED** |

## 4. Bootstrap Interpretation

当前：

```json
"apps": []
```

是正确结果。

P2.2 执行时尚未注册：

- Attendance Provider；
- Inventory Provider；
- LIMS Provider。

空 Registry 证明 Portal Skeleton 可以在没有业务 Provider 的情况下独立启动，而不是通过静态 import 或伪造数据维持页面。

## 5. Security Result

本轮没有：

- 执行 `docker compose down -v`；
- 删除 Docker volume；
- 重建 `frontend` site；
- stash / reset / clean 正式 Attendance 工作区；
- 修改 Attendance 代码；
- 修改业务数据；
- 创建 Portal 业务 DocType；
- 配置或暴露飞书密钥；
- 提交 cookie / credential / runtime artifact。

Temporary Compose override、cookie / credential 临时文件和测试产物已清理。

## 6. Runtime Observation A — Registry Probe

原始 `scripts/portal/p2_2_runtime_smoke.sh` 使用 bare Python 执行 Registry probe。

真实 Frappe Runtime 中，probe 应显式进入 site context。

本次实际验收使用 site-aware probe 后通过。

远端脚本在本次 P2.2 closeout 中同步修正为：

```text
frappe.init(site=SITE_NAME)
frappe.connect()
...
frappe.destroy()
```

后续不再使用无 site context 的 Registry runtime probe。

## 7. Runtime Observation B — Attendance Hook Warning

`install-app hbos_portal` 期间出现 Attendance hook import warning。

处理结论：

- warning 未阻断 `hbos_portal` install；
- 未阻断 Bootstrap；
- 未阻断 Registry；
- 未改变 Attendance bind mount；
- 未修改 Attendance 代码；
- smoke 结束后正式 site app list 与 baseline 完全一致。

因此本项记录为：

```text
NON-BLOCKING OBSERVATION
```

它属于 Attendance 工作流自己的治理 / 运行态核查范围，不在 Portal P2.2 中越界修复。

如果后续 P3 注册 Attendance Provider 时该 warning 再现，应在 Attendance Authority 下单独定位。

## 8. P2.2 Definition of Done

```text
P2.2 Runtime Smoke Test = PASS
Frappe Session Boundary = PASS
Bootstrap Runtime = PASS
Empty Registry Runtime = PASS
Environment Restoration = PASS
Business Data Preservation = PASS
```

## 9. 下一阶段

P2.2 通过后进入：

```text
P3 — Business App Registration
```

原则：

1. 一次只接一个真实 Provider；
2. 首个 Provider 先验证 Manifest / Access / Stable Route；
3. 再增加 Summary / Tasks / Search；
4. 不在同一个提交中同时重构 Attendance / Inventory / LIMS；
5. PR #15 在至少一个真实 Business App Provider 通过前继续保持 Draft。
