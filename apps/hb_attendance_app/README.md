# HBOS Attendance App

Lightweight Frappe app for HBOS M1-FIX attendance import flows.

This app stores import batch logs and provides controlled local Excel import methods. It must not contain real employee data, source Excel files, generated CSV files, database exports, logs, or secrets.
